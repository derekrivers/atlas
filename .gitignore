# Python
__pycache__/
*.py[cod]
*.egg-info/

# Virtual environments
.venv/

# Tool caches
.pytest_cache/
.mypy_cache/
.ruff_cache/

# Build artifacts
dist/
build/
apps/operator-ui/dist/
apps/operator-ui/node_modules/
apps/operator-ui/coverage/

# uv: uv.lock is committed deliberately — do not ignore it.

# Local Atlas database (knowledge-core: default SQLite at .atlas/atlas.db)
.atlas/

# Throwaway DB written by scripts/scratch_seed.py for hand-testing the CLI
scratch.db

# Local secrets: Linear token / state map and any other env-sourced creds
# are injected at the client boundary and never committed (ATLAS-41).
.env
*:Zone.Identifier
