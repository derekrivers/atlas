# Render written only by `atlas apply` (ADR-0006/0007); do not hand-edit.
# plan_run_id: 0f8c9eba-eaf3-4abd-bbcf-50fd7a7db6ed
# prompt_version: staged[planner-stage-epics-v1.2.0+planner-stage-tickets-v1.6.0+planner-stage-dependencies-v1.0.0]
# ticket_key_high_water: 150
# epic_key_high_water: 11
tickets:
- id: 06701bee-33ed-44e6-b735-52dfd7c5edc8
  product_id: 6363ff3f-22db-40b3-9a47-aeba5d0a7586
  epic_id: aa0a9535-cf03-4a01-a6d3-313520606d0d
  key: ATLAS-1
  title: Create repository skeleton per bootstrap guide
  objective: Establish the canonical directory structure for the Atlas repository so that all subsequent phases have a consistent, agreed-upon layout to build into.
  context: 'The bootstrap guide specifies the exact folder structure required before any code is written. This is the first physical act of creating the Atlas harness: the directories, the root README, and the initial git history. Nothing else in Phase 0 can proceed until the skeleton exists.'
  status: needs_human_decision
  ticket_type: infrastructure
  risk_level: low
  priority: 100
  relevant_docs:
  - docs/atlas/bootstrap-guide.md
  - docs/atlas/implementation-roadmap.md
  acceptance_criteria:
  - 'Top-level directories exist: docs/atlas, docs/architecture, docs/decisions, docs/planning, docs/product, docs/runbooks, docs/tech-debt, atlas/, tests/.'
  - A root README.md is present (content may be minimal).
  - A .gitignore appropriate for Python projects is present.
  - The repository has at least one commit containing the skeleton.
  - No application code, CI config, or pyproject.toml is added in this ticket.
  non_goals:
  - Python project initialisation (pyproject.toml, uv) — that is a separate ticket.
  - CI pipeline configuration.
  - Placing any Atlas documents into the directories.
  - Creating apps/, workers/, or infra/ directories (deferred to the phases that need them).
  implementation_notes:
  - Follow the folder snapshot in bootstrap-guide.md §18 exactly.
  - Do not create apps/, workers/, or infra/ — those arrive with the phases that need them.
  - The docs/planning/ directory should be created but left empty; it is the render target for atlas apply.
  test_requirements:
  - A shell script or pytest fixture asserts that every required directory path exists in the repository tree.
  - Verify .gitignore excludes __pycache__, *.pyc, .env, and uv.lock is NOT ignored (it must be committed).
  documentation_requirements:
  - Root README.md must exist with at minimum the project name and a one-line description.
  definition_of_done:
  - All required directories exist and are tracked by git (use .gitkeep where needed).
  - CI (once added) can check out the repo and find the expected structure.
  - No extraneous top-level directories are present.
  estimated_effort: null
  external_linear_id: 19c45032-ba4f-4e9a-adb7-90d8dd308a5b
  external_github_issue_id: null
  tags:
  - repository
  - bootstrap
  - infrastructure
  component: repository
  linear_synced_at: '2026-07-04T18:05:38.538342Z'
  last_observed_linear_state_id: 311a3a97-c409-4cce-96ab-0a3bfc2a5541
  status_entered_at: '2026-07-05T18:08:48.998969Z'
  review_cycle_count: 0
  source_anchor: docs/atlas/bootstrap-guide.md#3-create-the-repository
  created_by_type: agent
  created_by_id: planner
  created_at: '2026-07-04T18:05:38.538342Z'
  updated_at: '2026-07-04T18:05:38.538342Z'
  completed_at: null
- id: 658ec1e9-4804-46e4-97e2-ede7263c794b
  product_id: 6363ff3f-22db-40b3-9a47-aeba5d0a7586
  epic_id: aa0a9535-cf03-4a01-a6d3-313520606d0d
  key: ATLAS-2
  title: Initialise Python project with uv, pytest, ruff, mypy, and pre-commit
  objective: Set up the Python project toolchain so that tests, linting, and type-checking can run locally and in CI from a single `uv sync` invocation.
  context: ADR-0001 mandates Python 3.11+ as the implementation language. The bootstrap guide specifies uv as the package manager, with pytest, ruff, mypy, and pre-commit as the dev toolchain. This ticket creates pyproject.toml, uv.lock, and the pre-commit configuration so the project is runnable before any application code exists.
  status: needs_human_decision
  ticket_type: infrastructure
  risk_level: low
  priority: 90
  relevant_docs:
  - docs/atlas/bootstrap-guide.md
  - docs/decisions/0001-use-python-for-atlas-platform.md
  - docs/atlas/implementation-roadmap.md
  acceptance_criteria:
  - '`uv sync` completes without error on a fresh clone.'
  - '`uv run pytest` exits 0 (even with zero tests collected).'
  - '`uv run ruff check .` exits 0 on the initial codebase.'
  - '`uv run mypy atlas/` exits 0 (or with only missing-stub warnings, not errors).'
  - pre-commit hooks are configured and `pre-commit run --all-files` passes.
  - uv.lock is committed to the repository.
  - Python version is pinned to 3.11+ in pyproject.toml.
  non_goals:
  - Installing Typer, Rich, or any application-layer dependency — those arrive with the CLI ticket.
  - Configuring import-linter (that is Phase 3.5).
  - Writing any application code or tests beyond a placeholder.
  - CI pipeline configuration — that is a separate ticket.
  implementation_notes:
  - Use `uv init` or hand-author pyproject.toml; commit uv.lock.
  - Typer and Rich are NOT Phase 0 dependencies per the bootstrap guide.
  - Pydantic, PyYAML, NetworkX, SQLAlchemy, Alembic, and Jinja2 are the runtime stack but only need to be listed if they are needed by Phase 0 code; otherwise defer to the phase that first uses them.
  - The atlas/ package must have an __init__.py so mypy can find it.
  test_requirements:
  - A trivial test file (tests/test_smoke.py) with one passing assertion confirms pytest is wired correctly.
  - Pre-commit configuration includes ruff and mypy hooks.
  documentation_requirements: []
  definition_of_done:
  - pyproject.toml and uv.lock are committed.
  - All four tool invocations (pytest, ruff, mypy, pre-commit) pass on a clean checkout.
  - The pre-commit config is committed as .pre-commit-config.yaml.
  estimated_effort: null
  external_linear_id: 7ebadb4e-667c-4caf-8a1d-7b23d7be77fd
  external_github_issue_id: null
  tags:
  - python
  - toolchain
  - bootstrap
  - infrastructure
  component: repository
  linear_synced_at: '2026-07-04T18:05:38.538342Z'
  last_observed_linear_state_id: 311a3a97-c409-4cce-96ab-0a3bfc2a5541
  status_entered_at: '2026-07-05T15:53:13.776434Z'
  review_cycle_count: 0
  source_anchor: docs/atlas/bootstrap-guide.md#6-initialise-python-project
  created_by_type: agent
  created_by_id: planner
  created_at: '2026-07-04T18:05:38.538342Z'
  updated_at: '2026-07-04T18:05:38.538342Z'
  completed_at: null
- id: 29a22a05-6039-4654-adaf-acaa92deed23
  product_id: 6363ff3f-22db-40b3-9a47-aeba5d0a7586
  epic_id: aa0a9535-cf03-4a01-a6d3-313520606d0d
  key: ATLAS-3
  title: 'Add CI pipeline: tests, lint, and type-check on every PR'
  objective: Configure GitHub Actions to run pytest, ruff, and mypy automatically on every pull request so that CI is the system-tier evidence producer from Phase 0 onward.
  context: 'ADR-0008 designates CI as the system-tier evidence producer; a repository must have CI configured before any ticket in it can be verified. The implementation roadmap lists this as ATLAS-3 and the Phase 0 milestone test requires CI to be green. The bootstrap guide §15 lists ''Add CI: tests, lint, type-check on every PR'' as a day-one checklist item.'
  status: needs_human_decision
  ticket_type: infrastructure
  risk_level: medium
  priority: 80
  relevant_docs:
  - docs/atlas/bootstrap-guide.md
  - docs/atlas/implementation-roadmap.md
  - docs/decisions/0008-ci-sourced-evidence-with-trust-tiers.md
  acceptance_criteria:
  - A GitHub Actions workflow file exists at .github/workflows/ci.yml.
  - The workflow triggers on pull_request and push to main.
  - The workflow runs `uv run pytest` and fails the build on test failure.
  - The workflow runs `uv run ruff check .` and fails the build on lint errors.
  - The workflow runs `uv run mypy atlas/` and fails the build on type errors.
  - The workflow passes on the current codebase (green on first run).
  - 'CI job names follow the evidence-pipeline job-name convention (prefix: test, lint, build) so evidence typing works in Phase 6.'
  non_goals:
  - Coverage reporting or coverage thresholds — deferred to Phase 6.
  - The doc linter CI step — that is added in the doc linter ticket.
  - Deployment or release workflows.
  - Matrix builds across multiple Python versions (single version is sufficient for Phase 0).
  - Caching uv dependencies (nice-to-have, not required for green CI).
  implementation_notes:
  - Use the official uv GitHub Action or install uv via pip in the workflow.
  - Job names must use recognised prefixes (test, lint) per evidence-pipeline.md job-name convention table so Phase 6 evidence ingestion types them correctly without heuristics.
  - Pin the Python version in the workflow to match pyproject.toml.
  test_requirements:
  - Open a test PR with a deliberate ruff violation and confirm CI fails.
  - Open a test PR with a deliberate mypy error and confirm CI fails.
  - Confirm the workflow passes on the main branch after merging the skeleton and Python setup tickets.
  documentation_requirements:
  - The workflow file is self-documenting via job names; no additional doc required at this stage.
  definition_of_done:
  - CI is green on the main branch.
  - A PR with a deliberate lint error causes CI to fail.
  - The workflow file is committed under .github/workflows/.
  estimated_effort: null
  external_linear_id: c9f9f86e-9476-4a68-a1e2-043050be1eaa
  external_github_issue_id: null
  tags:
  - ci
  - github-actions
  - infrastructure
  - bootstrap
  component: ci
  linear_synced_at: '2026-07-04T18:05:38.538342Z'
  last_observed_linear_state_id: 311a3a97-c409-4cce-96ab-0a3bfc2a5541
  status_entered_at: '2026-07-05T15:53:13.776434Z'
  review_cycle_count: 0
  source_anchor: docs/atlas/implementation-roadmap.md#epic-bootstrap-repository
  created_by_type: agent
  created_by_id: planner
  created_at: '2026-07-04T18:05:38.538342Z'
  updated_at: '2026-07-04T18:05:38.538342Z'
  completed_at: null
- id: 416acf30-0fc0-426c-b96f-ea58fff5e5f2
  product_id: 6363ff3f-22db-40b3-9a47-aeba5d0a7586
  epic_id: aa0a9535-cf03-4a01-a6d3-313520606d0d
  key: ATLAS-4
  title: 'Implement doc linter v1: ADR model validation, MANIFEST cross-links, legacy name ban, and planning-file guard'
  objective: Build a doc linter that mechanically enforces internal consistency of the Atlas document set, so that documentation drift is caught before it misleads agents or breaks anchoring.
  context: 'ADR-0003 requires a doc linter as the enforcement mechanism for the single-repository principle. ADR-0006 requires the linter to flag hand-edits to docs/planning/ outside atlas apply. The implementation roadmap (ATLAS-4) specifies four checks: ADR model validation, MANIFEST cross-links and intra-doc links, legacy v1/v2/v3 name ban, and the planning-file hand-edit guard. The Phase 0 milestone test requires the linter to pass on the whole repository and fail on a seeded bad fixture.'
  status: needs_human_decision
  ticket_type: feature
  risk_level: medium
  priority: 70
  relevant_docs:
  - docs/atlas/bootstrap-guide.md
  - docs/atlas/implementation-roadmap.md
  - docs/decisions/0003-use-single-repository-for-harness-docs.md
  - docs/decisions/0006-source-of-truth-hierarchy.md
  acceptance_criteria:
  - 'The linter validates ADR files: each must have Status, Context, Decision, Rationale, and Consequences sections; a missing section is a linter error.'
  - 'The linter checks docs/MANIFEST.md cross-links: every file listed in MANIFEST.md exists on disk, and every broken link is reported.'
  - The linter bans legacy document names containing 'v1', 'v2', or 'v3' in active (non-archive) doc paths.
  - The linter flags any file under docs/planning/ whose git status shows it was modified outside an atlas apply run (i.e. modified but not by the apply command).
  - '`uv run python -m atlas.tools.doc_linter` exits 0 on the current repository.'
  - Running the linter against a seeded fixture with a missing ADR section exits non-zero and names the violation.
  - Running the linter against a seeded fixture with a hand-edited planning file exits non-zero and names the file.
  non_goals:
  - JSON Schema validation of doc examples against Pydantic models — that is doc linter v2 in Phase 1 (ATLAS-16).
  - Checking workflow job names against the evidence-pipeline convention — that is Phase 6.
  - Fixing the documentation drift it finds — that is a separate ticket (ATLAS-5).
  - Linting non-markdown files.
  implementation_notes:
  - Implement as atlas/tools/doc_linter.py, runnable as `python -m atlas.tools.doc_linter`.
  - The planning-file guard should compare the git index state of docs/planning/ — files modified outside atlas apply will appear as dirty in git status.
  - Keep each check as a separate function so they can be tested and extended independently.
  - The linter must be wired into CI and pre-commit in this ticket.
  test_requirements:
  - Unit tests for each check function with passing and failing fixtures.
  - A seeded bad ADR fixture (missing Rationale section) causes the linter to exit non-zero.
  - A seeded hand-edited planning file causes the linter to exit non-zero.
  - A seeded MANIFEST.md with a broken link causes the linter to exit non-zero.
  - The linter passes on the real repository in CI.
  documentation_requirements:
  - A brief docstring on the module explaining the four checks and how to run the linter.
  definition_of_done:
  - All four checks are implemented and tested.
  - The linter is wired into CI (the CI workflow runs it and fails on linter errors).
  - The linter is wired into pre-commit.
  - The linter passes on the current repository and fails on each seeded bad fixture.
  estimated_effort: null
  external_linear_id: 3eb84053-40d5-43d7-8d06-224fc3fe2a88
  external_github_issue_id: null
  tags:
  - doc-linter
  - documentation
  - ci
  - bootstrap
  component: tools
  linear_synced_at: '2026-07-04T18:05:38.538342Z'
  last_observed_linear_state_id: 311a3a97-c409-4cce-96ab-0a3bfc2a5541
  status_entered_at: '2026-07-05T15:53:13.776434Z'
  review_cycle_count: 0
  source_anchor: docs/atlas/implementation-roadmap.md#epic-bootstrap-repository
  created_by_type: agent
  created_by_id: planner
  created_at: '2026-07-04T18:05:38.538342Z'
  updated_at: '2026-07-04T18:05:38.538342Z'
  completed_at: null
- id: 9580f968-be68-43c8-8f93-d90f76198f0b
  product_id: 6363ff3f-22db-40b3-9a47-aeba5d0a7586
  epic_id: aa0a9535-cf03-4a01-a6d3-313520606d0d
  key: ATLAS-5
  title: Repair documentation drift surfaced by the doc linter
  objective: Resolve all linter errors found when running doc linter v1 against the current repository so that the linter passes cleanly and the document set is internally consistent.
  context: 'The doc linter ticket (ATLAS-4) will surface existing inconsistencies in the Atlas document set: broken MANIFEST links, ADR files missing required sections, legacy v1/v2/v3 names in active paths, or other drift. This ticket exists to fix those findings. It cannot be scoped precisely until the linter runs, but it is a required Phase 0 step: the milestone test requires the linter to pass on the whole repository.'
  status: needs_human_decision
  ticket_type: documentation
  risk_level: low
  priority: 60
  relevant_docs:
  - docs/atlas/bootstrap-guide.md
  - docs/atlas/implementation-roadmap.md
  - docs/decisions/0003-use-single-repository-for-harness-docs.md
  acceptance_criteria:
  - '`uv run python -m atlas.tools.doc_linter` exits 0 on the full repository with no suppressions.'
  - All ADR files in docs/decisions/ have the required sections (Status, Context, Decision, Rationale, Consequences).
  - docs/MANIFEST.md exists and all links in it resolve to real files.
  - No active (non-archive) document path contains 'v1', 'v2', or 'v3' in its filename.
  - No file under docs/planning/ is flagged as hand-edited outside atlas apply.
  non_goals:
  - Changing the substance or decisions recorded in any ADR — only structural/formatting fixes.
  - Adding new documentation beyond what is needed to satisfy linter checks.
  - Fixing issues the linter does not check (e.g. prose quality).
  - Creating docs/MANIFEST.md from scratch if it does not exist — that is a documentation task scoped here only if the linter requires it.
  implementation_notes:
  - Run the linter locally first to enumerate all failures before starting fixes.
  - If docs/MANIFEST.md does not exist, create it listing all canonical docs.
  - Fixes should be minimal and surgical — do not rewrite documents, only add missing sections or fix broken links.
  test_requirements:
  - CI runs the doc linter and it passes after this ticket is merged.
  - 'No regressions: the linter must still fail on the seeded bad fixtures from the linter ticket.'
  documentation_requirements:
  - docs/MANIFEST.md must exist and be accurate after this ticket.
  definition_of_done:
  - The doc linter exits 0 on the full repository in CI.
  - All linter-reported issues are resolved and documented in the PR description.
  estimated_effort: null
  external_linear_id: 86e41384-a3be-411c-b014-d446b67f2026
  external_github_issue_id: null
  tags:
  - documentation
  - doc-linter
  - bootstrap
  component: repository
  linear_synced_at: '2026-07-04T18:05:38.538342Z'
  last_observed_linear_state_id: 311a3a97-c409-4cce-96ab-0a3bfc2a5541
  status_entered_at: '2026-07-05T15:53:13.776434Z'
  review_cycle_count: 0
  source_anchor: docs/atlas/implementation-roadmap.md#epic-bootstrap-repository
  created_by_type: agent
  created_by_id: planner
  created_at: '2026-07-04T18:05:38.538342Z'
  updated_at: '2026-07-04T18:05:38.538342Z'
  completed_at: null
- id: 62fd0716-25b3-4c23-beca-359dadcb16a7
  product_id: 6363ff3f-22db-40b3-9a47-aeba5d0a7586
  epic_id: aa0a9535-cf03-4a01-a6d3-313520606d0d
  key: ATLAS-6
  title: Land ADR-0006 through ADR-0009 and the Planning Engine Specification as canonical documents; update root control documents
  objective: Commit ADR-0006, ADR-0007, ADR-0008, ADR-0009, and the Planning Engine Specification as accepted canonical documents, and update ARCHITECTURE.md and other root control documents to reflect the decisions they encode.
  context: The implementation roadmap (ATLAS-6) requires these four ADRs and the Planning Engine Specification to be landed as canonical before Phase 0 closes. ADR-0006 clarifies the source-of-truth hierarchy; ADR-0007 defines generative planning with deterministic reconciliation; ADR-0008 establishes CI-sourced evidence with trust tiers; ADR-0009 defines single-operator governance. ARCHITECTURE.md must be updated to state the hierarchy per ADR-0006's consequences. The root control documents (PRODUCT.md, ROADMAP.md, WORKFLOW.md, AGENTS.md) must be consistent with these decisions.
  status: needs_human_decision
  ticket_type: documentation
  risk_level: medium
  priority: 50
  relevant_docs:
  - docs/atlas/implementation-roadmap.md
  - docs/atlas/bootstrap-guide.md
  - docs/decisions/0006-source-of-truth-hierarchy.md
  - docs/decisions/0007-generative-planning-with-deterministic-reconciliation.md
  - docs/decisions/0008-ci-sourced-evidence-with-trust-tiers.md
  - docs/decisions/0009-single-operator-governance.md
  - docs/atlas/planning-engine-specification.md
  - ARCHITECTURE.md
  acceptance_criteria:
  - 'ADR-0006, ADR-0007, ADR-0008, and ADR-0009 are present in docs/decisions/ with Status: Accepted and all required sections.'
  - docs/atlas/planning-engine-specification.md is present and referenced from ROADMAP.md.
  - ARCHITECTURE.md states the three-layer source-of-truth hierarchy (intent in docs, operational state in DB, planning files as renders) per ADR-0006.
  - ARCHITECTURE.md references ADR-0007, ADR-0008, and ADR-0009 where relevant.
  - AGENTS.md lists the new canonical documents in its Required Reading section.
  - The doc linter passes on the full repository after these changes.
  non_goals:
  - Implementing any behaviour described in the ADRs — that is Phase 1 and later.
  - Updating docs/planning/ files (those are renders written only by atlas apply).
  - Writing ADR-0010 or ADR-0011 — those are out of Phase 0 scope.
  - Creating the Planning Engine Specification from scratch if it already exists — only ensure it is canonical and linked.
  implementation_notes:
  - If any of the four ADRs already exist as drafts, update their Status to Accepted and verify all sections are present.
  - ARCHITECTURE.md update must explicitly name the three layers and cite ADR-0006 per the ADR's own consequences section.
  - AGENTS.md Required Reading list should include the planning-engine-specification.md.
  test_requirements:
  - The doc linter passes after all changes are committed.
  - Each ADR file passes the ADR model validation check (all required sections present).
  - ROADMAP.md contains a reference to docs/atlas/planning-engine-specification.md.
  documentation_requirements:
  - ARCHITECTURE.md must be updated to reflect ADR-0006's source-of-truth hierarchy.
  - AGENTS.md Required Reading must include all four new ADRs and the Planning Engine Specification.
  definition_of_done:
  - 'All four ADRs and the Planning Engine Specification are committed with Status: Accepted.'
  - ARCHITECTURE.md, ROADMAP.md, and AGENTS.md are updated and consistent with the new ADRs.
  - The doc linter passes on the full repository in CI.
  estimated_effort: null
  external_linear_id: 2a53d994-820a-44bc-a22b-ad09894f2ebc
  external_github_issue_id: null
  tags:
  - documentation
  - adr
  - bootstrap
  - governance
  component: repository
  linear_synced_at: '2026-07-04T18:05:38.538342Z'
  last_observed_linear_state_id: 311a3a97-c409-4cce-96ab-0a3bfc2a5541
  status_entered_at: '2026-07-05T15:53:13.776434Z'
  review_cycle_count: 0
  source_anchor: docs/atlas/implementation-roadmap.md#epic-bootstrap-repository
  created_by_type: agent
  created_by_id: planner
  created_at: '2026-07-04T18:05:38.538342Z'
  updated_at: '2026-07-04T18:05:38.538342Z'
  completed_at: null
- id: 6537ed61-70c6-4618-972f-fefd19f91ca8
  product_id: 6363ff3f-22db-40b3-9a47-aeba5d0a7586
  epic_id: 29e04b28-877b-4112-9706-73423855c3d8
  key: ATLAS-7
  title: Shared types and enums
  objective: Define and implement all canonical enums and shared primitive types in atlas.core that every other layer imports, including ActorType, EntityStatus, RiskLevel, EvidenceStatus, TicketStatus, TicketType, and the trust-tier derivation logic per ADR-0008.
  context: The Knowledge Core epic establishes atlas.core as the single contract for all Atlas entities. Before any model can be defined, the shared enumerations and primitive types must exist. These types are referenced by every model in the system and must never be redefined in higher layers. The trust-tier derivation (system > human > agent, with agent capped at PENDING) is specified in ADR-0008 and must be implemented here as a pure function so the Evidence model and storage layer can enforce it without importing from higher layers.
  status: needs_human_decision
  ticket_type: feature
  risk_level: medium
  priority: 100
  relevant_docs:
  - docs/decisions/0008-ci-sourced-evidence-with-trust-tiers.md
  - docs/decisions/0009-single-operator-governance.md
  - docs/decisions/0006-source-of-truth-hierarchy.md
  - docs/decisions/0001-use-python-for-atlas-platform.md
  acceptance_criteria:
  - 'ActorType enum exists with values: system, human, agent.'
  - 'EntityStatus enum exists covering at minimum: draft, active, archived.'
  - 'RiskLevel enum exists with values: low, medium, high, critical.'
  - 'EvidenceStatus enum exists with values: pending, passed, failed, warning, not_applicable.'
  - TicketStatus enum exists covering all states in the state-mapping table (backlog, planned, blocked, ready_for_agent, in_progress, pr_open, review_required, changes_requested, needs_human_decision, done, rejected).
  - 'TicketType enum exists with values: feature, bug, tech_debt, spike, documentation, infrastructure, research.'
  - 'A pure function derive_trust_tier(actor_type: ActorType) -> TrustTier exists and returns the correct tier; agent-submitted evidence is capped at PENDING by a separate pure function cap_evidence_status(actor_type, requested_status) -> EvidenceStatus.'
  non_goals:
  - Do not define any Pydantic models in this ticket — only enums and primitive types.
  - Do not implement storage or database logic.
  - Do not implement any CLI commands.
  - Do not define GraphNode or graph-related types — those belong to the Dependency Engine.
  implementation_notes:
  - Place all enums in atlas/core/enums.py; export from atlas/core/__init__.py.
  - Use Python 3.11 StrEnum or Enum with string values for JSON serialisation compatibility.
  - 'TrustTier can be a simple enum: system, human, agent.'
  - The cap function is a pure deterministic function with no side effects — ADR-0005 applies.
  test_requirements:
  - Unit tests assert every enum member exists and has the expected string value.
  - Unit tests for derive_trust_tier cover all three ActorType values.
  - Unit tests for cap_evidence_status confirm agent-submitted PASSED is capped to PENDING, while system and human are unchanged.
  - Tests are importable from atlas.core with no circular imports.
  documentation_requirements: []
  definition_of_done:
  - All enums and primitive types are implemented in atlas.core.
  - All unit tests pass under pytest.
  - ruff and mypy pass with no errors.
  - No higher-layer module is imported from atlas.core.
  estimated_effort: null
  external_linear_id: b241978d-0806-40c7-b684-9e88247dfdcb
  external_github_issue_id: null
  tags:
  - models
  - enums
  - core
  - trust-tier
  component: atlas.core
  linear_synced_at: '2026-07-04T18:05:38.538342Z'
  last_observed_linear_state_id: 311a3a97-c409-4cce-96ab-0a3bfc2a5541
  status_entered_at: '2026-07-05T15:53:13.776434Z'
  review_cycle_count: 0
  source_anchor: docs/atlas/implementation-roadmap.md#epic-knowledge-core
  created_by_type: agent
  created_by_id: planner
  created_at: '2026-07-04T18:05:38.538342Z'
  updated_at: '2026-07-04T18:05:38.538342Z'
  completed_at: null
- id: a1c3ddc9-f538-4957-b761-92772f7db918
  product_id: 6363ff3f-22db-40b3-9a47-aeba5d0a7586
  epic_id: 29e04b28-877b-4112-9706-73423855c3d8
  key: ATLAS-8
  title: Product, ADR, Epic, Ticket, and TicketDependency Pydantic models
  objective: Implement the canonical Pydantic models for Product, ADR, Epic, Ticket, and TicketDependency in atlas.core, establishing the single contract for all planning entities used by every subsequent phase.
  context: 'These five models are the backbone of the Atlas data model. Every later phase — planning, dependency engine, PM engine, context renderer — imports from atlas.core and never redefines these types. The Ticket model must carry all fields referenced in the roadmap and design documents: key, epic_key, title, objective, context, ticket_type, risk_level, priority, status, source_anchor, relevant_docs, tags, component, acceptance_criteria, non_goals, test_requirements, definition_of_done, estimated_effort (nullable, operator-supplied only), external_linear_id, status_entered_at, last_observed_linear_state_id, review_cycle_count, updated_at, created_at. TicketDependency stores only the depends_on direction; blocks is derived. The ADR model must carry number, title, status (accepted/proposed/deprecated/superseded), and file path.'
  status: needs_human_decision
  ticket_type: feature
  risk_level: high
  priority: 90
  relevant_docs:
  - docs/decisions/0006-source-of-truth-hierarchy.md
  - docs/decisions/0007-generative-planning-with-deterministic-reconciliation.md
  - docs/atlas/dependency-engine.md
  - docs/atlas/planning-engine-specification.md
  - docs/atlas/pm-engine-and-linear-sync.md
  acceptance_criteria:
  - 'Product model exists with fields: id (UUID), key, title, description, created_at, updated_at.'
  - 'ADR model exists with fields: id (UUID), number (int), title, status (ADRStatus enum: proposed, accepted, deprecated, superseded), file_path, created_at.'
  - 'Epic model exists with fields: id (UUID), key (nullable until assigned), title, objective, status (EntityStatus), risk_level, priority, source_anchor, created_at, updated_at.'
  - Ticket model exists with all required fields including estimated_effort (nullable int), external_linear_id (nullable str), status_entered_at (nullable datetime), last_observed_linear_state_id (nullable str), review_cycle_count (int default 0), source_anchor, tags (list[str]), component (nullable str).
  - 'TicketDependency model exists with fields: id (UUID), source_ticket_id (UUID), target_ticket_id (UUID), dependency_type (DependencyType enum with depends_on), reason (nullable str), created_at.'
  - All models use Pydantic v2 with strict validation; UUID fields default to uuid4; datetime fields are timezone-aware.
  - All models are importable from atlas.core with no circular imports and pass mypy strict checks.
  non_goals:
  - Do not implement SQLAlchemy ORM table definitions in this ticket — that is the storage layer ticket.
  - Do not implement the Lesson, Evidence, PlanRun, ContextPack, or AgentRun models here.
  - Do not implement any serialisation or YAML output logic.
  - Do not implement graph node/edge models — those belong to the Dependency Engine.
  implementation_notes:
  - Place models in atlas/core/models.py or a models/ subpackage; export from atlas/core/__init__.py.
  - Use Pydantic v2 model_config with frozen=False (rows are mutable operational state).
  - estimated_effort validation (>= 1 or None) belongs on the model as a field_validator; the storage-layer setter enforces it again as a belt-and-suspenders guard.
  - DependencyType enum can live in atlas/core/enums.py alongside the other enums from the previous ticket.
  - ADRStatus is a new enum; add it to atlas/core/enums.py.
  test_requirements:
  - Unit tests construct each model with valid data and assert field values round-trip correctly.
  - Unit tests assert that invalid data (wrong types, missing required fields) raises Pydantic ValidationError.
  - Unit tests confirm estimated_effort accepts None and positive integers but the model-level validator rejects zero and negative values.
  - Tests confirm all models are importable from atlas.core without importing any higher-layer module.
  documentation_requirements: []
  definition_of_done:
  - All five models are implemented and importable from atlas.core.
  - All unit tests pass under pytest.
  - ruff and mypy pass with no errors.
  - No higher-layer module is imported from atlas.core.
  estimated_effort: null
  external_linear_id: b3200846-8d4f-400e-9ba5-dc4ac323131b
  external_github_issue_id: null
  tags:
  - models
  - core
  - planning
  - ticket
  component: atlas.core
  linear_synced_at: '2026-07-04T18:05:38.538342Z'
  last_observed_linear_state_id: 311a3a97-c409-4cce-96ab-0a3bfc2a5541
  status_entered_at: '2026-07-05T15:53:13.776434Z'
  review_cycle_count: 0
  source_anchor: docs/atlas/implementation-roadmap.md#epic-knowledge-core
  created_by_type: agent
  created_by_id: planner
  created_at: '2026-07-04T18:05:38.538342Z'
  updated_at: '2026-07-04T18:05:38.538342Z'
  completed_at: null
- id: dad2b9f8-1a7b-4d59-95f8-4ce6e0b6fa4e
  product_id: 6363ff3f-22db-40b3-9a47-aeba5d0a7586
  epic_id: 29e04b28-877b-4112-9706-73423855c3d8
  key: ATLAS-9
  title: Lesson model with DRAFT default and status lifecycle
  objective: Implement the Lesson Pydantic model in atlas.core with a status field defaulting to DRAFT for agent-authored lessons, enforcing the ADR-0009 lesson promotion gate so agent-written lessons are excluded from context retrieval until the operator promotes them to ACTIVE.
  context: 'ADR-0009 establishes that agent-authored lessons must be created with status DRAFT and are excluded from context-pack retrieval until the operator promotes them to ACTIVE. This is the single governance mechanism that prevents loop-poisoning: a bad agent-written lesson must not be injected into future context packs. The Lesson model is the only component that writes lessons; the Learning System (Phase 9) is the writer, but the model contract must be established here so the Context Renderer (Phase 5) can filter on status: ACTIVE without depending on Phase 9.'
  status: needs_human_decision
  ticket_type: feature
  risk_level: medium
  priority: 80
  relevant_docs:
  - docs/decisions/0009-single-operator-governance.md
  - docs/atlas/learning-system.md
  - docs/atlas/context-renderer.md
  - docs/atlas/implementation-roadmap.md
  acceptance_criteria:
  - 'Lesson model exists with fields: id (UUID), title, body, status (LessonStatus enum: draft, active, archived), confidence (nullable float 0.0–1.0), tags (list[str]), ticket_type (nullable TicketType), related_ticket_ids (list[UUID]), created_by_type (ActorType), created_by_id (str), created_at, updated_at.'
  - 'LessonStatus enum exists in atlas.core.enums with values: draft, active, archived.'
  - The Lesson model's default status is draft.
  - A class method or factory Lesson.for_agent(...) creates a Lesson with created_by_type=agent and status=draft, making the DRAFT default explicit for the agent-authored path.
  - confidence field validates that when not None, the value is in [0.0, 1.0].
  - Lesson model is importable from atlas.core with no circular imports.
  non_goals:
  - Do not implement lesson extraction, promotion CLI, or retrieval logic — those belong to Phase 9 and Phase 5 respectively.
  - Do not implement storage table definitions here.
  - 'Do not implement any filtering or query logic — the retrieval filter (status: ACTIVE only) is the Context Renderer''s responsibility.'
  implementation_notes:
  - Add LessonStatus to atlas/core/enums.py.
  - Place Lesson in atlas/core/models.py alongside the other models.
  - The for_agent classmethod is a convenience; the model itself does not enforce that agents can only create DRAFT — enforcement is at the storage/repository layer.
  test_requirements:
  - Unit test confirms default status is draft.
  - Unit test confirms Lesson.for_agent() sets created_by_type=agent and status=draft.
  - Unit test confirms confidence=None is valid, confidence=0.5 is valid, confidence=1.1 raises ValidationError.
  - Unit test confirms the model is importable from atlas.core without importing higher-layer modules.
  documentation_requirements: []
  definition_of_done:
  - Lesson model implemented and importable from atlas.core.
  - All unit tests pass under pytest.
  - ruff and mypy pass with no errors.
  estimated_effort: null
  external_linear_id: 01a99cf9-f552-4a33-87db-41f93e849379
  external_github_issue_id: null
  tags:
  - models
  - core
  - lessons
  - governance
  component: atlas.core
  linear_synced_at: '2026-07-04T18:05:38.538342Z'
  last_observed_linear_state_id: 311a3a97-c409-4cce-96ab-0a3bfc2a5541
  status_entered_at: '2026-07-05T15:53:13.776434Z'
  review_cycle_count: 0
  source_anchor: docs/decisions/0009-single-operator-governance.md#decision
  created_by_type: agent
  created_by_id: planner
  created_at: '2026-07-04T18:05:38.538342Z'
  updated_at: '2026-07-04T18:05:38.538342Z'
  completed_at: null
- id: f87ad18d-e8cc-41fb-a451-41b55f67545c
  product_id: 6363ff3f-22db-40b3-9a47-aeba5d0a7586
  epic_id: 29e04b28-877b-4112-9706-73423855c3d8
  key: ATLAS-10
  title: Evidence model with trust fields and append-only semantics
  objective: 'Implement the Evidence Pydantic model in atlas.core with all trust-tier fields required by ADR-0008: commit_sha, external_run_id, payload_hash, created_by_type, and the append-only constraint, so the evidence pipeline and verification engine have a stable, schema-validated contract.'
  context: ADR-0008 specifies that evidence records are append-only, pinned to a commit SHA, and trust-tiered by created_by_type. System-tier records require commit_sha, external_run_id, and payload_hash; agent-submitted records are capped at PENDING. The Evidence model is the contract that the evidence pipeline (Phase 6) writes to and the Verification Engine (Phase 7) reads from. Establishing it in atlas.core in Phase 1 ensures all later phases import from a single definition.
  status: needs_human_decision
  ticket_type: feature
  risk_level: high
  priority: 80
  relevant_docs:
  - docs/decisions/0008-ci-sourced-evidence-with-trust-tiers.md
  - docs/atlas/evidence-pipeline.md
  - docs/atlas/verification-engine.md
  - docs/atlas/implementation-roadmap.md
  acceptance_criteria:
  - 'Evidence model exists with fields: id (UUID), ticket_id (UUID), evidence_type (EvidenceType enum), status (EvidenceStatus), summary (str), raw_payload (nullable dict, capped at 64KB in the storage layer), commit_sha (nullable str), external_run_id (nullable str), payload_hash (nullable str), source_uri (nullable str), created_by_type (ActorType), created_by_id (str), created_at (datetime, immutable after creation).'
  - 'EvidenceType enum exists in atlas.core.enums with values: test_result, lint_result, build_result, coverage_report, documentation_update, pr_review, manual_approval.'
  - A model-level validator enforces that when created_by_type is system, commit_sha, external_run_id, and payload_hash are all non-null.
  - 'A pure function enforce_trust_cap(evidence: Evidence) -> Evidence returns a copy with status set to PENDING when created_by_type is agent and the requested status is not PENDING.'
  - Evidence model is importable from atlas.core with no circular imports and passes mypy strict checks.
  non_goals:
  - Do not implement the GitHub polling client or evidence ingestion pipeline — those are Phase 6.
  - Do not implement storage table definitions or the append-only enforcement at the database level — that is the storage layer ticket.
  - Do not implement the 64KB raw_payload cap at the model level — that is a storage/repository concern.
  - Do not implement VerificationCheck model here — that is a separate ticket.
  implementation_notes:
  - Add EvidenceType to atlas/core/enums.py.
  - Place Evidence in atlas/core/models.py.
  - enforce_trust_cap is a pure function in atlas/core/trust.py or alongside the model; it must not mutate the input.
  - The append-only constraint (no updates or deletes) is enforced at the repository layer, not the model layer.
  test_requirements:
  - Unit test confirms system-tier Evidence with missing commit_sha raises ValidationError.
  - Unit test confirms agent-tier Evidence with status=PASSED is corrected to PENDING by enforce_trust_cap.
  - Unit test confirms system-tier Evidence with status=PASSED is unchanged by enforce_trust_cap.
  - Unit test confirms all EvidenceType enum values exist.
  - Unit test confirms Evidence is importable from atlas.core without importing higher-layer modules.
  documentation_requirements: []
  definition_of_done:
  - Evidence model implemented and importable from atlas.core.
  - All unit tests pass under pytest.
  - ruff and mypy pass with no errors.
  estimated_effort: null
  external_linear_id: db5f9125-315c-4919-848f-2b77a83aa6dc
  external_github_issue_id: null
  tags:
  - models
  - core
  - evidence
  - trust-tier
  component: atlas.core
  linear_synced_at: '2026-07-04T18:05:38.538342Z'
  last_observed_linear_state_id: 311a3a97-c409-4cce-96ab-0a3bfc2a5541
  status_entered_at: '2026-07-05T15:53:13.776434Z'
  review_cycle_count: 0
  source_anchor: docs/decisions/0008-ci-sourced-evidence-with-trust-tiers.md#decision
  created_by_type: agent
  created_by_id: planner
  created_at: '2026-07-04T18:05:38.538342Z'
  updated_at: '2026-07-04T18:05:38.538342Z'
  completed_at: null
- id: cb8c52e2-ba7b-49b0-b4d2-1e7289c0fbc9
  product_id: 6363ff3f-22db-40b3-9a47-aeba5d0a7586
  epic_id: 29e04b28-877b-4112-9706-73423855c3d8
  key: ATLAS-11
  title: PlanRun, ContextPack, and AgentRun models
  objective: Implement the PlanRun, ContextPack, and AgentRun Pydantic models in atlas.core, including PlanRun's generation_stages field for multi-call provenance (ADR-0010) and ContextPack's input_doc_shas for staleness detection.
  context: PlanRun is the provenance record for every atlas plan / atlas apply cycle. It must carry input_doc_shas, model/provider, prompt version, raw_output_hash, diff summary, approver, status lifecycle (proposed → applied/rejected/failed), similarity_threshold, and the generation_stages list for multi-call provenance per ADR-0010. ContextPack carries the rendered context for a ticket including input_doc_shas so the PM Engine can detect staleness. AgentRun records are created by Atlas from observation (not agent self-report) per ADR-0008. All three models are imported by multiple later phases and must be defined once in atlas.core.
  status: needs_human_decision
  ticket_type: feature
  risk_level: high
  priority: 75
  relevant_docs:
  - docs/atlas/planning-engine-specification.md
  - docs/atlas/planning-large-corpora.md
  - docs/decisions/0010-multi-call-generation-with-single-proposal-reconciliation.md
  - docs/atlas/context-renderer.md
  - docs/atlas/symphony-integration.md
  acceptance_criteria:
  - 'PlanRun model exists with fields: id (UUID), status (PlanRunStatus enum: proposed, applied, rejected, failed), input_doc_shas (dict[str, str]), model_name (str), provider (str), prompt_version (str), prompt_hash (str), raw_output_hash (str), diff_summary (nullable str), similarity_threshold (float), approved_by (nullable str), applied_at (nullable datetime), failure_reason (nullable str), generation_stages (list[GenerationStage]), created_at.'
  - 'GenerationStage model exists with fields: stage_name (str), prompt_version (str), prompt_hash (str), raw_output_hash (str), truncated (bool).'
  - 'PlanRunStatus enum exists in atlas.core.enums with values: proposed, applied, rejected, failed.'
  - 'ContextPack model exists with fields: id (UUID), ticket_id (UUID), rendered_markdown (str), token_estimate (int), input_doc_shas (dict[str, str]), pack_version (str), created_at.'
  - 'AgentRun model exists with fields: id (UUID), ticket_id (UUID), input_context_pack_id (nullable UUID), started_at (datetime), ended_at (nullable datetime), outcome (nullable str), pr_url (nullable str), created_at.'
  - All models are importable from atlas.core with no circular imports and pass mypy strict checks.
  non_goals:
  - Do not implement the planning CLI, reconciler, or prompt renderer — those are Phase 2.
  - Do not implement storage table definitions here.
  - Do not implement context pack generation logic — that is Phase 5.
  - Do not implement AgentRun creation from Linear/GitHub observation — that is Phase 8.
  implementation_notes:
  - Add PlanRunStatus to atlas/core/enums.py.
  - Place all three models in atlas/core/models.py.
  - generation_stages defaults to an empty list for the single-call path.
  - ContextPack.token_estimate is an integer (chars/4 estimator result); precision is not required per the context-renderer design.
  test_requirements:
  - Unit tests construct each model with valid data and assert field values.
  - Unit test confirms PlanRun with an empty generation_stages list is valid (single-call degenerate case).
  - Unit test confirms GenerationStage with truncated=True is valid.
  - Unit test confirms all models are importable from atlas.core without importing higher-layer modules.
  documentation_requirements: []
  definition_of_done:
  - PlanRun, ContextPack, and AgentRun models implemented and importable from atlas.core.
  - All unit tests pass under pytest.
  - ruff and mypy pass with no errors.
  estimated_effort: null
  external_linear_id: 0aba5e29-ac60-4c91-8654-6ea600b423dd
  external_github_issue_id: null
  tags:
  - models
  - core
  - planning
  - provenance
  component: atlas.core
  linear_synced_at: '2026-07-04T18:05:38.538342Z'
  last_observed_linear_state_id: 311a3a97-c409-4cce-96ab-0a3bfc2a5541
  status_entered_at: '2026-07-05T15:53:13.776434Z'
  review_cycle_count: 0
  source_anchor: docs/atlas/planning-engine-specification.md#6-planrun-schema
  created_by_type: agent
  created_by_id: planner
  created_at: '2026-07-04T18:05:38.538342Z'
  updated_at: '2026-07-04T18:05:38.538342Z'
  completed_at: null
- id: a29df0b2-5728-4d7f-b63d-d03240485574
  product_id: 6363ff3f-22db-40b3-9a47-aeba5d0a7586
  epic_id: 29e04b28-877b-4112-9706-73423855c3d8
  key: ATLAS-12
  title: JSON Schema generation from Pydantic models and doc-linter v2 schema validation
  objective: Generate JSON Schema from all atlas.core Pydantic models and extend the doc linter to validate JSON examples in canonical documentation against the generated schemas, so schema drift between docs and code is caught in CI.
  context: 'The roadmap specifies that JSON Schema is generated from the Pydantic models (ATLAS-16) and that doc linter v2 validates JSON examples in canonical docs against those schemas. This closes the loop between the models-as-contract principle and the documentation: any JSON example in a design doc that diverges from the actual model is caught mechanically in CI. The doc linter v1 (Phase 0) already validates ADR structure and MANIFEST links; v2 adds schema validation as an additional check.'
  status: needs_human_decision
  ticket_type: feature
  risk_level: medium
  priority: 60
  relevant_docs:
  - docs/atlas/implementation-roadmap.md
  - docs/atlas/planning-engine-specification.md
  - docs/decisions/0006-source-of-truth-hierarchy.md
  acceptance_criteria:
  - A script or module atlas/tools/generate_schemas.py generates JSON Schema files for all atlas.core models into a designated output directory (e.g. docs/schemas/).
  - The generated schemas are committed to the repository and the generation script is wired into CI so a schema drift (model changed but schema not regenerated) fails CI.
  - The doc linter v2 (extending atlas/tools/doc_linter.py) scans canonical docs for fenced JSON blocks annotated with a schema reference comment and validates each block against the corresponding generated schema.
  - The doc linter v2 fails with a clear error message when a JSON example in a doc does not validate against its schema.
  - The doc linter v2 passes on the existing canonical docs (no spurious failures on valid examples).
  - A seeded bad fixture (a JSON example with a missing required field) causes the doc linter to fail — this is the milestone test.
  non_goals:
  - Do not validate YAML planning renders against schemas — that is a separate concern.
  - Do not implement automatic doc repair or schema migration.
  - Do not add schema validation to the planning proposal parser here — that is the planning engine's gate 1.
  - Do not generate OpenAPI specs — JSON Schema only.
  implementation_notes:
  - Use Pydantic v2's model_json_schema() to generate schemas.
  - 'The annotation convention for doc JSON blocks can be a comment on the line before the fence: <!-- schema: <model-name> -->.'
  - The doc linter should gracefully skip JSON blocks with no schema annotation rather than failing.
  - Schema files should be committed under docs/schemas/ and treated as generated artifacts (not hand-edited).
  test_requirements:
  - Unit test confirms the schema generation script produces valid JSON Schema for at least the Ticket and Evidence models.
  - 'Integration test: the doc linter v2 fails on a fixture doc containing a JSON block with a missing required field.'
  - 'Integration test: the doc linter v2 passes on a fixture doc containing a valid JSON block.'
  - CI step runs schema generation and diffs against committed schemas; fails if they diverge.
  documentation_requirements:
  - Add a brief note to docs/MANIFEST.md (or equivalent) listing docs/schemas/ as a generated artifact written only by the schema generation script.
  definition_of_done:
  - JSON Schema files generated and committed for all atlas.core models.
  - Doc linter v2 implemented and wired into CI.
  - Seeded bad fixture causes linter failure.
  - All tests pass under pytest.
  - ruff and mypy pass with no errors.
  estimated_effort: null
  external_linear_id: 8d2a5d4f-d105-49c6-81e1-feb16498441d
  external_github_issue_id: null
  tags:
  - models
  - schema
  - doc-linter
  - ci
  component: atlas.core
  linear_synced_at: '2026-07-04T18:05:38.538342Z'
  last_observed_linear_state_id: 311a3a97-c409-4cce-96ab-0a3bfc2a5541
  status_entered_at: '2026-07-05T15:53:13.776434Z'
  review_cycle_count: 0
  source_anchor: docs/atlas/implementation-roadmap.md#epic-knowledge-core
  created_by_type: agent
  created_by_id: planner
  created_at: '2026-07-04T18:05:38.538342Z'
  updated_at: '2026-07-04T18:05:38.538342Z'
  completed_at: null
- id: 80b57238-345d-4be3-82c2-df5532807a95
  product_id: 6363ff3f-22db-40b3-9a47-aeba5d0a7586
  epic_id: 29e04b28-877b-4112-9706-73423855c3d8
  key: ATLAS-13
  title: YAML serialisation layer for docs/planning renders
  objective: Implement the YAML serialisation layer in atlas.core that converts the canonical Pydantic models into the docs/planning render format (epics.yaml, tickets.yaml, dependencies.yaml), with a fixed field order, generated header, and round-trip fidelity.
  context: The planning renders written by atlas apply must have a stable, deterministic format. The YAML serialisation layer is the single implementation that converts model instances into the render format; atlas apply calls it and nothing else writes planning YAML. The format includes a generated header (plan_run_id, prompt_version), fixed field ordering for human readability and stable diffs, and natural-key sorting. Round-trip fidelity means a YAML file can be parsed back into model instances without data loss.
  status: needs_human_decision
  ticket_type: feature
  risk_level: medium
  priority: 65
  relevant_docs:
  - docs/atlas/bootstrap-guide.md
  - docs/atlas/planning-engine-specification.md
  - docs/decisions/0006-source-of-truth-hierarchy.md
  acceptance_criteria:
  - A module atlas/core/yaml_serialiser.py (or equivalent) provides functions to serialise a list of Epic models to epics.yaml format, a list of Ticket models to tickets.yaml format, and a list of TicketDependency models to dependencies.yaml format.
  - Each serialised file includes a generated header comment with plan_run_id and prompt_version.
  - Field order in the YAML output is fixed and deterministic (not dict insertion order).
  - Entities are sorted by natural key order (ATLAS-E<n> for epics, ATLAS-<n> for tickets) within each file.
  - A corresponding deserialisation function parses each YAML file back into the appropriate model instances without data loss (round-trip test passes).
  - The serialiser raises a typed error if passed a model instance with a null key (unassigned keys must not appear in renders).
  non_goals:
  - Do not implement the atlas apply command here — that is Phase 2.
  - Do not implement Mermaid DAG rendering — that is Phase 2 (ATLAS-27).
  - Do not implement YAML parsing for the planning proposal — the proposal uses JSON, not YAML.
  - Do not implement any file I/O beyond what is needed for the serialiser functions themselves; file writing is the caller's responsibility.
  implementation_notes:
  - Use PyYAML with default_flow_style=False and explicit field ordering via a custom representer or by constructing an OrderedDict.
  - 'Natural key sorting: sort ATLAS-E1, ATLAS-E2, ..., ATLAS-E10 numerically (not lexicographically).'
  - The generated header is a YAML comment block at the top of the file.
  - Keep the serialiser as a pure function (models in, string out) with no file I/O — the caller (atlas apply) handles writing.
  test_requirements:
  - 'Unit test: serialise a fixture list of Epic models and assert the output YAML contains the expected header and fields in the correct order.'
  - 'Unit test: serialise then deserialise a fixture list of Ticket models and assert the round-trip produces identical model instances.'
  - 'Unit test: serialiser raises a typed error when passed a Ticket with key=None.'
  - 'Unit test: entities are sorted by natural key in the output.'
  documentation_requirements: []
  definition_of_done:
  - YAML serialisation and deserialisation functions implemented in atlas.core.
  - All unit tests pass under pytest.
  - ruff and mypy pass with no errors.
  - Round-trip test passes for all three render types.
  estimated_effort: null
  external_linear_id: 0e90cb66-a978-4cd0-a141-5f4a68243fc9
  external_github_issue_id: null
  tags:
  - serialisation
  - yaml
  - planning
  - core
  component: atlas.core
  linear_synced_at: '2026-07-04T18:05:38.538342Z'
  last_observed_linear_state_id: 311a3a97-c409-4cce-96ab-0a3bfc2a5541
  status_entered_at: '2026-07-05T15:53:13.776434Z'
  review_cycle_count: 0
  source_anchor: docs/atlas/bootstrap-guide.md#10-generated-yaml-shapes
  created_by_type: agent
  created_by_id: planner
  created_at: '2026-07-04T18:05:38.538342Z'
  updated_at: '2026-07-04T18:05:38.538342Z'
  completed_at: null
- id: b3ce446e-f373-432b-8d7a-774526220422
  product_id: 6363ff3f-22db-40b3-9a47-aeba5d0a7586
  epic_id: 29e04b28-877b-4112-9706-73423855c3d8
  key: ATLAS-14
  title: 'Storage layer: SQLAlchemy ORM tables and Alembic migrations'
  objective: Implement the SQLAlchemy ORM table definitions and initial Alembic migration for all atlas.core entities, providing a SQLite-backed (PostgreSQL-compatible) storage layer that is the source of truth for operational state per ADR-0006.
  context: ADR-0006 establishes the Atlas database as the source of truth for operational state. ADR-0002 specifies SQLite locally behind the same SQLAlchemy layer as PostgreSQL. Every entity defined in atlas.core (Product, ADR, Epic, Ticket, TicketDependency, Lesson, Evidence, PlanRun, ContextPack, AgentRun) needs a corresponding ORM table. The storage layer lives in atlas.storage and imports from atlas.core; it must never be imported by atlas.core (layer spine rule). The initial Alembic migration creates all tables from scratch.
  status: needs_human_decision
  ticket_type: infrastructure
  risk_level: high
  priority: 70
  relevant_docs:
  - docs/decisions/0002-use-postgresql-as-source-of-truth.md
  - docs/decisions/0006-source-of-truth-hierarchy.md
  - ARCHITECTURE.md
  - docs/atlas/implementation-roadmap.md
  acceptance_criteria:
  - 'SQLAlchemy ORM table definitions exist in atlas/storage/tables.py for all entities: products, adrs, epics, tickets, ticket_dependencies, lessons, evidence, plan_runs, generation_stages, context_packs, agent_runs, and a key_counters table.'
  - An Alembic migration (initial migration) creates all tables from scratch on a fresh database.
  - The storage layer uses SQLite by default (connection string from config/env) and is PostgreSQL-compatible (no SQLite-specific types used for shared columns).
  - A database session factory (atlas/storage/database.py) provides a context-manager-based session with proper commit/rollback semantics.
  - 'The Evidence table enforces append-only semantics at the ORM level: no update method is exposed on the EvidenceRepository; the ORM mapper has no update path for evidence rows.'
  - 'The key_counters table exists with columns: counter_name (str, primary key), current_value (int); used by the key authority (Phase 2).'
  non_goals:
  - Do not implement repository classes (query methods) in this ticket — those are the next ticket.
  - Do not implement any CLI commands.
  - Do not implement PostgreSQL-specific features (JSONB, etc.) — use JSON type that works on both.
  - Do not implement multi-database routing or connection pooling beyond SQLAlchemy defaults.
  implementation_notes:
  - Use SQLAlchemy 2.x declarative base with mapped_column and Mapped type annotations.
  - Use JSON type (sqlalchemy.types.JSON) for dict fields — compatible with both SQLite and PostgreSQL.
  - Alembic env.py should import the ORM Base metadata for autogenerate support.
  - The key_counters table is the foundation for the key authority (ATLAS-25 in Phase 2); initialise it with rows for 'ticket' and 'epic' counters at value 0.
  - Place ORM definitions in atlas/storage/tables.py; session factory in atlas/storage/database.py; Alembic config in alembic/.
  test_requirements:
  - 'Integration test: run the Alembic migration against an in-memory SQLite database and assert all expected tables exist.'
  - 'Integration test: insert a row into each table and assert it can be retrieved.'
  - 'Unit test: confirm the database session factory provides a working session with rollback on exception.'
  - 'Test: confirm no atlas.core module is imported from atlas.storage (import-linter or manual check).'
  documentation_requirements: []
  definition_of_done:
  - All ORM table definitions implemented in atlas.storage.
  - Initial Alembic migration created and tested against SQLite.
  - All integration tests pass under pytest.
  - ruff and mypy pass with no errors.
  - atlas.storage does not import from any layer above atlas.storage in the spine.
  estimated_effort: null
  external_linear_id: 963b7803-a4e2-4e2a-97b3-2e06db2daa87
  external_github_issue_id: null
  tags:
  - storage
  - sqlalchemy
  - alembic
  - migration
  component: atlas.storage
  linear_synced_at: '2026-07-04T18:05:38.538342Z'
  last_observed_linear_state_id: 311a3a97-c409-4cce-96ab-0a3bfc2a5541
  status_entered_at: '2026-07-05T15:53:13.776434Z'
  review_cycle_count: 0
  source_anchor: docs/decisions/0002-use-postgresql-as-source-of-truth.md#decision
  created_by_type: agent
  created_by_id: planner
  created_at: '2026-07-04T18:05:38.538342Z'
  updated_at: '2026-07-04T18:05:38.538342Z'
  completed_at: null
- id: 89fa3c5f-d6f4-403f-9e2b-afab68906cd1
  product_id: 6363ff3f-22db-40b3-9a47-aeba5d0a7586
  epic_id: 29e04b28-877b-4112-9706-73423855c3d8
  key: ATLAS-15
  title: Repository classes for all entities with append-only enforcement
  objective: Implement repository classes in atlas.storage for all atlas.core entities, providing the typed query and write interface used by all higher layers, with append-only enforcement for Evidence and dangling polymorphic dependency-target detection.
  context: Repository classes are the single interface through which higher layers read and write operational state. Each repository wraps a SQLAlchemy session and exposes typed methods (add, get_by_id, get_by_key, list, etc.). The EvidenceRepository must enforce append-only semantics (no update or delete methods). The TicketDependencyRepository must detect dangling polymorphic targets (a dependency pointing to a non-existent entity). The TicketRepository must expose set_estimated_effort as the single writer of that field, per the dependency-engine design.
  status: needs_human_decision
  ticket_type: feature
  risk_level: high
  priority: 65
  relevant_docs:
  - docs/atlas/dependency-engine.md
  - docs/atlas/evidence-pipeline.md
  - docs/decisions/0006-source-of-truth-hierarchy.md
  - docs/decisions/0008-ci-sourced-evidence-with-trust-tiers.md
  acceptance_criteria:
  - 'Repository classes exist for: ProductRepo, ADRRepo, EpicRepo, TicketRepo, TicketDependencyRepo, LessonRepo, EvidenceRepo, PlanRunRepo, ContextPackRepo, AgentRunRepo.'
  - 'Each repository exposes at minimum: add(entity), get_by_id(id), list() returning typed results.'
  - EvidenceRepo exposes no update or delete methods; attempting to call a non-existent update method raises AttributeError (enforced by not defining it).
  - EvidenceRepo.add rejects any system-tier record missing commit_sha, external_run_id, or payload_hash with a typed EvidenceValidationError.
  - 'EvidenceRepo.add enforces the trust cap: agent-submitted records with status != PENDING are stored as PENDING.'
  - TicketRepo.set_estimated_effort(key, effort) accepts a positive integer or None, rejects <= 0 with EffortValidationError, rejects unknown keys with TicketNotFoundError, and does NOT update updated_at.
  - TicketDependencyRepo detects dangling targets (a dependency whose target UUID does not exist in any entity table) and raises DanglingTargetError.
  non_goals:
  - Do not implement graph projection or readiness detection — those are Phase 3.
  - Do not implement Linear sync or PM Engine logic.
  - Do not implement full-text search or complex query methods beyond what Phase 1 requires.
  - Do not implement the key counter increment logic — that is Phase 2 (key authority).
  implementation_notes:
  - Place repository classes in atlas/storage/repositories.py or a repositories/ subpackage.
  - Use typed exceptions defined in atlas/storage/exceptions.py.
  - Each repository takes a SQLAlchemy Session as a constructor argument for testability.
  - Tests should use an in-memory SQLite database with the Alembic migration applied.
  - The dangling-target check in TicketDependencyRepo queries the tickets, epics, and adrs tables to verify the target UUID exists.
  test_requirements:
  - 'Unit tests for each repository: add an entity, retrieve it by id, assert field values match.'
  - 'Unit test: EvidenceRepo.add with a system-tier record missing commit_sha raises EvidenceValidationError.'
  - 'Unit test: EvidenceRepo.add with an agent-tier record with status=PASSED stores the record with status=PENDING.'
  - 'Unit test: TicketRepo.set_estimated_effort with effort=0 raises EffortValidationError.'
  - 'Unit test: TicketRepo.set_estimated_effort with an unknown key raises TicketNotFoundError.'
  - 'Unit test: TicketDependencyRepo raises DanglingTargetError when adding a dependency whose target does not exist.'
  documentation_requirements: []
  definition_of_done:
  - All repository classes implemented in atlas.storage.
  - All unit tests pass under pytest.
  - ruff and mypy pass with no errors.
  - EvidenceRepo append-only and trust-cap enforcement verified by tests.
  - TicketRepo.set_estimated_effort single-writer contract verified by tests.
  estimated_effort: null
  external_linear_id: 6bf69119-66a8-4edf-b8ba-8cad13627593
  external_github_issue_id: null
  tags:
  - storage
  - repository
  - evidence
  - append-only
  component: atlas.storage
  linear_synced_at: '2026-07-04T18:05:38.538342Z'
  last_observed_linear_state_id: 311a3a97-c409-4cce-96ab-0a3bfc2a5541
  status_entered_at: '2026-07-05T15:53:13.776434Z'
  review_cycle_count: 0
  source_anchor: docs/atlas/implementation-roadmap.md#epic-knowledge-core
  created_by_type: agent
  created_by_id: planner
  created_at: '2026-07-04T18:05:38.538342Z'
  updated_at: '2026-07-04T18:05:38.538342Z'
  completed_at: null
- id: 3b58101e-ed0e-4791-9f14-de655926c028
  product_id: 6363ff3f-22db-40b3-9a47-aeba5d0a7586
  epic_id: 29e04b28-877b-4112-9706-73423855c3d8
  key: ATLAS-16
  title: Model and storage round-trip tests including dangling dependency detection
  objective: Implement a comprehensive test suite that verifies every entity round-trips through YAML serialisation and the database, and that the schema-drift linter fails on a seeded mismatched JSON example — the Phase 1 milestone test.
  context: 'The Phase 1 milestone test is: every entity round-trips through YAML and the database; the schema-drift linter fails on a seeded mismatched JSON example. This ticket implements that milestone test suite as a standalone, CI-enforced test module. It also covers the dangling polymorphic dependency-target detection specified in the roadmap (ATLAS-19). These tests are the acceptance gate for the entire Knowledge Core epic.'
  status: needs_human_decision
  ticket_type: feature
  risk_level: medium
  priority: 55
  relevant_docs:
  - docs/atlas/implementation-roadmap.md
  - docs/atlas/dependency-engine.md
  - docs/atlas/planning-engine-specification.md
  acceptance_criteria:
  - A test module tests/test_knowledge_core_milestone.py (or equivalent) exists and is run in CI.
  - 'Round-trip test: for each entity type (Epic, Ticket, TicketDependency, Lesson, Evidence, PlanRun, ContextPack, AgentRun), a fixture instance is written to an in-memory SQLite database via the repository, retrieved, and asserted equal to the original.'
  - 'YAML round-trip test: Epic, Ticket, and TicketDependency instances are serialised to YAML and deserialised back; the result is asserted equal to the original.'
  - 'Dangling dependency test: adding a TicketDependency whose target UUID does not exist in any entity table raises DanglingTargetError.'
  - 'Schema-drift linter test: a seeded fixture doc containing a JSON block with a missing required field causes the doc linter v2 to fail with a non-zero exit code.'
  - All milestone tests pass in CI on a clean checkout with no pre-existing database.
  non_goals:
  - Do not implement new model or storage logic in this ticket — only tests.
  - Do not test planning engine logic (reconciler, gates) — those are Phase 2 tests.
  - Do not test graph projection or readiness — those are Phase 3 tests.
  - Do not implement performance or load tests.
  implementation_notes:
  - Use pytest fixtures (conftest.py) to provide a fresh in-memory database session for each test.
  - The seeded bad fixture for the schema-drift test can be a temporary file created in the test itself.
  - 'Fixture instances should cover edge cases: nullable fields set to None, lists at minimum length, etc.'
  - The round-trip equality assertion should compare model instances field-by-field, not rely on __eq__ unless it is explicitly defined.
  test_requirements:
  - All tests use pytest fixtures with an in-memory SQLite database and the Alembic migration applied.
  - 'Tests are hermetic: no shared state between test cases.'
  - The schema-drift linter test invokes the doc linter as a subprocess or via its public API and asserts a non-zero exit code on the bad fixture.
  - CI must run this test module as part of the standard test suite.
  documentation_requirements: []
  definition_of_done:
  - All milestone tests implemented and passing in CI.
  - Dangling dependency detection test passes.
  - Schema-drift linter failure test passes.
  - ruff and mypy pass with no errors on the test module.
  estimated_effort: null
  external_linear_id: fc381298-3fdf-4f44-a84a-e0f21e1fe6e2
  external_github_issue_id: null
  tags:
  - testing
  - round-trip
  - storage
  - milestone
  component: null
  linear_synced_at: '2026-07-04T18:05:38.538342Z'
  last_observed_linear_state_id: 311a3a97-c409-4cce-96ab-0a3bfc2a5541
  status_entered_at: '2026-07-05T15:53:13.776434Z'
  review_cycle_count: 0
  source_anchor: docs/atlas/implementation-roadmap.md#phase-1--knowledge-core-models-are-the-single-contract
  created_by_type: agent
  created_by_id: planner
  created_at: '2026-07-04T18:05:38.538342Z'
  updated_at: '2026-07-04T18:05:38.538342Z'
  completed_at: null
- id: 77f7d110-0268-4eca-a3dd-ff7f631f2732
  product_id: 6363ff3f-22db-40b3-9a47-aeba5d0a7586
  epic_id: 5bd967eb-98c0-42a6-bec3-042a34145078
  key: ATLAS-17
  title: Document ingestion and heading-anchor index with git blob SHAs
  objective: Implement the document loader that reads all planning-engine input documents from HEAD, records each document's git blob SHA, and builds a heading-anchor index mapping every valid anchor slug to its document path and heading text.
  context: The planning engine specification (§2.1) requires that atlas plan collects PRODUCT.md, ARCHITECTURE.md, ROADMAP.md, WORKFLOW.md, all accepted ADRs, and all docs/atlas/ documents, recording the git blob SHA of every input. The anchor slug algorithm (§2.3) is GitHub-style and must be the single implementation reused by the doc linter and renderer. Inputs are read from HEAD only; a dirty or untracked working tree fails with a typed error. The anchor index is the foundation for gate 4 (anchor resolution) and AT-5 (provenance).
  status: needs_human_decision
  ticket_type: feature
  risk_level: medium
  priority: 100
  relevant_docs:
  - docs/atlas/planning-engine-specification.md
  - docs/atlas/bootstrap-guide.md
  - docs/decisions/0006-source-of-truth-hierarchy.md
  acceptance_criteria:
  - All specified input document globs are collected and their git blob SHAs recorded.
  - 'The heading-anchor index is built using the GitHub-style slug algorithm (§2.3): lowercase, strip non-[a-z0-9 -], spaces to hyphens, duplicate headings suffixed -1/-2.'
  - Headings inside fenced code blocks are excluded from the index.
  - A dirty or untracked working tree within the input set raises a typed ingestion error and no PlanRun is created.
  - 'The anchor index is queryable: given a slug string, it returns the document path and heading text or raises a typed missing-anchor error.'
  - 'Unit tests cover: slug generation, duplicate heading suffixing, fenced-code-block exclusion, dirty-tree rejection, and SHA recording.'
  non_goals:
  - Do not implement the planner prompt renderer or LLM call.
  - Do not implement the follow-up inbox ingestion (that is a separate consumer).
  - Do not implement domain/ document discovery beyond the specified globs.
  implementation_notes:
  - Use gitpython or subprocess git cat-file to read blob SHAs from HEAD.
  - The slug function must be importable as a standalone utility so the doc linter and renderer can reuse it without importing the full ingestion module.
  - Store the anchor index as a dict[str, ResolvedAnchor] where ResolvedAnchor carries doc_path, heading_text, and blob_sha.
  test_requirements:
  - Unit tests for the slug algorithm against known inputs including duplicates and special characters.
  - Unit test that a fenced code block heading is not indexed.
  - Unit test that a dirty working tree raises the typed error.
  - Integration test that all specified document globs are collected from a fixture repo state.
  documentation_requirements:
  - Docstrings on the public slug function and AnchorIndex class describing the algorithm and error types.
  definition_of_done:
  - All unit and integration tests pass in CI.
  - The slug function is importable from atlas.planning.ingestion.
  - A fixture run against the real Atlas docs directory produces a non-empty anchor index with correct SHAs.
  estimated_effort: null
  external_linear_id: bf8b5801-6a36-4d93-af30-719f1e161699
  external_github_issue_id: null
  tags:
  - planning
  - ingestion
  - anchors
  component: atlas.planning
  linear_synced_at: '2026-07-04T18:05:38.538342Z'
  last_observed_linear_state_id: 311a3a97-c409-4cce-96ab-0a3bfc2a5541
  status_entered_at: '2026-07-05T15:53:13.776434Z'
  review_cycle_count: 0
  source_anchor: docs/atlas/planning-engine-specification.md#21-atlas-plan
  created_by_type: agent
  created_by_id: planner
  created_at: '2026-07-04T18:05:38.538342Z'
  updated_at: '2026-07-04T18:05:38.538342Z'
  completed_at: null
- id: 86051f46-754b-40f9-8856-4c2dbec18ed8
  product_id: 6363ff3f-22db-40b3-9a47-aeba5d0a7586
  epic_id: 5bd967eb-98c0-42a6-bec3-042a34145078
  key: ATLAS-18
  title: Versioned planner prompt renderer with Jinja2 and prompt-hash recording
  objective: Implement the versioned prompt template system that renders the planner prompt from a Jinja2 template with StrictUndefined, validates front-matter variables, and records the prompt hash for PlanRun provenance.
  context: ADR-0007 requires that prompt templates are versioned artifacts referenced by PlanRun. The planning engine specification §2.1 requires the planner prompt to contain the documents, the current backlog, and the output schema. The prompt hash must be recorded so the chain input_doc_shas → prompt_hash → raw_output_hash → proposal → renders is legible. Templates live in atlas/planning/prompts/ and the renderer must default to the current release version.
  status: needs_human_decision
  ticket_type: feature
  risk_level: medium
  priority: 90
  relevant_docs:
  - docs/atlas/planning-engine-specification.md
  - docs/decisions/0007-generative-planning-with-deterministic-reconciliation.md
  - docs/decisions/0010-multi-call-generation-with-single-proposal-reconciliation.md
  acceptance_criteria:
  - Templates are stored under atlas/planning/prompts/ with a version identifier in the filename or front matter.
  - The renderer uses Jinja2 with StrictUndefined so any missing variable raises an error rather than rendering empty.
  - Front-matter variable validation runs before rendering and raises a typed error on missing required variables.
  - The rendered prompt string is hashed (SHA-256) and the hash is returned alongside the rendered text.
  - The renderer accepts a version parameter and defaults to the current release version.
  - 'Unit tests cover: StrictUndefined raising on missing variable, front-matter validation failure, hash stability for identical inputs.'
  non_goals:
  - Do not implement the LLM call itself.
  - Do not implement staged multi-call prompt templates (those are a separate ticket per ADR-0010).
  - Do not implement prompt versioning beyond filename/front-matter convention.
  implementation_notes:
  - Use jinja2.Environment(undefined=StrictUndefined).
  - Front matter can be a YAML block at the top of the template file listing required variables.
  - Return a RenderedPrompt dataclass carrying rendered_text, prompt_hash, and prompt_version.
  test_requirements:
  - Unit test that a template with a missing variable raises a Jinja2 UndefinedError.
  - Unit test that front-matter validation rejects a template missing a required variable declaration.
  - Unit test that the same inputs produce the same hash on repeated calls.
  - Unit test that the version parameter selects the correct template file.
  documentation_requirements:
  - README or docstring in atlas/planning/prompts/ explaining the versioning convention and required front-matter fields.
  definition_of_done:
  - All unit tests pass in CI.
  - A rendered prompt for a fixture document set produces a stable hash.
  - The prompt_version and prompt_hash are accessible for PlanRun recording.
  estimated_effort: null
  external_linear_id: c3dd40cc-9056-4e8c-83b3-c43fbae87e80
  external_github_issue_id: null
  tags:
  - planning
  - prompts
  - provenance
  component: atlas.planning
  linear_synced_at: '2026-07-04T18:05:38.538342Z'
  last_observed_linear_state_id: 311a3a97-c409-4cce-96ab-0a3bfc2a5541
  status_entered_at: '2026-07-05T15:53:13.776434Z'
  review_cycle_count: 0
  source_anchor: docs/atlas/implementation-roadmap.md#epic-generative-planning-with-deterministic-reconciliation
  created_by_type: agent
  created_by_id: planner
  created_at: '2026-07-04T18:05:38.538342Z'
  updated_at: '2026-07-04T18:05:38.538342Z'
  completed_at: null
- id: 872accf2-47f6-4e5d-b482-1301cbc248e6
  product_id: 6363ff3f-22db-40b3-9a47-aeba5d0a7586
  epic_id: 5bd967eb-98c0-42a6-bec3-042a34145078
  key: ATLAS-19
  title: Proposal parsing and validation gates 1–7
  objective: Implement the proposal parser that extracts the JSON proposal from raw model output (tolerating markdown fences and surrounding prose), and the seven validation gates that must all pass before a diff is presented.
  context: 'The planning engine specification §2.1 and §5 define the parser and gates. The parser must tolerate a surrounding markdown code fence or leading/trailing prose, extracting the embedded JSON object; raw_output_hash is over the true raw output. Gates are: (1) schema validity via Pydantic, (2) dependency DAG acyclicity, (3) all dependency targets resolve, (4) every ticket has ≥1 acceptance criterion and a source_anchor resolving to a real heading, (5) no orphan epics and no epic-less tickets unless tech_debt, (6) key integrity (no non-null keys absent from current backlog), (7) size guard (≤7 acceptance criteria, ≤10 dependencies per ticket). A gate failure records a failed PlanRun with a machine-readable failure_reason.'
  status: needs_human_decision
  ticket_type: feature
  risk_level: high
  priority: 85
  relevant_docs:
  - docs/atlas/planning-engine-specification.md
  - docs/decisions/0007-generative-planning-with-deterministic-reconciliation.md
  acceptance_criteria:
  - The parser extracts the JSON object from raw output that includes a surrounding markdown code fence or leading/trailing prose.
  - raw_output_hash is computed over the true raw output string before any extraction.
  - Gate 1 (schema validity) rejects a proposal that fails Pydantic validation with a typed failure reason.
  - Gate 4 rejects any ticket whose source_anchor does not resolve to a real heading in the anchor index at the recorded SHA.
  - Gate 6 rejects any proposal item carrying a non-null key that is absent from the current backlog.
  - Gate 7 rejects any ticket with more than 7 acceptance criteria or more than 10 dependencies.
  - Each gate failure produces a machine-readable failure_reason string identifying the gate number and the offending item.
  non_goals:
  - Do not implement the reconciler (that is a separate ticket).
  - Do not implement PlanRun persistence (that is a separate ticket).
  - Do not implement the LLM call or prompt rendering.
  implementation_notes:
  - Use a string/escape-aware brace-scan extractor (not regex) to find the outermost JSON object, consistent with ATLAS-108's fence-tolerant parsing requirement.
  - Gates 2 and 3 require a projected dependency graph; use NetworkX for acyclicity check.
  - Gate failures should be collected per-item where possible so the operator sees all violations, not just the first.
  test_requirements:
  - 'Unit test for each gate: a fixture proposal that violates exactly that gate fails with the correct failure_reason.'
  - Unit test that a proposal wrapped in a markdown fence is parsed correctly and raw_output_hash matches the fenced string.
  - Unit test that a proposal with all gates passing returns a validated Proposal object.
  - Unit test that gate 4 fails when an anchor is not in the index.
  documentation_requirements:
  - Docstrings on each gate function describing what it checks and what failure_reason it emits.
  definition_of_done:
  - All unit tests pass in CI.
  - A fixture proposal with a known gate violation fails at the correct gate.
  - A valid fixture proposal passes all seven gates and returns a Proposal object.
  estimated_effort: null
  external_linear_id: 72791d5e-f942-40f6-b040-3521659e48c1
  external_github_issue_id: null
  tags:
  - planning
  - validation
  - parsing
  component: atlas.planning
  linear_synced_at: '2026-07-04T18:05:38.538342Z'
  last_observed_linear_state_id: 311a3a97-c409-4cce-96ab-0a3bfc2a5541
  status_entered_at: '2026-07-05T15:53:13.776434Z'
  review_cycle_count: 0
  source_anchor: docs/atlas/planning-engine-specification.md#5-validation-gates
  created_by_type: agent
  created_by_id: planner
  created_at: '2026-07-04T18:05:38.538342Z'
  updated_at: '2026-07-04T18:05:38.538342Z'
  completed_at: null
- id: 8b5da8c0-11ce-4613-b973-9d757d6d918e
  product_id: 6363ff3f-22db-40b3-9a47-aeba5d0a7586
  epic_id: 5bd967eb-98c0-42a6-bec3-042a34145078
  key: ATLAS-20
  title: 'Deterministic reconciler: key/anchor/similarity matching and diff generation'
  objective: Implement the pure deterministic reconciler that matches a validated proposal against the current backlog using key, anchor, and similarity passes, and produces a typed Plan Diff with ADD, MODIFY, PROPOSE_ARCHIVE, and CONFLICT entries.
  context: 'ADR-0007 and planning engine specification §4 define the reconciler. Matching passes in order: (1) key match, (2) anchor match (1:1 unambiguous), (3) similarity match using Sørensen–Dice ≥ DEFAULT_SIMILARITY_THRESHOLD=0.85 over casefold token sets of title+objective. Unmatched proposal items become ADD; unmatched existing items become PROPOSE_ARCHIVE. Duplicate echoed keys are CONFLICT. Tickets with status in_progress, pr_open, review_required, changes_requested, done, or rejected are frozen; any MODIFY or PROPOSE_ARCHIVE touching them becomes CONFLICT. Dependency edges reconcile by resolved (source, target) identity. The reconciler is pure code with no model calls.'
  status: needs_human_decision
  ticket_type: feature
  risk_level: high
  priority: 80
  relevant_docs:
  - docs/atlas/planning-engine-specification.md
  - docs/decisions/0007-generative-planning-with-deterministic-reconciliation.md
  acceptance_criteria:
  - Key match pass correctly identifies proposal items echoing existing backlog keys.
  - Anchor match pass correctly identifies unambiguous 1:1 (anchor, entity_type) pairs; ambiguous groups fall through to similarity.
  - Similarity match uses Sørensen–Dice over casefold token sets; two empty sets score 1.0; threshold is a per-run parameter defaulting to 0.85.
  - Unmatched proposal items produce ADD entries; unmatched existing items produce PROPOSE_ARCHIVE entries.
  - Duplicate echoed keys produce CONFLICT entries naming every claimant.
  - Any MODIFY or PROPOSE_ARCHIVE touching a frozen ticket (in_progress, pr_open, review_required, changes_requested, done, rejected) produces a CONFLICT entry.
  - Dependency edges reconcile by resolved (source, target) identity with ADD/MODIFY/PROPOSE_ARCHIVE/CONFLICT entries following the same immutability rule.
  non_goals:
  - Do not implement key assignment (that is the key authority ticket).
  - Do not implement PlanRun persistence.
  - Do not implement the diff presentation/display (that is the atlas plan CLI ticket).
  - Do not implement similarity matching for dependency edges (edges reconcile by identity only).
  implementation_notes:
  - The Sørensen–Dice implementation must be the single implementation reused by AT-7 content coverage (ATLAS-123/124).
  - 'Normalisation: casefold, replace non-alphanumeric with space, whitespace-split into token set.'
  - Greedy similarity assignment by descending score under a total deterministic order; exact score ties competing for the same item are CONFLICT.
  - The reconciler takes the current backlog as a list of existing entities and the validated Proposal as inputs; it has no I/O.
  test_requirements:
  - 'Unit test for each matching pass with fixtures covering: exact key match, anchor match, similarity match above and below threshold.'
  - Unit test that a frozen ticket touched by MODIFY produces CONFLICT.
  - Unit test that duplicate echoed keys produce CONFLICT naming all claimants.
  - Unit test that the similarity threshold parameter is respected.
  - Unit test for dependency edge reconciliation including frozen-source CONFLICT.
  documentation_requirements:
  - Docstring on the reconcile() function describing the matching pass order, threshold semantics, and immutability rule.
  definition_of_done:
  - All unit tests pass in CI.
  - A fixture reconciliation with known matches and conflicts produces the expected typed diff.
  - The similarity function is importable as a standalone utility.
  estimated_effort: null
  external_linear_id: c69396b0-e11a-43c5-9809-a63555c066e2
  external_github_issue_id: null
  tags:
  - planning
  - reconciler
  - diffing
  component: atlas.planning
  linear_synced_at: '2026-07-04T18:05:38.538342Z'
  last_observed_linear_state_id: 311a3a97-c409-4cce-96ab-0a3bfc2a5541
  status_entered_at: '2026-07-05T15:53:13.776434Z'
  review_cycle_count: 0
  source_anchor: docs/atlas/planning-engine-specification.md#4-reconciler
  created_by_type: agent
  created_by_id: planner
  created_at: '2026-07-04T18:05:38.538342Z'
  updated_at: '2026-07-04T18:05:38.538342Z'
  completed_at: null
- id: eacc2ace-4ca8-4917-a4be-7dc606e91fca
  product_id: 6363ff3f-22db-40b3-9a47-aeba5d0a7586
  epic_id: 5bd967eb-98c0-42a6-bec3-042a34145078
  key: ATLAS-21
  title: 'Key authority: monotonic key counter and assignment on apply only'
  objective: Implement the key authority that assigns monotonic ATLAS-n keys to tickets and ATLAS-En keys to epics on apply, using a persisted counter that never reuses keys including archived ones.
  context: ADR-0007 requires that the model never mints ticket keys; keys are assigned by the reconciler/apply step only. The planning engine specification §2.2 specifies that ADD items receive monotonic ATLAS-n keys from a persisted counter on apply. Tickets take ATLAS-<n> and epics ATLAS-E<n>, each from its own monotonic counter. The counter lives in the key_counters table (knowledge-core.md). The counter must never reuse a key, including for archived items.
  status: needs_human_decision
  ticket_type: feature
  risk_level: high
  priority: 75
  relevant_docs:
  - docs/atlas/planning-engine-specification.md
  - docs/decisions/0007-generative-planning-with-deterministic-reconciliation.md
  acceptance_criteria:
  - A key_counters table exists in the database with separate counters for ticket keys and epic keys.
  - assign_keys(diff) increments the counter and assigns ATLAS-<n> to each ADD ticket and ATLAS-E<n> to each ADD epic in the diff.
  - The counter is incremented atomically within the apply transaction so concurrent calls cannot produce duplicate keys.
  - 'Archived tickets'' keys are never reused: the counter only increments, never resets.'
  - Unit tests verify monotonicity across multiple assign_keys calls including after a simulated archive.
  - The key assignment is the only place new ATLAS-<n> keys are created; no other code path mints keys.
  non_goals:
  - Do not implement the full atlas apply command (that is a separate ticket).
  - Do not implement Linear key sync.
  - Do not implement key validation beyond the counter (gate 6 in the parser handles that).
  implementation_notes:
  - Use a SELECT FOR UPDATE or equivalent SQLAlchemy pattern to ensure atomic counter increment.
  - The key_counters table should have rows keyed by counter_name ('ticket', 'epic') with a current_value integer.
  - Return a mapping from new:<n> placeholder to assigned key for use by the apply step.
  test_requirements:
  - Unit test that two sequential assign_keys calls produce non-overlapping key sets.
  - Unit test that the counter persists across process restarts (using a test database).
  - Unit test that archived keys are not reused after the counter advances past them.
  documentation_requirements:
  - Docstring on assign_keys describing the atomicity guarantee and the no-reuse invariant.
  definition_of_done:
  - All unit tests pass in CI.
  - The key_counters table migration is applied and the counter increments correctly in a test database.
  - No other code path in the codebase mints ATLAS-<n> keys.
  estimated_effort: null
  external_linear_id: 0161c32a-e520-4d05-81c3-06034b79d3fb
  external_github_issue_id: null
  tags:
  - planning
  - key-authority
  - identity
  component: atlas.planning
  linear_synced_at: '2026-07-04T18:05:38.538342Z'
  last_observed_linear_state_id: 311a3a97-c409-4cce-96ab-0a3bfc2a5541
  status_entered_at: '2026-07-05T15:53:13.776434Z'
  review_cycle_count: 0
  source_anchor: docs/atlas/planning-engine-specification.md#22-atlas-apply
  created_by_type: agent
  created_by_id: planner
  created_at: '2026-07-04T18:05:38.538342Z'
  updated_at: '2026-07-04T18:05:38.538342Z'
  completed_at: null
- id: 5ecb0d9c-76f9-470f-a761-ad5c5977559d
  product_id: 6363ff3f-22db-40b3-9a47-aeba5d0a7586
  epic_id: 5bd967eb-98c0-42a6-bec3-042a34145078
  key: ATLAS-22
  title: '`atlas plan` CLI command: propose and print diff, never write renders'
  objective: Implement the atlas plan CLI command that orchestrates document ingestion, prompt rendering, LLM call, proposal parsing, validation gates, reconciliation, PlanRun persistence at status:proposed, and diff printing — without writing any planning renders.
  context: 'The planning engine specification §2.1 defines the full atlas plan flow. The command must: collect inputs and record SHAs, load the current backlog, render the prompt, call the configured model, parse the proposal (tolerating fences), run all seven gates, run the reconciler, persist a PlanRun at status:proposed, and print the diff. It must never write to docs/planning/. Failures before raw output exists exit with a typed message and no PlanRun; failures after raw output produce a failed PlanRun with machine-readable failure_reason.'
  status: needs_human_decision
  ticket_type: feature
  risk_level: high
  priority: 70
  relevant_docs:
  - docs/atlas/planning-engine-specification.md
  - docs/atlas/bootstrap-guide.md
  - docs/decisions/0007-generative-planning-with-deterministic-reconciliation.md
  acceptance_criteria:
  - atlas plan collects all specified input documents, records their git blob SHAs, and fails with a typed error on a dirty working tree.
  - atlas plan calls the configured model and records raw_output_hash over the true raw output.
  - atlas plan runs all seven validation gates; a gate failure persists a failed PlanRun with the machine-readable failure_reason and exits non-zero.
  - atlas plan runs the reconciler and prints the diff in the format specified by §2.4 (summary line first, then one block per entry).
  - atlas plan persists a PlanRun at status:proposed on success.
  - atlas plan never writes to docs/planning/ under any code path.
  - 'A truncation (stop_reason: max_tokens) is recorded as a failed PlanRun with a distinct truncation failure_reason.'
  non_goals:
  - Do not implement atlas apply in this ticket.
  - Do not implement staged multi-call generation (ADR-0010); single-call only for this ticket.
  - Do not implement the follow-up inbox as a plan input source.
  - Do not implement Linear writes.
  implementation_notes:
  - Use Typer for the CLI command.
  - The model client should be injectable for testing (pass a stub that returns fixture JSON).
  - 'The diff presentation must match §2.4: summary counts line, then per-entry blocks with type, key/new:<n>, title, anchor, and for MODIFY a per-field before/after list.'
  test_requirements:
  - 'Integration test using a fixture model stub that returns a valid proposal: verify PlanRun is persisted at status:proposed and diff is printed.'
  - 'Integration test using a fixture model stub that returns a gate-failing proposal: verify PlanRun is persisted at status:failed with correct failure_reason.'
  - Unit test that atlas plan exits non-zero on a dirty working tree.
  - Unit test that docs/planning/ is not written under any code path.
  documentation_requirements:
  - Update docs/runbooks/ or CLI --help to describe atlas plan usage and failure modes.
  definition_of_done:
  - All integration and unit tests pass in CI.
  - Running atlas plan against the real Atlas docs with a live model produces a printed diff and a PlanRun row.
  - docs/planning/ is confirmed unmodified after atlas plan runs.
  estimated_effort: null
  external_linear_id: 3c82d896-67c4-40ec-b86f-90c50f0d025d
  external_github_issue_id: null
  tags:
  - planning
  - cli
  - plan-command
  component: atlas.cli
  linear_synced_at: '2026-07-04T18:05:38.538342Z'
  last_observed_linear_state_id: 311a3a97-c409-4cce-96ab-0a3bfc2a5541
  status_entered_at: '2026-07-05T15:53:13.776434Z'
  review_cycle_count: 0
  source_anchor: docs/atlas/planning-engine-specification.md#21-atlas-plan
  created_by_type: agent
  created_by_id: planner
  created_at: '2026-07-04T18:05:38.538342Z'
  updated_at: '2026-07-04T18:05:38.538342Z'
  completed_at: null
- id: efa14ba6-fd67-48c5-8e25-4099c417cd9d
  product_id: 6363ff3f-22db-40b3-9a47-aeba5d0a7586
  epic_id: 5bd967eb-98c0-42a6-bec3-042a34145078
  key: ATLAS-23
  title: '`atlas apply` CLI command: diff review, confirmation, render writes, and PlanRun finalisation'
  objective: Implement the atlas apply CLI command that loads the most recent proposed PlanRun, checks for staleness, presents the diff for operator confirmation, assigns keys to ADD items, writes planning renders, regenerates roadmap.mmd, and finalises the PlanRun to status:applied.
  context: 'The planning engine specification §2.2 defines the full atlas apply flow. Apply must: load the latest proposed PlanRun, refuse if input doc SHAs no longer match (stale plan), display the diff and require explicit confirmation (interactive y/N or --yes), assign keys atomically, write docs/planning/epics.yaml, tickets.yaml, dependencies.yaml, and roadmap.mmd, then finalise the PlanRun. The DB commit (counter increment + backlog rows + finalising transition) is the single linearisation point; renders are written to temp files and atomically moved after the commit. Milestone 1 apply materialises ADD items only; MODIFY and PROPOSE_ARCHIVE are excluded from renders; a diff containing MODIFY is refused.'
  status: needs_human_decision
  ticket_type: feature
  risk_level: high
  priority: 65
  relevant_docs:
  - docs/atlas/planning-engine-specification.md
  - docs/decisions/0006-source-of-truth-hierarchy.md
  - docs/decisions/0007-generative-planning-with-deterministic-reconciliation.md
  acceptance_criteria:
  - atlas apply refuses with a typed error if no proposed PlanRun exists.
  - atlas apply refuses with a typed error if fresh ingestion yields input doc SHAs that differ from the recorded input_doc_shas (stale plan).
  - atlas apply requires explicit confirmation (interactive y/N or --yes); with neither a TTY nor --yes it refuses rather than assuming consent.
  - The DB commit (key counter increment + backlog rows + PlanRun finalisation) is atomic in one transaction.
  - Renders are written to temp files and atomically moved into docs/planning/ only after the DB commit succeeds.
  - A diff containing MODIFY entries is refused (not applied with invented semantics).
  - A diff touching a frozen ticket (CONFLICT) is refused.
  non_goals:
  - Do not implement MODIFY application (deferred per §8).
  - Do not implement PROPOSE_ARCHIVE application (items are excluded from renders, not deleted).
  - Do not implement Linear writes.
  - Do not implement the follow-up inbox processed/ move (that is a separate consumer ticket).
  implementation_notes:
  - Use Python's os.replace() for atomic file moves.
  - The Mermaid DAG render (roadmap.mmd) reuses the renderer from atlas/planning/mermaid.py.
  - Rejecting a diff sets PlanRun status to rejected.
  test_requirements:
  - 'Integration test: a valid proposed PlanRun with only ADD entries is applied; renders are written; PlanRun is finalised to applied.'
  - 'Unit test: stale plan detection refuses apply when a doc SHA has changed.'
  - 'Unit test: a diff with MODIFY entries is refused.'
  - 'Unit test: a diff with CONFLICT entries is refused.'
  - 'Unit test: without --yes and without a TTY, apply refuses.'
  - 'Unit test: a crash after DB commit but before render move is repaired by re-running apply.'
  documentation_requirements:
  - Update CLI --help and docs/runbooks/ to describe atlas apply usage, the staleness check, and the atomicity guarantee.
  definition_of_done:
  - All integration and unit tests pass in CI.
  - Running atlas apply after a successful atlas plan produces correct YAML renders and a finalised PlanRun.
  - A simulated crash after DB commit is repaired by re-running apply.
  estimated_effort: null
  external_linear_id: c7896bf8-7d68-4589-a7ba-2b3a2f55916f
  external_github_issue_id: null
  tags:
  - planning
  - cli
  - apply-command
  component: atlas.cli
  linear_synced_at: '2026-07-04T18:05:38.538342Z'
  last_observed_linear_state_id: 311a3a97-c409-4cce-96ab-0a3bfc2a5541
  status_entered_at: '2026-07-05T15:53:13.776434Z'
  review_cycle_count: 0
  source_anchor: docs/atlas/planning-engine-specification.md#22-atlas-apply
  created_by_type: agent
  created_by_id: planner
  created_at: '2026-07-04T18:05:38.538342Z'
  updated_at: '2026-07-04T18:05:38.538342Z'
  completed_at: null
- id: f47b316d-3138-4393-a924-f43067a8c5e0
  product_id: 6363ff3f-22db-40b3-9a47-aeba5d0a7586
  epic_id: 5bd967eb-98c0-42a6-bec3-042a34145078
  key: ATLAS-24
  title: PlanRun persistence and provenance recording
  objective: 'Implement the PlanRun model, database table, and repository that records full provenance for each planning run: input doc SHAs, model/provider, prompt version and hash, raw output hash, diff summary, approver, and status lifecycle.'
  context: ADR-0007 requires a PlanRun entity to record provenance. The planning engine specification §6 defines the PlanRun schema (deferred to data-model-and-schemas.md §3.10). A PlanRun row is inserted at status:proposed; exactly one finalising transition to applied, rejected, or failed is permitted. All other fields are immutable after insert; rows are never deleted. The PlanRun must record input_doc_shas, model, provider, prompt_version, prompt_hash, raw_output_hash, diff_summary, approved_by, applied_at, and failure_reason.
  status: needs_human_decision
  ticket_type: feature
  risk_level: high
  priority: 60
  relevant_docs:
  - docs/atlas/planning-engine-specification.md
  - docs/decisions/0007-generative-planning-with-deterministic-reconciliation.md
  - docs/decisions/0006-source-of-truth-hierarchy.md
  acceptance_criteria:
  - 'The PlanRun SQLAlchemy model and Alembic migration exist with all required fields: id, status, input_doc_shas (JSON), model, provider, prompt_version, prompt_hash, raw_output_hash, diff_summary, approved_by, applied_at, failure_reason, created_at.'
  - PlanRunRepo.insert() creates a row at status:proposed and returns the PlanRun id.
  - PlanRunRepo.finalise() transitions status to applied, rejected, or failed; a second call raises a typed error (immutable after finalisation).
  - PlanRunRepo.latest_proposed() returns the most recent proposed PlanRun or None.
  - Rows are never deleted; the repository has no delete method.
  - 'Unit tests cover: insert, finalise to each terminal status, double-finalise rejection, latest_proposed with zero and multiple rows.'
  non_goals:
  - Do not implement the generation_stages field for multi-call provenance (that is ATLAS-105, a separate ticket).
  - Do not implement PlanRun querying beyond latest_proposed.
  - Do not implement a PlanRun CLI command.
  implementation_notes:
  - input_doc_shas should be stored as a JSON column mapping doc_path to blob_sha.
  - diff_summary stores the counts per diff entry type (ADD, MODIFY, PROPOSE_ARCHIVE, CONFLICT) as a JSON object.
  - Use SQLAlchemy mapped_column with server_default for created_at.
  test_requirements:
  - Unit test that insert() creates a row at status:proposed.
  - Unit test that finalise() to applied sets approved_by and applied_at.
  - Unit test that a second finalise() call raises a typed ImmutablePlanRunError.
  - Unit test that latest_proposed() returns None when no proposed runs exist.
  - Unit test that rows survive a process restart (using a test SQLite database).
  documentation_requirements:
  - Docstrings on PlanRunRepo methods describing the immutability contract and the finalising transition rules.
  definition_of_done:
  - All unit tests pass in CI.
  - The Alembic migration applies cleanly to a fresh database.
  - A PlanRun row inserted and finalised in a test database round-trips correctly through the repository.
  estimated_effort: null
  external_linear_id: aa2bdcf7-a1bd-4cd5-8116-913980592551
  external_github_issue_id: null
  tags:
  - planning
  - provenance
  - planrun
  - storage
  component: atlas.planning
  linear_synced_at: '2026-07-04T18:05:38.538342Z'
  last_observed_linear_state_id: 311a3a97-c409-4cce-96ab-0a3bfc2a5541
  status_entered_at: '2026-07-05T15:53:13.776434Z'
  review_cycle_count: 0
  source_anchor: docs/atlas/planning-engine-specification.md#6-planrun-schema
  created_by_type: agent
  created_by_id: planner
  created_at: '2026-07-04T18:05:38.538342Z'
  updated_at: '2026-07-04T18:05:38.538342Z'
  completed_at: null
- id: e2a0d8c1-34ae-4ba8-b661-27c0ccc35062
  product_id: 6363ff3f-22db-40b3-9a47-aeba5d0a7586
  epic_id: 5bd967eb-98c0-42a6-bec3-042a34145078
  key: ATLAS-25
  title: Staged planner prompt templates for multi-call generation (epics, tickets-per-epic, dependencies)
  objective: Implement the three versioned per-stage prompt templates (epics, tickets-per-epic, dependencies) as projections of the §3.11 proposal contract, enabling bounded multi-call generation per ADR-0010.
  context: 'ADR-0010 and planning-large-corpora.md §4 define staged generation. Three bounded stages are needed: (1) epics-only, (2) tickets for one epic seeded with the assembled epic list, (3) dependencies seeded with the assembled ticket list. Each template is a versioned Jinja2 artifact in atlas/planning/prompts/. The per-stage emission schemas are projections of §3.11. The model references environment-assigned indices (new_epic:<n>, new:<n>) but never mints them. Templates must include anti-copy instructions to prevent the model from copying roadmap keys.'
  status: needs_human_decision
  ticket_type: feature
  risk_level: medium
  priority: 55
  relevant_docs:
  - docs/atlas/planning-large-corpora.md
  - docs/decisions/0010-multi-call-generation-with-single-proposal-reconciliation.md
  - docs/atlas/planning-engine-specification.md
  acceptance_criteria:
  - 'Three versioned template files exist in atlas/planning/prompts/: stage-epics, stage-tickets, stage-dependencies, each with a version identifier.'
  - The stage-epics template instructs the model to emit only the epics projection of §3.11 with key:null for new epics.
  - The stage-tickets template is seeded with the assembled epic list (new_epic:<n> indices) and instructs the model to emit tickets for exactly one epic with epic_ref referencing the given index.
  - The stage-dependencies template is seeded with the assembled ticket list (new:<n> indices) and instructs the model to emit dependency edges referencing those indices.
  - 'All templates include an explicit anti-copy instruction: new items must use null keys, never copy ATLAS-<n> keys from the corpus.'
  - The stage-tickets template includes a null-key example showing key:null (not a roadmap key).
  non_goals:
  - Do not implement the multi-call orchestration logic (that is a separate ticket).
  - Do not implement re-plan seeding with existing backlog (that is ATLAS-144, a later ticket).
  - Do not implement the single-call template (that already exists or is covered by the prompt renderer ticket).
  implementation_notes:
  - Follow the existing prompts README versioning convention (e.g. planner-stage-epics-v1.0.0).
  - The stage-tickets template must make clear that key must be null for new tickets and must show a concrete null-key example.
  - Templates should specify the exact JSON output schema for their stage projection.
  test_requirements:
  - Unit test that each template renders without error given fixture variables.
  - Unit test that the stage-tickets template rendered with a specific epic index produces output referencing that index.
  - Unit test that the anti-copy instruction is present in each template.
  documentation_requirements:
  - Update atlas/planning/prompts/README with the three new templates, their version identifiers, and the seeding variables each requires.
  definition_of_done:
  - All three template files exist and render correctly in unit tests.
  - The prompts README documents the new templates.
  - CI passes with the new template files present.
  estimated_effort: null
  external_linear_id: 0575166b-7b7f-4c18-9eac-23a5d516bf9c
  external_github_issue_id: null
  tags:
  - planning
  - prompts
  - staged-generation
  component: atlas.planning
  linear_synced_at: '2026-07-04T18:05:38.538342Z'
  last_observed_linear_state_id: 311a3a97-c409-4cce-96ab-0a3bfc2a5541
  status_entered_at: '2026-07-05T15:53:13.776434Z'
  review_cycle_count: 0
  source_anchor: docs/atlas/planning-large-corpora.md#4-recommended-design-staged-generation-single-proposal-reconciliation
  created_by_type: agent
  created_by_id: planner
  created_at: '2026-07-04T18:05:38.538342Z'
  updated_at: '2026-07-04T18:05:38.538342Z'
  completed_at: null
- id: fa7784c7-38de-47ad-908a-10c7d91bf8b9
  product_id: 6363ff3f-22db-40b3-9a47-aeba5d0a7586
  epic_id: 5bd967eb-98c0-42a6-bec3-042a34145078
  key: ATLAS-26
  title: 'Multi-call generation orchestration: environment-owned index assembly into one full-state proposal'
  objective: Implement the multi-call generation orchestrator that runs the three staged prompts in sequence, assembles environment-owned new_epic:<n> and new:<n> indices, and concatenates the stage outputs into one §3.11 full-state proposal for the existing parser, gates, and reconciler.
  context: ADR-0010 and planning-large-corpora.md §4/§4.1 define the orchestration. The environment assigns new_epic:<n> by stage-1 emission order and new:<n> by assembled position in stage-2. The assembled proposal is one §3.11 envelope passed unchanged to the existing parser. The orchestrator must handle per-stage truncation detection (ATLAS-101 logic per stage) and directed retry on projection-validation failure (up to 3 attempts, telling the model what it violated). The PlanRun records each stage's prompt_version, prompt_hash, and raw_output_hash in generation_stages.
  status: needs_human_decision
  ticket_type: feature
  risk_level: high
  priority: 50
  relevant_docs:
  - docs/atlas/planning-large-corpora.md
  - docs/decisions/0010-multi-call-generation-with-single-proposal-reconciliation.md
  - docs/atlas/planning-engine-specification.md
  acceptance_criteria:
  - The orchestrator runs stage 1 (epics), assigns new_epic:<n> indices by emission order, and passes the indexed list to stage 2.
  - The orchestrator runs stage 2 once per epic, assigns new:<n> indices by assembled position across all batches, and passes the full indexed ticket list to stage 3.
  - The orchestrator runs stage 3 (dependencies) and assembles the final §3.11 proposal envelope.
  - A stage that returns stop_reason:max_tokens is recorded as a truncation failure naming the stage; the run fails honestly.
  - 'Directed retry on projection-validation failure: up to 3 attempts per stage, the model told what it violated; truncation and json-decode errors do not retry.'
  - The assembled proposal is passed unchanged to the existing parser, gates, and reconciler.
  non_goals:
  - Do not modify the parser, gates, or reconciler.
  - Do not implement re-plan seeding with existing backlog (ATLAS-144).
  - Do not implement batch-sizing heuristics beyond one epic per stage-2 call.
  implementation_notes:
  - 'The orchestrator is a pure function: stage_outputs_in, assembled_proposal_out, no I/O beyond model calls.'
  - 'Index assignment is deterministic: new_epic:<n> by list position from stage 1, new:<n> by list position in the concatenated stage-2 batches.'
  - Each stage's (prompt_version, prompt_hash, raw_output_hash) must be returned for PlanRun generation_stages recording.
  test_requirements:
  - 'Integration test with fixture model stubs for all three stages: verify the assembled proposal has correct new_epic:<n> and new:<n> references.'
  - Unit test that a stage-1 truncation produces a typed failure naming stage 1.
  - Unit test that directed retry is attempted up to 3 times on a projection-validation failure and then fails.
  - Unit test that the assembled proposal JSON is valid §3.11 structure.
  documentation_requirements:
  - Docstring on the orchestrator function describing the index assignment rules and the assembly order.
  definition_of_done:
  - All integration and unit tests pass in CI.
  - A fixture run through the orchestrator produces a valid §3.11 proposal with correct index references.
  - Truncation and retry paths are exercised by tests.
  estimated_effort: null
  external_linear_id: c400feb2-9f9f-4746-ba2e-c8c5b04ac02b
  external_github_issue_id: null
  tags:
  - planning
  - staged-generation
  - orchestration
  component: atlas.planning
  linear_synced_at: '2026-07-04T18:05:38.538342Z'
  last_observed_linear_state_id: 311a3a97-c409-4cce-96ab-0a3bfc2a5541
  status_entered_at: '2026-07-05T15:53:13.776434Z'
  review_cycle_count: 0
  source_anchor: docs/atlas/planning-large-corpora.md#41-environment-owned-index-assembly-worked-example
  created_by_type: agent
  created_by_id: planner
  created_at: '2026-07-04T18:05:38.538342Z'
  updated_at: '2026-07-04T18:05:38.538342Z'
  completed_at: null
- id: c058ef40-e318-44b1-b524-d630b5d6966b
  product_id: 6363ff3f-22db-40b3-9a47-aeba5d0a7586
  epic_id: 5bd967eb-98c0-42a6-bec3-042a34145078
  key: ATLAS-27
  title: 'PlanRun multi-call provenance: generation_stages field, migration, and schema regen'
  objective: Add the generation_stages field to the PlanRun model and database table to record per-stage provenance (prompt_version, prompt_hash, raw_output_hash) for multi-call generation, with the single-call path as the degenerate one-stage case.
  context: ADR-0010 §consequences and planning-large-corpora.md §5.3 require the PlanRun to gain a generation_stages field. Each stage records its (prompt_version, prompt_hash, raw_output_hash). The PlanRun also keeps a raw_output_hash over the assembled proposal. The single-call path is one stage in the list. This requires an Alembic migration and JSON Schema regeneration.
  status: needs_human_decision
  ticket_type: feature
  risk_level: medium
  priority: 45
  relevant_docs:
  - docs/atlas/planning-large-corpora.md
  - docs/decisions/0010-multi-call-generation-with-single-proposal-reconciliation.md
  - docs/atlas/planning-engine-specification.md
  acceptance_criteria:
  - 'The PlanRun model gains a generation_stages field: a JSON list of per-stage records each carrying prompt_version, prompt_hash, and raw_output_hash.'
  - An Alembic migration adds the generation_stages column (nullable JSON, defaulting to an empty list for existing rows).
  - The single-call path records a one-element generation_stages list.
  - The multi-call path records one element per stage in emission order.
  - JSON Schema is regenerated from the updated Pydantic models.
  - Unit tests verify that generation_stages round-trips through the database for both single-call and multi-call cases.
  non_goals:
  - Do not implement the orchestrator itself (that is a separate ticket).
  - Do not implement a CLI command to display generation_stages.
  - Do not change any other PlanRun fields.
  implementation_notes:
  - Store generation_stages as a JSON column (list of dicts).
  - Define a GenerationStage Pydantic model with prompt_version, prompt_hash, raw_output_hash fields.
  - The migration must be additive (nullable column with default) to not break existing rows.
  test_requirements:
  - Unit test that a PlanRun with a one-element generation_stages list round-trips correctly.
  - Unit test that a PlanRun with a three-element generation_stages list round-trips correctly.
  - Migration test that the Alembic migration applies cleanly to a database with existing PlanRun rows.
  documentation_requirements:
  - Update the PlanRun model docstring to describe the generation_stages field and the single-call degenerate case.
  definition_of_done:
  - All unit and migration tests pass in CI.
  - The Alembic migration applies cleanly to a fresh and an existing database.
  - JSON Schema reflects the updated model.
  estimated_effort: null
  external_linear_id: f33568cf-fda9-4fb9-ae56-d2f0ec95f29b
  external_github_issue_id: null
  tags:
  - planning
  - provenance
  - planrun
  - migration
  component: atlas.planning
  linear_synced_at: '2026-07-04T18:05:38.538342Z'
  last_observed_linear_state_id: 311a3a97-c409-4cce-96ab-0a3bfc2a5541
  status_entered_at: '2026-07-05T15:53:13.776434Z'
  review_cycle_count: 0
  source_anchor: docs/atlas/planning-large-corpora.md#53-provenance-across-multiple-calls
  created_by_type: agent
  created_by_id: planner
  created_at: '2026-07-04T18:05:38.538342Z'
  updated_at: '2026-07-04T18:05:38.538342Z'
  completed_at: null
- id: 2ed09947-aa07-40ba-8aca-72358525625d
  product_id: 6363ff3f-22db-40b3-9a47-aeba5d0a7586
  epic_id: 5bd967eb-98c0-42a6-bec3-042a34145078
  key: ATLAS-28
  title: Per-stage truncation handling and batch sizing for staged generation
  objective: 'Implement per-stage truncation detection (stop_reason: max_tokens) that fails honestly naming the stage, and a batch-sizing heuristic that keeps each stage-2 call well inside the 64K output ceiling.'
  context: Planning-large-corpora.md §5.4 and §7 require truncation detection per stage and a batch-sizing mechanism. ATLAS-101's stop_reason==max_tokens detection applies per stage. Per-epic ticket batching keeps each call far inside 64K; a pathologically large epic that still exceeds the bound fails honestly naming the stage. The batch-sizing heuristic is the lever that prevents truncation in normal operation.
  status: needs_human_decision
  ticket_type: feature
  risk_level: medium
  priority: 40
  relevant_docs:
  - docs/atlas/planning-large-corpora.md
  - docs/decisions/0010-multi-call-generation-with-single-proposal-reconciliation.md
  acceptance_criteria:
  - Each stage call checks stop_reason; a max_tokens stop_reason raises a typed StageTruncationError naming the stage index and stage type.
  - A StageTruncationError causes the PlanRun to be recorded as failed with a failure_reason that names the stage.
  - 'The batch-sizing heuristic estimates the output token budget for a stage-2 call and refuses to dispatch a call estimated to exceed a configurable ceiling (default: 50K tokens).'
  - The token estimator uses chars/4 (consistent with the context renderer's estimator).
  - 'Unit tests cover: truncation detection for each stage type, batch-size refusal when estimated tokens exceed ceiling.'
  non_goals:
  - Do not implement automatic epic splitting when a batch is too large (that is a follow-up).
  - Do not implement dynamic ceiling adjustment.
  - Do not implement truncation recovery (a truncated stage fails the run).
  implementation_notes:
  - StageTruncationError should carry stage_index, stage_type, and the raw partial output for provenance.
  - The batch-size estimator should estimate the expected output size based on the number of tickets in the epic and a per-ticket token estimate derived from the prompt template.
  - The ceiling is a module constant with a config override.
  test_requirements:
  - Unit test that a stage-1 response with stop_reason:max_tokens raises StageTruncationError naming stage 1.
  - Unit test that a stage-2 response with stop_reason:max_tokens raises StageTruncationError naming the epic batch.
  - Unit test that a batch estimated above the ceiling is refused before the model call.
  documentation_requirements:
  - Docstring on the truncation detection function describing the failure contract.
  definition_of_done:
  - All unit tests pass in CI.
  - A fixture run with a simulated max_tokens response produces a failed PlanRun with the correct failure_reason.
  - The batch-size refusal is exercised by a unit test.
  estimated_effort: null
  external_linear_id: 8fa83e70-177f-4ada-bca4-950e884f7969
  external_github_issue_id: null
  tags:
  - planning
  - staged-generation
  - truncation
  component: atlas.planning
  linear_synced_at: '2026-07-04T18:05:38.538342Z'
  last_observed_linear_state_id: 311a3a97-c409-4cce-96ab-0a3bfc2a5541
  status_entered_at: '2026-07-05T15:53:13.776434Z'
  review_cycle_count: 0
  source_anchor: docs/atlas/planning-large-corpora.md#54-truncation-within-a-stage
  created_by_type: agent
  created_by_id: planner
  created_at: '2026-07-04T18:05:38.538342Z'
  updated_at: '2026-07-04T18:05:38.538342Z'
  completed_at: null
- id: a6184394-931b-4dd1-8507-56758aa2878b
  product_id: 6363ff3f-22db-40b3-9a47-aeba5d0a7586
  epic_id: 5bd967eb-98c0-42a6-bec3-042a34145078
  key: ATLAS-29
  title: Acceptance test suite AT-1 through AT-7 against the seeded Atlas documents
  objective: Implement the full acceptance test suite AT-1..AT-7 as defined in the planning engine specification §7, running against the seeded Atlas documents in CI, covering both single-call and staged-generation paths.
  context: 'The planning engine specification §7 defines seven acceptance tests that constitute Milestone 1. AT-1: validity (all gates pass, acyclic DAG, every ticket traceable). AT-2: stability (two runs on unchanged docs yield empty or MODIFY-only diff with similarity ≥0.95). AT-3: locality (editing one section produces anchored diff). AT-4: immutability (in_progress/done ticket touched → CONFLICT, apply refuses). AT-5: provenance (PlanRun input_doc_shas match git tree; stale plan refused). AT-6: key authority (no model-minted keys; counter monotonic). AT-7: reference corpus coverage (anchor_coverage ≥ 0.50 AND unified exact∪content ≥ 0.80, containment-aware, citation excluded). AT-7 uses the implementation roadmap as the reference corpus.'
  status: needs_human_decision
  ticket_type: feature
  risk_level: high
  priority: 35
  relevant_docs:
  - docs/atlas/planning-engine-specification.md
  - docs/atlas/planning-large-corpora.md
  - docs/decisions/0007-generative-planning-with-deterministic-reconciliation.md
  acceptance_criteria:
  - 'AT-1 passes: atlas plan on the seeded Atlas docs produces a proposal passing all seven gates with an acyclic DAG and every ticket having a valid source_anchor.'
  - 'AT-2 passes: two sequential atlas plan runs on unchanged docs produce a reconciled diff that is empty or contains only MODIFY entries with similarity ≥ 0.95 on free-text fields.'
  - 'AT-4 passes: a fixture with an in_progress ticket touched by the proposal surfaces as CONFLICT and atlas apply refuses it.'
  - 'AT-5 passes: a PlanRun''s input_doc_shas match the git tree at apply time; atlas apply refuses a plan after a doc edit.'
  - 'AT-6 passes: no key in any applied backlog originates from the model; the key counter is monotonic across archives.'
  - 'AT-7 passes: anchor_coverage ≥ ANCHOR_COVERAGE_FLOOR=0.50 AND unified (exact∪content) ≥ UNIFIED_COVERAGE_FLOOR=0.80 against the implementation roadmap, with containment-aware content coverage and citation excluded from the gate.'
  non_goals:
  - Do not implement AT-3 as an automated CI test (it requires a doc edit fixture; mark as manual or implement as a fixture-based test).
  - Do not implement AT-7 content-coverage bar pinning (ATLAS-124); report content_coverage but gate only on the exact-anchor floor and the 0.80 unified floor.
  - Do not implement the scripts/at7_miss_analysis.py offline tool in this ticket.
  implementation_notes:
  - AT-7 requires the anchor_coverage and content_coverage functions from the acceptance suite; implement them as standalone functions in atlas/planning/acceptance.py.
  - The Sørensen–Dice and containment functions must be the same implementations used by the reconciler (single implementation rule).
  - ANCHOR_COVERAGE_FLOOR=0.50 and UNIFIED_COVERAGE_FLOOR=0.80 are module constants.
  - AT-2 requires a model stub or a live model call; use a fixture stub for CI determinism.
  test_requirements:
  - 'AT-1: pytest test that runs atlas plan against fixture Atlas docs and asserts all gates pass.'
  - 'AT-2: pytest test that runs atlas plan twice and asserts the second diff is empty or MODIFY-only with similarity ≥ 0.95.'
  - 'AT-4: pytest test with a fixture backlog containing an in_progress ticket; assert CONFLICT and apply refusal.'
  - 'AT-5: pytest test that modifies a doc after plan and asserts apply refuses with stale-plan error.'
  - 'AT-6: pytest test that inspects all applied keys and asserts none were model-minted.'
  - 'AT-7: pytest test that computes anchor_coverage and unified coverage against the roadmap and asserts both floors.'
  documentation_requirements:
  - Document the AT-1..AT-7 test locations and how to run them in docs/runbooks/running-atlas-plan.md.
  definition_of_done:
  - All AT-1..AT-7 tests pass in CI against the seeded Atlas documents.
  - AT-7 reports both anchor_coverage and unified coverage with the floor constants visible in the test output.
  - The acceptance test suite is wired into the CI pipeline.
  estimated_effort: null
  external_linear_id: 36263518-db37-45f7-9aa5-50fb27a23663
  external_github_issue_id: null
  tags:
  - planning
  - acceptance-tests
  - at-1-at-7
  component: atlas.planning
  linear_synced_at: '2026-07-04T18:05:38.538342Z'
  last_observed_linear_state_id: 311a3a97-c409-4cce-96ab-0a3bfc2a5541
  status_entered_at: '2026-07-05T15:53:13.776434Z'
  review_cycle_count: 0
  source_anchor: docs/atlas/planning-engine-specification.md#7-acceptance-tests-milestone-1
  created_by_type: agent
  created_by_id: planner
  created_at: '2026-07-04T18:05:38.538342Z'
  updated_at: '2026-07-04T18:05:38.538342Z'
  completed_at: null
- id: 714d316c-0414-45ab-9aa1-f6a7e7c3a641
  product_id: 6363ff3f-22db-40b3-9a47-aeba5d0a7586
  epic_id: 5bd967eb-98c0-42a6-bec3-042a34145078
  key: ATLAS-30
  title: AT-7 exact-anchor floor and content-coverage metric encoding (ATLAS-123)
  objective: 'Encode the resolved AT-7 pair metric into the acceptance suite: ANCHOR_COVERAGE_FLOOR=0.50 as a live exact-anchor floor gate, and content_coverage computed and reported but not yet gated, with the containment-aware coverage function beside the reconciler''s similarity function.'
  context: Planning engine specification §7.2 and ATLAS-112 resolution define the AT-7 pair metric. The exact-anchor floor (anchor_coverage ≥ 0.50) is a live gate; the unified floor (exact∪content ≥ 0.80) is the second gate once pinned by ATLAS-124. This ticket encodes the exact-anchor floor as live and content_coverage as reported-not-gated. The containment function (|roadmap_tokens ∩ proposed_tokens| / |roadmap_tokens|) is a new function beside reconciler.similarity, which is unchanged. Citation is excluded from the gate.
  status: needs_human_decision
  ticket_type: feature
  risk_level: medium
  priority: 30
  relevant_docs:
  - docs/atlas/planning-engine-specification.md
  acceptance_criteria:
  - ANCHOR_COVERAGE_FLOOR=0.50 is a module constant in the acceptance suite and the AT-7 exact-anchor leg gates on it.
  - 'content_coverage is computed using the containment-aware metric: a roadmap ticket is covered iff max(symmetric_dice, containment) ≥ CONTENT_COVERAGE_THRESHOLD=0.5.'
  - The containment function is a separate function from reconciler.similarity; reconciler.similarity is unchanged.
  - Citation (ATLAS-<n> keys in ticket titles) is excluded from the gated coverage union.
  - content_coverage is reported in AT-7 test output but does not gate (the gate is the exact-anchor floor only until ATLAS-124 pins the unified floor).
  - Unit tests verify the containment function on known inputs including the length-asymmetry case described in §7.2.
  non_goals:
  - Do not implement the unified floor gate (UNIFIED_COVERAGE_FLOOR=0.80) as a live gate; that is ATLAS-124.
  - Do not implement the scripts/at7_miss_analysis.py offline tool.
  - Do not modify the reconciler's similarity function.
  implementation_notes:
  - 'Containment: |roadmap_tokens ∩ proposed_tokens| / |roadmap_tokens|; roadmap_tokens is the casefold token set of the roadmap ticket title.'
  - CONTENT_COVERAGE_THRESHOLD=0.5 is a module constant, set for correctness and never tuned against the metric.
  - The covered-key sets for exact-anchor and content are computed separately; their union is reported as the unified coverage figure.
  test_requirements:
  - 'Unit test: containment(''ticket synchronisation'', ''ticket synchronisation sync_tick pull status push definitions'') = 1.0.'
  - 'Unit test: containment of disjoint token sets = 0.0.'
  - 'Unit test: AT-7 exact-anchor leg fails when anchor_coverage < 0.50.'
  - 'Unit test: content_coverage is computed and present in test output even when not gating.'
  documentation_requirements:
  - Docstring on the containment function describing its asymmetry and the length-asymmetry recovery it provides.
  definition_of_done:
  - All unit tests pass in CI.
  - AT-7 test output shows both anchor_coverage and content_coverage with the floor constants.
  - The containment function is importable from atlas.planning.acceptance.
  estimated_effort: null
  external_linear_id: a9d72fd8-d1f2-4055-86cb-1a1b56ea9d8a
  external_github_issue_id: null
  tags:
  - planning
  - acceptance-tests
  - at-7
  - coverage-metric
  component: atlas.planning
  linear_synced_at: '2026-07-04T18:05:38.538342Z'
  last_observed_linear_state_id: 311a3a97-c409-4cce-96ab-0a3bfc2a5541
  status_entered_at: '2026-07-05T15:53:13.776434Z'
  review_cycle_count: 0
  source_anchor: docs/atlas/planning-engine-specification.md#72-at-7-work-coverage-finding-atlas-112
  created_by_type: agent
  created_by_id: planner
  created_at: '2026-07-04T18:05:38.538342Z'
  updated_at: '2026-07-04T18:05:38.538342Z'
  completed_at: null
- id: 3bb50cff-bd56-4f2c-bc98-162badd9a75e
  product_id: 6363ff3f-22db-40b3-9a47-aeba5d0a7586
  epic_id: 5bd967eb-98c0-42a6-bec3-042a34145078
  key: ATLAS-31
  title: Acceptance test coverage for staged generation path (AT-1/AT-7 staged, AT-2 multi-call stability)
  objective: 'Extend the acceptance test suite to cover the staged multi-call generation path: AT-1 and AT-7 over the assembled proposal from staged generation, and AT-2 stability across the multi-call sequence.'
  context: Planning-large-corpora.md §5.2 and §6 require AT-2 stability and AT-7 coverage to be validated over the staged path, not just the single-call path. The reconciler, AT-2, and AT-7 are unchanged; this ticket adds test coverage that exercises the orchestrator → assembled proposal → parser → gates → reconciler chain. AT-2 across the staged sequence validates that the reconciler's key/anchor/similarity matching absorbs model drift into MODIFY-only diffs even when generation is split.
  status: needs_human_decision
  ticket_type: feature
  risk_level: medium
  priority: 25
  relevant_docs:
  - docs/atlas/planning-large-corpora.md
  - docs/decisions/0010-multi-call-generation-with-single-proposal-reconciliation.md
  - docs/atlas/planning-engine-specification.md
  acceptance_criteria:
  - 'AT-1 passes over the staged path: the assembled proposal from the three-stage orchestrator passes all seven gates.'
  - 'AT-7 passes over the staged path: anchor_coverage ≥ 0.50 on the assembled proposal against the roadmap.'
  - 'AT-2 passes over the staged path: two sequential staged runs on unchanged docs yield a reconciled diff that is empty or MODIFY-only with similarity ≥ 0.95.'
  - The staged-path tests use fixture model stubs that return deterministic stage outputs for CI reproducibility.
  - A test documents the expected generation_stages provenance chain for a staged run.
  non_goals:
  - Do not re-implement AT-3, AT-4, AT-5, AT-6 for the staged path (they are path-independent).
  - Do not implement live model calls in CI (use fixture stubs).
  - Do not implement the ATLAS-124 unified floor gate in this ticket.
  implementation_notes:
  - Fixture stubs should return realistic stage outputs derived from a real staged run to make the tests meaningful.
  - AT-2 staged requires two reconciler runs against the same backlog state; use an in-memory SQLite database.
  - The tests should be clearly labelled as staged-path variants of the AT-1/AT-2/AT-7 tests.
  test_requirements:
  - 'pytest test: staged orchestrator with fixture stubs produces an assembled proposal that passes all gates (AT-1 staged).'
  - 'pytest test: two staged runs with the same fixture stubs produce an empty or MODIFY-only reconciled diff (AT-2 staged).'
  - 'pytest test: AT-7 anchor_coverage ≥ 0.50 on the assembled proposal from fixture stubs.'
  - 'pytest test: generation_stages in the PlanRun has one entry per stage.'
  documentation_requirements:
  - Update docs/runbooks/running-atlas-plan.md to describe how to run the staged-path acceptance tests.
  definition_of_done:
  - All staged-path acceptance tests pass in CI.
  - The tests are wired into the CI pipeline alongside the single-call AT-1..AT-7 tests.
  - A comment in the test file explains the relationship to the single-call tests.
  estimated_effort: null
  external_linear_id: 214169f1-fcbb-4dc1-8548-b1ac72edc3aa
  external_github_issue_id: null
  tags:
  - planning
  - acceptance-tests
  - staged-generation
  - at-2
  - at-7
  component: atlas.planning
  linear_synced_at: '2026-07-04T18:05:38.538342Z'
  last_observed_linear_state_id: 311a3a97-c409-4cce-96ab-0a3bfc2a5541
  status_entered_at: '2026-07-05T15:53:13.776434Z'
  review_cycle_count: 0
  source_anchor: docs/atlas/planning-large-corpora.md#52-the-reconciler-at-2-at-7--unchanged
  created_by_type: agent
  created_by_id: planner
  created_at: '2026-07-04T18:05:38.538342Z'
  updated_at: '2026-07-04T18:05:38.538342Z'
  completed_at: null
- id: d37c78b8-fc73-463e-af2c-9c39b825100c
  product_id: 6363ff3f-22db-40b3-9a47-aeba5d0a7586
  epic_id: 5bd967eb-98c0-42a6-bec3-042a34145078
  key: ATLAS-32
  title: 'Runbook: docs/runbooks/running-atlas-plan.md'
  objective: Write the operator runbook for running atlas plan and atlas apply, covering prerequisites, common failure modes, how to interpret the diff, how to run the acceptance tests, and how to recover from a crashed apply.
  context: The implementation roadmap (ATLAS-30) requires a runbook at docs/runbooks/running-atlas-plan.md. The runbook is the operator's reference for the plan/apply loop. It must cover the full flow from document editing through plan, diff review, apply, and PlanRun provenance. It should also cover the staged generation path and how to diagnose truncation failures.
  status: needs_human_decision
  ticket_type: documentation
  risk_level: low
  priority: 20
  relevant_docs:
  - docs/atlas/planning-engine-specification.md
  - docs/atlas/planning-large-corpora.md
  - docs/atlas/bootstrap-guide.md
  acceptance_criteria:
  - The runbook exists at docs/runbooks/running-atlas-plan.md.
  - 'The runbook covers prerequisites: environment setup, model configuration, database initialisation.'
  - The runbook explains the diff output format (ADD/MODIFY/PROPOSE_ARCHIVE/CONFLICT) and how to interpret each entry type.
  - 'The runbook covers common failure modes: dirty working tree, stale plan, gate failures, truncation.'
  - The runbook explains how to recover from a crashed apply (re-run apply to complete the pending move).
  - The runbook documents how to run the AT-1..AT-7 acceptance tests.
  non_goals:
  - Do not document Linear sync or Symphony dispatch (those are later phases).
  - Do not duplicate the planning engine specification; link to it instead.
  - Do not include implementation details that belong in code docstrings.
  implementation_notes:
  - Use concrete command examples with expected output snippets.
  - Include a quick-start section for the common case (first run on a clean repo).
  - Reference the planning engine specification for authoritative definitions rather than duplicating them.
  test_requirements:
  - The doc linter passes on the runbook (valid markdown, no broken internal links).
  - The runbook is referenced from docs/MANIFEST.md.
  documentation_requirements:
  - The runbook must be added to docs/MANIFEST.md.
  - All command examples must be tested against the actual CLI before the ticket is closed.
  definition_of_done:
  - The runbook exists at docs/runbooks/running-atlas-plan.md and is referenced in docs/MANIFEST.md.
  - The doc linter passes on the runbook.
  - All command examples in the runbook have been verified against the actual CLI.
  estimated_effort: null
  external_linear_id: 83f76b76-a6a4-4f99-b02e-5d7bbc8744e3
  external_github_issue_id: null
  tags:
  - planning
  - runbook
  - documentation
  component: null
  linear_synced_at: '2026-07-04T18:05:38.538342Z'
  last_observed_linear_state_id: 311a3a97-c409-4cce-96ab-0a3bfc2a5541
  status_entered_at: '2026-07-05T15:53:13.776434Z'
  review_cycle_count: 0
  source_anchor: docs/atlas/implementation-roadmap.md#epic-generative-planning-with-deterministic-reconciliation
  created_by_type: agent
  created_by_id: planner
  created_at: '2026-07-04T18:05:38.538342Z'
  updated_at: '2026-07-04T18:05:38.538342Z'
  completed_at: null
- id: f31af522-c92f-4571-be60-0d2fb336be3a
  product_id: 6363ff3f-22db-40b3-9a47-aeba5d0a7586
  epic_id: 81b3d50c-06dc-42fb-8cac-c67f53541f91
  key: ATLAS-33
  title: Graph schema and build from storage
  objective: Implement the NetworkX DiGraph projection from relational storage, providing the pure `project_graph` function and the `build_dependency_graph` entry point that reads tickets, epics, ADRs, and dependency rows and returns a fully attributed DiGraph.
  context: The dependency engine requires a NetworkX DiGraph projected on demand from the relational tables (never persisted; the database is authoritative per ADR-0006). Nodes are keyed by entity key (ticket ATLAS-<n>, epic ATLAS-E<n>, ADR ADR-<nnnn>), carry typed attributes (node_type, key, entity_id, present, status, priority, risk_level, estimated_effort, ticket_type, acceptance_criteria_count, epic_key for tickets; status, priority, risk_level for epics; status, number for ADRs), and edges carry dependency_type, reason, and dependency_id. Absent targets are represented as present=False nodes rather than being dropped. The pure core `project_graph(tickets, epics, adrs, dependencies)` takes models in and returns a graph out with no I/O, enabling deterministic testing.
  status: needs_human_decision
  ticket_type: feature
  risk_level: high
  priority: 100
  relevant_docs:
  - docs/atlas/dependency-engine.md
  - docs/atlas/implementation-roadmap.md
  - docs/decisions/0002-use-postgresql-as-source-of-truth.md
  - docs/decisions/0006-source-of-truth-hierarchy.md
  acceptance_criteria:
  - '`project_graph(tickets, epics, adrs, dependencies)` is a pure function (no I/O) that returns a NetworkX DiGraph; given identical inputs it returns a structurally identical graph.'
  - Every ticket node carries node_type, key, entity_id, present=True, status, priority, risk_level, estimated_effort (may be null), ticket_type, acceptance_criteria_count, and epic_key as native graph attributes.
  - Every epic node carries node_type, key, entity_id, present=True, status, priority, and risk_level.
  - Every ADR node carries node_type, key (synthesised ADR-{number:04d}), entity_id, present=True, status, and number.
  - A dependency target that resolves to no stored entity is represented as an absent node (present=False) with its edge intact; no absent node is silently dropped.
  - '`build_dependency_graph(db)` reads tickets, epics, ADRs, and TicketDependency rows from storage and delegates to `project_graph`; it never writes and never persists the graph.'
  - 'Unit tests cover: a graph with tickets, epics, and ADR nodes; a graph with an absent (dangling) target node; edge attributes (dependency_type, reason, dependency_id) are present on every edge.'
  non_goals:
  - Graph persistence — the graph is always projected on demand; never stored.
  - Graph validation (acyclicity, dangling-target detection) — that is a separate ticket.
  - Readiness, critical-path, or blocker analyses — those are separate tickets.
  - Component nodes — schema-supported but unused until Phase 5.
  implementation_notes:
  - Entry points live in `atlas/dependencies/graph.py`.
  - Node identity is the entity key string; UUID-based dependency endpoints resolve to keys once at build time.
  - Only `depends_on` is stored; `blocks` is the derived reverse view and is never stored.
  - A plain DiGraph (one edge per (source, target)) is sufficient; a same-pair collision keeps the lowest-id row deterministically.
  - The §4.1 GraphNode/GraphEdge Pydantic models are reserved for a possible future persisted graph; the current implementation uses native graph attributes.
  test_requirements:
  - Pure unit tests for `project_graph` using in-memory model fixtures (no database).
  - Integration test for `build_dependency_graph` against a seeded SQLite database.
  - Test that an absent target produces a present=False node with the edge intact.
  - Test that node and edge attributes match the specification exactly.
  documentation_requirements:
  - Docstrings on `project_graph` and `build_dependency_graph` describing parameters, return type, and the absent-node contract.
  definition_of_done:
  - All unit and integration tests pass in CI.
  - '`project_graph` and `build_dependency_graph` are importable from `atlas.dependencies.graph`.'
  - No I/O in `project_graph` (verified by test isolation — no DB fixture needed).
  - Ruff and mypy pass with no new violations.
  estimated_effort: null
  external_linear_id: 38d070d4-7557-4b68-aa57-d904b0299db7
  external_github_issue_id: null
  tags:
  - dependency-engine
  - graph
  - storage
  - networkx
  component: atlas.dependencies
  linear_synced_at: '2026-07-04T18:05:38.538342Z'
  last_observed_linear_state_id: 311a3a97-c409-4cce-96ab-0a3bfc2a5541
  status_entered_at: '2026-07-05T15:53:13.776434Z'
  review_cycle_count: 0
  source_anchor: docs/atlas/dependency-engine.md#graph-projection-build
  created_by_type: agent
  created_by_id: planner
  created_at: '2026-07-04T18:05:38.538342Z'
  updated_at: '2026-07-04T18:05:38.538342Z'
  completed_at: null
- id: bd9c16df-9719-4ecd-8024-84d621127542
  product_id: 6363ff3f-22db-40b3-9a47-aeba5d0a7586
  epic_id: 81b3d50c-06dc-42fb-8cac-c67f53541f91
  key: ATLAS-34
  title: Estimated effort population for tickets
  objective: Add `estimated_effort` to the Ticket model and implement `TicketRepo.set_estimated_effort` as the single writer of this field, with typed validation errors for invalid values and an explicit null-clear path.
  context: 'The critical-path analysis requires `estimated_effort` on tickets. This field is operator-supplied and NEVER computed. `atlas apply` inserts every ticket with the field null; the operator supplies it out-of-band through `TicketRepo.set_estimated_effort`. The setter must accept a positive integer (>= 1) or null (to clear), reject <= 0 with a typed `EffortValidationError`, and reject an unknown key with `TicketNotFoundError`. The setter deliberately does NOT bump `updated_at` to avoid triggering spurious Linear definition re-pushes in Phase 4. This per-field ownership partition is a durable invariant: `atlas apply` owns doc-sourced definition fields; the operator owns `estimated_effort`.'
  status: needs_human_decision
  ticket_type: feature
  risk_level: medium
  priority: 90
  relevant_docs:
  - docs/atlas/dependency-engine.md
  - docs/atlas/implementation-roadmap.md
  - docs/atlas/planning-engine-specification.md
  acceptance_criteria:
  - '`Ticket` model and `tickets` table carry an `estimated_effort` column (nullable integer); `atlas apply` inserts every ticket with the field null.'
  - '`TicketRepo.set_estimated_effort(key, effort)` accepts a positive integer (>= 1) and persists it; it also accepts null to clear a prior value.'
  - Calling `set_estimated_effort` with effort <= 0 raises a typed `EffortValidationError` without persisting anything.
  - Calling `set_estimated_effort` with an unknown key raises `TicketNotFoundError`.
  - '`set_estimated_effort` does NOT update the ticket''s `updated_at` timestamp.'
  - An Alembic migration adds the column to the existing `tickets` table with a null default.
  - 'Unit tests cover: set a valid effort, clear with null, reject <= 0, reject unknown key, confirm updated_at is unchanged.'
  non_goals:
  - Computing or inferring effort from acceptance-criteria count, risk, or any other heuristic.
  - Exposing effort via the CLI — that is the dependency CLI ticket.
  - Syncing estimated_effort to Linear.
  implementation_notes:
  - The setter is the SINGLE writer of this column; `atlas apply`'s materialise path must omit the field so it defaults to null on INSERT.
  - Any future MODIFY-apply path must patch only doc-sourced definition columns and leave `estimated_effort` untouched.
  - EffortValidationError and TicketNotFoundError should be typed exceptions in `atlas.storage` or `atlas.core`.
  test_requirements:
  - Unit tests for `set_estimated_effort` covering all four cases (valid set, null clear, <= 0 rejection, unknown key).
  - Test that `updated_at` is unchanged after a successful `set_estimated_effort` call.
  - 'Migration smoke test: the column exists after applying the migration and is null by default.'
  documentation_requirements:
  - Docstring on `set_estimated_effort` documenting the single-writer invariant, the no-updated_at contract, and the valid value range.
  definition_of_done:
  - Migration applies cleanly on a fresh database and on an existing database.
  - All unit tests pass in CI.
  - Ruff and mypy pass with no new violations.
  - '`set_estimated_effort` is importable from `atlas.storage`.'
  estimated_effort: null
  external_linear_id: 753e3bb2-edcf-40c9-a9c9-4aca55dba297
  external_github_issue_id: null
  tags:
  - dependency-engine
  - storage
  - ticket-model
  - effort
  component: atlas.storage
  linear_synced_at: '2026-07-04T18:05:38.538342Z'
  last_observed_linear_state_id: 311a3a97-c409-4cce-96ab-0a3bfc2a5541
  status_entered_at: '2026-07-05T15:53:13.776434Z'
  review_cycle_count: 0
  source_anchor: docs/atlas/dependency-engine.md#effort-population
  created_by_type: agent
  created_by_id: planner
  created_at: '2026-07-04T18:05:38.538342Z'
  updated_at: '2026-07-04T18:05:38.538342Z'
  completed_at: null
- id: 2d867a2a-092f-49d4-ab9e-c7d283b882b6
  product_id: 6363ff3f-22db-40b3-9a47-aeba5d0a7586
  epic_id: 81b3d50c-06dc-42fb-8cac-c67f53541f91
  key: ATLAS-35
  title: 'Graph validation: acyclicity and dangling-target detection'
  objective: Implement `validate_graph(graph)` that checks the dependency DiGraph for acyclicity, self-edges, duplicate edges, dangling polymorphic targets, and terminal-to-non-terminal depends_on edges, raising typed errors on any violation.
  context: Validation must run on every graph build and on every dependency mutation. Any failure is a typed error; `atlas apply` and the PM Engine refuse to proceed on an invalid graph. Acyclicity failures must report the full cycle path. Dangling targets (present=False nodes) are a hard error that blocks readiness computation. The validation function is the gate that readiness, critical-path, and blocker analyses run after — they consume an already-validated graph. The TERMINAL_STATUSES constant defined here is the single source used by all analyses.
  status: needs_human_decision
  ticket_type: feature
  risk_level: high
  priority: 95
  relevant_docs:
  - docs/atlas/dependency-engine.md
  - docs/atlas/implementation-roadmap.md
  acceptance_criteria:
  - '`validate_graph(graph)` raises a typed `CycleError` carrying the full cycle path when the graph contains a cycle.'
  - '`validate_graph` raises a typed `SelfEdgeError` for any self-loop edge.'
  - '`validate_graph` raises a typed `DuplicateEdgeError` for any duplicate (source, target, dependency_type) triple.'
  - '`validate_graph` raises a typed `DanglingTargetError` for any node with present=False.'
  - '`validate_graph` raises a typed `TerminalDependsOnNonTerminalError` when a terminal-status ticket has a depends_on edge to a non-terminal ticket.'
  - '`TERMINAL_STATUSES` is defined in `atlas/dependencies/validation.py` (or a shared location) and is the single source imported by readiness and critical-path modules.'
  - Unit tests cover each violation type individually and confirm a valid graph passes without error.
  non_goals:
  - Readiness detection — that is a separate ticket.
  - Automatically repairing invalid graphs.
  - Validating node attribute completeness beyond what is needed for the structural checks above.
  implementation_notes:
  - Use NetworkX's `find_cycle` for cycle detection; report the full cycle path in the error.
  - Implement in `atlas/dependencies/validation.py`.
  - The `component` target type is absent-but-dormant until Phase 5 (D2); a component absent node is still a DanglingTargetError here.
  - All typed errors should subclass a common `GraphValidationError` base for easy catching.
  test_requirements:
  - Unit test for each of the five violation types using minimal fixture graphs.
  - Unit test confirming a well-formed graph (no violations) passes `validate_graph` without raising.
  - Test that `TERMINAL_STATUSES` is importable and contains the expected status values.
  documentation_requirements:
  - Docstring on `validate_graph` listing each check and the typed error it raises.
  definition_of_done:
  - All unit tests pass in CI.
  - '`validate_graph` and `TERMINAL_STATUSES` are importable from `atlas.dependencies.validation`.'
  - Ruff and mypy pass with no new violations.
  estimated_effort: null
  external_linear_id: 9a22ef3e-934b-4339-bcd5-0c775918db90
  external_github_issue_id: null
  tags:
  - dependency-engine
  - graph
  - validation
  - acyclicity
  component: atlas.dependencies
  linear_synced_at: '2026-07-04T18:05:38.538342Z'
  last_observed_linear_state_id: 311a3a97-c409-4cce-96ab-0a3bfc2a5541
  status_entered_at: '2026-07-05T15:53:13.776434Z'
  review_cycle_count: 0
  source_anchor: docs/atlas/dependency-engine.md#validation-rules
  created_by_type: agent
  created_by_id: planner
  created_at: '2026-07-04T18:05:38.538342Z'
  updated_at: '2026-07-04T18:05:38.538342Z'
  completed_at: null
- id: eca263f4-c4e9-4de3-8abf-0b8dc7a83ef6
  product_id: 6363ff3f-22db-40b3-9a47-aeba5d0a7586
  epic_id: 81b3d50c-06dc-42fb-8cac-c67f53541f91
  key: ATLAS-36
  title: Readiness detection
  objective: Implement `is_ready(graph, key)` and `ready_tickets(graph)` in `atlas/dependencies/readiness.py`, returning typed `ReadinessResult` objects that carry the failing condition(s) with machine-readable `NotReadyCode` values.
  context: 'A ticket is ready exactly when: (1) status is `planned` or `backlog`; (2) every depends_on ticket target has status `done`; (3) every depends_on ADR target has status `accepted`; (4) the ticket has >= 1 acceptance criterion; (5) no dependency target is missing (dangling). The function reads node attributes from the already-validated graph only — never storage. `ReadinessResult.ready` is derived from the reasons list so it can never contradict them. Each failure is a `NotReadyReason` with a machine `NotReadyCode`. Readiness does not require a rendered context pack. This predicate is consumed by the PM Engine in Phase 4.'
  status: needs_human_decision
  ticket_type: feature
  risk_level: medium
  priority: 85
  relevant_docs:
  - docs/atlas/dependency-engine.md
  - docs/atlas/implementation-roadmap.md
  acceptance_criteria:
  - '`is_ready(graph, key)` returns a `ReadinessResult` with `ready=True` only when all five conditions hold.'
  - '`ReadinessResult` carries a list of `NotReadyReason` objects each with a `NotReadyCode`; `ready` is derived from the list (empty reasons → ready=True).'
  - 'The five `NotReadyCode` values cover: wrong status, depends_on ticket not done, depends_on ADR not accepted, no acceptance criteria, dangling target.'
  - '`ready_tickets(graph)` returns the set of ticket keys for which `is_ready` returns ready=True.'
  - Neither function queries storage; they read only node attributes from the passed graph.
  - A present=False (dangling) target node produces a DANGLING_TARGET NotReadyReason rather than crashing or treating the dependency as satisfied.
  - 'Unit tests cover: a fully ready ticket, each of the five not-ready conditions individually, and a ticket with multiple simultaneous failures.'
  non_goals:
  - Context-pack rendering — readiness does not require a rendered pack.
  - Epic rollup readiness — epics are grouping, not gating.
  - Modifying ticket status — this is a pure read predicate.
  - Graph validation — the caller is responsible for passing a validated graph.
  implementation_notes:
  - Implement in `atlas/dependencies/readiness.py`.
  - Import `TERMINAL_STATUSES` from `atlas.dependencies.validation` — never redefine it.
  - Target node_type selects condition 2 vs 3 (ticket→done, ADR→accepted); epic and component targets are not gating.
  - The function must not raise on a present=False node; it must return a DANGLING_TARGET reason.
  test_requirements:
  - Unit tests using fixture graphs (no database) for each of the five not-ready conditions.
  - Unit test for a ticket that is ready (all conditions satisfied).
  - Unit test for `ready_tickets` returning the correct subset from a mixed graph.
  - Test that `ReadinessResult.ready` is always consistent with the reasons list.
  documentation_requirements:
  - Docstring on `is_ready` and `ready_tickets` describing the five conditions and the typed result.
  definition_of_done:
  - All unit tests pass in CI.
  - '`is_ready` and `ready_tickets` are importable from `atlas.dependencies.readiness`.'
  - Ruff and mypy pass with no new violations.
  estimated_effort: null
  external_linear_id: 0e776bd2-0fda-4446-871c-d2f89350d02b
  external_github_issue_id: null
  tags:
  - dependency-engine
  - readiness
  - graph
  component: atlas.dependencies
  linear_synced_at: '2026-07-04T18:05:38.538342Z'
  last_observed_linear_state_id: 311a3a97-c409-4cce-96ab-0a3bfc2a5541
  status_entered_at: '2026-07-05T15:53:13.776434Z'
  review_cycle_count: 0
  source_anchor: docs/atlas/dependency-engine.md#readiness-predicate
  created_by_type: agent
  created_by_id: planner
  created_at: '2026-07-04T18:05:38.538342Z'
  updated_at: '2026-07-04T18:05:38.538342Z'
  completed_at: null
- id: f98c5ecf-33b1-48bb-9638-d7caa2ee93df
  product_id: 6363ff3f-22db-40b3-9a47-aeba5d0a7586
  epic_id: 81b3d50c-06dc-42fb-8cac-c67f53541f91
  key: ATLAS-37
  title: Critical path analysis
  objective: Implement `critical_path(graph)` in `atlas/dependencies/critical_path.py` that returns a typed `CriticalPath` of ordered `CriticalPathStep` objects representing the longest effort-weighted execution chain over non-terminal tickets.
  context: 'The critical path is computed over the subgraph of tickets not in a terminal status. Edge direction A→B means A depends_on B (B executes first), so the execution DAG reverses these edges. The longest path is weighted by `estimated_effort` (null counts as 1 at compute time only; the stored value is never mutated). Tie-breaking: greater downstream dependent count, then higher priority integer, then key order. Output is an ordered list of `CriticalPathStep` (ticket key, weighted effort, cumulative effort). An empty non-terminal subgraph returns an empty path. The result is advisory and never gates dispatch.'
  status: needs_human_decision
  ticket_type: feature
  risk_level: medium
  priority: 80
  relevant_docs:
  - docs/atlas/dependency-engine.md
  - docs/atlas/implementation-roadmap.md
  - docs/decisions/0005-code-calculates-agents-interpret.md
  acceptance_criteria:
  - '`critical_path(graph)` returns a `CriticalPath` containing an ordered list of `CriticalPathStep` objects (key, effort_weight, cumulative_effort).'
  - The path is computed over the execution DAG (edges reversed from the depends_on direction) of non-terminal tickets only; terminal tickets are excluded.
  - null `estimated_effort` is weighted as 1 at compute time; the stored node attribute is never mutated.
  - 'Tie-breaking applies in order: greater downstream dependent count, then higher priority integer, then lexicographic key order — producing a deterministic result.'
  - An empty non-terminal subgraph returns a `CriticalPath` with an empty steps list, not an error.
  - 'Unit tests cover: a linear chain (verify execution order and cumulative effort), a branching graph (verify the longer branch is chosen), a graph with null efforts, and tie-breaking.'
  non_goals:
  - Gating dispatch or readiness on the critical path — it is advisory only.
  - Persisting the critical path result.
  - Computing critical path for epics.
  - Graph validation — the caller passes a validated graph.
  implementation_notes:
  - Implement in `atlas/dependencies/critical_path.py`.
  - Import `TERMINAL_STATUSES` from `atlas.dependencies.validation`.
  - Downstream dependent count is the count of direct dependents (tickets that depends_on the node) over the full graph regardless of status.
  - Use NetworkX's DAG longest-path algorithm on the reversed execution subgraph.
  - The result is deterministic given the same graph; no randomness.
  test_requirements:
  - 'Unit test: chain A depends_on B depends_on C with efforts 5, 3, 2 yields path [C, B, A] with cumulative [2, 5, 10].'
  - 'Unit test: branching graph selects the longer-effort branch.'
  - 'Unit test: null efforts are treated as 1.'
  - 'Unit test: tie-breaking by downstream count, then priority, then key order.'
  - 'Unit test: empty non-terminal subgraph returns empty CriticalPath.'
  documentation_requirements:
  - Docstring on `critical_path` describing the execution-order convention, the null-effort weighting, and the tie-breaking rules.
  definition_of_done:
  - All unit tests pass in CI.
  - '`critical_path` is importable from `atlas.dependencies.critical_path`.'
  - Ruff and mypy pass with no new violations.
  estimated_effort: null
  external_linear_id: 28d04dfb-bf3a-4846-94be-3161df45226a
  external_github_issue_id: null
  tags:
  - dependency-engine
  - critical-path
  - graph
  component: atlas.dependencies
  linear_synced_at: '2026-07-04T18:05:38.538342Z'
  last_observed_linear_state_id: 311a3a97-c409-4cce-96ab-0a3bfc2a5541
  status_entered_at: '2026-07-05T15:53:13.776434Z'
  review_cycle_count: 0
  source_anchor: docs/atlas/dependency-engine.md#critical-path
  created_by_type: agent
  created_by_id: planner
  created_at: '2026-07-04T18:05:38.538342Z'
  updated_at: '2026-07-04T18:05:38.538342Z'
  completed_at: null
- id: c183cfc5-0033-4f68-b399-5ddc15ed5159
  product_id: 6363ff3f-22db-40b3-9a47-aeba5d0a7586
  epic_id: 81b3d50c-06dc-42fb-8cac-c67f53541f91
  key: ATLAS-38
  title: Blocker detection
  objective: Implement `blocked(graph, key)`, `all_blocked(graph)`, `unlocks(graph, key)`, and the high-risk blocker report in `atlas/dependencies/blockers.py`, reading only graph node attributes.
  context: 'Blocker analysis answers: what is blocking a given ticket, what tickets are blocked, what does completing a ticket unlock, and which blocking tickets carry high/critical risk. `blocked(graph, key)` returns the set of unfinished depends_on targets of a ticket. `unlocks(graph, key)` returns the tickets that become ready if the given ticket completes. The high-risk blocker report surfaces any blocking ticket with risk_level in {high, critical} that a non-terminal ticket depends on — advisory only, never gates readiness or dispatch. All functions read graph attributes only, never storage.'
  status: needs_human_decision
  ticket_type: feature
  risk_level: medium
  priority: 75
  relevant_docs:
  - docs/atlas/dependency-engine.md
  - docs/atlas/implementation-roadmap.md
  acceptance_criteria:
  - '`blocked(graph, key)` returns the set of depends_on target keys that are not in a terminal status (i.e. unfinished blockers of the given ticket).'
  - '`all_blocked(graph)` returns a mapping of ticket key → set of blocker keys for every ticket that has at least one unfinished blocker.'
  - '`unlocks(graph, key)` returns the set of ticket keys that would become ready (per the readiness predicate) if the given ticket''s status were `done`.'
  - The high-risk blocker report returns all (blocked_ticket_key, blocker_key) pairs where the blocker has risk_level in {high, critical} and the blocked ticket is non-terminal.
  - All four functions read only graph node attributes; no storage queries.
  - 'Unit tests cover: a ticket with multiple blockers, a ticket with no blockers, `unlocks` for a ticket that unblocks several dependents, and the high-risk report with mixed risk levels.'
  non_goals:
  - Modifying ticket status based on blocker state.
  - Detecting stale-block anomalies — that is the PM Engine's responsibility.
  - Graph validation — the caller passes a validated graph.
  implementation_notes:
  - Implement in `atlas/dependencies/blockers.py`.
  - Import `TERMINAL_STATUSES` from `atlas.dependencies.validation`.
  - For `unlocks`, simulate the ticket as done and re-run the readiness predicate on its direct dependents.
  - The high-risk report is advisory; it must not raise or gate anything.
  test_requirements:
  - Unit test for `blocked` with a ticket that has two unfinished blockers and one finished blocker.
  - Unit test for `all_blocked` on a graph with multiple blocked tickets.
  - Unit test for `unlocks` confirming the correct set of newly-ready tickets.
  - Unit test for the high-risk report with blockers of varying risk levels.
  documentation_requirements:
  - Docstrings on all four public functions describing inputs, return types, and the advisory nature of the high-risk report.
  definition_of_done:
  - All unit tests pass in CI.
  - '`blocked`, `all_blocked`, `unlocks`, and the high-risk report are importable from `atlas.dependencies.blockers`.'
  - Ruff and mypy pass with no new violations.
  estimated_effort: null
  external_linear_id: 7b64c321-2d07-4fd0-92ba-e485acacccf8
  external_github_issue_id: null
  tags:
  - dependency-engine
  - blockers
  - graph
  component: atlas.dependencies
  linear_synced_at: '2026-07-04T18:05:38.538342Z'
  last_observed_linear_state_id: 311a3a97-c409-4cce-96ab-0a3bfc2a5541
  status_entered_at: '2026-07-05T15:53:13.776434Z'
  review_cycle_count: 0
  source_anchor: docs/atlas/dependency-engine.md#blocker-analysis
  created_by_type: agent
  created_by_id: planner
  created_at: '2026-07-04T18:05:38.538342Z'
  updated_at: '2026-07-04T18:05:38.538342Z'
  completed_at: null
- id: 59f863ac-3af6-457b-b1f6-a71421cf13e3
  product_id: 6363ff3f-22db-40b3-9a47-aeba5d0a7586
  epic_id: 81b3d50c-06dc-42fb-8cac-c67f53541f91
  key: ATLAS-39
  title: Graph visualisation (Mermaid advisory view)
  objective: Implement the `atlas deps graph` subcommand that prints an advisory Mermaid `graph TD` to stdout, overlaying readiness, blocker, and critical-path state as node classes and emphasised critical-path links, without writing any file.
  context: 'The `graph` subcommand is an advisory analysis view — it is NOT the canonical `docs/planning/roadmap.mmd`, which only `atlas apply` writes (ADR-0007). It validates first (refusing a cyclic or dangling graph), then builds the graph, runs the analyses, and emits a Mermaid diagram to stdout with node classes: `critical` > `blocked` > `ready` (one class per ticket), and critical-path links emphasised as `==>`. The renderer reuses the natural-key ordering and label-quoting primitives from `atlas/planning/mermaid.py` rather than duplicating them.'
  status: needs_human_decision
  ticket_type: feature
  risk_level: low
  priority: 60
  relevant_docs:
  - docs/atlas/dependency-engine.md
  - docs/atlas/implementation-roadmap.md
  - docs/decisions/0006-source-of-truth-hierarchy.md
  acceptance_criteria:
  - '`atlas deps graph` prints a valid Mermaid `graph TD` block to stdout and exits 0 on a valid graph.'
  - 'Node classes are applied: `critical` for tickets on the critical path, `blocked` for tickets with unfinished blockers, `ready` for ready tickets; `critical` takes precedence over `blocked` which takes precedence over `ready`.'
  - Critical-path edges are rendered as `==>` (emphasised); all other edges as `-->`.
  - The command validates the graph first and exits non-zero with a typed error message if the graph is cyclic or has dangling targets — no partial render is emitted.
  - '`atlas deps graph` writes NO file; it is stdout-only and does not touch `docs/planning/roadmap.mmd`.'
  - '`atlas deps graph --json` emits a machine-readable JSON representation of the graph state (nodes with their class, edges with their type).'
  non_goals:
  - Writing `docs/planning/roadmap.mmd` — that is `atlas apply`'s exclusive responsibility.
  - Interactive graph rendering or a web UI.
  - Persisting the visualisation result.
  implementation_notes:
  - Implement the Mermaid emission in `atlas/dependencies/visualisation.py`; wire into the CLI in `atlas/cli.py` under `atlas deps graph`.
  - Reuse `atlas/planning/mermaid.py` primitives for natural-key ordering and label quoting — do not duplicate.
  - The `--db` flag selects the database path, consistent with other `atlas deps` subcommands.
  test_requirements:
  - Unit test that a valid fixture graph produces a non-empty Mermaid string containing `graph TD`.
  - Unit test that a cyclic graph causes a non-zero exit and no Mermaid output.
  - Unit test that node classes are correctly assigned (critical > blocked > ready precedence).
  - Unit test that `--json` output is valid JSON with the expected structure.
  documentation_requirements:
  - Help text on the `atlas deps graph` command explaining it is advisory and does not write roadmap.mmd.
  definition_of_done:
  - All unit tests pass in CI.
  - '`atlas deps graph` is wired into the CLI and produces valid Mermaid output on a fixture database.'
  - Ruff and mypy pass with no new violations.
  estimated_effort: null
  external_linear_id: d7ea01b3-e873-4f37-8b3f-4fac6c6ea833
  external_github_issue_id: null
  tags:
  - dependency-engine
  - cli
  - visualisation
  - mermaid
  component: atlas.cli
  linear_synced_at: '2026-07-04T18:05:38.538342Z'
  last_observed_linear_state_id: 311a3a97-c409-4cce-96ab-0a3bfc2a5541
  status_entered_at: '2026-07-05T15:53:13.776434Z'
  review_cycle_count: 0
  source_anchor: docs/atlas/dependency-engine.md#cli
  created_by_type: agent
  created_by_id: planner
  created_at: '2026-07-04T18:05:38.538342Z'
  updated_at: '2026-07-04T18:05:38.538342Z'
  completed_at: null
- id: 19a1f28a-5aa8-4d99-9a0a-3283fa202beb
  product_id: 6363ff3f-22db-40b3-9a47-aeba5d0a7586
  epic_id: 81b3d50c-06dc-42fb-8cac-c67f53541f91
  key: ATLAS-40
  title: Dependency CLI
  objective: 'Implement the `atlas deps` command group with subcommands: `ready`, `blocked [KEY] [--high-risk]`, `critical-path`, `unlocks <KEY>`, `validate`, `effort <KEY> (VALUE | --clear)`, and `graph`, all as thin surfaces over the Phase 3 analysis functions.'
  context: 'The CLI is a thin surface over the already-implemented Phase 3 functions — it calls them and never reimplements a computation. All computation subcommands (ready, blocked, critical-path, unlocks, graph) validate first: they build the graph, run `validate_graph`, and refuse an invalid graph (printing typed violations, exiting with a precondition code) rather than computing on it. All subcommands support `--json` for machine consumption and `--db` to select the database. `effort KEY VALUE` sets estimated_effort via `TicketRepo.set_estimated_effort`; `effort KEY --clear` sets it null. KEY and `--high-risk` are mutually exclusive on `blocked`.'
  status: needs_human_decision
  ticket_type: feature
  risk_level: low
  priority: 55
  relevant_docs:
  - docs/atlas/dependency-engine.md
  - docs/atlas/implementation-roadmap.md
  - docs/atlas/bootstrap-guide.md
  acceptance_criteria:
  - '`atlas deps ready` lists all ready ticket keys; `--json` emits a JSON array.'
  - '`atlas deps blocked KEY` shows what blocks one ticket; `atlas deps blocked` (no key) shows every blocked ticket; `atlas deps blocked --high-risk` emits the graph-wide high/critical-risk blocker report; KEY and `--high-risk` are mutually exclusive.'
  - '`atlas deps critical-path` shows the longest effort-weighted execution chain with cumulative effort.'
  - '`atlas deps unlocks KEY` shows the tickets that would become ready if KEY completed.'
  - '`atlas deps validate` runs `validate_graph` and exits 0 on a valid graph or non-zero with typed violation messages.'
  - '`atlas deps effort KEY VALUE` sets estimated_effort (exits non-zero on <= 0 or unknown key without persisting); `atlas deps effort KEY --clear` sets it null.'
  - All computation subcommands (ready, blocked, critical-path, unlocks, graph) validate the graph first and refuse an invalid graph with a non-zero exit.
  non_goals:
  - Reimplementing any analysis logic — the CLI delegates entirely to the analysis modules.
  - A standalone dependency API endpoint.
  - Interactive TUI or web interface.
  implementation_notes:
  - Wire the `atlas deps` group into `atlas/cli.py` using Typer.
  - The `effort` subcommand does not build the graph; it is a direct ticket write via `TicketRepo.set_estimated_effort`.
  - Use Rich for human-readable table output; fall back to plain text when Rich is unavailable.
  - The `--db` flag defaults to the configured database path.
  test_requirements:
  - CLI integration tests using a seeded SQLite fixture database for each subcommand.
  - Test that `--json` output is valid JSON for `ready`, `blocked`, `critical-path`, and `unlocks`.
  - Test that computation subcommands exit non-zero on a cyclic graph.
  - Test that `effort` exits non-zero on invalid value and unknown key without persisting.
  - Test that `blocked --high-risk` and `blocked KEY` are mutually exclusive (exits non-zero if both supplied).
  documentation_requirements:
  - Help text on every subcommand describing its purpose, flags, and exit codes.
  - Update `docs/atlas/dependency-engine.md` CLI section if the implemented interface differs from the design.
  definition_of_done:
  - All CLI integration tests pass in CI.
  - '`atlas deps` and all seven subcommands are wired and functional against a fixture database.'
  - Ruff and mypy pass with no new violations.
  - '`atlas deps --help` and each subcommand''s `--help` produce useful output.'
  estimated_effort: null
  external_linear_id: bb3a412b-d58f-4db7-84e3-c4e6d710d07a
  external_github_issue_id: null
  tags:
  - dependency-engine
  - cli
  - deps
  component: atlas.cli
  linear_synced_at: '2026-07-04T18:05:38.538342Z'
  last_observed_linear_state_id: 311a3a97-c409-4cce-96ab-0a3bfc2a5541
  status_entered_at: '2026-07-05T15:53:13.776434Z'
  review_cycle_count: 0
  source_anchor: docs/atlas/dependency-engine.md#cli
  created_by_type: agent
  created_by_id: planner
  created_at: '2026-07-04T18:05:38.538342Z'
  updated_at: '2026-07-04T18:05:38.538342Z'
  completed_at: null
- id: 6af1cf1e-8279-42bc-8991-a023c7c52925
  product_id: 6363ff3f-22db-40b3-9a47-aeba5d0a7586
  epic_id: 81b3d50c-06dc-42fb-8cac-c67f53541f91
  key: ATLAS-41
  title: Dependency engine milestone test suite
  objective: Implement the Phase 3 milestone acceptance test suite verifying that readiness, blockers, and critical path are computed correctly on fixture graphs including cycle and dangling-target failure cases.
  context: 'The Phase 3 milestone test is: readiness, blockers, and critical path computed correctly on fixture graphs including cycle and dangling-target failures. These tests exercise the full stack from graph build through validation through analysis, using fixture data that covers the key correctness properties. They are the falsifiable acceptance gate for the Dependency Graph epic and must pass in CI.'
  status: needs_human_decision
  ticket_type: infrastructure
  risk_level: medium
  priority: 50
  relevant_docs:
  - docs/atlas/dependency-engine.md
  - docs/atlas/implementation-roadmap.md
  acceptance_criteria:
  - A fixture graph with a known ready ticket, a known blocked ticket, and a known critical path produces the correct results from `ready_tickets`, `blocked`, and `critical_path`.
  - A fixture graph containing a cycle causes `validate_graph` to raise `CycleError` with the cycle path; the computation functions refuse to run on it.
  - A fixture graph containing a dangling target causes `validate_graph` to raise `DanglingTargetError`; `is_ready` returns a DANGLING_TARGET not-ready reason for the affected ticket.
  - The critical path on a multi-branch fixture graph selects the correct longest-effort chain and produces correct cumulative effort values.
  - All milestone tests are collected under `tests/test_dependency_milestone.py` (or equivalent) and pass in CI with no skips.
  - The test suite runs without network access and without a running database (fixture graphs are in-memory).
  non_goals:
  - Testing the CLI layer — that is covered by the CLI ticket's integration tests.
  - Performance benchmarking.
  - Testing Linear or PM Engine integration.
  implementation_notes:
  - Use pytest fixtures to build the in-memory graphs.
  - The milestone tests should be clearly labelled (e.g. `@pytest.mark.milestone`) for easy identification in CI output.
  - These tests are the acceptance gate; they must not be skipped in CI.
  test_requirements:
  - 'Fixture: a DAG with 5+ tickets, mixed statuses, known ready/blocked/critical-path outcomes.'
  - 'Fixture: a graph with a cycle (A→B→C→A); assert CycleError is raised.'
  - 'Fixture: a graph with a dangling target; assert DanglingTargetError and DANGLING_TARGET not-ready reason.'
  - 'Fixture: a branching graph; assert the correct branch is the critical path.'
  - All fixtures are pure in-memory (no DB, no I/O).
  documentation_requirements:
  - A brief comment at the top of the test file explaining it is the Phase 3 milestone acceptance suite.
  definition_of_done:
  - All milestone tests pass in CI with no skips.
  - The test file is committed under `tests/`.
  - Ruff and mypy pass with no new violations.
  - CI output clearly identifies the milestone tests as passing.
  estimated_effort: null
  external_linear_id: d04f87e6-4084-4527-a347-9f323e6c671a
  external_github_issue_id: null
  tags:
  - dependency-engine
  - testing
  - acceptance-tests
  component: atlas.dependencies
  linear_synced_at: '2026-07-04T18:05:38.538342Z'
  last_observed_linear_state_id: 311a3a97-c409-4cce-96ab-0a3bfc2a5541
  status_entered_at: '2026-07-05T15:53:13.776434Z'
  review_cycle_count: 0
  source_anchor: docs/atlas/implementation-roadmap.md#epic-dependency-graph
  created_by_type: agent
  created_by_id: planner
  created_at: '2026-07-04T18:05:38.538342Z'
  updated_at: '2026-07-04T18:05:38.538342Z'
  completed_at: null
- id: b4ff5128-a75c-4083-966f-c974c7f450a0
  product_id: 6363ff3f-22db-40b3-9a47-aeba5d0a7586
  epic_id: 9e0a7ff0-b3da-4504-8e3c-d090a253f343
  key: ATLAS-42
  title: Consolidate natural-key sort primitive and break dependencies→planning import cycle
  objective: Move the duplicated natural-key sort helper into a single canonical location in atlas.core and eliminate the import inversion where atlas.dependencies imports from atlas.planning, so the layer spine is clean before import-linter enforcement is installed.
  context: 'The Phase 3 closure report (referenced in the roadmap Phase 3.5 section) identified two architectural problems: (1) the natural-key sort helper is duplicated across modules rather than living in a single core primitive, and (2) atlas.dependencies imports from atlas.planning, which is an inverted edge in the canonical layer spine (atlas.planning sits above atlas.dependencies). ARCHITECTURE.md defines the layer spine with atlas.planning above atlas.dependencies, meaning atlas.dependencies must never import from atlas.planning. This ticket resolves both issues as a prerequisite to installing import-linter enforcement.'
  status: needs_human_decision
  ticket_type: tech_debt
  risk_level: medium
  priority: 10
  relevant_docs:
  - ARCHITECTURE.md
  - docs/atlas/implementation-roadmap.md
  - docs/atlas/dependency-engine.md
  acceptance_criteria:
  - A single `natural_key` sort function exists in `atlas.core` (e.g. `atlas/core/keys.py` or `atlas/core/sorting.py`) and is the only definition in the codebase — no duplicate implementations remain.
  - All call sites that previously used a local or planning-module natural-key helper are updated to import from `atlas.core`.
  - No module under `atlas.dependencies` imports from `atlas.planning` — verified by a grep/import scan in the test suite.
  - All existing tests pass without modification to test logic (only import paths change).
  - A new unit test confirms the consolidated `natural_key` function produces the expected sort order for a representative set of keys (e.g. ATLAS-1, ATLAS-2, ATLAS-10, ATLAS-E1).
  non_goals:
  - Do not install or configure import-linter — that is the next ticket.
  - Do not move any other shared utilities beyond the natural-key helper.
  - Do not change the behaviour of the natural-key sort, only its location.
  - Do not refactor any other import inversions beyond the dependencies→planning cycle identified in the Phase 3 closure report.
  implementation_notes:
  - The natural-key helper likely lives in `atlas/planning/` or is duplicated between `atlas/planning/` and `atlas/dependencies/`; locate all copies before moving.
  - The canonical destination is `atlas/core/` — a new module such as `atlas/core/keys.py` is appropriate if one does not already exist.
  - 'The import inversion to break: find every `from atlas.planning import ...` or `import atlas.planning...` inside `atlas/dependencies/` and replace with an import from `atlas.core` or a local computation.'
  - Run `python -c 'import atlas.dependencies'` and `python -c 'import atlas.planning'` after the change to confirm no circular import errors.
  test_requirements:
  - Unit test for the consolidated `natural_key` sort in `atlas.core` covering numeric ordering (ATLAS-1 < ATLAS-2 < ATLAS-10) and epic keys.
  - Import-scan test (or static assertion) that no file under `atlas/dependencies/` contains an import from `atlas.planning`.
  - Full existing test suite passes (`uv run pytest`) with no regressions.
  documentation_requirements:
  - Add a brief docstring to the consolidated `natural_key` function explaining its sort semantics and canonical location.
  definition_of_done:
  - Single `natural_key` implementation in `atlas.core`; all duplicates removed.
  - Zero imports from `atlas.planning` inside `atlas/dependencies/`.
  - CI green (tests, lint, type-check).
  - PR reviewed and merged.
  estimated_effort: null
  external_linear_id: f8214b17-76d2-44f0-bee2-d38dfa642a2a
  external_github_issue_id: null
  tags:
  - layer-spine
  - refactor
  - core
  - dependencies
  component: atlas.core
  linear_synced_at: '2026-07-04T18:05:38.538342Z'
  last_observed_linear_state_id: 311a3a97-c409-4cce-96ab-0a3bfc2a5541
  status_entered_at: '2026-07-05T15:53:13.776434Z'
  review_cycle_count: 0
  source_anchor: docs/atlas/implementation-roadmap.md#epic-layer-spine
  created_by_type: agent
  created_by_id: planner
  created_at: '2026-07-04T18:05:38.538342Z'
  updated_at: '2026-07-04T18:05:38.538342Z'
  completed_at: null
- id: 48cc28f8-14c2-4d18-816f-e1bed806db05
  product_id: 6363ff3f-22db-40b3-9a47-aeba5d0a7586
  epic_id: 9e0a7ff0-b3da-4504-8e3c-d090a253f343
  key: ATLAS-43
  title: Install import-linter layers contract enforcing the atlas layer spine
  objective: Add an import-linter `layers` contract to `pyproject.toml` that mechanically enforces the canonical layer spine defined in ARCHITECTURE.md, wire it into pre-commit and CI, and confirm it catches a known inverted edge so architectural regressions are detected automatically on every PR.
  context: ARCHITECTURE.md defines a canonical layer spine for the `atlas` package (cli → planning → pm → context → dependencies → evidence → storage → linear → github → core). The architecture document explicitly states that an import-linter `layers` contract in `pyproject.toml` enforces this in pre-commit and CI. Without this enforcement, inverted edges (such as the dependencies→planning inversion fixed in the preceding ticket) can silently re-appear. This ticket installs the first architecture-fitness sensor for the atlas package, implementing Phase 3.5's mechanical guard.
  status: needs_human_decision
  ticket_type: infrastructure
  risk_level: medium
  priority: 9
  relevant_docs:
  - ARCHITECTURE.md
  - docs/atlas/implementation-roadmap.md
  acceptance_criteria:
  - '`import-linter` is added as a dev dependency (`uv add --dev import-linter`) and appears in `pyproject.toml` and `uv.lock`.'
  - A `[tool.importlinter]` section in `pyproject.toml` defines a `layers` contract listing the canonical layer order from ARCHITECTURE.md (cli, planning, pm, context, dependencies, evidence, storage, linear, github, core), with `atlas.tools` and `atlas.__main__` excluded as unconstrained consumers.
  - Running `lint-imports` (or `uv run lint-imports`) on the clean codebase exits 0.
  - A test or CI step demonstrates that introducing a synthetic inverted import (e.g. a temporary `from atlas.planning import ...` inside `atlas/dependencies/`) causes `lint-imports` to exit non-zero.
  - The `lint-imports` check is added to the CI workflow (`.github/workflows/ci.yml`) so it runs on every PR.
  - The `lint-imports` check is added to `.pre-commit-config.yaml` so it runs locally before commit.
  non_goals:
  - Do not enforce import rules for `atlas.tools` or `atlas.__main__` — these are intentionally unconstrained per ARCHITECTURE.md.
  - Do not add any other static-analysis tools beyond import-linter in this ticket.
  - Do not fix any import violations beyond those already resolved in the preceding consolidation ticket — this ticket only installs the guard on a clean codebase.
  - Do not configure import-linter for any package other than `atlas`.
  implementation_notes:
  - 'The `[tool.importlinter]` `layers` contract lists layers high-to-low as: `atlas.cli`, `atlas.planning`, `atlas.pm`, `atlas.context`, `atlas.dependencies`, `atlas.evidence`, `atlas.storage`, `atlas.linear`, `atlas.github`, `atlas.core`.'
  - '`atlas.tools` and `atlas.__main__` are consumers outside the spine and must be listed under `exclude_type_checking_imports` or simply not included in the layers list — import-linter''s `layers` contract only checks the listed modules.'
  - 'The pre-commit hook should use `repo: local` with `language: system` and `entry: uv run lint-imports` to stay consistent with the project''s `uv`-based toolchain.'
  - Verify the contract name in `pyproject.toml` is descriptive, e.g. `name = atlas-layer-spine`.
  - The synthetic-inversion test can be a pytest test that shells out to `lint-imports` against a temporary fixture directory, or a documented CI matrix step — choose whichever is simpler to maintain.
  test_requirements:
  - CI step running `lint-imports` exits 0 on the main branch after this ticket lands.
  - A fixture test (or documented manual verification step) confirms that a deliberate inverted import causes `lint-imports` to fail — this validates the sensor actually catches violations.
  - Pre-commit hook runs `lint-imports` and blocks a commit that introduces an inverted edge.
  documentation_requirements:
  - Add a comment in `pyproject.toml` above the `[tool.importlinter]` section referencing ARCHITECTURE.md#layer-spine as the authoritative source for the layer order.
  - Update `ARCHITECTURE.md` (or add a note) confirming that the contract is now live and enforced in CI and pre-commit.
  definition_of_done:
  - '`lint-imports` passes on the clean codebase in CI.'
  - Pre-commit hook runs `lint-imports` and is documented in the repo's pre-commit config.
  - A deliberate inverted import demonstrably fails the check (verified in CI or a test).
  - PR reviewed and merged.
  estimated_effort: null
  external_linear_id: e2212e97-e3cb-4a93-aa84-4c9e80c7d270
  external_github_issue_id: null
  tags:
  - layer-spine
  - import-linter
  - ci
  - pre-commit
  - architecture-fitness
  component: null
  linear_synced_at: '2026-07-04T18:05:38.538342Z'
  last_observed_linear_state_id: 311a3a97-c409-4cce-96ab-0a3bfc2a5541
  status_entered_at: '2026-07-05T15:53:13.776434Z'
  review_cycle_count: 0
  source_anchor: ARCHITECTURE.md#layer-spine
  created_by_type: agent
  created_by_id: planner
  created_at: '2026-07-04T18:05:38.538342Z'
  updated_at: '2026-07-04T18:05:38.538342Z'
  completed_at: null
- id: 280b2444-8f3f-48c6-844b-47d2b6386aad
  product_id: 6363ff3f-22db-40b3-9a47-aeba5d0a7586
  epic_id: 9e0a7ff0-b3da-4504-8e3c-d090a253f343
  key: ATLAS-44
  title: 'Roadmap-coverage sensor: every ticket referenced in a closure report must appear in the implementation roadmap'
  objective: Implement a lightweight sensor that verifies every ticket key cited in a Phase closure report document exists in `docs/atlas/implementation-roadmap.md`, so omissions from the roadmap are caught mechanically rather than discovered in review.
  context: 'The Phase 3.5 roadmap entry (ATLAS-115) calls for a roadmap-coverage sensor: every ticket referenced in a closure report must appear in the roadmap. Closure reports are the canonical record of what was built in a phase; if a ticket key appears in a closure report but not in the roadmap, the roadmap has drifted from reality. This sensor closes that gap by making the check mechanical and wiring it into CI, consistent with the doc-linter philosophy established in Phase 0 (ATLAS-4) and the architecture-fitness sensor pattern introduced by the import-linter ticket.'
  status: needs_human_decision
  ticket_type: infrastructure
  risk_level: low
  priority: 8
  relevant_docs:
  - docs/atlas/implementation-roadmap.md
  - ARCHITECTURE.md
  acceptance_criteria:
  - 'A sensor script or doc-linter rule exists (e.g. in `atlas/tools/doc_linter.py` or a dedicated `atlas/tools/roadmap_coverage.py`) that: (a) scans all closure report documents under `docs/` for `ATLAS-<n>` key references, and (b) checks each referenced key against the set of keys present in `docs/atlas/implementation-roadmap.md`.'
  - The sensor exits non-zero and prints a clear error listing the missing keys when any referenced key is absent from the roadmap.
  - The sensor exits 0 when all referenced keys are present in the roadmap.
  - The sensor is wired into the CI workflow so it runs on every PR.
  - A seeded fixture test demonstrates the sensor fails on a closure report referencing a key not in the roadmap, and passes when the key is present.
  non_goals:
  - Do not validate the reverse direction (roadmap keys that have no closure report) — that is a separate concern.
  - Do not parse or validate the content of closure reports beyond extracting `ATLAS-<n>` key references.
  - Do not integrate with the database or operational state — this is a pure document scan.
  - Do not add this check to pre-commit (CI is sufficient for this sensor; pre-commit is reserved for fast local checks).
  implementation_notes:
  - Closure reports are likely Markdown files under `docs/` whose names contain 'closure' or are in a `docs/atlas/` subdirectory — the sensor should scan a configurable glob, defaulting to `docs/**/*closure*.md`.
  - 'Key extraction: a simple regex `ATLAS-\d+` over the file content is sufficient; avoid false positives from code blocks by optionally stripping fenced blocks first (reuse the heading-scan exclusion logic from the doc linter if available).'
  - 'Roadmap key extraction: scan `docs/atlas/implementation-roadmap.md` for the same pattern.'
  - The sensor can be a standalone CLI entry point (`uv run python -m atlas.tools.roadmap_coverage`) or integrated into the existing doc-linter runner.
  - Keep the implementation simple — this is a text scan, not a semantic analysis.
  test_requirements:
  - Unit test with a fixture closure report containing a key present in a fixture roadmap — sensor exits 0.
  - Unit test with a fixture closure report containing a key absent from a fixture roadmap — sensor exits non-zero and names the missing key.
  - CI step running the sensor against the real `docs/` tree exits 0 after this ticket lands.
  documentation_requirements:
  - Add a brief comment in the CI workflow step explaining what the roadmap-coverage sensor checks and why.
  definition_of_done:
  - Sensor implemented and exits 0 on the current `docs/` tree.
  - Fixture tests pass (present key → 0, absent key → non-zero).
  - CI step added and green.
  - PR reviewed and merged.
  estimated_effort: null
  external_linear_id: ec6976e0-e96f-4f66-8f38-3b41daea23cd
  external_github_issue_id: null
  tags:
  - layer-spine
  - doc-linter
  - roadmap
  - ci
  - architecture-fitness
  component: atlas.tools
  linear_synced_at: '2026-07-04T18:05:38.538342Z'
  last_observed_linear_state_id: 311a3a97-c409-4cce-96ab-0a3bfc2a5541
  status_entered_at: '2026-07-05T15:53:13.776434Z'
  review_cycle_count: 0
  source_anchor: docs/atlas/implementation-roadmap.md#epic-layer-spine
  created_by_type: agent
  created_by_id: planner
  created_at: '2026-07-04T18:05:38.538342Z'
  updated_at: '2026-07-04T18:05:38.538342Z'
  completed_at: null
- id: b5a18843-9af6-4df0-aed9-7a8454c88d01
  product_id: 6363ff3f-22db-40b3-9a47-aeba5d0a7586
  epic_id: e0d47d14-bbd1-4d5d-96bc-f9e7adfeb3cf
  key: ATLAS-45
  title: Linear integration with ADR-0006 field ownership boundary
  objective: 'Implement the atlas.linear boundary layer: LinearClient with create/update/fetch operations, field-ownership allow-list enforcing definitions Atlas→Linear and status Linear→Atlas, LinearStatusMap with operator-configured state-id mapping, and the InMemoryLinearClient fake for contract testing.'
  context: 'ADR-0006 establishes strict field ownership: ticket definitions (title, description) flow Atlas→Linear; ticket status flows Linear→Atlas; nothing else crosses. The atlas.linear package is the Phase 4 provider boundary that enforces this mechanically. LinearClient must reject any attempt to push status fields, and status mapping must use stable Linear state IDs (not rename-fragile names). The LinearStatusMap is sourced from the LINEAR_STATE_MAP env var. Secrets (LINEAR_API_KEY, LINEAR_TEAM_ID, LINEAR_PROJECT_ID, LINEAR_STATE_MAP) are read only at this boundary and never logged.'
  status: needs_human_decision
  ticket_type: feature
  risk_level: high
  priority: 100
  relevant_docs:
  - docs/atlas/pm-engine-and-linear-sync.md
  - docs/decisions/0006-source-of-truth-hierarchy.md
  - ARCHITECTURE.md
  acceptance_criteria:
  - LinearClient exposes create_issue, update_issue, fetch_issue, fetch_workflow_states, set_state, and fetch_comments; returns LinearIssue DTOs.
  - definition_payload(ticket) is built only from OWNED_DEFINITION_FIELDS (title, description summary); the client rejects any key outside OWNED_LINEAR_INPUT_KEYS, making status push mechanically impossible.
  - LinearStatusMap is loaded from LINEAR_STATE_MAP env var (JSON dict of linear_state_id→TicketStatus); a missing or empty map raises LinearStatusMapError on the live path.
  - status_from_issue(issue, status_map) returns the mapped TicketStatus or None for unmapped ids; never guesses.
  - validate_against_states confirms each configured state id exists in the workspace and rejects type-contradictory mappings; stale UUIDs fail loudly.
  - InMemoryLinearClient implements the same protocol as LinearClient and is used in all unit/contract tests; CI runs with no network and no secrets.
  - An opt-in live smoke test (ATLAS_LIVE_TESTS=1) exercises the real workspace and is skipped in default CI.
  non_goals:
  - The sync loop that calls this client on a cadence (that is the scheduler ticket).
  - Webhook-based transport (pull-only per ADR-0008).
  - Labels and priority sync (deferred; tracked in debt register).
  - Epic↔Linear project mapping.
  implementation_notes:
  - Use stdlib urllib or httpx for GraphQL transport; no webhooks.
  - LINEAR_PROJECT_ID is the project UUID (not slugId); scope issue creation to the project so Symphony's poll sees it.
  - The real client and the fake share a Protocol/ABC so tests inject either transparently.
  - state_id_for(status) inverts the configured map; exactly one state must map to ready_for_agent and needs_human_decision, validated at load.
  test_requirements:
  - 'Unit tests for definition_payload: verify no state key is ever included.'
  - 'Unit tests for status_from_issue: mapped id returns correct status, unmapped returns None.'
  - Contract tests against InMemoryLinearClient covering create, update, fetch, set_state, fetch_comments.
  - Test that validate_against_states raises on a stale or type-contradictory mapping.
  - Test that a missing LINEAR_STATE_MAP raises LinearStatusMapError.
  - 'import-linter check: atlas.linear imports nothing from atlas.pm or higher layers.'
  documentation_requirements:
  - Inline docstrings on LinearClient methods documenting field-ownership constraints.
  - Comment in client.py referencing ADR-0006 field-ownership table.
  definition_of_done:
  - All unit and contract tests pass in CI with no network calls.
  - import-linter layers contract passes (atlas.linear below atlas.pm).
  - Live smoke test documented and skipped by default in CI.
  estimated_effort: null
  external_linear_id: 7d83f080-aac7-4236-bd8a-41b3ef793d4c
  external_github_issue_id: null
  tags:
  - linear-sync
  - pm-engine
  - field-ownership
  - boundary
  component: atlas.linear
  linear_synced_at: '2026-07-04T18:05:38.538342Z'
  last_observed_linear_state_id: 311a3a97-c409-4cce-96ab-0a3bfc2a5541
  status_entered_at: '2026-07-05T15:53:13.776434Z'
  review_cycle_count: 0
  source_anchor: docs/atlas/pm-engine-and-linear-sync.md#field-ownership-boundary-atlas-41
  created_by_type: agent
  created_by_id: planner
  created_at: '2026-07-04T18:05:38.538342Z'
  updated_at: '2026-07-04T18:05:38.538342Z'
  completed_at: null
- id: d3656909-ccf7-44a3-b3e6-87e3e3b6f939
  product_id: 6363ff3f-22db-40b3-9a47-aeba5d0a7586
  epic_id: e0d47d14-bbd1-4d5d-96bc-f9e7adfeb3cf
  key: ATLAS-46
  title: Ticket synchronisation sync_tick (pull status, push definitions)
  objective: 'Implement sync_tick in atlas/pm/sync.py: pull Linear status for all tracked tickets and apply mapped state changes (Atlas status written by apply_linear_status, the sole post-creation status writer); push definition updates for tickets whose Atlas updated_at is newer and are in a pre-dispatch or Ready for Agent status.'
  context: 'The sync loop is the heartbeat of the PM Engine. Steps 1 and 2 of the five-step loop: (1) fetch Linear states for tickets with an external_linear_id in a non-terminal Atlas status, apply mapped transitions via apply_linear_status, and count/log unmapped states; (2) push owned definition fields (title, description summary) for tickets whose updated_at is newer than last push. apply_linear_status is the single writer of Atlas ticket status post-creation; it also stamps status_entered_at and increments review_cycle_count on changes_requested→pr_open transitions. The sync is pull-based (ADR-0008); ticks are idempotent.'
  status: needs_human_decision
  ticket_type: feature
  risk_level: high
  priority: 90
  relevant_docs:
  - docs/atlas/pm-engine-and-linear-sync.md
  - docs/decisions/0006-source-of-truth-hierarchy.md
  - docs/decisions/0008-ci-sourced-evidence-with-trust-tiers.md
  acceptance_criteria:
  - sync_tick fetches Linear states for all tickets with external_linear_id in a non-terminal Atlas status and applies mapped transitions via apply_linear_status.
  - apply_linear_status is the sole post-creation writer of Atlas ticket status; it stamps status_entered_at on every real status change and increments review_cycle_count only on changes_requested→pr_open transitions; neither field bumps updated_at.
  - Definition push sends only OWNED_DEFINITION_FIELDS for tickets whose updated_at is newer, and only while the ticket is in a pre-dispatch status or ready_for_agent.
  - Unmapped Linear state ids are counted and logged (not applied); the count is available in the SyncResult returned by sync_tick.
  - 'sync_tick is idempotent: running it twice on unchanged state produces no additional writes.'
  - SyncResult carries counts of status_updates, definition_pushes, unmapped_states, and errors for observability.
  non_goals:
  - Readiness promotion (step 3, separate ticket).
  - Follow-up comment scanning (step 4, separate ticket).
  - Anomaly and dwell detection (step 5, separate tickets).
  - The recurring scheduler that calls sync_tick (separate ticket).
  - Out-of-ownership anomaly logging (ATLAS-118, separate ticket).
  implementation_notes:
  - apply_linear_status must be the single writer; no other code path may set ticket status after creation.
  - status_entered_at and review_cycle_count must not bump updated_at (to avoid spurious definition re-push).
  - SyncResult is a plain dataclass/Pydantic model, not persisted — it is ephemeral per tick.
  - The definition push guard checks updated_at > last_synced_at (a field on the ticket or a separate sync-state record).
  test_requirements:
  - 'Unit tests using InMemoryLinearClient: a mapped status change updates Atlas ticket status exactly once.'
  - 'Unit test: an unmapped state id increments unmapped_states count and does not change Atlas status.'
  - 'Unit test: definition push fires only when updated_at is newer and ticket is in eligible status.'
  - 'Unit test: apply_linear_status stamps status_entered_at on status change and does not bump updated_at.'
  - 'Unit test: review_cycle_count increments only on changes_requested→pr_open transition.'
  - 'Idempotency test: two consecutive sync_ticks on unchanged Linear state produce identical SyncResult with zero writes on the second call.'
  documentation_requirements:
  - Docstring on sync_tick describing the two steps and idempotency guarantee.
  - Docstring on apply_linear_status documenting sole-writer contract and fields it stamps.
  definition_of_done:
  - All unit tests pass in CI with InMemoryLinearClient (no network).
  - apply_linear_status is demonstrably the only post-creation status writer (grep/import-linter check).
  - SyncResult fields are documented and covered by tests.
  estimated_effort: null
  external_linear_id: 4f6f19af-2e18-456d-9c80-d399ba97bb5a
  external_github_issue_id: null
  tags:
  - linear-sync
  - pm-engine
  - sync-tick
  - status
  component: atlas.pm
  linear_synced_at: '2026-07-04T18:05:38.538342Z'
  last_observed_linear_state_id: 311a3a97-c409-4cce-96ab-0a3bfc2a5541
  status_entered_at: '2026-07-05T15:53:13.776434Z'
  review_cycle_count: 0
  source_anchor: docs/atlas/pm-engine-and-linear-sync.md#sync-loop
  created_by_type: agent
  created_by_id: planner
  created_at: '2026-07-04T18:05:38.538342Z'
  updated_at: '2026-07-04T18:05:38.538342Z'
  completed_at: null
- id: 8041f947-c1c0-4e38-9743-7dfac32a637d
  product_id: 6363ff3f-22db-40b3-9a47-aeba5d0a7586
  epic_id: e0d47d14-bbd1-4d5d-96bc-f9e7adfeb3cf
  key: ATLAS-47
  title: 'Ready-state promotion: sole writer into Ready for Agent'
  objective: 'Implement the readiness promotion step (step 3 of the sync loop): run the dependency-engine readiness predicate each tick, and for each newly ready ticket write Ready for Agent in Linear via LinearClient.set_state, making the PM Engine the sole writer into that state.'
  context: The PM Engine is the only component permitted to move a ticket into Ready for Agent (symphony-integration.md state mapping). Promotion requires the dependency-engine readiness predicate (all depends_on tickets done, all depends_on ADRs accepted, ≥1 acceptance criterion, no dangling targets). The Linear write is the sanctioned outbound status write Atlas→Linear; Atlas's own ready_for_agent status is reconciled by the next pull (one tick of latency). The write is idempotent. The target Linear state is resolved by inverting the configured status map; exactly one state must map to ready_for_agent.
  status: needs_human_decision
  ticket_type: feature
  risk_level: high
  priority: 85
  relevant_docs:
  - docs/atlas/pm-engine-and-linear-sync.md
  - docs/atlas/dependency-engine.md
  - docs/atlas/symphony-integration.md
  acceptance_criteria:
  - promote_ready runs the dependency-engine ready_tickets predicate and calls LinearClient.set_state for each newly ready ticket not already in ready_for_agent.
  - The PM Engine is the sole writer into the Ready for Agent Linear state; no other code path calls set_state with that target.
  - The target Linear state id is resolved by inverting the configured status map; load-time validation confirms exactly one state maps to ready_for_agent.
  - The write is Linear-only; Atlas status is updated by the next pull tick (not by promote_ready directly).
  - 'Promotion is idempotent: a ticket already in ready_for_agent is never re-promoted.'
  - A ticket that fails the readiness predicate (e.g. a dependency not done) is not promoted and the reason is logged.
  non_goals:
  - Context-pack rendering before promotion (deferred to Phase 5/8).
  - Embedding the pack in the Linear description (Phase 8, ATLAS-82).
  - Promoting tickets in blocked status (promote_ready touches only planned/backlog).
  - The needs_human_decision routing (that is the review-cycling anomaly ticket).
  implementation_notes:
  - Reuse the build_dependency_graph + ready_tickets functions from atlas.dependencies; do not re-implement readiness logic.
  - state_id_for(ready_for_agent) must be validated at scheduler startup, not per tick.
  - The promotion write path must not include any definition field (stateId only, consistent with the allow-list).
  test_requirements:
  - 'Unit test: a ticket satisfying all readiness conditions triggers set_state to the ready_for_agent Linear state.'
  - 'Unit test: a ticket with an unfinished dependency is not promoted.'
  - 'Unit test: a ticket already in ready_for_agent is not re-promoted (idempotency).'
  - 'Unit test: load-time validation raises if zero or multiple states map to ready_for_agent.'
  - 'Integration test: promote_ready called twice on the same ready ticket produces exactly one set_state call.'
  documentation_requirements:
  - Docstring on promote_ready documenting sole-writer contract and one-tick latency for Atlas status.
  definition_of_done:
  - All unit tests pass in CI with InMemoryLinearClient.
  - Sole-writer invariant is enforced (no other call site for set_state targeting ready_for_agent).
  - Load-time validation of the status map is tested and documented.
  estimated_effort: null
  external_linear_id: 7c9927a7-9b1f-49ed-b033-04a8fdefbeb3
  external_github_issue_id: null
  tags:
  - pm-engine
  - readiness
  - linear-sync
  - promotion
  component: atlas.pm
  linear_synced_at: '2026-07-04T18:05:38.538342Z'
  last_observed_linear_state_id: 311a3a97-c409-4cce-96ab-0a3bfc2a5541
  status_entered_at: '2026-07-05T15:53:13.776434Z'
  review_cycle_count: 0
  source_anchor: docs/atlas/pm-engine-and-linear-sync.md#sync-loop
  created_by_type: agent
  created_by_id: planner
  created_at: '2026-07-04T18:05:38.538342Z'
  updated_at: '2026-07-04T18:05:38.538342Z'
  completed_at: null
- id: ee56971e-19d5-43a9-8540-5e13d62a7bcf
  product_id: 6363ff3f-22db-40b3-9a47-aeba5d0a7586
  epic_id: e0d47d14-bbd1-4d5d-96bc-f9e7adfeb3cf
  key: ATLAS-48
  title: Blocked state detection and stale-block anomaly logging
  objective: 'Implement the stale-block detection pass (step 5 of the sync loop): when a ticket is in the blocked status but its structural blockers have all cleared (blocked(graph, key) is empty), append one STALE_BLOCK DebtItem per episode and surface the candidate in the delivery report.'
  context: 'A ticket may be marked blocked while its dependency-graph blockers have resolved, leaving it stranded. The PM Engine detects this structurally (not time-based) using the same dependency graph promote_ready projects. The detection is report-only: it never changes ticket state. Per-episode dedup is enforced by Ticket.status_entered_at: one STALE_BLOCK row per episode (since the ticket entered blocked), not one per tick. A NULL status_entered_at is skipped. The graph is the one already projected this tick.'
  status: needs_human_decision
  ticket_type: feature
  risk_level: medium
  priority: 70
  relevant_docs:
  - docs/atlas/pm-engine-and-linear-sync.md
  - docs/atlas/dependency-engine.md
  acceptance_criteria:
  - _detect_stale_block iterates tickets in blocked status and appends one STALE_BLOCK DebtItem when blocked(graph, key) is empty and no STALE_BLOCK row exists for the ticket since status_entered_at.
  - 'Detection is report-only: _detect_stale_block never calls set_state or changes any ticket status.'
  - 'Per-episode dedup: a ticket stranded across multiple ticks produces exactly one STALE_BLOCK row per blocked episode.'
  - A ticket with NULL status_entered_at is skipped (never logged).
  - The graph consumed is the same one projected by promote_ready this tick (not a second projection).
  - STALE_BLOCK items appear in the delivery report output.
  non_goals:
  - Automatically routing a stale-blocked ticket to a new status (report-only by design).
  - Detecting tickets that are structurally blocked but not marked blocked (out of scope per design doc).
  - Dwell-based detection for tickets genuinely blocked (no dwell horizon for blocked status).
  implementation_notes:
  - DebtItem is append-only, system-written (created_by_type=system), one row per observation.
  - The dedup query checks for any STALE_BLOCK row with observed_at >= ticket.status_entered_at.
  - Reuse the blocked(graph, key) function from atlas.dependencies.blocker_analysis.
  test_requirements:
  - 'Unit test: a ticket in blocked with empty blocked(graph, key) and no prior STALE_BLOCK row since status_entered_at produces one DebtItem.'
  - 'Unit test: a ticket in blocked with a non-empty blocked(graph, key) produces no DebtItem.'
  - 'Unit test: calling _detect_stale_block twice in the same episode produces exactly one DebtItem (dedup).'
  - 'Unit test: a ticket with NULL status_entered_at is skipped.'
  - 'Unit test: _detect_stale_block makes no set_state calls.'
  documentation_requirements:
  - Docstring on _detect_stale_block documenting report-only contract and per-episode dedup.
  definition_of_done:
  - All unit tests pass in CI.
  - No set_state call is reachable from _detect_stale_block (verified by test and code review).
  - STALE_BLOCK items visible in atlas pm report output.
  estimated_effort: null
  external_linear_id: 9b20dcd5-159f-428b-88bf-b3988f77b68e
  external_github_issue_id: null
  tags:
  - pm-engine
  - anomaly-detection
  - blocked
  - debt-item
  component: atlas.pm
  linear_synced_at: '2026-07-04T18:05:38.538342Z'
  last_observed_linear_state_id: 311a3a97-c409-4cce-96ab-0a3bfc2a5541
  status_entered_at: '2026-07-05T15:53:13.776434Z'
  review_cycle_count: 0
  source_anchor: docs/atlas/pm-engine-and-linear-sync.md#anomaly-and-dwell-detection
  created_by_type: agent
  created_by_id: planner
  created_at: '2026-07-04T18:05:38.538342Z'
  updated_at: '2026-07-04T18:05:38.538342Z'
  completed_at: null
- id: b3b6c3ac-a583-43bf-a129-50611ada5da9
  product_id: 6363ff3f-22db-40b3-9a47-aeba5d0a7586
  epic_id: e0d47d14-bbd1-4d5d-96bc-f9e7adfeb3cf
  key: ATLAS-49
  title: 'Follow-up comment scanning: producer writing inbox stubs'
  objective: 'Implement step 4 of the sync loop: scan recent Linear issue comments for the atlas:proposed-follow-up tag and write one stub file per tagged comment to docs/planning/inbox/<ticket-key>-<n>.md, deduped by source comment id, without committing the stubs or creating any ticket or Atlas/Linear state.'
  context: 'Agents file follow-up observations as Linear comments tagged atlas:proposed-follow-up. The PM Engine reads these (read-only LinearClient.fetch_comments) and writes stub files to the working tree. The stub is self-identifying: the source comment id is written as an HTML comment on the first line for dedup. The write is atomic (temp+rename). The producer does not commit stubs, does not create tickets, and does not write Atlas or Linear state. Dedup spans both inbox/ and inbox/processed/ so indices stay monotonic after atlas apply moves stubs. The consumer (ATLAS-122) is a separate ticket.'
  status: needs_human_decision
  ticket_type: feature
  risk_level: medium
  priority: 65
  relevant_docs:
  - docs/atlas/pm-engine-and-linear-sync.md
  - docs/atlas/planning-engine-specification.md
  acceptance_criteria:
  - For each ticket with an external_linear_id in a non-terminal status, fetch_comments is called and comments containing the atlas:proposed-follow-up tag are identified.
  - One stub file is written to docs/planning/inbox/<ticket-key>-<n>.md per tagged comment; the stub carries the source comment id as an HTML comment on the first line, the verbatim comment body, and an honest source reference.
  - 'Dedup: a comment whose id already has a stub under inbox/ or inbox/processed/ is skipped; each comment is stubbed at most once.'
  - The write is atomic (write to temp file, then rename into place).
  - The producer creates no ticket, writes no Atlas database state, writes no Linear state, and does not commit the stubs.
  - <n> is the next free index for the ticket key, computed across both inbox/ and inbox/processed/.
  non_goals:
  - Committing the inbox stubs (operator's responsibility).
  - 'The consumer half: atlas plan reading the inbox and atlas apply moving stubs to processed/ (ATLAS-122).'
  - Creating tickets directly from follow-up comments.
  - Parsing or validating the follow-up content beyond tag detection.
  implementation_notes:
  - 'The HTML comment dedup key format: <!-- atlas-source-comment-id: <id> --> on the stub''s first line, separate from the verbatim body.'
  - fetch_comments is read-only; the producer never calls create_issue, update_issue, or set_state.
  - Scan only tickets with external_linear_id in a non-terminal Atlas status to bound the comment fetch volume.
  test_requirements:
  - 'Unit test: a comment containing atlas:proposed-follow-up produces one stub file with correct content and HTML comment dedup key.'
  - 'Unit test: a comment whose id already has a stub in inbox/ is skipped (no second stub written).'
  - 'Unit test: a comment whose id already has a stub in inbox/processed/ is skipped.'
  - 'Unit test: stub filename index is monotonic across inbox/ and inbox/processed/.'
  - 'Unit test: the write is atomic (temp file used, then renamed).'
  - 'Live gate (operator-run): a real atlas:proposed-follow-up comment in Linear produces one stub file.'
  documentation_requirements:
  - Docstring on the scan function documenting the producer-only contract and dedup mechanism.
  - Comment in the stub template showing the HTML comment format.
  definition_of_done:
  - All unit tests pass in CI with InMemoryLinearClient.
  - Live gate documented in the ticket and confirmed by operator before closing.
  - No Atlas or Linear state is written by the producer (verified by test).
  estimated_effort: null
  external_linear_id: 8c364b9b-e354-4bd7-a34a-cce3f8205778
  external_github_issue_id: null
  tags:
  - pm-engine
  - follow-up
  - linear-sync
  - inbox
  component: atlas.pm
  linear_synced_at: '2026-07-04T18:05:38.538342Z'
  last_observed_linear_state_id: 311a3a97-c409-4cce-96ab-0a3bfc2a5541
  status_entered_at: '2026-07-05T15:53:13.776434Z'
  review_cycle_count: 0
  source_anchor: docs/atlas/pm-engine-and-linear-sync.md#follow-up-ingestion
  created_by_type: agent
  created_by_id: planner
  created_at: '2026-07-04T18:05:38.538342Z'
  updated_at: '2026-07-04T18:05:38.538342Z'
  completed_at: null
- id: ee47cedd-443c-4d9c-ae7a-b5737f51548a
  product_id: 6363ff3f-22db-40b3-9a47-aeba5d0a7586
  epic_id: e0d47d14-bbd1-4d5d-96bc-f9e7adfeb3cf
  key: ATLAS-50
  title: 'Delivery-anomaly model: DebtItem and recurrence predicate'
  objective: Implement the DebtItem model (append-only delivery-anomaly record), the debt_items table migration, DebtItemRepo with the add method and the recurring(ticket_id, anomaly_type, threshold) query-time predicate, and the AnomalyType enum covering OUT_OF_OWNERSHIP_TRANSITION, DWELL_BREACH, REVIEW_CYCLE, and STALE_BLOCK.
  context: 'ADR-0011 resolves that DebtItem denotes the delivery-anomaly record: one row per observation, system-written (created_by_type=system), append-only. Recurrence is a query-time predicate (DebtItemRepo.recurring), never a stored counter and never a creation gate. This is the foundational model that all anomaly-logging passes (ATLAS-118, ATLAS-119, ATLAS-120, ATLAS-44) write to. The model must be in place before any anomaly writer can land.'
  status: needs_human_decision
  ticket_type: infrastructure
  risk_level: high
  priority: 95
  relevant_docs:
  - docs/decisions/0011-debtitem-denotes-delivery-anomalies.md
  - docs/atlas/pm-engine-and-linear-sync.md
  acceptance_criteria:
  - 'DebtItem Pydantic model and SQLAlchemy ORM model exist with fields: id, ticket_id (nullable for tick-level records), anomaly_type (AnomalyType enum), observed_at, created_by_type (always system), detail (text), and any per-type fields required by the anomaly writers.'
  - AnomalyType enum covers OUT_OF_OWNERSHIP_TRANSITION, DWELL_BREACH, REVIEW_CYCLE, and STALE_BLOCK.
  - Alembic migration creates the debt_items table; migration is reversible.
  - DebtItemRepo.add persists a new row; it never updates or deletes existing rows (append-only enforced).
  - DebtItemRepo.recurring(ticket_id, anomaly_type, threshold=3) returns True when the count of rows for that ticket and type meets or exceeds the threshold; it is a pure query, never a creation gate.
  - JSON schema is regenerated from the updated Pydantic models.
  non_goals:
  - The code-quality debt register (a distinct future entity per ADR-0011 D2; not DebtItem, not debt_items).
  - Any mutable status lifecycle on DebtItem rows (append-only by design).
  - The anomaly-logging passes themselves (those are separate tickets that depend on this model).
  - TickFailure model (tick-level crash record, separate from DebtItem).
  implementation_notes:
  - ticket_id is nullable to support tick-level records (TickFailure is a separate model; DebtItem rows always have a ticket_id in v1 anomaly writers).
  - observed_at is set at insert time by the repository, not by the caller.
  - The recurring predicate takes a recorded_since parameter for windowed queries (used by dwell and stale-block dedup).
  test_requirements:
  - 'Unit test: DebtItemRepo.add persists a row and a second call creates a second row (append-only).'
  - 'Unit test: DebtItemRepo.recurring returns False below threshold, True at and above threshold.'
  - 'Unit test: the migration applies cleanly on a fresh database and rolls back cleanly.'
  - 'Unit test: DebtItem model round-trips through YAML and the database.'
  - Test that created_by_type is always system on rows created by DebtItemRepo.add.
  documentation_requirements:
  - Docstring on DebtItem referencing ADR-0011 and the append-only contract.
  - Docstring on DebtItemRepo.recurring clarifying it is a query-time predicate, never a creation gate.
  definition_of_done:
  - Migration applies and rolls back cleanly in CI.
  - All unit tests pass.
  - JSON schema regenerated and committed.
  - No update or delete path exists on DebtItem rows (verified by code review and test).
  estimated_effort: null
  external_linear_id: 0bb61fc2-8113-454f-a543-a1a6d05d8696
  external_github_issue_id: null
  tags:
  - pm-engine
  - anomaly-detection
  - debt-item
  - migration
  - storage
  component: atlas.storage
  linear_synced_at: '2026-07-04T18:05:38.538342Z'
  last_observed_linear_state_id: 311a3a97-c409-4cce-96ab-0a3bfc2a5541
  status_entered_at: '2026-07-05T15:53:13.776434Z'
  review_cycle_count: 0
  source_anchor: docs/decisions/0011-debtitem-denotes-delivery-anomalies.md#decision
  created_by_type: agent
  created_by_id: planner
  created_at: '2026-07-04T18:05:38.538342Z'
  updated_at: '2026-07-04T18:05:38.538342Z'
  completed_at: null
- id: 36dece7d-d533-42ae-85ec-ef1cf940aa40
  product_id: 6363ff3f-22db-40b3-9a47-aeba5d0a7586
  epic_id: e0d47d14-bbd1-4d5d-96bc-f9e7adfeb3cf
  key: ATLAS-51
  title: Out-of-ownership transition logging (ATLAS-118)
  objective: 'Implement the out-of-ownership transition logging pass woven into sync_tick''s pull step: when status_from_issue returns None (an unmapped Linear state), append one OUT_OF_OWNERSHIP_TRANSITION DebtItem per transition, deduped by Ticket.last_observed_linear_state_id so a persistent unmapped state logs one row, not one per tick.'
  context: 'Step 1 of the sync loop logs anomalies when a Linear state does not follow the ownership table (status_from_issue returns None). The dedup key is Ticket.last_observed_linear_state_id: the row fires only when the observed state id changes into an out-of-ownership state. A re-occurrence (unmapped→mapped→unmapped) is a genuine new transition and logs again. This is the first writer of the DebtItem model. It never changes ticket state.'
  status: needs_human_decision
  ticket_type: feature
  risk_level: medium
  priority: 80
  relevant_docs:
  - docs/atlas/pm-engine-and-linear-sync.md
  - docs/decisions/0011-debtitem-denotes-delivery-anomalies.md
  acceptance_criteria:
  - When status_from_issue returns None for a ticket, one OUT_OF_OWNERSHIP_TRANSITION DebtItem is appended if the observed Linear state id differs from Ticket.last_observed_linear_state_id.
  - Ticket.last_observed_linear_state_id is updated to the current state id after the dedup check, regardless of whether a row was appended.
  - A persistent unmapped state across multiple ticks produces exactly one DebtItem row (dedup by state id change).
  - A re-occurrence (unmapped→mapped→unmapped) produces a second DebtItem row (genuine new transition).
  - The logging pass never calls set_state or changes any ticket status.
  - The anomaly count is included in SyncResult.
  non_goals:
  - Automatically routing tickets with out-of-ownership transitions to a different status.
  - Recurrence-based escalation (recurrence is a query-time predicate in DebtItemRepo, not a creation gate).
  - Dwell or review-cycle anomaly logging (separate tickets).
  implementation_notes:
  - last_observed_linear_state_id is a new nullable string column on the Ticket model; add via Alembic migration.
  - The dedup check and the update of last_observed_linear_state_id must be in the same transaction as the DebtItem insert.
  - Woven into sync_tick's pull step, not a separate pass.
  test_requirements:
  - 'Unit test: an unmapped state id on first observation appends one OUT_OF_OWNERSHIP_TRANSITION row and updates last_observed_linear_state_id.'
  - 'Unit test: the same unmapped state id on a second tick produces no additional row.'
  - 'Unit test: unmapped→mapped→unmapped produces two rows total.'
  - 'Unit test: the logging pass makes no set_state calls.'
  - 'Unit test: SyncResult anomaly count reflects the number of rows appended this tick.'
  documentation_requirements:
  - Docstring on the logging logic documenting the dedup mechanism and the last_observed_linear_state_id field.
  definition_of_done:
  - Alembic migration for last_observed_linear_state_id applies and rolls back cleanly.
  - All unit tests pass in CI.
  - No set_state call reachable from the logging pass.
  estimated_effort: null
  external_linear_id: 828d1419-c4d3-4028-be3f-32666ec45700
  external_github_issue_id: null
  tags:
  - pm-engine
  - anomaly-detection
  - linear-sync
  - debt-item
  component: atlas.pm
  linear_synced_at: '2026-07-04T18:05:38.538342Z'
  last_observed_linear_state_id: 311a3a97-c409-4cce-96ab-0a3bfc2a5541
  status_entered_at: '2026-07-05T15:53:13.776434Z'
  review_cycle_count: 0
  source_anchor: docs/atlas/pm-engine-and-linear-sync.md#anomaly-and-dwell-detection
  created_by_type: agent
  created_by_id: planner
  created_at: '2026-07-04T18:05:38.538342Z'
  updated_at: '2026-07-04T18:05:38.538342Z'
  completed_at: null
- id: f553dff8-69a2-47d5-b7ba-1b33d862b7b0
  product_id: 6363ff3f-22db-40b3-9a47-aeba5d0a7586
  epic_id: e0d47d14-bbd1-4d5d-96bc-f9e7adfeb3cf
  key: ATLAS-52
  title: Dwell-breach logging (ATLAS-119)
  objective: 'Implement the dwell-breach detection pass (step 5 of the sync loop): for tickets in in_progress, pr_open, or review_required that have exceeded their configured dwell horizon, append one DWELL_BREACH DebtItem per episode, deduped by Ticket.status_entered_at.'
  context: 'Dwell horizons (defaults: in_progress 24h, pr_open 48h, review_required 7d) are configurable. The detection is report-only: it never changes ticket state. Per-episode dedup is enforced by status_entered_at: one DWELL_BREACH row per episode (since the ticket entered its current status). A NULL status_entered_at is skipped. status_entered_at is stamped by apply_linear_status (the sole post-creation status writer) on every real status change. This pass requires the status_entered_at field on Ticket, which must be added via migration if not already present.'
  status: needs_human_decision
  ticket_type: feature
  risk_level: medium
  priority: 75
  relevant_docs:
  - docs/atlas/pm-engine-and-linear-sync.md
  - docs/decisions/0011-debtitem-denotes-delivery-anomalies.md
  acceptance_criteria:
  - _detect_dwell iterates tickets in in_progress, pr_open, and review_required; for each ticket past its horizon with no DWELL_BREACH row since status_entered_at, appends one DWELL_BREACH DebtItem.
  - 'Dwell horizons are configurable (defaults: in_progress 24h, pr_open 48h, review_required 7d).'
  - 'Per-episode dedup: a ticket past its horizon across multiple ticks produces exactly one DWELL_BREACH row per episode.'
  - A ticket with NULL status_entered_at is skipped (never logged).
  - _detect_dwell never calls set_state or changes any ticket status.
  - DWELL_BREACH items appear in the delivery report output.
  non_goals:
  - Automatically routing dwell-breached tickets to a different status (report-only).
  - Dwell detection for the blocked status (no horizon for blocked by design).
  - Review-cycle detection (separate ticket).
  implementation_notes:
  - status_entered_at is a nullable datetime column on Ticket; add via Alembic migration if not already present (coordinate with ATLAS-118/120 which also need it).
  - 'The dedup query: no DWELL_BREACH row with observed_at >= ticket.status_entered_at for this ticket.'
  - now is taken fresh per tick by the scheduler; _detect_dwell receives it as a parameter for testability.
  test_requirements:
  - 'Unit test: a ticket in in_progress past 24h with no prior DWELL_BREACH since status_entered_at produces one DebtItem.'
  - 'Unit test: calling _detect_dwell twice in the same episode produces exactly one DebtItem.'
  - 'Unit test: a ticket with NULL status_entered_at is skipped.'
  - 'Unit test: a ticket not past its horizon produces no DebtItem.'
  - 'Unit test: _detect_dwell makes no set_state calls.'
  - 'Unit test: configurable horizons are respected (override defaults in test).'
  documentation_requirements:
  - Docstring on _detect_dwell documenting report-only contract, configurable horizons, and per-episode dedup.
  definition_of_done:
  - Alembic migration for status_entered_at applies and rolls back cleanly (if not already added).
  - All unit tests pass in CI.
  - No set_state call reachable from _detect_dwell.
  - DWELL_BREACH items visible in atlas pm report output.
  estimated_effort: null
  external_linear_id: 423478b2-6694-429b-a70a-7e4188ee7d7c
  external_github_issue_id: null
  tags:
  - pm-engine
  - anomaly-detection
  - dwell
  - debt-item
  component: atlas.pm
  linear_synced_at: '2026-07-04T18:05:38.538342Z'
  last_observed_linear_state_id: 311a3a97-c409-4cce-96ab-0a3bfc2a5541
  status_entered_at: '2026-07-05T15:53:13.776434Z'
  review_cycle_count: 0
  source_anchor: docs/atlas/pm-engine-and-linear-sync.md#anomaly-and-dwell-detection
  created_by_type: agent
  created_by_id: planner
  created_at: '2026-07-04T18:05:38.538342Z'
  updated_at: '2026-07-04T18:05:38.538342Z'
  completed_at: null
- id: 7e3f7111-f5db-43af-8fce-89ef40ff4d04
  product_id: 6363ff3f-22db-40b3-9a47-aeba5d0a7586
  epic_id: e0d47d14-bbd1-4d5d-96bc-f9e7adfeb3cf
  key: ATLAS-53
  title: Review-cycling detection and Needs Human routing (ATLAS-120)
  objective: 'Implement the review-cycling detection pass (step 5 of the sync loop): when a ticket''s review_cycle_count exceeds 3 while in changes_requested or pr_open, route it to needs_human_decision via LinearClient.set_state and append one REVIEW_CYCLE DebtItem, deduped per pr_open episode.'
  context: Review cycling is the one anomaly that changes ticket state. The round-trip count is tracked on Ticket.review_cycle_count, incremented by apply_linear_status only on changes_requested→pr_open transitions. The routing uses the same sanctioned set_state path as readiness promotion, resolved via state_id_for(needs_human_decision). The REVIEW_CYCLE DebtItem is a deterministic failure-analysis summary (no model call, no Linear comment). Dedup is per pr_open episode via status_entered_at. The write is Linear-only; the next pull reconciles Atlas status.
  status: needs_human_decision
  ticket_type: feature
  risk_level: high
  priority: 72
  relevant_docs:
  - docs/atlas/pm-engine-and-linear-sync.md
  - docs/atlas/symphony-integration.md
  acceptance_criteria:
  - _detect_review_cycle routes a ticket to needs_human_decision via set_state when review_cycle_count > 3 and the ticket is in changes_requested or pr_open.
  - One REVIEW_CYCLE DebtItem is appended per pr_open episode (deduped by status_entered_at boundary).
  - The routing uses state_id_for(needs_human_decision), validated at load (exactly one state must map to needs_human_decision).
  - The write is Linear-only; Atlas status is reconciled by the next pull tick.
  - 'The write is idempotent: a ticket already routed to needs_human_decision is not re-routed.'
  - review_cycle_count is monotonic in v1 (no reset on human intervention).
  non_goals:
  - Resetting review_cycle_count on human intervention (monotonic in v1).
  - LLM-generated failure analysis or Linear comments (the DebtItem is the deterministic summary).
  - Dwell or out-of-ownership anomaly logging (separate tickets).
  - Intra-ticket retry orchestration (Symphony's responsibility).
  implementation_notes:
  - state_id_for(needs_human_decision) uses the same inverted status map as state_id_for(ready_for_agent); validate both at scheduler startup.
  - The REVIEW_CYCLE DebtItem detail field carries a human-readable summary of the cycle count and ticket key.
  - The route runs first, then the DebtItem is appended (so the row is written even if set_state fails, for observability).
  test_requirements:
  - 'Unit test: a ticket with review_cycle_count=4 in pr_open triggers set_state to needs_human_decision and appends one REVIEW_CYCLE DebtItem.'
  - 'Unit test: a ticket with review_cycle_count=3 (not exceeding threshold) is not routed.'
  - 'Unit test: calling _detect_review_cycle twice in the same pr_open episode produces exactly one DebtItem.'
  - 'Unit test: a ticket already in needs_human_decision is not re-routed (idempotency).'
  - 'Unit test: load-time validation raises if zero or multiple states map to needs_human_decision.'
  documentation_requirements:
  - Docstring on _detect_review_cycle documenting that it is the one anomaly that changes ticket state.
  definition_of_done:
  - All unit tests pass in CI.
  - Load-time validation of needs_human_decision state is tested.
  - REVIEW_CYCLE items visible in atlas pm report output.
  estimated_effort: null
  external_linear_id: eca2797d-f5a3-4752-8c89-b95e616af2ef
  external_github_issue_id: null
  tags:
  - pm-engine
  - anomaly-detection
  - review-cycle
  - debt-item
  component: atlas.pm
  linear_synced_at: '2026-07-04T18:05:38.538342Z'
  last_observed_linear_state_id: 311a3a97-c409-4cce-96ab-0a3bfc2a5541
  status_entered_at: '2026-07-05T15:53:13.776434Z'
  review_cycle_count: 0
  source_anchor: docs/atlas/pm-engine-and-linear-sync.md#anomaly-and-dwell-detection
  created_by_type: agent
  created_by_id: planner
  created_at: '2026-07-04T18:05:38.538342Z'
  updated_at: '2026-07-04T18:05:38.538342Z'
  completed_at: null
- id: 37f3f8e8-e44a-4148-95dc-4a4c07906ec2
  product_id: 6363ff3f-22db-40b3-9a47-aeba5d0a7586
  epic_id: e0d47d14-bbd1-4d5d-96bc-f9e7adfeb3cf
  key: ATLAS-54
  title: State-transition history model (TicketStatusTransition)
  objective: 'Implement the TicketStatusTransition append-only model and migration: every real status change written by apply_linear_status appends one row recording the from_status, to_status, and transitioned_at timestamp, enabling true historical per-state cycle time computation.'
  context: ATLAS-121 in the roadmap. apply_linear_status is the sole post-creation status writer; it must append a TicketStatusTransition row on every real status change. This log is the prerequisite for historical cycle time in the delivery report (ATLAS-126). Until it lands, the delivery report can only report current-dwell proxy metrics. The model is append-only; rows are never updated or deleted.
  status: needs_human_decision
  ticket_type: infrastructure
  risk_level: high
  priority: 88
  relevant_docs:
  - docs/atlas/pm-engine-and-linear-sync.md
  - docs/atlas/implementation-roadmap.md
  acceptance_criteria:
  - 'TicketStatusTransition Pydantic and SQLAlchemy models exist with fields: id, ticket_id, from_status (nullable for initial), to_status, transitioned_at.'
  - Alembic migration creates the ticket_status_transitions table; migration is reversible.
  - apply_linear_status appends one TicketStatusTransition row on every real status change (from_status != to_status).
  - 'Rows are append-only: no update or delete path exists on TicketStatusTransition.'
  - JSON schema is regenerated from the updated Pydantic models.
  - TicketStatusTransitionRepo.for_ticket(ticket_id) returns rows ordered by transitioned_at ascending.
  non_goals:
  - Computing cycle time from the log (that is the delivery metrics upgrade ticket).
  - Storing transitions for non-status field changes.
  - Any mutable lifecycle on transition rows.
  implementation_notes:
  - from_status is nullable to represent the initial state before the first recorded transition.
  - transitioned_at is set by the repository at insert time (not by the caller) to ensure consistency.
  - The append-only invariant is enforced at the repository layer (no update/delete methods exposed).
  test_requirements:
  - 'Unit test: apply_linear_status on a real status change appends one TicketStatusTransition row with correct from/to/timestamp.'
  - 'Unit test: apply_linear_status on a no-op (same status) appends no row.'
  - 'Unit test: the migration applies and rolls back cleanly.'
  - 'Unit test: TicketStatusTransition round-trips through the database.'
  - 'Unit test: for_ticket returns rows in transitioned_at ascending order.'
  documentation_requirements:
  - Docstring on TicketStatusTransition referencing its role as the cycle-time log.
  - Docstring on apply_linear_status noting it appends a transition row on every real change.
  definition_of_done:
  - Migration applies and rolls back cleanly in CI.
  - All unit tests pass.
  - JSON schema regenerated and committed.
  - No update or delete path on TicketStatusTransition rows.
  estimated_effort: null
  external_linear_id: 93b0ea90-83e4-4e9e-9d87-6fa203ec1ae9
  external_github_issue_id: null
  tags:
  - pm-engine
  - storage
  - migration
  - cycle-time
  - status
  component: atlas.storage
  linear_synced_at: '2026-07-04T18:05:38.538342Z'
  last_observed_linear_state_id: 311a3a97-c409-4cce-96ab-0a3bfc2a5541
  status_entered_at: '2026-07-05T15:53:13.776434Z'
  review_cycle_count: 0
  source_anchor: docs/atlas/pm-engine-and-linear-sync.md#delivery-metrics
  created_by_type: agent
  created_by_id: planner
  created_at: '2026-07-04T18:05:38.538342Z'
  updated_at: '2026-07-04T18:05:38.538342Z'
  completed_at: null
- id: 20b98fd1-08aa-473a-b72c-a14ce6052718
  product_id: 6363ff3f-22db-40b3-9a47-aeba5d0a7586
  epic_id: e0d47d14-bbd1-4d5d-96bc-f9e7adfeb3cf
  key: ATLAS-55
  title: Tick-failure record model (TickFailure) and recorded_since predicate
  objective: 'Implement the TickFailure append-only model and migration: one row per tick-level crash, system-attributed, with a failure_signature (exception type FQCN) and detail field; and the DebtItemRepo.recorded_since(signature, since) query-time dedup predicate used by the scheduler''s create-on-crash logic.'
  context: 'ATLAS-125 in the roadmap. TickFailure is a separate model from DebtItem (no ticket_id; it is tick-level). The scheduler (ATLAS-50) is the sole writer: when sync_tick raises, the scheduler records one TickFailure and continues. The dedup predicate recorded_since(signature, since) returns True if a TickFailure with that signature exists since the given timestamp, enabling the scheduler to bound row volume (default 1-hour window). The count surfaces in the delivery report.'
  status: needs_human_decision
  ticket_type: infrastructure
  risk_level: medium
  priority: 82
  relevant_docs:
  - docs/atlas/pm-engine-and-linear-sync.md
  - docs/atlas/implementation-roadmap.md
  acceptance_criteria:
  - 'TickFailure Pydantic and SQLAlchemy models exist with fields: id, failure_signature (exception FQCN), detail (text), recorded_at.'
  - Alembic migration creates the tick_failures table; migration is reversible.
  - TickFailureRepo.add persists a new row; rows are append-only (no update or delete).
  - TickFailureRepo.recorded_since(signature, since) returns True if any row with that signature has recorded_at >= since.
  - JSON schema is regenerated from the updated Pydantic models.
  - TickFailure count (total and within the dedup window) is accessible for the delivery report.
  non_goals:
  - The scheduler that calls TickFailureRepo.add (that is the PM scheduler ticket).
  - Surfacing TickFailure in the delivery report (that is the delivery metrics ticket).
  - Any mutable lifecycle on TickFailure rows.
  - Per-ticket crash records (TickFailure is tick-level, not ticket-level).
  implementation_notes:
  - failure_signature is the caught exception's fully-qualified type name (e.g. requests.exceptions.ConnectionError).
  - recorded_at is set by the repository at insert time.
  - The dedup window is supplied by the caller (the scheduler); the model and repo carry no default window.
  test_requirements:
  - 'Unit test: TickFailureRepo.add persists a row; a second call creates a second row.'
  - 'Unit test: recorded_since returns True when a matching row exists within the window, False otherwise.'
  - 'Unit test: the migration applies and rolls back cleanly.'
  - 'Unit test: TickFailure round-trips through the database.'
  documentation_requirements:
  - Docstring on TickFailure documenting tick-level scope and separation from DebtItem.
  - Docstring on recorded_since documenting the windowed dedup contract.
  definition_of_done:
  - Migration applies and rolls back cleanly in CI.
  - All unit tests pass.
  - JSON schema regenerated and committed.
  estimated_effort: null
  external_linear_id: 6aa4aa9c-a7da-4ba3-ad6c-9a48e6dc9f69
  external_github_issue_id: null
  tags:
  - pm-engine
  - storage
  - migration
  - scheduler
  - tick-failure
  component: atlas.storage
  linear_synced_at: '2026-07-04T18:05:38.538342Z'
  last_observed_linear_state_id: 311a3a97-c409-4cce-96ab-0a3bfc2a5541
  status_entered_at: '2026-07-05T15:53:13.776434Z'
  review_cycle_count: 0
  source_anchor: docs/atlas/pm-engine-and-linear-sync.md#sync-loop
  created_by_type: agent
  created_by_id: planner
  created_at: '2026-07-04T18:05:38.538342Z'
  updated_at: '2026-07-04T18:05:38.538342Z'
  completed_at: null
- id: 4c2979aa-5add-400e-a638-cccd2f6d8e4e
  product_id: 6363ff3f-22db-40b3-9a47-aeba5d0a7586
  epic_id: e0d47d14-bbd1-4d5d-96bc-f9e7adfeb3cf
  key: ATLAS-56
  title: 'PM scheduler: recurring sync_tick loop with create-on-crash'
  objective: 'Implement the PM scheduler in atlas/pm/scheduler.py: a plain loop that calls sync_tick on a configurable cadence, records a TickFailure on crash and continues, supports --once for a single tick, and shuts down gracefully on SIGTERM/SIGINT after the in-flight tick completes.'
  context: 'ATLAS-50 in the roadmap. The scheduler is the recurring driver of the sync loop. It is a plain loop with an interruptible sleep between ticks (default 60s, --interval). --once runs exactly one tick and exits. Graceful shutdown: SIGTERM/SIGINT sets a shutdown flag consulted only after the in-flight tick returns. Create-on-crash: when sync_tick raises, the scheduler calls TickFailureRepo.add (if recorded_since returns False for the signature within the CRASH_DEDUP_WINDOW of 1 hour) and continues. The scheduler is exposed via atlas pm sync CLI command.'
  status: needs_human_decision
  ticket_type: feature
  risk_level: medium
  priority: 78
  relevant_docs:
  - docs/atlas/pm-engine-and-linear-sync.md
  - docs/atlas/implementation-roadmap.md
  acceptance_criteria:
  - The scheduler loop calls sync_tick on a configurable interval (default 60s, --interval flag).
  - --once runs exactly one tick and exits cleanly.
  - SIGTERM/SIGINT sets a shutdown flag; the loop exits only after the in-flight tick completes (never mid-write).
  - When sync_tick raises, the scheduler records one TickFailure (if recorded_since returns False for the signature within CRASH_DEDUP_WINDOW=1h) and continues to the next tick.
  - The scheduler is exposed as atlas pm sync [--once] [--interval N] CLI command.
  - CRASH_DEDUP_WINDOW is a module constant (default 1 hour); two distinct exception types produce separate TickFailure rows.
  non_goals:
  - Distributed job scheduling or cron integration.
  - Restarting agent sessions (Symphony's responsibility).
  - The sync_tick implementation itself (separate ticket).
  - The TickFailure model (separate ticket).
  implementation_notes:
  - Use signal.signal(SIGTERM, ...) and signal.signal(SIGINT, ...) to set the shutdown flag.
  - The interruptible sleep should use a short-poll or threading.Event.wait so SIGINT is responsive.
  - CRASH_DEDUP_WINDOW = datetime.timedelta(hours=1) as a module-level constant.
  - failure_signature = type(exc).__module__ + '.' + type(exc).__qualname__.
  test_requirements:
  - 'Unit test: --once calls sync_tick exactly once and exits.'
  - 'Unit test: a crashing sync_tick causes one TickFailure to be recorded (when not within dedup window) and the loop continues.'
  - 'Unit test: a second crash with the same signature within CRASH_DEDUP_WINDOW produces no additional TickFailure row.'
  - 'Unit test: SIGTERM after tick start does not interrupt the in-flight tick; the loop exits after it completes.'
  - 'Unit test: the --interval flag is respected (mock sleep).'
  documentation_requirements:
  - Docstring on the scheduler loop documenting graceful shutdown and create-on-crash behaviour.
  - CLI help text for atlas pm sync documenting --once and --interval.
  definition_of_done:
  - All unit tests pass in CI.
  - atlas pm sync --once runs a single tick and exits with code 0 on success.
  - Graceful shutdown tested (SIGTERM after tick start).
  estimated_effort: null
  external_linear_id: fa482747-074c-43a0-863d-86c3bb943b2a
  external_github_issue_id: null
  tags:
  - pm-engine
  - scheduler
  - sync-tick
  - cli
  component: atlas.pm
  linear_synced_at: '2026-07-04T18:05:38.538342Z'
  last_observed_linear_state_id: 311a3a97-c409-4cce-96ab-0a3bfc2a5541
  status_entered_at: '2026-07-05T15:53:13.776434Z'
  review_cycle_count: 0
  source_anchor: docs/atlas/pm-engine-and-linear-sync.md#sync-loop
  created_by_type: agent
  created_by_id: planner
  created_at: '2026-07-04T18:05:38.538342Z'
  updated_at: '2026-07-04T18:05:38.538342Z'
  completed_at: null
- id: 0f73f53b-ba39-45d3-bee6-a0c3599b4df7
  product_id: 6363ff3f-22db-40b3-9a47-aeba5d0a7586
  epic_id: e0d47d14-bbd1-4d5d-96bc-f9e7adfeb3cf
  key: ATLAS-57
  title: Delivery metrics CLI report (atlas pm report)
  objective: 'Implement atlas pm report: a pure-reader CLI command that computes and displays throughput (tickets done/week), cycle time per state from the TicketStatusTransition log, ready-queue depth, anomaly counts by type with recurring flags, and dwell breaches — all from stored tickets and DebtItems, with no Linear calls and no writes.'
  context: 'ATLAS-47 in the roadmap. The delivery report is a pure reader: it makes no Linear calls and writes nothing, computing every metric from stored tickets and DebtItems. Throughput is bucketed by ISO week of status_entered_at for done tickets. Cycle time per state is computed from the TicketStatusTransition log (completed episodes only). Ready-queue depth is the count of tickets in ready_for_agent. Anomaly counts are grouped by AnomalyType with recurring ones called out. Dwell breaches are the DWELL_BREACH subset. Output is CLI/markdown with a --json form.'
  status: needs_human_decision
  ticket_type: feature
  risk_level: low
  priority: 60
  relevant_docs:
  - docs/atlas/pm-engine-and-linear-sync.md
  - docs/decisions/0005-code-calculates-agents-interpret.md
  acceptance_criteria:
  - atlas pm report prints throughput (tickets done/week, bucketed by ISO week of status_entered_at; done tickets with null entry time fall in an 'unknown' bucket).
  - Cycle time per state is computed from TicketStatusTransition completed episodes (min/median/max hours and episode count per state); states with no completed episodes are omitted.
  - Ready-queue depth is the count of tickets in ready_for_agent.
  - Anomaly counts are grouped by AnomalyType; recurring anomalies (DebtItemRepo.recurring predicate) are flagged per type.
  - Dwell breaches (DWELL_BREACH DebtItems) are listed per ticket.
  - --json emits a machine-readable form of the same data.
  - The command makes no Linear API calls and writes no state.
  non_goals:
  - A dashboard or web UI (CLI/markdown only per Revision 1).
  - Historical cycle time before TicketStatusTransition rows exist (the report shows what the log contains).
  - 'LLM-generated narrative or interpretation (ADR-0005: code calculates, agents interpret).'
  - TickFailure count in the report (that is a follow-up once the scheduler lands).
  implementation_notes:
  - All metric computations are deterministic Python (ADR-0005); no LLM calls.
  - Cycle time uses datetime subtraction over ordered TicketStatusTransition rows; the current open episode (no exit) is excluded.
  - build_delivery_report is a pure function taking stored data as input, returning a typed report object; the CLI renders it.
  - Median is computed with statistics.median over episode durations in hours.
  test_requirements:
  - 'Unit test: throughput computation buckets done tickets by ISO week correctly; null entry time goes to ''unknown''.'
  - 'Unit test: cycle time computation over a fixture transition log produces correct min/median/max per state.'
  - 'Unit test: ready-queue depth counts only ready_for_agent tickets.'
  - 'Unit test: anomaly counts group correctly and recurring flag is set when DebtItemRepo.recurring returns True.'
  - 'Unit test: --json output is valid JSON matching the report schema.'
  - 'Unit test: the command makes no network calls (verified by injecting a mock that raises on any HTTP call).'
  documentation_requirements:
  - CLI help text for atlas pm report documenting each metric and the --json flag.
  - Docstring on build_delivery_report referencing ADR-0005 (code calculates).
  definition_of_done:
  - All unit tests pass in CI.
  - atlas pm report runs on a fixture database and produces correct output.
  - --json output is valid and documented.
  - No network calls made by the command.
  estimated_effort: null
  external_linear_id: 10324c77-8364-4d91-8e6d-7a0f2546cb46
  external_github_issue_id: null
  tags:
  - pm-engine
  - metrics
  - cli
  - delivery-report
  component: atlas.cli
  linear_synced_at: '2026-07-04T18:05:38.538342Z'
  last_observed_linear_state_id: 311a3a97-c409-4cce-96ab-0a3bfc2a5541
  status_entered_at: '2026-07-05T15:53:13.776434Z'
  review_cycle_count: 0
  source_anchor: docs/atlas/pm-engine-and-linear-sync.md#delivery-metrics
  created_by_type: agent
  created_by_id: planner
  created_at: '2026-07-04T18:05:38.538342Z'
  updated_at: '2026-07-04T18:05:38.538342Z'
  completed_at: null
- id: cb343250-4af6-486e-a7a2-539acc0de58a
  product_id: 6363ff3f-22db-40b3-9a47-aeba5d0a7586
  epic_id: e0d47d14-bbd1-4d5d-96bc-f9e7adfeb3cf
  key: ATLAS-58
  title: 'Historical cycle time upgrade: compute from TicketStatusTransition log'
  objective: Upgrade atlas pm report to compute true historical per-state cycle time from the TicketStatusTransition log (ATLAS-121), replacing the current-dwell proxy, by aggregating completed episodes (state entry to exit) into min/median/max hours per state across all tickets.
  context: ATLAS-126 in the roadmap. Once TicketStatusTransition rows exist (from the state-transition history ticket), the delivery report can compute true historical cycle time rather than the current-dwell proxy. A completed episode is a pair of consecutive transitions (T_i, T_{i+1}) for a ticket; the duration is T_{i+1}.transitioned_at - T_i.transitioned_at. The current open episode (no exit) is excluded. This ticket upgrades build_delivery_report to use the log; it depends on the TicketStatusTransition model being in place.
  status: needs_human_decision
  ticket_type: feature
  risk_level: low
  priority: 55
  relevant_docs:
  - docs/atlas/pm-engine-and-linear-sync.md
  - docs/atlas/implementation-roadmap.md
  acceptance_criteria:
  - build_delivery_report computes per-state cycle time from TicketStatusTransition completed episodes (min/median/max hours, episode count) rather than current-dwell proxy.
  - A completed episode is a consecutive transition pair (T_i, T_{i+1}); duration = T_{i+1}.transitioned_at - T_i.transitioned_at.
  - The current open episode (last transition with no subsequent exit) is excluded from aggregation.
  - A state re-visited N times contributes N episodes to the aggregation.
  - States with no completed episodes do not appear in the output.
  - The computation is deterministic timestamp subtraction (ADR-0005); no LLM calls.
  non_goals:
  - Cycle time for the current open episode (excluded by design).
  - Percentile breakdowns beyond min/median/max (follow-up if needed).
  - Backfilling historical transitions for tickets created before the model existed.
  implementation_notes:
  - Read TicketStatusTransitionRepo.for_ticket for each ticket; pair consecutive rows to form episodes.
  - Aggregate durations per to_status (the state entered at T_i) across all tickets.
  - statistics.median over a list of float hours; handle single-episode states (median = that value).
  test_requirements:
  - 'Unit test: a fixture transition log with known durations produces correct min/median/max per state.'
  - 'Unit test: a state visited twice contributes two episodes.'
  - 'Unit test: the current open episode is excluded.'
  - 'Unit test: a state with no completed episodes is absent from the output.'
  - 'Regression test: the upgraded report produces the same throughput, ready-queue, and anomaly outputs as before.'
  documentation_requirements:
  - Update docstring on build_delivery_report to reflect the transition-log source.
  - Note in CLI help that cycle time requires TicketStatusTransition rows (populated after the state-transition model lands).
  definition_of_done:
  - All unit tests pass in CI.
  - atlas pm report on a fixture database with transition rows shows correct historical cycle time.
  - Regression tests confirm other report metrics are unchanged.
  estimated_effort: null
  external_linear_id: 95650aa7-41c1-4db4-ba51-d722bf9aeb24
  external_github_issue_id: null
  tags:
  - pm-engine
  - metrics
  - cycle-time
  - delivery-report
  component: atlas.pm
  linear_synced_at: '2026-07-04T18:05:38.538342Z'
  last_observed_linear_state_id: 311a3a97-c409-4cce-96ab-0a3bfc2a5541
  status_entered_at: '2026-07-05T15:53:13.776434Z'
  review_cycle_count: 0
  source_anchor: docs/atlas/pm-engine-and-linear-sync.md#delivery-metrics
  created_by_type: agent
  created_by_id: planner
  created_at: '2026-07-04T18:05:38.538342Z'
  updated_at: '2026-07-04T18:05:38.538342Z'
  completed_at: null
- id: 7814b4f1-3ee1-45fb-b7ec-447c0cc478e6
  product_id: 6363ff3f-22db-40b3-9a47-aeba5d0a7586
  epic_id: e0d47d14-bbd1-4d5d-96bc-f9e7adfeb3cf
  key: ATLAS-59
  title: 'Follow-up consumer: atlas plan reads inbox, atlas apply retires stubs'
  objective: 'Implement the consumer half of follow-up ingestion (ATLAS-122): atlas plan reads committed docs/planning/inbox/ stubs as a separate input source alongside the main corpus, and atlas apply moves applied or rejected stubs to docs/planning/inbox/processed/ after the plan is applied or rejected.'
  context: 'ATLAS-122 in the roadmap. The producer (ATLAS-45) writes stubs to the working tree; the operator commits them. The consumer closes the loop: atlas plan reads the committed inbox as a separate input document set (its own anchor index and input_doc_shas, distinct from the main corpus), and atlas apply retires stubs to processed/ idempotently. Follow-ups enter the backlog only through plan/apply (ADR-0007), never as direct ticket creation. An uncommitted stub is a typed dirty error (fail-closed). An empty inbox is a no-op.'
  status: needs_human_decision
  ticket_type: feature
  risk_level: medium
  priority: 58
  relevant_docs:
  - docs/atlas/pm-engine-and-linear-sync.md
  - docs/atlas/planning-engine-specification.md
  acceptance_criteria:
  - atlas plan reads committed docs/planning/inbox/ stubs (excluding processed/) as a separate input source; each stub is included in the anchor index and input_doc_shas.
  - An uncommitted stub in the inbox fails ingestion with a typed dirty error (fail-closed, consistent with the main corpus contract).
  - An empty inbox is a no-op; atlas plan proceeds normally.
  - atlas apply moves stubs that fed the applied or rejected plan to docs/planning/inbox/processed/ idempotently (the move is safe to repeat).
  - The staleness re-check in atlas apply step 2 includes inbox SHAs, so an inbox change between plan and apply reads as stale.
  - Follow-ups enter the backlog only through plan/apply; no direct ticket creation path exists.
  non_goals:
  - The producer that writes stubs (ATLAS-45, separate ticket).
  - Validating or parsing the follow-up content beyond including it as a document.
  - Automatically committing inbox stubs (operator's responsibility).
  - Any direct ticket creation from inbox stubs.
  implementation_notes:
  - The inbox glob is docs/planning/inbox/*.md (excluding processed/ subdir).
  - The move to processed/ is atomic (rename); if the destination already exists, skip (idempotent).
  - The inbox input source is distinct from the main corpus globs in the ingestion layer.
  - processed/ stubs are excluded from the inbox glob to avoid re-reading retired stubs.
  test_requirements:
  - 'Unit test: atlas plan with a committed stub includes it in input_doc_shas and the anchor index.'
  - 'Unit test: an uncommitted stub raises a typed dirty error.'
  - 'Unit test: an empty inbox produces no change to plan behaviour.'
  - 'Unit test: atlas apply moves stubs to processed/ after apply; re-running apply is idempotent.'
  - 'Unit test: a stub changed between plan and apply causes a stale-plan refusal.'
  documentation_requirements:
  - Update planning-engine-specification.md §2.1 inline reference to the inbox as a separate input source (if not already present).
  - Docstring on the inbox ingestion function documenting the fail-closed dirty-error contract.
  definition_of_done:
  - All unit tests pass in CI.
  - atlas plan with a fixture committed stub includes it in the plan run's input_doc_shas.
  - atlas apply moves the stub to processed/ and the move is idempotent.
  estimated_effort: null
  external_linear_id: 6970a15b-1035-43c4-8465-811fac353d14
  external_github_issue_id: null
  tags:
  - pm-engine
  - follow-up
  - planning
  - inbox
  component: atlas.planning
  linear_synced_at: '2026-07-04T18:05:38.538342Z'
  last_observed_linear_state_id: 311a3a97-c409-4cce-96ab-0a3bfc2a5541
  status_entered_at: '2026-07-05T15:53:13.776434Z'
  review_cycle_count: 0
  source_anchor: docs/atlas/pm-engine-and-linear-sync.md#follow-up-ingestion
  created_by_type: agent
  created_by_id: planner
  created_at: '2026-07-04T18:05:38.538342Z'
  updated_at: '2026-07-04T18:05:38.538342Z'
  completed_at: null
- id: 5e210d54-be6d-46c2-981d-60504a865454
  product_id: 6363ff3f-22db-40b3-9a47-aeba5d0a7586
  epic_id: 9f6a68c3-3279-45aa-ab8c-462b43007dbd
  key: ATLAS-60
  title: 'Ticket tags and component fields: storage layer'
  objective: Add free-form tags and component fields to the stored Ticket model and database schema so the context renderer can match tickets against ADRs and lessons by tag/component.
  context: 'The context renderer (Phase 5) matches lessons and ADRs against a ticket''s tags and component. These fields do not yet exist on the Ticket model or in the database. This ticket adds the storage half: the Pydantic model field, the SQLAlchemy column, and the Alembic migration. The planner-emission half (ATLAS-128) is a separate ticket that depends on this one.'
  status: needs_human_decision
  ticket_type: feature
  risk_level: medium
  priority: 50
  relevant_docs:
  - docs/atlas/context-renderer.md
  - docs/atlas/implementation-roadmap.md
  acceptance_criteria:
  - 'Ticket model gains a `tags: list[str]` field (default empty list) and a `component: str | None` field (default null).'
  - An Alembic migration adds the corresponding columns to the `tickets` table with the correct defaults.
  - Existing tickets round-trip through the database with tags=[] and component=null without error.
  - TicketRepo read/write paths carry the new fields transparently.
  - Unit tests confirm the migration applies cleanly on a fresh database and that the fields persist and retrieve correctly.
  non_goals:
  - Planner emission of tags/component — that is ATLAS-128.
  - Context renderer retrieval logic — that is later tickets.
  - Any UI or CLI surface for editing tags/component directly.
  implementation_notes:
  - tags stored as JSON array column (SQLAlchemy JSON type); component as nullable VARCHAR.
  - 'Migration must be backward-compatible: existing rows get tags=[] and component=NULL.'
  - Follow the existing Alembic migration naming convention.
  test_requirements:
  - 'Unit test: create a Ticket with tags and component, persist, reload, assert round-trip equality.'
  - 'Unit test: create a Ticket with defaults (tags=[], component=None), persist, reload, assert defaults preserved.'
  - 'Migration test: apply migration on a fresh SQLite database and confirm columns exist with correct types.'
  documentation_requirements: []
  definition_of_done:
  - Migration applies cleanly; all existing tests pass; new unit tests pass in CI.
  estimated_effort: null
  external_linear_id: 402caa08-21bd-4839-8688-6632b8e89955
  external_github_issue_id: null
  tags:
  - context-renderer
  - storage
  - migration
  - ticket-model
  component: atlas.storage
  linear_synced_at: '2026-07-04T18:05:38.538342Z'
  last_observed_linear_state_id: 311a3a97-c409-4cce-96ab-0a3bfc2a5541
  status_entered_at: '2026-07-05T15:53:13.776434Z'
  review_cycle_count: 0
  source_anchor: docs/atlas/implementation-roadmap.md#epic-execution-context
  created_by_type: agent
  created_by_id: planner
  created_at: '2026-07-04T18:05:38.538342Z'
  updated_at: '2026-07-04T18:05:38.538342Z'
  completed_at: null
- id: e355e702-5fc3-4d3b-8135-136c34b394bf
  product_id: 6363ff3f-22db-40b3-9a47-aeba5d0a7586
  epic_id: 9f6a68c3-3279-45aa-ab8c-462b43007dbd
  key: ATLAS-61
  title: Planner emits ticket tags and component fields
  objective: Extend the ProposalTicket schema and planner materialisation so that tags and component are proposed by the planner and carried through validation and apply into the stored ticket.
  context: Once the storage layer (ATLAS-127) exists, the planner must emit tags and component in its proposals so newly applied tickets carry meaningful categorical metadata from day one. This ticket adds the fields to ProposalTicket, updates the planner prompt templates, and ensures materialisation and validation carry them through without loss.
  status: needs_human_decision
  ticket_type: feature
  risk_level: medium
  priority: 49
  relevant_docs:
  - docs/atlas/context-renderer.md
  - docs/atlas/planning-engine-specification.md
  - docs/atlas/implementation-roadmap.md
  acceptance_criteria:
  - 'ProposalTicket Pydantic model gains `tags: list[str]` and `component: str | None` fields.'
  - The planner prompt template instructs the model to emit tags (1–4 lowercase labels) and component (single string or null).
  - Materialisation (_materialise / apply path) maps ProposalTicket.tags and .component onto the stored Ticket fields.
  - Validation gate 1 (schema) accepts proposals with tags=[] and component=null as valid.
  - Unit tests confirm that a proposal with tags and component round-trips through parse → materialise → storage correctly.
  - AT-1 still passes after this change (no regression on existing acceptance tests).
  non_goals:
  - Storage schema changes — those are ATLAS-127.
  - Context renderer retrieval using tags/component — later tickets.
  - Enforcing a controlled vocabulary for tags — free-form labels only in v1.
  implementation_notes:
  - ProposalTicket tags default to [] and component to None so existing single-call proposals without these fields remain valid (gate 1 must not reject them).
  - 'Prompt template update should include the same guidance as the stage-2 per-epic template: tags ≈1–4 lowercase labels, component = primary subsystem or null.'
  test_requirements:
  - 'Unit test: parse a ProposalTicket JSON with tags and component; assert fields present on the model.'
  - 'Unit test: materialise a ProposalTicket with tags/component; assert stored Ticket carries them.'
  - 'Regression: AT-1 acceptance test passes with the updated schema.'
  documentation_requirements: []
  definition_of_done:
  - ProposalTicket schema updated; materialisation carries fields; AT-1 passes; unit tests pass in CI.
  estimated_effort: null
  external_linear_id: 00a86ef8-df15-4b08-8a93-9cd4f485577e
  external_github_issue_id: null
  tags:
  - context-renderer
  - planning
  - proposal-schema
  - ticket-model
  component: atlas.planning
  linear_synced_at: '2026-07-04T18:05:38.538342Z'
  last_observed_linear_state_id: 311a3a97-c409-4cce-96ab-0a3bfc2a5541
  status_entered_at: '2026-07-05T15:53:13.776434Z'
  review_cycle_count: 0
  source_anchor: docs/atlas/implementation-roadmap.md#epic-execution-context
  created_by_type: agent
  created_by_id: planner
  created_at: '2026-07-04T18:05:38.538342Z'
  updated_at: '2026-07-04T18:05:38.538342Z'
  completed_at: null
- id: df18c89e-5a18-4333-9e7b-c1975db85a1d
  product_id: 6363ff3f-22db-40b3-9a47-aeba5d0a7586
  epic_id: 9f6a68c3-3279-45aa-ab8c-462b43007dbd
  key: ATLAS-62
  title: Relocate anchor/slug primitive to atlas.core.anchors
  objective: Move slugify, heading parsing, SourceDocument, ResolvedAnchor, AnchorIndex, and the anchor error hierarchy from atlas.planning.ingestion to atlas.core.anchors so that atlas.context retrievers can import them without violating the layer spine.
  context: The context renderer lives in atlas.context, which sits below atlas.planning in the layer spine (ARCHITECTURE.md). The anchor/slug primitive currently lives in atlas.planning.ingestion. atlas.context cannot import from atlas.planning without inverting the spine. This is a pure move — no behaviour change — that unblocks ATLAS-52 (documentation retrieval) and all other atlas.context retrievers that need anchor resolution.
  status: needs_human_decision
  ticket_type: tech_debt
  risk_level: medium
  priority: 48
  relevant_docs:
  - ARCHITECTURE.md
  - docs/atlas/context-renderer.md
  - docs/atlas/implementation-roadmap.md
  acceptance_criteria:
  - slugify, heading parsing, SourceDocument, ResolvedAnchor, AnchorIndex, and the anchor error hierarchy are importable from atlas.core.anchors.
  - atlas.planning.ingestion re-exports or re-imports from atlas.core.anchors so existing callers are unbroken (or all callers are updated).
  - import-linter layers contract still passes after the move.
  - 'No behaviour change: all existing anchor-resolution tests pass without modification.'
  - atlas.context modules can import from atlas.core.anchors without triggering an import-linter violation.
  non_goals:
  - Any behaviour change to the anchor/slug logic itself.
  - Adding new anchor functionality — this is a pure relocation.
  - Updating the planner prompt templates.
  implementation_notes:
  - Use a compatibility re-export in atlas.planning.ingestion to avoid breaking existing imports in a single step.
  - Check for any circular import risks introduced by the move before finalising.
  test_requirements:
  - Run the full existing anchor/slug test suite after the move and confirm all tests pass.
  - Run import-linter and confirm no layer violations.
  - 'Add a smoke import test: `from atlas.core.anchors import AnchorIndex` succeeds; `from atlas.context import something_using_anchors` succeeds.'
  documentation_requirements: []
  definition_of_done:
  - All existing tests pass; import-linter passes; atlas.context can import anchor primitives; CI green.
  estimated_effort: null
  external_linear_id: b4a4de80-7e5c-4f5f-8dd0-2e0e00efbe39
  external_github_issue_id: null
  tags:
  - context-renderer
  - layer-spine
  - refactor
  - anchors
  component: atlas.core
  linear_synced_at: '2026-07-04T18:05:38.538342Z'
  last_observed_linear_state_id: 311a3a97-c409-4cce-96ab-0a3bfc2a5541
  status_entered_at: '2026-07-05T15:53:13.776434Z'
  review_cycle_count: 0
  source_anchor: docs/atlas/implementation-roadmap.md#epic-execution-context
  created_by_type: agent
  created_by_id: planner
  created_at: '2026-07-04T18:05:38.538342Z'
  updated_at: '2026-07-04T18:05:38.538342Z'
  completed_at: null
- id: 24b6546f-15f7-4f03-a888-073958cd295a
  product_id: 6363ff3f-22db-40b3-9a47-aeba5d0a7586
  epic_id: 9f6a68c3-3279-45aa-ab8c-462b43007dbd
  key: ATLAS-63
  title: ADR retrieval for context packs
  objective: 'Implement ADR retrieval for the context renderer: given a ticket, return the accepted ADRs that are dependency targets of the ticket plus any accepted ADR whose title tokens intersect the ticket''s component/tags, rendered as decision + consequences only, capped at 5.'
  context: 'The context renderer (context-renderer.md §Retrieval rules v1) specifies that ADRs are retrieved by two criteria: explicit dependency targets of the ticket that are ADRs, and accepted ADRs whose title tokens intersect the ticket''s component/tags. Only the decision and consequences sections are included (context and alternatives omitted). The cap is 5 ADRs. This ticket implements that retrieval function in atlas.context.'
  status: needs_human_decision
  ticket_type: feature
  risk_level: medium
  priority: 47
  relevant_docs:
  - docs/atlas/context-renderer.md
  - docs/atlas/implementation-roadmap.md
  acceptance_criteria:
  - retrieve_adrs(ticket, graph, db) returns at most 5 ADR records, each rendered as decision + consequences sections only.
  - ADRs that are explicit depends_on targets of the ticket are always included (up to the cap).
  - Additional ADRs are selected by title-token intersection with the ticket's tags and component, ranked by intersection size.
  - Only ADRs with status=accepted are returned; draft or superseded ADRs are excluded.
  - 'Unit tests cover: ticket with explicit ADR dependency, ticket with tag-matching ADRs, cap enforcement at 5, exclusion of non-accepted ADRs.'
  non_goals:
  - Full ADR text inclusion — only decision and consequences sections.
  - Vector/embedding-based ADR matching — tag intersection only in v1.
  - Context pack assembly — this ticket delivers the retrieval function only.
  implementation_notes:
  - 'Title-token intersection: casefold, split on non-alphanumeric, set intersection — reuse the reconciler tokeniser from atlas.core.'
  - 'Section extraction for decision/consequences: parse the ADR markdown, extract the ## Decision and ## Consequences headings and their content.'
  test_requirements:
  - 'Unit test: fixture ticket with two explicit ADR depends_on targets → both returned.'
  - 'Unit test: fixture ticket with tags matching three ADR titles → those three returned.'
  - 'Unit test: six matching ADRs → only five returned (cap enforced).'
  - 'Unit test: non-accepted ADR (draft) not returned even if tags match.'
  documentation_requirements: []
  definition_of_done:
  - retrieve_adrs function implemented in atlas.context; all unit tests pass in CI; import-linter passes.
  estimated_effort: null
  external_linear_id: bc8ae44c-41b6-4a18-899d-fa0ee345c5ab
  external_github_issue_id: null
  tags:
  - context-renderer
  - adr-retrieval
  - retrieval
  component: atlas.context
  linear_synced_at: '2026-07-04T18:05:38.538342Z'
  last_observed_linear_state_id: 311a3a97-c409-4cce-96ab-0a3bfc2a5541
  status_entered_at: '2026-07-05T15:53:13.776434Z'
  review_cycle_count: 0
  source_anchor: docs/atlas/context-renderer.md#retrieval-rules-v1
  created_by_type: agent
  created_by_id: planner
  created_at: '2026-07-04T18:05:38.538342Z'
  updated_at: '2026-07-04T18:05:38.538342Z'
  completed_at: null
- id: 2bb53466-8804-4308-ba88-5c44182201ba
  product_id: 6363ff3f-22db-40b3-9a47-aeba5d0a7586
  epic_id: 9f6a68c3-3279-45aa-ab8c-462b43007dbd
  key: ATLAS-64
  title: Documentation retrieval with section-level extraction and doc SHAs
  objective: 'Implement section-level documentation retrieval for the context renderer: extract only the anchored heading''s section plus its parent heading chain from each relevant doc, record the git blob SHA of each extracted doc, and cap at 5 doc sections.'
  context: 'The context renderer retrieves docs at section granularity (context-renderer.md §Retrieval rules v1): the ticket''s relevant_docs plus the doc containing its source_anchor, extracting only the anchored section (heading to next same-level heading) plus the parent heading chain for orientation. Each extracted doc''s SHA is recorded in input_doc_shas for staleness detection. This ticket implements that retrieval in atlas.context, depending on the relocated anchor primitive (ATLAS-129).'
  status: needs_human_decision
  ticket_type: feature
  risk_level: medium
  priority: 46
  relevant_docs:
  - docs/atlas/context-renderer.md
  - docs/atlas/implementation-roadmap.md
  acceptance_criteria:
  - retrieve_docs(ticket, anchor_index) returns at most 5 doc sections, each as extracted markdown text plus the doc's git blob SHA.
  - The section extracted is from the anchored heading to the next same-level heading; the parent heading chain is prepended for orientation.
  - The doc containing the ticket's source_anchor is always included (if it resolves).
  - Each entry in the result carries the doc path and its git blob SHA at retrieval time.
  - 'Unit tests cover: anchor resolution to correct section, parent chain inclusion, cap at 5, SHA recording.'
  non_goals:
  - Whole-file inclusion — section-level only.
  - Compression of extracted sections — that is the compression ladder ticket.
  - Staleness detection logic — input_doc_shas are recorded here; staleness comparison is in the pack generation ticket.
  implementation_notes:
  - Use the AnchorIndex from atlas.core.anchors (ATLAS-129) for heading resolution.
  - 'Git blob SHA: read from the heading index built during document ingestion (ATLAS-21); do not shell out to git at render time.'
  - 'Section boundary: heading to next heading of the same or higher level (same-level = same # count).'
  test_requirements:
  - 'Unit test: fixture doc with nested headings → correct section extracted, parent chain prepended.'
  - 'Unit test: ticket with 6 relevant_docs → only 5 sections returned.'
  - 'Unit test: source_anchor doc included even when not in relevant_docs.'
  - 'Unit test: returned SHA matches the git blob SHA of the fixture doc.'
  documentation_requirements: []
  definition_of_done:
  - retrieve_docs implemented in atlas.context; unit tests pass; import-linter passes; CI green.
  estimated_effort: null
  external_linear_id: 2b5fe6dc-0407-4049-8d43-f4a5736f09b3
  external_github_issue_id: null
  tags:
  - context-renderer
  - doc-retrieval
  - retrieval
  - anchors
  component: atlas.context
  linear_synced_at: '2026-07-04T18:05:38.538342Z'
  last_observed_linear_state_id: 311a3a97-c409-4cce-96ab-0a3bfc2a5541
  status_entered_at: '2026-07-05T15:53:13.776434Z'
  review_cycle_count: 0
  source_anchor: docs/atlas/context-renderer.md#retrieval-rules-v1
  created_by_type: agent
  created_by_id: planner
  created_at: '2026-07-04T18:05:38.538342Z'
  updated_at: '2026-07-04T18:05:38.538342Z'
  completed_at: null
- id: c41c0a43-364c-41d9-a6d3-fccac2b82b1c
  product_id: 6363ff3f-22db-40b3-9a47-aeba5d0a7586
  epic_id: 9f6a68c3-3279-45aa-ab8c-462b43007dbd
  key: ATLAS-65
  title: Historical lesson retrieval (ACTIVE only, tag/component matching)
  objective: 'Implement lesson retrieval for the context renderer: return up to 3 ACTIVE lessons matched by tag intersection with the ticket''s tags and ticket_type, ranked by confidence then recency, excluding all DRAFT lessons.'
  context: 'ADR-0009 mandates that only ACTIVE lessons are retrievable into context packs — DRAFT lessons (agent-authored, not yet operator-promoted) must never appear. The context renderer retrieval rules (context-renderer.md §Retrieval rules v1) specify: status=ACTIVE only, matched by tag intersection with the ticket''s tags and ticket_type, ranked by confidence then recency, cap 3. This ticket implements that retrieval function in atlas.context.'
  status: needs_human_decision
  ticket_type: feature
  risk_level: high
  priority: 45
  relevant_docs:
  - docs/atlas/context-renderer.md
  - docs/decisions/0009-single-operator-governance.md
  - docs/atlas/implementation-roadmap.md
  acceptance_criteria:
  - retrieve_lessons(ticket, db) returns at most 3 Lesson records, all with status=ACTIVE.
  - DRAFT lessons are never returned, regardless of tag match.
  - Matching is by tag intersection between the ticket's tags and the lesson's tags, plus ticket_type match.
  - Results are ranked by confidence (descending, nulls last) then by created_at (descending).
  - 'Unit tests cover: DRAFT lesson excluded, ACTIVE lesson included, cap at 3, ranking by confidence then recency.'
  non_goals:
  - Vector/embedding-based lesson matching — tag intersection only in v1.
  - Lesson promotion or status changes — read-only retrieval only.
  - Lesson body compression — that is the compression ladder ticket.
  implementation_notes:
  - Filter at the database query level (status=ACTIVE) — do not fetch DRAFTs and filter in Python.
  - 'Tag intersection: set intersection of lesson.tags and ticket.tags; ticket_type match is an additional signal, not a hard filter.'
  - confidence is operator-assigned at promotion (0.0–1.0 or null); null sorts after any numeric value.
  test_requirements:
  - 'Unit test: fixture with one DRAFT and one ACTIVE lesson matching the ticket → only ACTIVE returned.'
  - 'Unit test: four ACTIVE matching lessons → only three returned (cap enforced).'
  - 'Unit test: ranking — lesson with higher confidence ranked first; equal confidence → more recent first.'
  - 'Unit test: ticket_type match contributes to selection when tags alone are insufficient.'
  documentation_requirements: []
  definition_of_done:
  - retrieve_lessons implemented in atlas.context; DRAFT exclusion enforced at query level; all unit tests pass; CI green.
  estimated_effort: null
  external_linear_id: d7f0d67f-8820-4f2c-a906-ba80fda01987
  external_github_issue_id: null
  tags:
  - context-renderer
  - lesson-retrieval
  - retrieval
  - governance
  component: atlas.context
  linear_synced_at: '2026-07-04T18:05:38.538342Z'
  last_observed_linear_state_id: 311a3a97-c409-4cce-96ab-0a3bfc2a5541
  status_entered_at: '2026-07-05T15:53:13.776434Z'
  review_cycle_count: 0
  source_anchor: docs/atlas/context-renderer.md#retrieval-rules-v1
  created_by_type: agent
  created_by_id: planner
  created_at: '2026-07-04T18:05:38.538342Z'
  updated_at: '2026-07-04T18:05:38.538342Z'
  completed_at: null
- id: a754e9ab-fb8b-4bae-af60-dbabcc07de56
  product_id: 6363ff3f-22db-40b3-9a47-aeba5d0a7586
  epic_id: 9f6a68c3-3279-45aa-ab8c-462b43007dbd
  key: ATLAS-66
  title: Dependency retrieval for context packs
  objective: 'Implement dependency retrieval for the context renderer: return the direct depends_on targets and dependents of a ticket as key, title, status, and one-line objective each, capped at 8.'
  context: The context renderer retrieval rules (context-renderer.md §Retrieval rules v1) specify that related tickets — direct depends_on targets and dependents — are included in the context pack as key, title, status, and one-line objective, capped at 8. This ticket implements that retrieval function in atlas.context, consuming the dependency graph built by atlas.dependencies.
  status: needs_human_decision
  ticket_type: feature
  risk_level: low
  priority: 44
  relevant_docs:
  - docs/atlas/context-renderer.md
  - docs/atlas/dependency-engine.md
  - docs/atlas/implementation-roadmap.md
  acceptance_criteria:
  - retrieve_related_tickets(ticket_key, graph, db) returns at most 8 related ticket summaries.
  - 'Each summary contains: key, title, status, and objective (one line, truncated if necessary).'
  - Both direct depends_on targets and direct dependents are included.
  - The ticket itself is not included in its own related-tickets list.
  - 'Unit tests cover: ticket with targets and dependents, cap at 8, self-exclusion.'
  non_goals:
  - Transitive dependency traversal — direct neighbours only.
  - Full ticket content inclusion — key/title/status/objective summary only.
  - Compression of related-ticket summaries — that is the compression ladder ticket.
  implementation_notes:
  - Consume the NetworkX DiGraph from atlas.dependencies.graph — do not re-query storage for graph traversal.
  - 'Objective truncation: first sentence or first 120 characters, whichever is shorter.'
  - 'Priority for cap: targets before dependents, then by key order.'
  test_requirements:
  - 'Unit test: ticket with 3 targets and 3 dependents → 6 summaries returned.'
  - 'Unit test: ticket with 10 neighbours → only 8 returned (cap enforced).'
  - 'Unit test: self not included in results.'
  - 'Unit test: summary fields (key, title, status, objective) all present.'
  documentation_requirements: []
  definition_of_done:
  - retrieve_related_tickets implemented in atlas.context; unit tests pass; import-linter passes; CI green.
  estimated_effort: null
  external_linear_id: 3d2ccc2b-dd39-47bf-9155-1a7c9d0eafa1
  external_github_issue_id: null
  tags:
  - context-renderer
  - dependency-retrieval
  - retrieval
  component: atlas.context
  linear_synced_at: '2026-07-04T18:05:38.538342Z'
  last_observed_linear_state_id: 311a3a97-c409-4cce-96ab-0a3bfc2a5541
  status_entered_at: '2026-07-05T15:53:13.776434Z'
  review_cycle_count: 0
  source_anchor: docs/atlas/context-renderer.md#retrieval-rules-v1
  created_by_type: agent
  created_by_id: planner
  created_at: '2026-07-04T18:05:38.538342Z'
  updated_at: '2026-07-04T18:05:38.538342Z'
  completed_at: null
- id: b48007e2-8170-4a80-8f62-2db2ef626f36
  product_id: 6363ff3f-22db-40b3-9a47-aeba5d0a7586
  epic_id: 9f6a68c3-3279-45aa-ab8c-462b43007dbd
  key: ATLAS-67
  title: Token budget and compression ladder
  objective: 'Implement the token budget estimator and compression ladder for the context renderer: estimate token count (chars/4), apply the four-rung compression sequence when over budget, and fail rendering if still over budget after all rungs.'
  context: 'The context renderer (context-renderer.md §Token budget and compression ladder) specifies a default budget of 12,000 tokens, estimated as chars/4. When over budget, compression is applied in order: (1) drop lesson bodies to titles; (2) drop related-ticket objectives to key+title; (3) trim doc sections to first paragraph plus command code blocks; (4) drop ADR consequences. If still over budget after all four rungs, rendering fails with a typed error (oversized ticket planning smell). This ticket implements the estimator and the compression ladder.'
  status: needs_human_decision
  ticket_type: feature
  risk_level: medium
  priority: 43
  relevant_docs:
  - docs/atlas/context-renderer.md
  - docs/atlas/implementation-roadmap.md
  acceptance_criteria:
  - estimate_tokens(text) returns len(text) // 4 as an integer; this value is recorded as token_estimate on the pack.
  - compress(pack_sections, budget) applies the four rungs in order until under budget or all rungs exhausted.
  - If still over budget after rung 4, a typed ContextPackOverBudgetError is raised (not silent truncation).
  - The budget is configurable (default 12000); the estimator is monotonic (more text → higher estimate).
  - 'Unit tests cover: under-budget pack (no compression), each rung applied individually, all-rungs-exhausted failure.'
  non_goals:
  - LLM-assisted summarisation — deferred per context-renderer.md open items.
  - Context pack assembly — this ticket delivers the estimator and compression functions only.
  - Changing the chars/4 estimator formula — precision is not required, monotonicity is.
  implementation_notes:
  - Compression rungs are applied sequentially; stop as soon as under budget.
  - 'Rung 3 (trim doc sections): keep the first paragraph (up to the first blank line) plus any fenced code blocks containing shell commands.'
  - ContextPackOverBudgetError should carry the estimated token count and the budget for diagnostics.
  test_requirements:
  - 'Unit test: pack under budget → returned unchanged, token_estimate correct.'
  - 'Unit test: pack over budget by lesson bodies → rung 1 applied, now under budget.'
  - 'Unit test: pack requiring all four rungs → still over budget → ContextPackOverBudgetError raised.'
  - 'Unit test: estimate_tokens is monotonic (longer text → higher estimate).'
  documentation_requirements: []
  definition_of_done:
  - estimate_tokens and compress implemented in atlas.context; all unit tests pass; CI green.
  estimated_effort: null
  external_linear_id: 0c594a96-f8cc-403c-9c99-8a7517361475
  external_github_issue_id: null
  tags:
  - context-renderer
  - compression
  - token-budget
  component: atlas.context
  linear_synced_at: '2026-07-04T18:05:38.538342Z'
  last_observed_linear_state_id: 311a3a97-c409-4cce-96ab-0a3bfc2a5541
  status_entered_at: '2026-07-05T15:53:13.776434Z'
  review_cycle_count: 0
  source_anchor: docs/atlas/context-renderer.md#token-budget-and-compression-ladder
  created_by_type: agent
  created_by_id: planner
  created_at: '2026-07-04T18:05:38.538342Z'
  updated_at: '2026-07-04T18:05:38.538342Z'
  completed_at: null
- id: d69d3e87-97ed-43e3-a229-1f5e8654e87f
  product_id: 6363ff3f-22db-40b3-9a47-aeba5d0a7586
  epic_id: 9f6a68c3-3279-45aa-ab8c-462b43007dbd
  key: ATLAS-68
  title: Context pack generation with input_doc_shas and staleness detection
  objective: 'Implement the full context pack generator: assemble all retrieved sections into a ContextPack with rendered_markdown in the fixed section order, record input_doc_shas, apply compression if needed, persist the pack, and detect staleness by comparing recorded SHAs against the current doc tree.'
  context: The context renderer (context-renderer.md) assembles retrieved docs, ADRs, related tickets, lessons, and verbatim ticket fields into a ContextPack with a fixed section order. Every pack records input_doc_shas for staleness detection. A pack is stale when any recorded SHA differs from the current tree. This ticket wires together the retrieval functions (ATLAS-51, ATLAS-52, ATLAS-53, ATLAS-54), the compression ladder (ATLAS-55), and the ContextPack model (ATLAS-15) into the complete generation pipeline.
  status: needs_human_decision
  ticket_type: feature
  risk_level: high
  priority: 42
  relevant_docs:
  - docs/atlas/context-renderer.md
  - docs/atlas/implementation-roadmap.md
  - docs/atlas/symphony-integration.md
  acceptance_criteria:
  - render_context_pack(ticket_key, db, graph, budget) returns a persisted ContextPack with all required sections populated.
  - 'rendered_markdown follows the fixed section order: Objective, Constraints, Acceptance Criteria, Non-goals, Relevant Docs, ADRs, Related Tickets, Lessons, Risks, Test Commands, Definition of Done.'
  - input_doc_shas records the git blob SHA of every extracted doc at render time.
  - token_estimate is set to the chars/4 estimate of rendered_markdown.
  - is_stale(pack, current_shas) returns True when any recorded SHA differs from the current tree.
  - The pack is persisted to the database via the ContextPack repository.
  non_goals:
  - CLI surface — that is ATLAS-58.
  - Validation checks — that is ATLAS-60.
  - PM Engine integration for staleness-triggered re-render — out of scope for this ticket.
  implementation_notes:
  - Verbatim ticket fields (objective, context, constraints, acceptance_criteria, non_goals, test_requirements, definition_of_done, risks) are included directly from the Ticket model.
  - Section order is fixed and must not vary between renders — this enables prompt caching.
  - On ContextPackOverBudgetError from the compression ladder, propagate the error rather than persisting a partial pack.
  test_requirements:
  - 'Integration test: render a pack for a fixture ticket; assert all sections present in correct order.'
  - 'Unit test: is_stale returns False when SHAs match, True when any SHA differs.'
  - 'Unit test: token_estimate equals len(rendered_markdown) // 4.'
  - 'Unit test: pack is persisted and retrievable by pack_id.'
  documentation_requirements: []
  definition_of_done:
  - render_context_pack and is_stale implemented; integration test passes; pack persisted correctly; CI green.
  estimated_effort: null
  external_linear_id: 6e133e88-3333-4702-96eb-2970c3d1f884
  external_github_issue_id: null
  tags:
  - context-renderer
  - pack-generation
  - staleness
  - provenance
  component: atlas.context
  linear_synced_at: '2026-07-04T18:05:38.538342Z'
  last_observed_linear_state_id: 311a3a97-c409-4cce-96ab-0a3bfc2a5541
  status_entered_at: '2026-07-05T15:53:13.776434Z'
  review_cycle_count: 0
  source_anchor: docs/atlas/context-renderer.md#staleness-and-provenance
  created_by_type: agent
  created_by_id: planner
  created_at: '2026-07-04T18:05:38.538342Z'
  updated_at: '2026-07-04T18:05:38.538342Z'
  completed_at: null
- id: 2890dad3-e9e7-45b8-8b96-0cec651b618a
  product_id: 6363ff3f-22db-40b3-9a47-aeba5d0a7586
  epic_id: 9f6a68c3-3279-45aa-ab8c-462b43007dbd
  key: ATLAS-69
  title: 'Context CLI: render, validate, and show commands'
  objective: 'Implement the atlas context CLI subcommands: render (generate and persist a context pack for a ticket), validate (check a pack''s required fields, token estimate, anchor resolution, and DRAFT exclusion), and show (display a stored pack).'
  context: 'The context renderer design (context-renderer.md §CLI) specifies three commands: `atlas context render <KEY> [--budget N] [--json]`, `atlas context validate <KEY>`, and `atlas context show <KEY>`. These are thin CLI surfaces over the pack generation (ATLAS-56) and validation logic. The validate command is also used by the PM Engine before promoting a ticket to Ready for Agent.'
  status: needs_human_decision
  ticket_type: feature
  risk_level: low
  priority: 41
  relevant_docs:
  - docs/atlas/context-renderer.md
  - docs/atlas/implementation-roadmap.md
  acceptance_criteria:
  - '`atlas context render <KEY>` generates and persists a context pack, printing the pack_id and token_estimate on success.'
  - '`atlas context render <KEY> --budget N` uses N as the token budget instead of the default 12000.'
  - '`atlas context render <KEY> --json` emits the full ContextPack as JSON to stdout.'
  - '`atlas context show <KEY>` retrieves and displays the most recent stored pack for the ticket.'
  - '`atlas context validate <KEY>` runs validation checks and exits 0 on pass, non-zero on failure with a human-readable report.'
  non_goals:
  - Validation logic implementation — that is ATLAS-60.
  - Pack generation logic — that is ATLAS-56.
  - Any dashboard or web interface.
  implementation_notes:
  - Use Typer for the CLI group, consistent with the existing atlas CLI structure.
  - The context group is `atlas context` with subcommands render, validate, show.
  - Error cases (ticket not found, pack over budget, validation failure) must exit non-zero with a clear message.
  test_requirements:
  - 'CLI integration test: `atlas context render <fixture-key>` exits 0 and prints pack_id.'
  - 'CLI integration test: `atlas context render <fixture-key> --json` emits valid JSON.'
  - 'CLI integration test: `atlas context validate <fixture-key>` exits 0 for a valid pack.'
  - 'CLI integration test: `atlas context show <fixture-key>` prints rendered_markdown.'
  documentation_requirements: []
  definition_of_done:
  - All three CLI subcommands implemented; integration tests pass; CI green; import-linter passes.
  estimated_effort: null
  external_linear_id: 5af247d0-c9b8-45ee-81d4-017dde55a1bd
  external_github_issue_id: null
  tags:
  - context-renderer
  - cli
  component: atlas.cli
  linear_synced_at: '2026-07-04T18:05:38.538342Z'
  last_observed_linear_state_id: 311a3a97-c409-4cce-96ab-0a3bfc2a5541
  status_entered_at: '2026-07-05T15:53:13.776434Z'
  review_cycle_count: 0
  source_anchor: docs/atlas/context-renderer.md#cli
  created_by_type: agent
  created_by_id: planner
  created_at: '2026-07-04T18:05:38.538342Z'
  updated_at: '2026-07-04T18:05:38.538342Z'
  completed_at: null
- id: ccd88654-7653-44af-abcd-9feb69571c38
  product_id: 6363ff3f-22db-40b3-9a47-aeba5d0a7586
  epic_id: 9f6a68c3-3279-45aa-ab8c-462b43007dbd
  key: ATLAS-70
  title: 'Context pack validation: required fields, token estimate, anchor resolution, DRAFT exclusion'
  objective: Implement the validate_context_pack function that checks all required fields are present, the token estimate is within budget, all anchors resolve at the recorded SHAs, and no DRAFT lessons are included — used by both the CLI and the PM Engine.
  context: 'The context renderer design (context-renderer.md §Validation) specifies: `atlas context validate <KEY>` checks that all anchors resolve at the recorded SHAs, objective and ≥1 acceptance criterion are present, ≥1 test command is present, token_estimate ≤ budget, and no DRAFT lessons are included. This function is the shared validation core used by the CLI (ATLAS-58) and the PM Engine before promoting a ticket to Ready for Agent.'
  status: needs_human_decision
  ticket_type: feature
  risk_level: medium
  priority: 40
  relevant_docs:
  - docs/atlas/context-renderer.md
  - docs/decisions/0009-single-operator-governance.md
  - docs/atlas/implementation-roadmap.md
  acceptance_criteria:
  - validate_context_pack(pack, db) returns a typed ValidationResult listing all failures (not just the first).
  - Validation fails if objective is absent or empty.
  - Validation fails if acceptance_criteria list is empty.
  - Validation fails if no test command is present in the pack.
  - Validation fails if token_estimate exceeds the budget recorded on the pack.
  - Validation fails if any anchor in the pack does not resolve at the recorded SHA.
  - Validation fails if any DRAFT lesson is referenced in the pack.
  non_goals:
  - CLI wiring — that is ATLAS-58.
  - Pack generation — that is ATLAS-56.
  - Automatic re-render on validation failure — the caller decides what to do.
  implementation_notes:
  - ValidationResult carries a list of ValidationFailure(field, reason) so callers can report all issues at once.
  - 'Anchor resolution: re-resolve each anchor against the SHA recorded in input_doc_shas, not the current HEAD.'
  - 'DRAFT lesson check: look up each lesson_id referenced in the pack and assert status=ACTIVE.'
  test_requirements:
  - 'Unit test: valid pack → ValidationResult.passed is True, no failures.'
  - 'Unit test: pack with empty objective → failure reported.'
  - 'Unit test: pack with token_estimate over budget → failure reported.'
  - 'Unit test: pack referencing a DRAFT lesson → failure reported.'
  - 'Unit test: pack with unresolvable anchor → failure reported.'
  - 'Unit test: multiple failures → all reported in one ValidationResult.'
  documentation_requirements: []
  definition_of_done:
  - validate_context_pack implemented; all unit tests pass; CI green; importable from atlas.context.
  estimated_effort: null
  external_linear_id: 55538708-7ad5-430e-9546-d60f377d7f83
  external_github_issue_id: null
  tags:
  - context-renderer
  - validation
  - governance
  component: atlas.context
  linear_synced_at: '2026-07-04T18:05:38.538342Z'
  last_observed_linear_state_id: 311a3a97-c409-4cce-96ab-0a3bfc2a5541
  status_entered_at: '2026-07-05T15:53:13.776434Z'
  review_cycle_count: 0
  source_anchor: docs/atlas/context-renderer.md#validation
  created_by_type: agent
  created_by_id: planner
  created_at: '2026-07-04T18:05:38.538342Z'
  updated_at: '2026-07-04T18:05:38.538342Z'
  completed_at: null
- id: cf50f86e-0d36-4cf5-b645-8f6cd917d2eb
  product_id: 6363ff3f-22db-40b3-9a47-aeba5d0a7586
  epic_id: 01499978-f6b2-4cd9-9924-4056720e5a2d
  key: ATLAS-71
  title: Evidence schema with trust fields and append-only enforcement
  objective: Extend the Evidence model with commit_sha, external_run_id, payload_hash, and created_by_type trust-tier fields, and enforce append-only semantics at the repository layer so agent-submitted records are capped at PENDING.
  context: ADR-0008 mandates that evidence records carry commit_sha, external_run_id, and payload_hash, and that the creating actor's trust tier determines the maximum status an evidence record may hold. System-tier records (CI ingestion) may carry any status; agent-tier records are capped at PENDING regardless of the submitted value. The Evidence model was introduced in Phase 1 (ATLAS-14) with basic fields; this ticket adds the trust-tier fields and enforces the append-only and capping rules at the EvidenceRepo layer. The Verification Engine (Phase 7) depends on these semantics being correct and auditable.
  status: needs_human_decision
  ticket_type: feature
  risk_level: high
  priority: 100
  relevant_docs:
  - docs/decisions/0008-ci-sourced-evidence-with-trust-tiers.md
  - docs/atlas/evidence-pipeline.md
  acceptance_criteria:
  - Evidence model carries commit_sha (required string), external_run_id (required string), payload_hash (required string), and created_by_type (ActorType enum) fields with non-null constraints enforced at the Pydantic and SQLAlchemy layers.
  - EvidenceRepo.add rejects any system-tier record missing commit_sha, external_run_id, or payload_hash with a typed validation error.
  - EvidenceRepo.add caps any agent-tier record's status at PENDING regardless of the submitted status value, and records the original submitted value in raw_payload for auditability.
  - EvidenceRepo.add never updates or deletes existing rows; every call inserts a new row, and a uniqueness constraint on (external_run_id, payload_hash) is enforced at the DB layer to prevent exact duplicates.
  - An Alembic migration adds the new columns to the evidence table with appropriate NOT NULL constraints and defaults for existing rows.
  - 'Unit tests cover: system-tier record accepted with all required fields; system-tier record rejected when any of the three required fields is missing; agent-tier PASSED record stored as PENDING; append-only insert verified by confirming row count increases on re-insert of same logical evidence.'
  non_goals:
  - Implementing the GitHub polling client or any CI ingestion logic — that is a separate ticket.
  - Implementing the Verification Engine rules that consume evidence — that is Phase 7.
  - Adding webhook receiver support — polling is the MVP transport per ADR-0008.
  - Changing the Evidence model fields introduced in Phase 1 beyond the additions specified here.
  implementation_notes:
  - The trust-tier cap logic belongs in EvidenceRepo.add, not in the Pydantic model, so the raw submitted status is preserved in raw_payload before capping.
  - Use a DB-level unique constraint on (external_run_id, payload_hash) as the dedup guard; the application layer should catch IntegrityError and treat it as a no-op rather than an error.
  - created_by_type should reuse the existing ActorType enum from atlas.core.
  test_requirements:
  - Unit tests for EvidenceRepo.add covering all trust-tier capping and required-field rejection cases.
  - 'Migration smoke test: apply migration on a fresh SQLite DB and confirm all new columns exist with correct types.'
  - 'Round-trip test: create a system-tier evidence record, read it back, confirm all fields match.'
  documentation_requirements:
  - Docstring on EvidenceRepo.add explaining the trust-tier capping rule and the required-field enforcement for system-tier records.
  definition_of_done:
  - Migration applied cleanly on SQLite; all new fields present.
  - All unit tests pass in CI.
  - EvidenceRepo.add enforces trust-tier capping and required-field rejection as specified.
  - No existing Phase 1 evidence tests are broken.
  estimated_effort: null
  external_linear_id: b12e6435-0975-46d7-aaae-def4db2a311e
  external_github_issue_id: null
  tags:
  - evidence
  - trust-tier
  - schema
  - storage
  component: atlas.evidence
  linear_synced_at: '2026-07-04T18:05:38.538342Z'
  last_observed_linear_state_id: 311a3a97-c409-4cce-96ab-0a3bfc2a5541
  status_entered_at: '2026-07-05T15:53:13.776434Z'
  review_cycle_count: 0
  source_anchor: docs/atlas/implementation-roadmap.md#epic-evidence-driven-delivery
  created_by_type: agent
  created_by_id: planner
  created_at: '2026-07-04T18:05:38.538342Z'
  updated_at: '2026-07-04T18:05:38.538342Z'
  completed_at: null
- id: 41aedae3-c6e0-46b7-8a37-cdb280ad55ee
  product_id: 6363ff3f-22db-40b3-9a47-aeba5d0a7586
  epic_id: 01499978-f6b2-4cd9-9924-4056720e5a2d
  key: ATLAS-72
  title: GitHub Checks/Workflow-Runs polling client
  objective: Implement a transport-agnostic GitHub polling client in atlas.github that fetches workflow runs and check runs for a PR head SHA, normalises raw API responses into a common payload shape, and handles ETags, rate-limit backoff, and deduplication.
  context: ADR-0008 mandates a pull-based transport for the MVP because a public webhook endpoint is not available. The evidence-pipeline.md design specifies that every tick the poller fetches workflow runs and check runs for tickets in pr_open, review_required, or changes_requested states, using conditional requests (ETags) to stay within rate limits and backing off on secondary-rate-limit responses. The client must be written transport-agnostic so a future HMAC-verified webhook receiver can replace polling with no schema change. This client is the boundary layer in atlas.github; the normaliser and evidence mappers are separate tickets.
  status: needs_human_decision
  ticket_type: feature
  risk_level: high
  priority: 90
  relevant_docs:
  - docs/decisions/0008-ci-sourced-evidence-with-trust-tiers.md
  - docs/atlas/evidence-pipeline.md
  acceptance_criteria:
  - GitHubClient in atlas/github/client.py exposes fetch_workflow_runs(repo, head_sha) and fetch_check_runs(repo, head_sha) returning typed response DTOs; a GitHubPRClient exposes fetch_pr_reviews(repo, pr_number).
  - 'Conditional GET requests use ETag headers: the client stores the last ETag per (repo, endpoint) and sends If-None-Match on subsequent calls; a 304 response returns the cached result without creating new evidence.'
  - On a secondary-rate-limit response (HTTP 429 or a Retry-After header), the client backs off for the indicated duration before retrying, up to a configurable maximum.
  - A fake/in-memory GitHubClient implementing the same protocol is provided for use in unit and integration tests, with no real network calls required.
  - The client is importable from atlas.github and does not import from atlas.evidence, atlas.planning, or any layer above atlas.github in the spine.
  - 'Unit tests cover: successful fetch returning typed DTOs; 304 returning cached result; 429 triggering backoff; network error raising a typed GitHubClientError.'
  non_goals:
  - Normalising raw API responses into Evidence records — that is the normaliser/mapper tickets.
  - Implementing the polling scheduler loop that calls the client on a cadence — that is the evidence CLI ticket.
  - Implementing webhook receiver support.
  - Fetching PR file lists — that is used by the documentation evidence ingestion ticket.
  implementation_notes:
  - Use Python stdlib urllib or httpx (already in the dependency tree if present); avoid adding a new HTTP library if one is already available.
  - ETag storage can be an in-memory dict keyed by (repo, endpoint) for the MVP; persistence across process restarts is a follow-up.
  - The DTO types should be simple dataclasses or Pydantic models in atlas.github.models.
  test_requirements:
  - Unit tests using the fake client covering all response scenarios (200, 304, 429, network error).
  - Import-linter check confirms atlas.github does not import from layers above it in the spine.
  - An opt-in live smoke test (ATLAS_LIVE_TESTS=1) exercises the real GitHub API against a known public repo and is skipped in default CI.
  documentation_requirements:
  - Module docstring in atlas/github/client.py explaining the transport-agnostic design and the ETag/backoff behaviour.
  definition_of_done:
  - All unit tests pass in CI with no real network calls.
  - Import-linter spine contract passes.
  - Fake client is usable as a drop-in for downstream tests.
  estimated_effort: null
  external_linear_id: 0aab38b8-acac-4d2c-9fc9-1f45fadbf108
  external_github_issue_id: null
  tags:
  - github
  - polling
  - ci
  - evidence
  component: atlas.github
  linear_synced_at: '2026-07-04T18:05:38.538342Z'
  last_observed_linear_state_id: 311a3a97-c409-4cce-96ab-0a3bfc2a5541
  status_entered_at: '2026-07-05T15:53:13.776434Z'
  review_cycle_count: 0
  source_anchor: docs/atlas/evidence-pipeline.md#poller
  created_by_type: agent
  created_by_id: planner
  created_at: '2026-07-04T18:05:38.538342Z'
  updated_at: '2026-07-04T18:05:38.538342Z'
  completed_at: null
- id: 2813ff5f-f150-4c10-b555-f35d125872da
  product_id: 6363ff3f-22db-40b3-9a47-aeba5d0a7586
  epic_id: 01499978-f6b2-4cd9-9924-4056720e5a2d
  key: ATLAS-73
  title: Test evidence mapping (CI job-name-driven evidence typing)
  objective: Implement the normaliser that maps GitHub workflow-run and check-run payloads to TEST_RESULT Evidence records, driven by the job-name prefix convention, and writes them as system-tier append-only records pinned to the PR head commit SHA.
  context: 'evidence-pipeline.md specifies that evidence typing is driven by CI job names: jobs prefixed ''test'' map to TEST_RESULT, ''lint'' to LINT_RESULT, ''build'' to BUILD_RESULT, ''coverage'' to COVERAGE_REPORT. Unrecognised jobs are ingested as BUILD_RESULT with a warning. This ticket covers the test prefix mapping and the shared normalisation infrastructure (status normalisation table, payload_hash computation, dedup key logic) that the build/lint/coverage mapper ticket will reuse. The normaliser sits in atlas.evidence and calls EvidenceRepo.add.'
  status: needs_human_decision
  ticket_type: feature
  risk_level: medium
  priority: 80
  relevant_docs:
  - docs/atlas/evidence-pipeline.md
  - docs/decisions/0008-ci-sourced-evidence-with-trust-tiers.md
  acceptance_criteria:
  - A normalise_workflow_run(payload, ticket_key) function in atlas/evidence/normaliser.py maps a workflow-run DTO to an Evidence record with evidence_type=TEST_RESULT when the job name starts with 'test' (case-insensitive prefix match).
  - 'GitHub conclusion values are mapped to EvidenceStatus per the table in evidence-pipeline.md: success→PASSED, failure→FAILED, cancelled/stale→WARNING, timed_out→FAILED, skipped/neutral→NOT_APPLICABLE, in-progress→PENDING.'
  - payload_hash is computed as SHA-256 of the canonical JSON serialisation of the raw payload dict; commit_sha is taken from the workflow run's head_sha field; external_run_id is the workflow run's id as a string.
  - 'Dedup logic: if a record with the same (external_run_id, payload_hash) already exists in EvidenceRepo, the normaliser skips insertion and returns None rather than raising.'
  - Unrecognised job name prefixes are ingested as BUILD_RESULT with a structured warning logged (not raised) and the original job name preserved in raw_payload.
  - 'Unit tests cover: test-prefixed job → TEST_RESULT; each GitHub conclusion → correct EvidenceStatus; dedup skip on identical (external_run_id, payload_hash); unrecognised prefix → BUILD_RESULT with warning.'
  non_goals:
  - Mapping lint, build, or coverage job prefixes — those are covered in the next ticket.
  - Implementing the polling scheduler or CLI — those are separate tickets.
  - PR review evidence ingestion — that is a separate ticket.
  - Documentation evidence ingestion — that is a separate ticket.
  implementation_notes:
  - The status normalisation table should be a module-level dict in atlas/evidence/normaliser.py so it is the single implementation reused by all mapper functions.
  - payload_hash must be computed before any field is stripped or transformed, over the raw payload as received from the GitHub client.
  - The normaliser must not import from atlas.github directly; it receives DTOs or dicts as arguments, keeping the dependency direction correct.
  test_requirements:
  - Unit tests for normalise_workflow_run covering all conclusion values and job-name prefix cases.
  - 'Integration test: normaliser called with a fake EvidenceRepo confirms a new record is inserted on first call and skipped on second call with identical payload.'
  documentation_requirements:
  - Docstring on normalise_workflow_run documenting the job-name prefix convention and the status mapping table.
  definition_of_done:
  - All unit and integration tests pass in CI.
  - Status normalisation table matches evidence-pipeline.md exactly.
  - Dedup logic confirmed by test.
  estimated_effort: null
  external_linear_id: b3bd5fdb-5c81-4393-bdb4-bd93c2ee184c
  external_github_issue_id: null
  tags:
  - evidence
  - ci
  - normaliser
  - test-result
  component: atlas.evidence
  linear_synced_at: '2026-07-04T18:05:38.538342Z'
  last_observed_linear_state_id: 311a3a97-c409-4cce-96ab-0a3bfc2a5541
  status_entered_at: '2026-07-05T15:53:13.776434Z'
  review_cycle_count: 0
  source_anchor: docs/atlas/evidence-pipeline.md#job-name-convention-ci-contract
  created_by_type: agent
  created_by_id: planner
  created_at: '2026-07-04T18:05:38.538342Z'
  updated_at: '2026-07-04T18:05:38.538342Z'
  completed_at: null
- id: 0feb206a-5ead-4d1e-b1a1-27edfe306f84
  product_id: 6363ff3f-22db-40b3-9a47-aeba5d0a7586
  epic_id: 01499978-f6b2-4cd9-9924-4056720e5a2d
  key: ATLAS-74
  title: Build, lint, and coverage evidence mapping
  objective: Extend the evidence normaliser to map GitHub workflow-run payloads with 'build', 'lint', and 'coverage' job-name prefixes to BUILD_RESULT, LINT_RESULT, and COVERAGE_REPORT Evidence records respectively, reusing the shared normalisation infrastructure from the test mapper.
  context: evidence-pipeline.md defines four job-name prefix mappings. The test mapper ticket delivers the shared infrastructure and the 'test' prefix. This ticket adds the remaining three prefixes ('build', 'lint', 'coverage') and ensures the full job-name convention table is complete. The doc linter is also specified to check workflow job names against this table; that check is noted here as a documentation requirement but the linter integration itself is out of scope for this ticket.
  status: needs_human_decision
  ticket_type: feature
  risk_level: medium
  priority: 75
  relevant_docs:
  - docs/atlas/evidence-pipeline.md
  - docs/decisions/0008-ci-sourced-evidence-with-trust-tiers.md
  acceptance_criteria:
  - normalise_workflow_run correctly maps job names prefixed 'build' to BUILD_RESULT, 'lint' to LINT_RESULT, and 'coverage' to COVERAGE_REPORT evidence types.
  - All three new mappings reuse the shared status normalisation table and payload_hash/dedup logic from the test mapper without duplicating them.
  - A check-run DTO (from the GitHub Checks API) is also normalised using the same job-name prefix convention, producing the same evidence types.
  - Unit tests cover each of the three new prefixes producing the correct evidence_type, and confirm that the shared dedup and status-normalisation logic is not duplicated.
  - The complete job-name prefix table (test, lint, build, coverage) is expressed as a single data structure in the normaliser module, not scattered across functions.
  non_goals:
  - Implementing the 'test' prefix mapping — that is the previous ticket.
  - PR review or documentation evidence ingestion — those are separate tickets.
  - Integrating the doc linter to validate workflow job names — that is a follow-up.
  - Coverage threshold enforcement — that is a Verification Engine rule, not an evidence rule per evidence-pipeline.md.
  implementation_notes:
  - Refactor the prefix dispatch from the test mapper into a single PREFIX_TO_EVIDENCE_TYPE dict at module level; all four prefixes are entries in that dict.
  - Check-run normalisation may share the same function with a 'source' discriminator argument, or be a thin wrapper — avoid duplicating the status table.
  test_requirements:
  - Unit tests for each new prefix (build, lint, coverage) confirming correct evidence_type.
  - Unit test confirming check-run DTOs are normalised with the same prefix convention.
  - Regression test confirming the test prefix still works after the refactor to a single prefix table.
  documentation_requirements:
  - Update the normaliser module docstring to document all four prefix mappings and note that the doc linter should validate workflow job names against this table.
  definition_of_done:
  - All unit tests pass in CI.
  - Single PREFIX_TO_EVIDENCE_TYPE dict covers all four prefixes.
  - No duplication of status normalisation logic.
  estimated_effort: null
  external_linear_id: 43ec715c-c1c2-4191-bb12-cf8cd18834c9
  external_github_issue_id: null
  tags:
  - evidence
  - ci
  - normaliser
  - build
  - lint
  - coverage
  component: atlas.evidence
  linear_synced_at: '2026-07-04T18:05:38.538342Z'
  last_observed_linear_state_id: 311a3a97-c409-4cce-96ab-0a3bfc2a5541
  status_entered_at: '2026-07-05T15:53:13.776434Z'
  review_cycle_count: 0
  source_anchor: docs/atlas/evidence-pipeline.md#job-name-convention-ci-contract
  created_by_type: agent
  created_by_id: planner
  created_at: '2026-07-04T18:05:38.538342Z'
  updated_at: '2026-07-04T18:05:38.538342Z'
  completed_at: null
- id: db9988c0-0ced-4e0f-b749-7a2230281cd1
  product_id: 6363ff3f-22db-40b3-9a47-aeba5d0a7586
  epic_id: 01499978-f6b2-4cd9-9924-4056720e5a2d
  key: ATLAS-75
  title: PR review evidence ingestion
  objective: Implement ingestion of GitHub PR review events as PR_REVIEW Evidence records, mapping review states (APPROVED, CHANGES_REQUESTED, COMMENTED) to EvidenceStatus values and recording the reviewer identity in raw_payload.
  context: 'evidence-pipeline.md specifies that PR reviews are ingested as PR_REVIEW evidence: APPROVED→PASSED, CHANGES_REQUESTED→FAILED, COMMENTED→WARNING. The reviewer is recorded in raw_payload. This ingestion is triggered by the poller fetching PR reviews via GitHubPRClient.fetch_pr_reviews. The resulting records are system-tier (created_by_type=system) and must carry commit_sha (the PR head SHA at review time), external_run_id (the review ID), and payload_hash.'
  status: needs_human_decision
  ticket_type: feature
  risk_level: medium
  priority: 70
  relevant_docs:
  - docs/atlas/evidence-pipeline.md
  - docs/decisions/0008-ci-sourced-evidence-with-trust-tiers.md
  acceptance_criteria:
  - A normalise_pr_review(review_payload, head_sha, ticket_key) function maps GitHub review state APPROVED→PASSED, CHANGES_REQUESTED→FAILED, COMMENTED→WARNING as PR_REVIEW evidence.
  - The reviewer's login is recorded in raw_payload under a 'reviewer' key.
  - commit_sha is set to the head_sha argument (the PR head at the time of the review fetch); external_run_id is the review's GitHub id as a string; payload_hash is SHA-256 of the canonical raw payload.
  - 'Dedup logic: a review with the same (external_run_id, payload_hash) is skipped on re-poll.'
  - Unknown review states are ingested as WARNING with the raw state preserved in raw_payload and a structured warning logged.
  - 'Unit tests cover: APPROVED→PASSED; CHANGES_REQUESTED→FAILED; COMMENTED→WARNING; unknown state→WARNING; dedup skip.'
  non_goals:
  - Fetching PR reviews from GitHub — that is the polling client ticket.
  - Documentation evidence ingestion — that is a separate ticket.
  - Using PR review evidence in the Verification Engine — that is Phase 7.
  - Recording reviewer identity beyond the login field in raw_payload.
  implementation_notes:
  - The PR review state mapping should be a module-level dict alongside the workflow-run status table.
  - head_sha must be passed explicitly by the caller (the poller), not inferred from the review payload, because GitHub review payloads do not always carry the head SHA directly.
  test_requirements:
  - Unit tests for normalise_pr_review covering all review states and the dedup case.
  - 'Integration test: normaliser called twice with the same review payload confirms only one Evidence row is created.'
  documentation_requirements:
  - Docstring on normalise_pr_review documenting the state mapping and the reviewer field in raw_payload.
  definition_of_done:
  - All unit and integration tests pass in CI.
  - PR review state mapping matches evidence-pipeline.md exactly.
  - Dedup confirmed by test.
  estimated_effort: null
  external_linear_id: 85f21c4c-6cf4-47af-92d2-582bb51216e6
  external_github_issue_id: null
  tags:
  - evidence
  - pr-review
  - github
  - normaliser
  component: atlas.evidence
  linear_synced_at: '2026-07-04T18:05:38.538342Z'
  last_observed_linear_state_id: 311a3a97-c409-4cce-96ab-0a3bfc2a5541
  status_entered_at: '2026-07-05T15:53:13.776434Z'
  review_cycle_count: 0
  source_anchor: docs/atlas/evidence-pipeline.md#status-normalisation
  created_by_type: agent
  created_by_id: planner
  created_at: '2026-07-04T18:05:38.538342Z'
  updated_at: '2026-07-04T18:05:38.538342Z'
  completed_at: null
- id: 4727b4b3-f130-454a-9edf-6b882a762b15
  product_id: 6363ff3f-22db-40b3-9a47-aeba5d0a7586
  epic_id: 01499978-f6b2-4cd9-9924-4056720e5a2d
  key: ATLAS-76
  title: Documentation evidence ingestion
  objective: Implement ingestion of documentation change evidence by inspecting the PR file list for changes under docs/ and producing a DOCUMENTATION_UPDATE Evidence record listing the touched paths.
  context: evidence-pipeline.md specifies that Atlas inspects the PR file list; changes under docs/ produce a DOCUMENTATION_UPDATE record (PASSED) listing the touched paths. Tickets with documentation requirements and no such record will fail verification in Phase 7 — the evidence layer only records, it does not enforce. The PR file list is fetched via the GitHub client. This is a system-tier record pinned to the PR head commit SHA.
  status: needs_human_decision
  ticket_type: feature
  risk_level: low
  priority: 65
  relevant_docs:
  - docs/atlas/evidence-pipeline.md
  - docs/decisions/0008-ci-sourced-evidence-with-trust-tiers.md
  acceptance_criteria:
  - A normalise_pr_files(file_list, head_sha, ticket_key) function produces a DOCUMENTATION_UPDATE Evidence record with status PASSED when at least one file in file_list has a path starting with 'docs/'.
  - The raw_payload of the record contains a 'touched_docs_paths' key listing all docs/ paths found in the file list.
  - When no docs/ files are present in the file list, the function returns None and creates no record (absence of documentation evidence is not itself a FAILED record — the Verification Engine handles the absence).
  - The record is system-tier (created_by_type=system) with commit_sha=head_sha, external_run_id derived from the PR number and head_sha, and payload_hash over the canonical file list.
  - 'Dedup: if a record with the same (external_run_id, payload_hash) already exists, the function skips insertion.'
  - 'Unit tests cover: file list with docs/ files → DOCUMENTATION_UPDATE PASSED; file list with no docs/ files → None; dedup skip.'
  non_goals:
  - Fetching the PR file list from GitHub — the caller (poller/CLI) supplies it.
  - Enforcing documentation requirements or failing tickets without docs evidence — that is the Verification Engine.
  - Inspecting file content, only paths.
  - Tracking non-docs/ file changes.
  implementation_notes:
  - The docs/ prefix check should be a simple startswith('docs/') on the normalised path; no glob or regex needed for v1.
  - external_run_id for documentation evidence can be constructed as 'pr-{pr_number}-docs-{head_sha[:8]}' to be unique per PR head.
  test_requirements:
  - Unit tests for normalise_pr_files covering all cases.
  - Integration test confirming dedup behaviour.
  documentation_requirements:
  - Docstring on normalise_pr_files explaining that absence of docs/ files produces no record and that enforcement is the Verification Engine's responsibility.
  definition_of_done:
  - All unit and integration tests pass in CI.
  - No DOCUMENTATION_UPDATE record created when no docs/ files are present.
  - Dedup confirmed by test.
  estimated_effort: null
  external_linear_id: f42e371f-5725-4506-b97f-fdaff8c368f8
  external_github_issue_id: null
  tags:
  - evidence
  - documentation
  - pr
  - normaliser
  component: atlas.evidence
  linear_synced_at: '2026-07-04T18:05:38.538342Z'
  last_observed_linear_state_id: 311a3a97-c409-4cce-96ab-0a3bfc2a5541
  status_entered_at: '2026-07-05T15:53:13.776434Z'
  review_cycle_count: 0
  source_anchor: docs/atlas/evidence-pipeline.md#status-normalisation
  created_by_type: agent
  created_by_id: planner
  created_at: '2026-07-04T18:05:38.538342Z'
  updated_at: '2026-07-04T18:05:38.538342Z'
  completed_at: null
- id: 45510344-4b06-4987-aa01-e18a06ca04b4
  product_id: 6363ff3f-22db-40b3-9a47-aeba5d0a7586
  epic_id: 01499978-f6b2-4cd9-9924-4056720e5a2d
  key: ATLAS-77
  title: Evidence tier and pinning enforcement (EvidenceRepo guard)
  objective: 'Implement the blanket repository-layer guard in EvidenceRepo that enforces trust-tier rules across all evidence types: system-tier records must carry all three required fields; agent-tier records are capped at PENDING; the guard is unconditional and not limited to specific evidence types.'
  context: 'evidence-pipeline.md specifies: ''The repository guard is blanket on the system tier: EvidenceRepo.add rejects any system-tier record missing any of the three regardless of evidence_type — there is no CI-evidence-type allowlist. Agent-submitted records remain capped at PENDING by the knowledge-core repository rule.'' This ticket ensures the guard is implemented as a single enforcement point in EvidenceRepo.add, covering all evidence types uniformly, and that the capping and rejection rules are tested exhaustively across all evidence types.'
  status: needs_human_decision
  ticket_type: feature
  risk_level: high
  priority: 95
  relevant_docs:
  - docs/atlas/evidence-pipeline.md
  - docs/decisions/0008-ci-sourced-evidence-with-trust-tiers.md
  acceptance_criteria:
  - EvidenceRepo.add raises a typed MissingEvidenceFieldError for any system-tier record missing commit_sha, external_run_id, or payload_hash, regardless of evidence_type.
  - EvidenceRepo.add caps any agent-tier record's status at PENDING regardless of the submitted status value and regardless of evidence_type.
  - The guard is implemented as a single code path in EvidenceRepo.add, not as per-evidence-type checks scattered across callers.
  - Human-tier records (created_by_type=human) are not subject to the PENDING cap and may carry any status.
  - Unit tests cover the guard for each of the four evidence types (TEST_RESULT, LINT_RESULT, BUILD_RESULT, COVERAGE_REPORT, PR_REVIEW, DOCUMENTATION_UPDATE) for both the system-tier rejection and agent-tier capping cases.
  - A test confirms that a human-tier PASSED record is stored as PASSED without capping.
  non_goals:
  - Implementing the normaliser functions that call EvidenceRepo.add — those are the mapper tickets.
  - Implementing Verification Engine rules that read evidence — that is Phase 7.
  - Adding per-evidence-type allowlists — the guard is explicitly blanket per the design doc.
  implementation_notes:
  - This ticket may overlap with the schema ticket (ATLAS-61 equivalent); if the schema ticket already implements a partial guard, this ticket completes and hardens it to be blanket and type-agnostic.
  - MissingEvidenceFieldError should be a typed exception in atlas.evidence.errors, not a ValueError.
  - The capping logic should log the original submitted status before overwriting it, for auditability.
  test_requirements:
  - Parametrised unit tests covering all evidence types for system-tier rejection and agent-tier capping.
  - Test confirming human-tier records bypass the PENDING cap.
  - Test confirming the guard raises a typed error (not a generic exception) on missing required fields.
  documentation_requirements:
  - Docstring on EvidenceRepo.add documenting the blanket guard, the three required fields for system-tier, and the PENDING cap for agent-tier.
  definition_of_done:
  - All parametrised unit tests pass in CI.
  - Guard is a single code path in EvidenceRepo.add.
  - Typed error raised on missing required fields confirmed by test.
  estimated_effort: null
  external_linear_id: 96984c1e-d3bc-4537-91f6-ef8155d6e0ca
  external_github_issue_id: null
  tags:
  - evidence
  - trust-tier
  - storage
  - enforcement
  component: atlas.evidence
  linear_synced_at: '2026-07-04T18:05:38.538342Z'
  last_observed_linear_state_id: 311a3a97-c409-4cce-96ab-0a3bfc2a5541
  status_entered_at: '2026-07-05T15:53:13.776434Z'
  review_cycle_count: 0
  source_anchor: docs/atlas/evidence-pipeline.md#tier-and-pinning-enforcement
  created_by_type: agent
  created_by_id: planner
  created_at: '2026-07-04T18:05:38.538342Z'
  updated_at: '2026-07-04T18:05:38.538342Z'
  completed_at: null
- id: f5e00650-d298-4017-9cf9-fa5b8a1bc663
  product_id: 6363ff3f-22db-40b3-9a47-aeba5d0a7586
  epic_id: 01499978-f6b2-4cd9-9924-4056720e5a2d
  key: ATLAS-78
  title: Evidence retention policy (raw_payload size cap)
  objective: 'Implement the raw_payload retention policy: cap raw_payload at 64KB, storing the first 64KB for oversized payloads along with the full payload''s SHA-256 hash and a source_uri for retrieval from GitHub.'
  context: 'evidence-pipeline.md specifies: ''raw_payload is capped at 64KB; larger payloads store the first 64KB, the full payload''s hash, and source_uri for retrieval from GitHub. Evidence rows are never deleted; a retention review is a Phase 10+ concern.'' This policy must be enforced at the EvidenceRepo layer before insertion so that no oversized payload ever reaches the database. The source_uri field must be added to the Evidence model if not already present.'
  status: needs_human_decision
  ticket_type: feature
  risk_level: medium
  priority: 60
  relevant_docs:
  - docs/atlas/evidence-pipeline.md
  - docs/decisions/0008-ci-sourced-evidence-with-trust-tiers.md
  acceptance_criteria:
  - EvidenceRepo.add truncates raw_payload to the first 64KB (65536 bytes of the JSON-serialised payload) when the payload exceeds this limit.
  - When truncation occurs, the full payload's SHA-256 hash is stored in a payload_full_hash field and a source_uri (the GitHub API URL for the run) is stored in a source_uri field on the Evidence record.
  - When the payload is within the 64KB limit, payload_full_hash and source_uri are null.
  - An Alembic migration adds payload_full_hash (nullable string) and source_uri (nullable string) columns to the evidence table.
  - Evidence rows are never deleted by any code path; there is no delete method on EvidenceRepo.
  - 'Unit tests cover: payload under 64KB stored in full with null payload_full_hash; payload over 64KB truncated with payload_full_hash and source_uri set.'
  non_goals:
  - Implementing automatic deletion or archival of old evidence rows — that is a Phase 10+ concern per the design doc.
  - Fetching the full payload from source_uri — that is a retrieval concern for future tooling.
  - Compressing payloads — truncation is the specified approach.
  implementation_notes:
  - The 64KB limit should be a module-level constant RAW_PAYLOAD_MAX_BYTES = 65536.
  - Truncation is applied to the JSON-serialised bytes of raw_payload, not to the dict in memory.
  - 'source_uri for a workflow run is the GitHub API URL: https://api.github.com/repos/{owner}/{repo}/actions/runs/{run_id}.'
  test_requirements:
  - Unit tests for the truncation logic with payloads at, below, and above the 64KB boundary.
  - Migration smoke test confirming new columns exist.
  - Test confirming no delete method exists on EvidenceRepo (or that calling any deletion raises NotImplementedError).
  documentation_requirements:
  - Docstring on EvidenceRepo.add documenting the 64KB cap, the truncation behaviour, and the source_uri field.
  definition_of_done:
  - All unit tests pass in CI.
  - Migration applied cleanly.
  - No delete path exists in EvidenceRepo.
  estimated_effort: null
  external_linear_id: 8607b620-003f-443a-90f0-d88304136230
  external_github_issue_id: null
  tags:
  - evidence
  - retention
  - storage
  component: atlas.evidence
  linear_synced_at: '2026-07-04T18:05:38.538342Z'
  last_observed_linear_state_id: 311a3a97-c409-4cce-96ab-0a3bfc2a5541
  status_entered_at: '2026-07-05T15:53:13.776434Z'
  review_cycle_count: 0
  source_anchor: docs/atlas/evidence-pipeline.md#retention
  created_by_type: agent
  created_by_id: planner
  created_at: '2026-07-04T18:05:38.538342Z'
  updated_at: '2026-07-04T18:05:38.538342Z'
  completed_at: null
- id: bb319d89-ee32-40cc-9b20-2d2280aa93a7
  product_id: 6363ff3f-22db-40b3-9a47-aeba5d0a7586
  epic_id: 01499978-f6b2-4cd9-9924-4056720e5a2d
  key: ATLAS-79
  title: Evidence validation (agent-tier PENDING cap and tier rule tests)
  objective: 'Implement and test the evidence validation layer that confirms trust-tier rules are correctly applied end-to-end: agent-created records are stored as PENDING, system-tier records carry all required fields, and the validation is exercised against the live storage layer.'
  context: 'The roadmap lists ATLAS-70 as ''Evidence validation (tier rules: agent-created capped at PENDING)''. While the enforcement is implemented in EvidenceRepo.add (the guard ticket), this ticket delivers the validation test suite that exercises the rules end-to-end through the storage layer, including a fixture that simulates an agent submitting a PASSED record and confirms it is stored as PENDING. This is the acceptance-test complement to the enforcement implementation.'
  status: needs_human_decision
  ticket_type: feature
  risk_level: medium
  priority: 55
  relevant_docs:
  - docs/atlas/evidence-pipeline.md
  - docs/decisions/0008-ci-sourced-evidence-with-trust-tiers.md
  acceptance_criteria:
  - An end-to-end test creates an agent-tier Evidence record with status PASSED via EvidenceRepo.add, reads it back from the database, and asserts the stored status is PENDING.
  - An end-to-end test creates a system-tier Evidence record with all required fields and asserts it is stored with the submitted status unchanged.
  - An end-to-end test attempts to create a system-tier Evidence record missing commit_sha and asserts a typed MissingEvidenceFieldError is raised and no row is inserted.
  - A test confirms that reading the latest system-tier record for a (check_type, commit_sha) pair returns the most recently inserted record, not an older one.
  - All tests run against an in-memory SQLite database (no external dependencies) and pass in CI.
  non_goals:
  - Re-implementing the enforcement logic — that belongs in EvidenceRepo.add.
  - Testing the normaliser functions — those have their own unit tests.
  - Testing the Verification Engine's consumption of evidence — that is Phase 7.
  - Performance or load testing of the evidence store.
  implementation_notes:
  - This ticket may be thin if the guard ticket already has comprehensive tests; the value here is the end-to-end storage-layer confirmation, not unit-level mocking.
  - Use the existing SQLAlchemy test session fixture from the storage layer tests.
  test_requirements:
  - All acceptance criteria are expressed as pytest tests in tests/evidence/test_validation.py.
  - Tests use an in-memory SQLite database via the existing test fixtures.
  - Tests are independent and can run in any order.
  documentation_requirements: []
  definition_of_done:
  - All tests in tests/evidence/test_validation.py pass in CI.
  - Tests exercise the storage layer, not mocks of EvidenceRepo.
  - 'Milestone test satisfied: an agent-submitted PASSED record is stored as PENDING.'
  estimated_effort: null
  external_linear_id: 4995784b-322d-4ba6-b730-cb2bc1af3495
  external_github_issue_id: null
  tags:
  - evidence
  - validation
  - trust-tier
  - testing
  component: atlas.evidence
  linear_synced_at: '2026-07-04T18:05:38.538342Z'
  last_observed_linear_state_id: 311a3a97-c409-4cce-96ab-0a3bfc2a5541
  status_entered_at: '2026-07-05T15:53:13.776434Z'
  review_cycle_count: 0
  source_anchor: docs/atlas/implementation-roadmap.md#epic-evidence-driven-delivery
  created_by_type: agent
  created_by_id: planner
  created_at: '2026-07-04T18:05:38.538342Z'
  updated_at: '2026-07-04T18:05:38.538342Z'
  completed_at: null
- id: df894067-392a-4cea-a7f7-05990608238a
  product_id: 6363ff3f-22db-40b3-9a47-aeba5d0a7586
  epic_id: 01499978-f6b2-4cd9-9924-4056720e5a2d
  key: ATLAS-80
  title: Evidence CLI (pull, list, show)
  objective: 'Implement the atlas evidence CLI subcommands: ''pull'' to manually trigger a polling tick for a ticket or all eligible tickets, ''list'' to display evidence records for a ticket filtered by type, and ''show'' to display a single evidence record by ID.'
  context: 'evidence-pipeline.md specifies the CLI surface: ''atlas evidence pull [--ticket KEY] (manual tick), atlas evidence list <KEY> [--type T], atlas evidence show <ID>.'' The pull command triggers the poller for the specified ticket (or all eligible tickets if no key is given), calling the GitHub client and normaliser pipeline. The list and show commands are pure readers over EvidenceRepo. The roadmap also notes ATLAS-130: the CLI must fail cleanly on a cold/never-migrated database, mapping a missing-schema OperationalError to EXIT_PRECONDITION across all three subcommands.'
  status: needs_human_decision
  ticket_type: feature
  risk_level: medium
  priority: 50
  relevant_docs:
  - docs/atlas/evidence-pipeline.md
  - docs/decisions/0008-ci-sourced-evidence-with-trust-tiers.md
  acceptance_criteria:
  - '''atlas evidence pull'' with no arguments fetches workflow runs, check runs, and PR reviews for all tickets in pr_open, review_required, or changes_requested states and ingests them via the normaliser pipeline.'
  - '''atlas evidence pull --ticket KEY'' restricts the pull to the single specified ticket.'
  - '''atlas evidence list KEY'' prints all Evidence records for the ticket in a human-readable table; ''--type T'' filters by evidence_type.'
  - '''atlas evidence show ID'' prints all fields of a single Evidence record by its database ID.'
  - All three subcommands catch a SQLAlchemy OperationalError caused by a missing schema (cold database) and exit with EXIT_PRECONDITION and a clear message rather than a traceback.
  - 'Unit tests cover: pull with --ticket KEY calls the poller for exactly that ticket; list with --type filter returns only matching records; show with a valid ID returns the record; cold-database OperationalError exits with EXIT_PRECONDITION.'
  non_goals:
  - Implementing a recurring scheduler loop — the CLI is a manual trigger; the scheduler is a PM Engine concern.
  - Implementing a dashboard or web UI.
  - Implementing webhook ingestion.
  - Fetching evidence from sources other than GitHub.
  implementation_notes:
  - The pull command should call the same poller function used by the PM Engine scheduler, not duplicate the polling logic.
  - EXIT_PRECONDITION should be a consistent exit code (e.g. 2) used across all Atlas CLI commands for precondition failures.
  - 'The list output should include at minimum: ID, evidence_type, status, commit_sha (truncated), created_at.'
  test_requirements:
  - Unit tests using the fake GitHubClient and an in-memory SQLite database.
  - Test for cold-database EXIT_PRECONDITION behaviour across all three subcommands.
  - CLI invocation tests using Typer's test runner (CliRunner).
  documentation_requirements:
  - Help text for each subcommand explaining its arguments and the cold-database behaviour.
  definition_of_done:
  - All unit tests pass in CI.
  - Cold-database EXIT_PRECONDITION confirmed by test.
  - '''atlas evidence pull'', ''atlas evidence list'', and ''atlas evidence show'' are registered in the atlas CLI.'
  estimated_effort: null
  external_linear_id: 46a31d4b-adfd-46c7-9a11-12db77965d05
  external_github_issue_id: null
  tags:
  - evidence
  - cli
  - poller
  component: atlas.cli
  linear_synced_at: '2026-07-04T18:05:38.538342Z'
  last_observed_linear_state_id: 311a3a97-c409-4cce-96ab-0a3bfc2a5541
  status_entered_at: '2026-07-05T15:53:13.776434Z'
  review_cycle_count: 0
  source_anchor: docs/atlas/evidence-pipeline.md#cli
  created_by_type: agent
  created_by_id: planner
  created_at: '2026-07-04T18:05:38.538342Z'
  updated_at: '2026-07-04T18:05:38.538342Z'
  completed_at: null
- id: 48b03206-1ed2-49a3-be6d-83c7ae0b55ef
  product_id: 6363ff3f-22db-40b3-9a47-aeba5d0a7586
  epic_id: f64bcf20-dbdf-4d8e-98b3-d615de1bc6a4
  key: ATLAS-81
  title: Verification rules and required-check matrix
  objective: Define and implement the required-check matrix that maps ticket_type to the set of VerificationCheckTypes that must pass, and provide the rule resolver that returns the required checks for a given ticket.
  context: The Verification Engine's first responsibility is knowing which checks are required for each ticket type. The required-check matrix (feature, bug, tech_debt, infrastructure, documentation, spike/research) is defined in verification-engine.md and must be implemented as configuration (YAML in repo) plus a Python resolver. The SECURITY check is surfaced as non-gating (required=False) for critical risk tickets. This is the foundation all evaluators depend on.
  status: needs_human_decision
  ticket_type: feature
  risk_level: medium
  priority: 100
  relevant_docs:
  - docs/atlas/verification-engine.md
  - docs/atlas/implementation-roadmap.md
  acceptance_criteria:
  - A YAML configuration file in the repository defines the required-check matrix matching the table in verification-engine.md (feature, bug, tech_debt, infrastructure, documentation, spike/research rows; tests, lint, acceptance_criteria, documentation, scope, human_approval columns).
  - A Python resolver function `required_checks(ticket_type, risk_level, has_documentation_requirements) -> list[VerificationCheckType]` returns the correct required checks for each combination, including the documentation_requirements conditional.
  - The SECURITY check is returned as a non-gating entry (required=False) for any ticket with risk_level == critical.
  - Unit tests cover every ticket_type row in the matrix, the documentation_requirements conditional, and the SECURITY non-gating surface.
  - The resolver raises a typed error for an unrecognised ticket_type rather than silently returning an empty list.
  non_goals:
  - Implementing any check evaluator — this ticket only defines which checks are required.
  - Persisting VerificationCheck records — that is the CLI/aggregation ticket.
  - Implementing the SECURITY evaluator — it is explicitly deferred in v1.
  implementation_notes:
  - Store the matrix as `atlas/verification/rules.yaml`; load it once at module import.
  - The resolver is a pure function with no I/O — it reads the loaded config and returns a list of check descriptors carrying check_type and required flag.
  - 'Keep the YAML schema simple: rows keyed by ticket_type, columns as boolean flags, plus a separate security_risk_threshold entry.'
  test_requirements:
  - Parametrised unit tests for every ticket_type × risk_level combination verifying the returned check list matches the matrix.
  - Test that documentation_requirements=True adds the documentation check for feature and infrastructure types.
  - Test that SECURITY appears as required=False for critical risk and is absent for lower risk levels.
  - Test that an unknown ticket_type raises a typed error.
  documentation_requirements:
  - Inline docstring on the resolver explaining the matrix lookup and the SECURITY special case.
  definition_of_done:
  - YAML matrix file committed under atlas/verification/.
  - Resolver implemented in atlas/verification/rules.py with full type annotations.
  - All unit tests pass in CI.
  - No evaluator logic present — scope is strictly rule resolution.
  estimated_effort: null
  external_linear_id: 7f56fbff-3213-4df8-a146-ebd75cdfb8ff
  external_github_issue_id: null
  tags:
  - verification
  - rules
  - configuration
  component: atlas.verification
  linear_synced_at: '2026-07-04T18:05:38.538342Z'
  last_observed_linear_state_id: 311a3a97-c409-4cce-96ab-0a3bfc2a5541
  status_entered_at: '2026-07-05T15:53:13.776434Z'
  review_cycle_count: 0
  source_anchor: docs/atlas/verification-engine.md#required-check-matrix
  created_by_type: agent
  created_by_id: planner
  created_at: '2026-07-04T18:05:38.538342Z'
  updated_at: '2026-07-04T18:05:38.538342Z'
  completed_at: null
- id: 5f76c215-dddb-4073-b4b7-223fa2d8d971
  product_id: 6363ff3f-22db-40b3-9a47-aeba5d0a7586
  epic_id: f64bcf20-dbdf-4d8e-98b3-d615de1bc6a4
  key: ATLAS-82
  title: Tests, lint, build, and coverage check evaluators
  objective: Implement the per-check evaluators for TESTS, LINT, BUILD, and COVERAGE check types that query the evidence store for the latest system-tier evidence pinned to the PR head commit and return a typed evaluation result.
  context: For machine-checkable checks (tests, lint, build, coverage), the Verification Engine must find the latest system-tier evidence record of the matching type with commit_sha equal to the PR head commit. Agent-tier evidence is ignored entirely for these checks. A new push resets these checks to PENDING. This implements the core 'no evidence = no completion' rule for CI-produced evidence.
  status: needs_human_decision
  ticket_type: feature
  risk_level: high
  priority: 90
  relevant_docs:
  - docs/atlas/verification-engine.md
  - docs/atlas/evidence-pipeline.md
  - docs/decisions/0008-ci-sourced-evidence-with-trust-tiers.md
  acceptance_criteria:
  - Each evaluator (tests, lint, build, coverage) queries EvidenceRepo for the latest system-tier record (created_by_type == system) of the matching evidence_type with commit_sha == head_commit.
  - If no system-tier record exists for the head commit, the evaluator returns PENDING regardless of any agent-tier records.
  - If the latest system-tier record has status PASSED, the evaluator returns PASSED; FAILED returns FAILED; other statuses map to their EvidenceStatus equivalents.
  - Agent-tier evidence records are never consulted for these four check types.
  - Each evaluator returns a typed CheckEvaluation carrying check_type, status, reason string, and the evidence_id(s) consulted.
  - Unit tests use fixture evidence records to verify PASSED, FAILED, PENDING, and the agent-tier-ignored cases.
  non_goals:
  - Implementing the documentation, acceptance_criteria, scope, or human_approval evaluators.
  - Persisting VerificationCheck records — that is the aggregation/CLI ticket.
  - Coverage threshold enforcement — v1 only requires that coverage evidence exists.
  implementation_notes:
  - Implement in atlas/verification/evaluators/ci.py with one function per check type or a single parametrised evaluator.
  - The evaluator is a pure function over evidence records — it takes a list of Evidence objects (pre-filtered by the caller) and the head_commit SHA, returning CheckEvaluation.
  - Reuse EvidenceRepo query methods from Phase 6; do not duplicate query logic.
  test_requirements:
  - 'Unit test: system-tier PASSED evidence at head commit → evaluator returns PASSED.'
  - 'Unit test: system-tier FAILED evidence at head commit → evaluator returns FAILED.'
  - 'Unit test: no system-tier evidence at head commit (only agent-tier) → evaluator returns PENDING.'
  - 'Unit test: system-tier evidence exists but for a different commit → evaluator returns PENDING.'
  - 'Unit test: multiple system-tier records for same check and commit → latest by (created_at, id) is used.'
  documentation_requirements: []
  definition_of_done:
  - Evaluators for TESTS, LINT, BUILD, COVERAGE implemented and type-annotated.
  - All unit tests pass in CI.
  - Agent-tier evidence is demonstrably ignored in tests.
  - No persistence logic present in this ticket's scope.
  estimated_effort: null
  external_linear_id: bbccda39-929d-462a-a06d-f1caca414945
  external_github_issue_id: null
  tags:
  - verification
  - evidence
  - evaluator
  - ci
  component: atlas.verification
  linear_synced_at: '2026-07-04T18:05:38.538342Z'
  last_observed_linear_state_id: 311a3a97-c409-4cce-96ab-0a3bfc2a5541
  status_entered_at: '2026-07-05T15:53:13.776434Z'
  review_cycle_count: 0
  source_anchor: docs/atlas/verification-engine.md#evaluation-semantics
  created_by_type: agent
  created_by_id: planner
  created_at: '2026-07-04T18:05:38.538342Z'
  updated_at: '2026-07-04T18:05:38.538342Z'
  completed_at: null
- id: bbd26ed6-e2c1-48f9-b507-b2817427f719
  product_id: 6363ff3f-22db-40b3-9a47-aeba5d0a7586
  epic_id: f64bcf20-dbdf-4d8e-98b3-d615de1bc6a4
  key: ATLAS-83
  title: Documentation check evaluator
  objective: Implement the DOCUMENTATION check evaluator that verifies a DOCUMENTATION_UPDATE evidence record exists for the PR head commit covering at least one path named in the ticket's documentation_requirements.
  context: The evidence pipeline (Phase 6) records DOCUMENTATION_UPDATE evidence when PR file changes include paths under docs/. The verification engine must check that such a record exists for the head commit and that it covers at least one path from the ticket's documentation_requirements field. Tickets with documentation requirements and no such record fail verification.
  status: needs_human_decision
  ticket_type: feature
  risk_level: medium
  priority: 80
  relevant_docs:
  - docs/atlas/verification-engine.md
  - docs/atlas/evidence-pipeline.md
  acceptance_criteria:
  - The evaluator queries for DOCUMENTATION_UPDATE evidence records with commit_sha == head_commit.
  - If the ticket has no documentation_requirements, the evaluator returns NOT_APPLICABLE.
  - If documentation_requirements is non-empty and no DOCUMENTATION_UPDATE record exists for the head commit, the evaluator returns PENDING.
  - If a DOCUMENTATION_UPDATE record exists and its raw_payload lists at least one path that matches a path in documentation_requirements, the evaluator returns PASSED.
  - If a DOCUMENTATION_UPDATE record exists but none of its paths match documentation_requirements, the evaluator returns FAILED.
  - 'Unit tests cover: no requirements (NOT_APPLICABLE), requirements with matching evidence (PASSED), requirements with non-matching evidence (FAILED), requirements with no evidence (PENDING).'
  non_goals:
  - Implementing any other check evaluator.
  - Modifying the evidence ingestion logic — the evaluator is read-only.
  - Glob or prefix path matching — v1 uses exact normalised path equality.
  implementation_notes:
  - Implement in atlas/verification/evaluators/documentation.py.
  - Path matching is exact normalised equality (strip leading slash, lowercase) — no glob, no prefix.
  - The raw_payload of DOCUMENTATION_UPDATE records contains the list of touched paths as produced by the evidence pipeline.
  test_requirements:
  - 'Unit test: ticket with empty documentation_requirements → NOT_APPLICABLE.'
  - 'Unit test: ticket with documentation_requirements, matching DOCUMENTATION_UPDATE evidence at head commit → PASSED.'
  - 'Unit test: ticket with documentation_requirements, DOCUMENTATION_UPDATE evidence at head commit but no path overlap → FAILED.'
  - 'Unit test: ticket with documentation_requirements, no DOCUMENTATION_UPDATE evidence at head commit → PENDING.'
  documentation_requirements: []
  definition_of_done:
  - Documentation evaluator implemented and type-annotated.
  - All unit tests pass in CI.
  - NOT_APPLICABLE, PASSED, FAILED, and PENDING cases all covered by tests.
  estimated_effort: null
  external_linear_id: de3b072a-62db-46d2-9e3a-360a8de387e7
  external_github_issue_id: null
  tags:
  - verification
  - documentation
  - evaluator
  component: atlas.verification
  linear_synced_at: '2026-07-04T18:05:38.538342Z'
  last_observed_linear_state_id: 311a3a97-c409-4cce-96ab-0a3bfc2a5541
  status_entered_at: '2026-07-05T15:53:13.776434Z'
  review_cycle_count: 0
  source_anchor: docs/atlas/verification-engine.md#evaluation-semantics
  created_by_type: agent
  created_by_id: planner
  created_at: '2026-07-04T18:05:38.538342Z'
  updated_at: '2026-07-04T18:05:38.538342Z'
  completed_at: null
- id: 8e18d44c-aa63-4692-ab1f-67082b61c77a
  product_id: 6363ff3f-22db-40b3-9a47-aeba5d0a7586
  epic_id: f64bcf20-dbdf-4d8e-98b3-d615de1bc6a4
  key: ATLAS-84
  title: Acceptance criteria check evaluator (operator confirmation)
  objective: Implement the ACCEPTANCE_CRITERIA check evaluator that requires a human-tier MANUAL_APPROVAL evidence record per criterion, identified by the SHA-256 hash of the criterion text, pinned to the PR head commit.
  context: In v1, acceptance criteria verification is operator-confirmed. atlas verify presents each criterion as a checklist; the operator's confirmations are stored as human-tier MANUAL_APPROVAL evidence records carrying raw_payload['acceptance_criterion_hash'] — the SHA-256 hex digest of the criterion text after strip(). The check passes only when every criterion has such a confirmation at the head commit. Rewording a criterion changes its hash, requiring re-confirmation.
  status: needs_human_decision
  ticket_type: feature
  risk_level: high
  priority: 80
  relevant_docs:
  - docs/atlas/verification-engine.md
  - docs/decisions/0009-single-operator-governance.md
  acceptance_criteria:
  - The evaluator computes the SHA-256 hex digest of each criterion text (after .strip()) and looks for a human-tier MANUAL_APPROVAL evidence record at the head commit carrying that hash in raw_payload['acceptance_criterion_hash'].
  - The check returns PASSED only when every criterion has a matching human-tier confirmation at the head commit.
  - A blanket MANUAL_APPROVAL record (no acceptance_criterion_hash key) does not satisfy any criterion.
  - If any criterion lacks a confirmation, the evaluator returns PENDING (not FAILED — an unconfirmed criterion is unproven, not rejected).
  - The evaluator returns a typed CheckEvaluation listing which criteria are confirmed and which are pending.
  - 'Unit tests cover: all confirmed (PASSED), partial confirmation (PENDING), no confirmations (PENDING), blanket approval ignored.'
  non_goals:
  - Implementing the interactive operator confirmation capture (the CLI prompt that writes the MANUAL_APPROVAL records) — that is a separate ticket.
  - LLM-assisted pre-assessment — explicitly deferred to a later phase.
  - Implementing any other check evaluator.
  implementation_notes:
  - Implement in atlas/verification/evaluators/acceptance.py.
  - Use hashlib.sha256(criterion.strip().encode()).hexdigest() for the hash.
  - The evaluator receives the ticket's acceptance_criteria list and the relevant evidence records as inputs.
  test_requirements:
  - 'Unit test: all criteria have matching human-tier MANUAL_APPROVAL at head commit → PASSED.'
  - 'Unit test: one criterion missing confirmation → PENDING.'
  - 'Unit test: no confirmations at all → PENDING.'
  - 'Unit test: blanket MANUAL_APPROVAL (no criterion hash) present → still PENDING for unconfirmed criteria.'
  - 'Unit test: confirmation exists for a different commit → not counted.'
  - 'Unit test: criterion text rewording changes hash → old confirmation no longer matches.'
  documentation_requirements: []
  definition_of_done:
  - Acceptance criteria evaluator implemented and type-annotated.
  - Hash computation matches the specification exactly.
  - All unit tests pass in CI.
  - Blanket approval non-satisfaction demonstrated in tests.
  estimated_effort: null
  external_linear_id: e7b9f4b0-abd0-4a5b-aaa5-a2c4689c97c4
  external_github_issue_id: null
  tags:
  - verification
  - acceptance-criteria
  - evaluator
  - human-approval
  component: atlas.verification
  linear_synced_at: '2026-07-04T18:05:38.538342Z'
  last_observed_linear_state_id: 311a3a97-c409-4cce-96ab-0a3bfc2a5541
  status_entered_at: '2026-07-05T15:53:13.776434Z'
  review_cycle_count: 0
  source_anchor: docs/atlas/verification-engine.md#evaluation-semantics
  created_by_type: agent
  created_by_id: planner
  created_at: '2026-07-04T18:05:38.538342Z'
  updated_at: '2026-07-04T18:05:38.538342Z'
  completed_at: null
- id: 54d08796-33c1-43bd-a968-4282ada3d78c
  product_id: 6363ff3f-22db-40b3-9a47-aeba5d0a7586
  epic_id: f64bcf20-dbdf-4d8e-98b3-d615de1bc6a4
  key: ATLAS-85
  title: Scope check evaluator (heuristic, operator waive/fail)
  objective: Implement the SCOPE check evaluator that compares the PR file list against the ticket's declared in-scope set (relevant_docs union source_anchor path), presents out-of-scope files for operator waive/fail decisions recorded as human-tier MANUAL_APPROVAL evidence, and returns a typed verdict.
  context: The scope evaluator derives the in-scope set from relevant_docs and the path part of source_anchor (no allowed_paths field in v1). Out-of-scope files require operator decisions stored as human-tier MANUAL_APPROVAL records carrying raw_payload['scope_decision_path']. PASSED = waive, FAILED = reject. The check passes when every changed file is in scope or waived; PENDING while any out-of-scope file is undecided.
  status: needs_human_decision
  ticket_type: feature
  risk_level: medium
  priority: 75
  relevant_docs:
  - docs/atlas/verification-engine.md
  acceptance_criteria:
  - The in-scope set is computed as the union of the ticket's relevant_docs paths and the path part of source_anchor (source_anchor.split('#', 1)[0]), using exact normalised path equality.
  - Files in the PR change list that are in the in-scope set are accepted without operator input.
  - For each out-of-scope file, the evaluator looks for a human-tier MANUAL_APPROVAL record at the head commit carrying raw_payload['scope_decision_path'] equal to the normalised file path.
  - PASSED scope decision (waive) means the file is acceptable; FAILED scope decision means the file should not be there.
  - The check returns PASSED when all changed files are in scope or waived; FAILED when any out-of-scope file's latest decision is FAILED; PENDING when any out-of-scope file has no decision.
  - For a file with multiple decisions, the latest by (created_at, id) decides.
  non_goals:
  - Directory, prefix, or glob path matching — v1 is exact normalised path equality only.
  - An allowed_paths ticket field — explicitly out of scope for v1.
  - Automatically routing to needs_human_decision — the PM Engine acts on the verdict, not the evaluator.
  - Implementing the interactive operator decision capture CLI.
  implementation_notes:
  - Implement in atlas/verification/evaluators/scope.py.
  - Normalise paths by stripping leading slash and lowercasing before comparison.
  - The evaluator receives the PR file list, the ticket model, and the relevant evidence records as inputs — no GitHub API calls inside the evaluator.
  test_requirements:
  - 'Unit test: all changed files in scope → PASSED.'
  - 'Unit test: out-of-scope file with waive decision → PASSED.'
  - 'Unit test: out-of-scope file with fail decision → FAILED.'
  - 'Unit test: out-of-scope file with no decision → PENDING.'
  - 'Unit test: multiple decisions for same file → latest by (created_at, id) wins.'
  - 'Unit test: non-human MANUAL_APPROVAL with scope_decision_path → ignored.'
  documentation_requirements: []
  definition_of_done:
  - Scope evaluator implemented and type-annotated.
  - In-scope derivation from relevant_docs and source_anchor path verified in tests.
  - All unit tests pass in CI.
  - PASSED, FAILED, and PENDING cases all covered.
  estimated_effort: null
  external_linear_id: 852f19b9-248e-48ac-a662-29f37dd0885c
  external_github_issue_id: null
  tags:
  - verification
  - scope
  - evaluator
  component: atlas.verification
  linear_synced_at: '2026-07-04T18:05:38.538342Z'
  last_observed_linear_state_id: 311a3a97-c409-4cce-96ab-0a3bfc2a5541
  status_entered_at: '2026-07-05T15:53:13.776434Z'
  review_cycle_count: 0
  source_anchor: docs/atlas/verification-engine.md#evaluation-semantics
  created_by_type: agent
  created_by_id: planner
  created_at: '2026-07-04T18:05:38.538342Z'
  updated_at: '2026-07-04T18:05:38.538342Z'
  completed_at: null
- id: b4afaf6e-4771-46d8-b37b-419da3505d09
  product_id: 6363ff3f-22db-40b3-9a47-aeba5d0a7586
  epic_id: f64bcf20-dbdf-4d8e-98b3-d615de1bc6a4
  key: ATLAS-86
  title: Human approval check evaluator
  objective: Implement the HUMAN_APPROVAL check evaluator that requires a blanket human-tier MANUAL_APPROVAL evidence record from the operator pinned to the PR head commit, distinguished from acceptance-criteria and scope decisions by the absence of both discriminator keys.
  context: For ticket types requiring human approval (feature/infrastructure at risk >= high, spike/research always), the operator must provide a blanket PR approval. This is a human-tier MANUAL_APPROVAL record carrying neither acceptance_criterion_hash nor scope_decision_path in raw_payload. The latest such record by (created_at, id) decides. PASSED = approved, FAILED = operator rejected. None at head commit = PENDING.
  status: needs_human_decision
  ticket_type: feature
  risk_level: medium
  priority: 75
  relevant_docs:
  - docs/atlas/verification-engine.md
  - docs/decisions/0009-single-operator-governance.md
  acceptance_criteria:
  - The evaluator finds human-tier MANUAL_APPROVAL records at the head commit that carry neither acceptance_criterion_hash nor scope_decision_path in raw_payload.
  - If no such record exists, the evaluator returns PENDING.
  - 'The latest such record by (created_at, id) decides: PASSED status → PASSED, FAILED status → FAILED.'
  - A MANUAL_APPROVAL record carrying acceptance_criterion_hash or scope_decision_path is not a blanket approval and is ignored.
  - A non-human MANUAL_APPROVAL record is ignored regardless of payload.
  - 'Unit tests cover: no record (PENDING), PASSED record (PASSED), FAILED record (FAILED), discriminator-carrying records ignored.'
  non_goals:
  - Implementing the interactive operator approval capture CLI.
  - Implementing any other check evaluator.
  - Multi-operator approval routing — single operator only per ADR-0009.
  implementation_notes:
  - Implement in atlas/verification/evaluators/human_approval.py.
  - The double-absence check (neither key present) is the discriminator — implement as a helper function reused by the acceptance and scope evaluators for consistency.
  test_requirements:
  - 'Unit test: no human-tier MANUAL_APPROVAL at head commit → PENDING.'
  - 'Unit test: human-tier MANUAL_APPROVAL with status PASSED, no discriminators → PASSED.'
  - 'Unit test: human-tier MANUAL_APPROVAL with status FAILED, no discriminators → FAILED.'
  - 'Unit test: human-tier MANUAL_APPROVAL carrying acceptance_criterion_hash → ignored.'
  - 'Unit test: human-tier MANUAL_APPROVAL carrying scope_decision_path → ignored.'
  - 'Unit test: non-human MANUAL_APPROVAL → ignored.'
  documentation_requirements: []
  definition_of_done:
  - Human approval evaluator implemented and type-annotated.
  - Discriminator logic verified in tests.
  - All unit tests pass in CI.
  estimated_effort: null
  external_linear_id: 24871ab3-cf4d-4b69-8671-12039e7f3648
  external_github_issue_id: null
  tags:
  - verification
  - human-approval
  - evaluator
  component: atlas.verification
  linear_synced_at: '2026-07-04T18:05:38.538342Z'
  last_observed_linear_state_id: 311a3a97-c409-4cce-96ab-0a3bfc2a5541
  status_entered_at: '2026-07-05T15:53:13.776434Z'
  review_cycle_count: 0
  source_anchor: docs/atlas/verification-engine.md#evaluation-semantics
  created_by_type: agent
  created_by_id: planner
  created_at: '2026-07-04T18:05:38.538342Z'
  updated_at: '2026-07-04T18:05:38.538342Z'
  completed_at: null
- id: c2cab140-540b-46d5-9c66-e1cff9cb8fc3
  product_id: 6363ff3f-22db-40b3-9a47-aeba5d0a7586
  epic_id: f64bcf20-dbdf-4d8e-98b3-d615de1bc6a4
  key: ATLAS-87
  title: Ticket-level verdict composition
  objective: Implement the verify(ticket) function that composes per-check evaluations into a single ticket verdict using the required-check matrix, applying fail-precedence over gating checks and returning PASSED only when every required check is PASSED.
  context: 'The verdict composer takes a ticket, its PR head commit, and the evidence records, runs each required evaluator, and folds the results: FAILED if any required check is FAILED (fail precedence), PASSED if all required checks are PASSED, otherwise PENDING. Non-required checks (like the deferred SECURITY surface) appear in the breakdown but do not gate. A required check with no wired evaluator holds the verdict at PENDING rather than silently passing.'
  status: needs_human_decision
  ticket_type: feature
  risk_level: high
  priority: 70
  relevant_docs:
  - docs/atlas/verification-engine.md
  - docs/atlas/implementation-roadmap.md
  acceptance_criteria:
  - verify(ticket, head_commit, evidence_records) calls the required evaluators determined by the rules resolver and returns a TicketVerification object carrying the overall verdict and a per-check breakdown.
  - The verdict is PASSED only when every required check (required=True) returns PASSED.
  - The verdict is FAILED if any required check returns FAILED (fail precedence over PENDING).
  - The verdict is PENDING if no required check is FAILED and at least one required check is not PASSED.
  - Non-required checks appear in the breakdown with their evaluation result but do not affect the verdict.
  - A required check type with no wired evaluator returns PENDING (not PASSED) — it holds the verdict at PENDING.
  - 'Unit tests cover: all required checks PASSED, one required check FAILED, mixed PENDING/PASSED, non-required check FAILED does not sink verdict.'
  non_goals:
  - Persisting VerificationCheck records — that is the CLI/persistence ticket.
  - PR-level aggregation — that is a separate ticket.
  - Implementing any individual evaluator — those are separate tickets.
  implementation_notes:
  - Implement in atlas/verification/verdict.py.
  - 'The fold is: collect required evaluations; if any FAILED → FAILED; if all PASSED → PASSED; else PENDING.'
  - The evaluator registry maps VerificationCheckType to evaluator function; an absent entry returns a PENDING CheckEvaluation.
  test_requirements:
  - 'Unit test: all required checks PASSED → PASSED verdict.'
  - 'Unit test: one required check FAILED → FAILED verdict regardless of others.'
  - 'Unit test: one required check PENDING, rest PASSED → PENDING verdict.'
  - 'Unit test: non-required check FAILED → verdict unaffected (PASSED if required checks pass).'
  - 'Unit test: required check type with no evaluator wired → PENDING verdict.'
  - 'Unit test: TicketVerification breakdown contains entries for all checks including non-required.'
  documentation_requirements: []
  definition_of_done:
  - verify(ticket, ...) implemented and type-annotated.
  - Fail-precedence and PENDING-hold behaviours verified in tests.
  - All unit tests pass in CI.
  - No persistence logic present.
  estimated_effort: null
  external_linear_id: 2c47e1e9-af18-4a13-992d-93393e6320e2
  external_github_issue_id: null
  tags:
  - verification
  - verdict
  - composition
  component: atlas.verification
  linear_synced_at: '2026-07-04T18:05:38.538342Z'
  last_observed_linear_state_id: 311a3a97-c409-4cce-96ab-0a3bfc2a5541
  status_entered_at: '2026-07-05T15:53:13.776434Z'
  review_cycle_count: 0
  source_anchor: docs/atlas/verification-engine.md#verdict-and-completion
  created_by_type: agent
  created_by_id: planner
  created_at: '2026-07-04T18:05:38.538342Z'
  updated_at: '2026-07-04T18:05:38.538342Z'
  completed_at: null
- id: 6f589650-96c7-49c9-ae07-69b15590950d
  product_id: 6363ff3f-22db-40b3-9a47-aeba5d0a7586
  epic_id: f64bcf20-dbdf-4d8e-98b3-d615de1bc6a4
  key: ATLAS-88
  title: PR-level verification aggregation
  objective: Implement verify(PR) that aggregates per-ticket verdicts for all tickets a PR closes, returning PASSED only when every closed ticket is PASSED, with the same fold rule as ticket-level verdict composition.
  context: 'A PR may close multiple tickets. PR completion is verified by composing the per-ticket verdicts: PASSED only when every closed ticket is PASSED, FAILED if any closed ticket is FAILED, otherwise PENDING. An empty close-set is PENDING (never a vacuous PASS). Each closed ticket is evaluated independently at the PR head commit. The set of closed tickets is an input (resolved by the CLI from the PR title/body), not something the aggregator resolves.'
  status: needs_human_decision
  ticket_type: feature
  risk_level: medium
  priority: 65
  relevant_docs:
  - docs/atlas/verification-engine.md
  acceptance_criteria:
  - evaluate_pr(tickets, head_commit, evidence_by_ticket) calls verify(ticket) for each ticket in the closed set and folds the results using the same FAILED > PENDING > PASSED precedence.
  - An empty tickets list returns PENDING (never PASSED).
  - Each ticket is evaluated independently — evidence scoped to one ticket does not satisfy another.
  - The PRVerification result carries the overall verdict and a per-ticket breakdown.
  - 'Unit tests cover: all tickets PASSED, one ticket FAILED, mixed PENDING/PASSED, empty ticket set.'
  non_goals:
  - Resolving which tickets a PR closes from GitHub — that is the CLI's responsibility.
  - Persisting VerificationCheck records.
  - Making GitHub API calls inside the aggregator.
  implementation_notes:
  - Implement in atlas/verification/verdict.py alongside the ticket-level function.
  - PRVerification is a Pydantic model carrying pr_verdict (EvidenceStatus), ticket_verdicts (list of TicketVerification), and head_commit.
  test_requirements:
  - 'Unit test: single ticket PASSED → PR PASSED.'
  - 'Unit test: one of two tickets FAILED → PR FAILED.'
  - 'Unit test: one ticket PENDING, one PASSED → PR PENDING.'
  - 'Unit test: empty ticket list → PR PENDING.'
  - 'Unit test: evidence for ticket A does not satisfy ticket B''s checks.'
  documentation_requirements: []
  definition_of_done:
  - evaluate_pr implemented and type-annotated.
  - Empty-set PENDING behaviour verified in tests.
  - All unit tests pass in CI.
  estimated_effort: null
  external_linear_id: 2af22bc0-dfe8-46de-80ee-b6315f1b96c0
  external_github_issue_id: null
  tags:
  - verification
  - verdict
  - pr-aggregation
  component: atlas.verification
  linear_synced_at: '2026-07-04T18:05:38.538342Z'
  last_observed_linear_state_id: 311a3a97-c409-4cce-96ab-0a3bfc2a5541
  status_entered_at: '2026-07-05T15:53:13.776434Z'
  review_cycle_count: 0
  source_anchor: docs/atlas/verification-engine.md#verdict-and-completion
  created_by_type: agent
  created_by_id: planner
  created_at: '2026-07-04T18:05:38.538342Z'
  updated_at: '2026-07-04T18:05:38.538342Z'
  completed_at: null
- id: 06325eba-2b26-4118-bb92-fab6f4eeaed7
  product_id: 6363ff3f-22db-40b3-9a47-aeba5d0a7586
  epic_id: f64bcf20-dbdf-4d8e-98b3-d615de1bc6a4
  key: ATLAS-89
  title: VerificationCheck persistence (append-only records)
  objective: Implement the VerificationCheck storage model and repository that persists one append-only VerificationCheck record per check per verification run, never mutating prior rows, with a re-run appending a fresh set.
  context: The Verification Engine must persist its verdicts as append-only VerificationCheck records (data-model §5). Each run of atlas verify appends a fresh set of records — one per check — rather than updating existing ones. This is consistent with the append-only evidence model (ADR-0008). The VerificationCheck schema lives in data-model-and-schemas.md §5.
  status: needs_human_decision
  ticket_type: feature
  risk_level: high
  priority: 60
  relevant_docs:
  - docs/atlas/verification-engine.md
  - docs/decisions/0008-ci-sourced-evidence-with-trust-tiers.md
  acceptance_criteria:
  - A VerificationCheck SQLAlchemy model and Alembic migration exist, matching the schema in data-model-and-schemas.md §5 (ticket_id, check_type, required, status, reason, evidence_ids, head_commit, created_at).
  - VerificationCheckRepo.add(check) inserts a new row and never updates existing rows.
  - VerificationCheckRepo.latest_for_ticket(ticket_id, head_commit) returns the most recent set of checks for a ticket at a given commit.
  - Rows are never deleted or updated — the repository raises an error if an update is attempted.
  - 'Unit tests verify append-only semantics: two runs for the same ticket and commit produce two sets of rows, not one updated set.'
  - The migration applies cleanly on a fresh database and on an existing database.
  non_goals:
  - Implementing any evaluator or verdict logic.
  - Implementing the CLI — that is a separate ticket.
  - Retention or deletion policies — rows are never deleted in v1.
  implementation_notes:
  - Implement in atlas/storage/models.py (model) and atlas/storage/repositories/verification.py (repo).
  - evidence_ids stored as a JSON array column.
  - created_at is server-default now(), never client-supplied.
  test_requirements:
  - 'Unit test: add two VerificationCheck records for the same ticket and commit → two rows exist.'
  - 'Unit test: latest_for_ticket returns the most recent set by created_at.'
  - 'Unit test: migration applies on a fresh SQLite database without error.'
  - 'Unit test: attempting to update a row raises a typed error.'
  documentation_requirements: []
  definition_of_done:
  - VerificationCheck model and migration committed.
  - VerificationCheckRepo implemented with add and latest_for_ticket methods.
  - Append-only semantics verified in tests.
  - All unit tests pass in CI.
  estimated_effort: null
  external_linear_id: e9b7f377-21f5-466d-9c97-577616ab15a6
  external_github_issue_id: null
  tags:
  - verification
  - storage
  - persistence
  - append-only
  component: atlas.storage
  linear_synced_at: '2026-07-04T18:05:38.538342Z'
  last_observed_linear_state_id: 311a3a97-c409-4cce-96ab-0a3bfc2a5541
  status_entered_at: '2026-07-05T15:53:13.776434Z'
  review_cycle_count: 0
  source_anchor: docs/atlas/verification-engine.md#reports
  created_by_type: agent
  created_by_id: planner
  created_at: '2026-07-04T18:05:38.538342Z'
  updated_at: '2026-07-04T18:05:38.538342Z'
  completed_at: null
- id: 76fc520e-6fc9-40e7-9d7d-e9a5d5592362
  product_id: 6363ff3f-22db-40b3-9a47-aeba5d0a7586
  epic_id: f64bcf20-dbdf-4d8e-98b3-d615de1bc6a4
  key: ATLAS-90
  title: 'Verification CLI: atlas verify command'
  objective: Implement the atlas verify --pr <N> --repo <OWNER/REPO> CLI command that resolves the PR head commit and changed files from GitHub, runs evaluate_pr, persists VerificationCheck records, and prints the per-ticket/per-check verdict report.
  context: The verification CLI is the operator-facing entry point for Phase 7. It resolves the PR head commit and file list from GitHub, resolves which tickets the PR closes (from the PR title ATLAS-NN key, with --tickets override), reads stored evidence, runs the pure evaluate_pr, persists the results as append-only VerificationCheck rows, and prints the report. Exit code is 0 for any verdict (PASSED/PENDING/FAILED) — only precondition failures are non-zero. The command is non-interactive and writes no evidence.
  status: needs_human_decision
  ticket_type: feature
  risk_level: medium
  priority: 55
  relevant_docs:
  - docs/atlas/verification-engine.md
  - docs/atlas/implementation-roadmap.md
  acceptance_criteria:
  - atlas verify --pr <N> --repo <OWNER/REPO> resolves the PR head commit and changed file list from the GitHub API.
  - The command resolves closed tickets from the PR title (ATLAS-NN key pattern) with --tickets ATLAS-a,ATLAS-b as an override.
  - evaluate_pr is called with the resolved tickets, head commit, and evidence from the store.
  - Results are persisted as append-only VerificationCheck rows (one per check per ticket).
  - The report prints the PR verdict, per-ticket verdict, and per-check breakdown (check_type, required, status, reason, evidence IDs).
  - --json emits the serialised PRVerification for automation.
  - Exit code is 0 for PASSED, PENDING, and FAILED verdicts; non-zero only for precondition failures (unknown PR, missing token, cold database, malformed --repo).
  non_goals:
  - Interactive operator confirmation capture (writing MANUAL_APPROVAL evidence records) — that is a separate follow-on.
  - A --strict mode that exits non-zero on FAILED — explicitly deferred.
  - A dashboard or web UI.
  - Making the command block a merge — it is advisory only in v1.
  implementation_notes:
  - Wire into atlas/cli.py as `atlas verify` subcommand using Typer.
  - Reuse the GitHub client from Phase 6 (atlas.github) for PR metadata.
  - 'PR title parsing: extract the first ATLAS-\d+ pattern from the title.'
  - The command is the only place that calls both evaluate_pr and VerificationCheckRepo.add in sequence.
  test_requirements:
  - 'Integration test with a mocked GitHub client: PR head commit and file list resolved correctly.'
  - 'Test: PR title with ATLAS-NN key → correct ticket resolved.'
  - 'Test: --tickets override takes precedence over PR title parsing.'
  - 'Test: VerificationCheck rows are persisted after a successful run.'
  - 'Test: cold database (missing schema) → non-zero exit with a clear precondition error message.'
  - 'Test: --json output is valid JSON matching the PRVerification schema.'
  documentation_requirements:
  - CLI help text for atlas verify explaining the exit-code contract and the non-interactive nature.
  definition_of_done:
  - atlas verify command implemented and wired into the CLI.
  - All integration and unit tests pass in CI.
  - 'Exit-code contract verified: PASSED/PENDING/FAILED all exit 0; precondition failures exit non-zero.'
  - No interactive evidence-writing present in this ticket's scope.
  estimated_effort: null
  external_linear_id: 86d37ec5-8ca1-4442-b06f-e62b98df98e1
  external_github_issue_id: null
  tags:
  - verification
  - cli
  - reporting
  component: atlas.cli
  linear_synced_at: '2026-07-04T18:05:38.538342Z'
  last_observed_linear_state_id: 311a3a97-c409-4cce-96ab-0a3bfc2a5541
  status_entered_at: '2026-07-05T15:53:13.776434Z'
  review_cycle_count: 0
  source_anchor: docs/atlas/verification-engine.md#reports
  created_by_type: agent
  created_by_id: planner
  created_at: '2026-07-04T18:05:38.538342Z'
  updated_at: '2026-07-04T18:05:38.538342Z'
  completed_at: null
- id: 3f869839-1b38-4f86-987e-618d7eda1600
  product_id: 6363ff3f-22db-40b3-9a47-aeba5d0a7586
  epic_id: f64bcf20-dbdf-4d8e-98b3-d615de1bc6a4
  key: ATLAS-91
  title: 'Phase 7 milestone test: no system-tier evidence blocks completion'
  objective: Implement the Phase 7 milestone acceptance test verifying that a ticket with passing agent-claimed evidence but no system-tier evidence cannot reach done, and that the same ticket completes once CI evidence lands for the head commit.
  context: 'The roadmap milestone test for Phase 7 is: ''a ticket with passing agent-claimed evidence but no system-tier evidence cannot reach done; the same ticket completes once CI evidence lands for the head commit.'' This test must be automated and run in CI to prove the ''no evidence = no completion'' rule is enforced end-to-end by the Verification Engine.'
  status: needs_human_decision
  ticket_type: feature
  risk_level: medium
  priority: 50
  relevant_docs:
  - docs/atlas/implementation-roadmap.md
  - docs/atlas/verification-engine.md
  - docs/decisions/0008-ci-sourced-evidence-with-trust-tiers.md
  acceptance_criteria:
  - A test fixture creates a ticket with agent-tier PASSED evidence for TESTS, LINT, and BUILD check types at a given head commit.
  - Running verify(ticket, head_commit, evidence) with only agent-tier evidence returns a verdict that is not PASSED (PENDING or FAILED) for the machine checks.
  - Adding system-tier PASSED evidence for the same checks at the same head commit causes verify(ticket, head_commit, evidence) to return PASSED for those checks.
  - The test is automated and runs in CI without network access (all evidence is fixture data).
  - The test is named and documented as the Phase 7 milestone test.
  non_goals:
  - Testing the CLI end-to-end with a real GitHub PR — the milestone test uses fixture data.
  - Testing every evaluator in detail — those are covered by the individual evaluator tickets.
  - Testing the PM Engine's response to the verdict.
  implementation_notes:
  - Use a fixture ticket of type 'bug' (requires tests, lint, acceptance_criteria, scope) to keep the required check set small.
  - Satisfy acceptance_criteria and scope checks with appropriate fixture evidence so the milestone test isolates the system-tier vs agent-tier distinction.
  - The test must not make network calls.
  test_requirements:
  - 'Test: agent-tier PASSED evidence only → machine checks return PENDING, overall verdict not PASSED.'
  - 'Test: system-tier PASSED evidence added → machine checks return PASSED, overall verdict PASSED (assuming other required checks are satisfied or not required for the fixture ticket type).'
  - Test is in tests/verification/test_milestone.py or equivalent, clearly labelled.
  documentation_requirements:
  - A comment in the test file explaining it is the Phase 7 milestone acceptance test and what it proves.
  definition_of_done:
  - Milestone test implemented and passing in CI.
  - Test clearly demonstrates agent-tier evidence alone is insufficient.
  - Test clearly demonstrates system-tier evidence satisfies the machine checks.
  - No network calls in the test.
  estimated_effort: null
  external_linear_id: d62e17b5-0909-4e76-a1da-89ffae72c630
  external_github_issue_id: null
  tags:
  - verification
  - acceptance-test
  - milestone
  component: atlas.verification
  linear_synced_at: '2026-07-04T18:05:38.538342Z'
  last_observed_linear_state_id: 311a3a97-c409-4cce-96ab-0a3bfc2a5541
  status_entered_at: '2026-07-05T15:53:13.776434Z'
  review_cycle_count: 0
  source_anchor: docs/atlas/implementation-roadmap.md#epic-completion-validation
  created_by_type: agent
  created_by_id: planner
  created_at: '2026-07-04T18:05:38.538342Z'
  updated_at: '2026-07-04T18:05:38.538342Z'
  completed_at: null
- id: 43414250-17a7-45f9-89b6-9f6907c7f9df
  product_id: 6363ff3f-22db-40b3-9a47-aeba5d0a7586
  epic_id: 4d034c8b-0752-4089-8ff9-2fd43f51c6f4
  key: ATLAS-92
  title: WORKFLOW.md and tracker configuration for Symphony dispatch
  objective: Ensure the canonical WORKFLOW.md and Linear tracker configuration are correct, complete, and tested so Symphony can poll and dispatch ready tickets without manual intervention.
  context: Symphony loads its behaviour from the repo-owned WORKFLOW.md. The front matter defines the tracker project slug, active/terminal states, polling interval, workspace hooks, and the Codex command. The body is the per-issue prompt template that instructs the agent how to move tickets, read the context pack, and report findings. Phase 8 requires this file to be the authoritative contract before any dispatch can occur. The roadmap seeds this as ATLAS-81.
  status: needs_human_decision
  ticket_type: infrastructure
  risk_level: high
  priority: 100
  relevant_docs:
  - WORKFLOW.md
  - docs/atlas/symphony-integration.md
  - docs/atlas/implementation-roadmap.md
  acceptance_criteria:
  - WORKFLOW.md front matter contains correct tracker.project_slug, active_states ([Ready for Agent, In Progress, PR Open, Changes Requested]), and terminal_states ([Done, Canceled, Duplicate]).
  - 'The codex.command in WORKFLOW.md pins model=gpt-5.5 and sets approval_policy: never and thread_sandbox: workspace-write.'
  - The prompt body instructs the agent to read the ATLAS CONTEXT PACK v1 block from the issue description and treat it as binding.
  - 'The prompt body specifies the correct state-transition sequence: Ready for Agent → In Progress → PR Open → Review Required, with Changes Requested resuming in the same workspace.'
  - 'The prompt body includes the hard limits: never mark own work Done, move to Needs Human on blockers, never author tickets directly.'
  - atlas preflight --check-model passes against the pinned model, confirming the Codex CLI version and entitlement are correct.
  - A unit test asserts that WORKFLOW.md parses as valid YAML and that all required front-matter keys are present with expected values.
  non_goals:
  - Do not implement the context pack embedding into Linear descriptions (that is a separate ticket).
  - Do not implement the PM Engine promotion logic (already delivered in Phase 4).
  - Do not add multi-product WORKFLOW.md rendering; the two per-product knobs are documented but the render mechanism is deferred.
  implementation_notes:
  - The WORKFLOW.md already exists in the repository; this ticket validates and hardens it rather than creating it from scratch.
  - The preflight check (C6) is documented in bootstrap-guide.md#codex-model-requirement-preflight-c6; implement atlas preflight --check-model if not already present.
  - The prompt body template uses Jinja2 variables (issue.identifier, issue.title, issue.state, attempt, issue.description); verify these are correct against the Symphony spec.
  test_requirements:
  - 'Unit test: WORKFLOW.md YAML parses without error and required front-matter keys (tracker.kind, tracker.project_slug, active_states, terminal_states, codex.command) are present.'
  - 'Unit test: active_states and terminal_states contain exactly the values specified in symphony-integration.md state-mapping table.'
  - 'Operator-run live gate: atlas preflight --check-model reports pass against the pinned gpt-5.5 model.'
  documentation_requirements:
  - Update docs/atlas/symphony-integration.md open items section to mark the WORKFLOW.md contract as resolved once this ticket is done.
  definition_of_done:
  - WORKFLOW.md front matter and prompt body match the symphony-integration.md specification exactly.
  - atlas preflight --check-model passes on the operator's machine.
  - All unit tests pass in CI.
  - No manual steps are required for Symphony to begin polling once a ticket reaches Ready for Agent.
  estimated_effort: null
  external_linear_id: 8bcb4fe2-326c-4239-92b5-65a74ba887f3
  external_github_issue_id: null
  tags:
  - symphony
  - workflow
  - tracker-config
  - dispatch
  component: atlas.symphony
  linear_synced_at: '2026-07-04T18:05:38.538342Z'
  last_observed_linear_state_id: 311a3a97-c409-4cce-96ab-0a3bfc2a5541
  status_entered_at: '2026-07-05T15:53:13.776434Z'
  review_cycle_count: 0
  source_anchor: docs/atlas/symphony-integration.md#workflow-contract
  created_by_type: agent
  created_by_id: planner
  created_at: '2026-07-04T18:05:38.538342Z'
  updated_at: '2026-07-04T18:05:38.538342Z'
  completed_at: null
- id: 5900fdc8-a35d-466e-9620-d0a924559f6d
  product_id: 6363ff3f-22db-40b3-9a47-aeba5d0a7586
  epic_id: 4d034c8b-0752-4089-8ff9-2fd43f51c6f4
  key: ATLAS-93
  title: Context pack embedding in Linear issue descriptions
  objective: When the PM Engine promotes a ticket to Ready for Agent, embed the rendered context pack into the Linear issue description so Symphony's agent prompt receives the full ATLAS CONTEXT PACK v1 block without requiring Atlas API access from the workspace.
  context: 'Symphony renders issue.description into the agent prompt via WORKFLOW.md. The context pack must therefore be embedded directly in the Linear description at promotion time. The PM Engine writes the description as: human-readable summary, then a separator, then the ATLAS CONTEXT PACK v1 header with pack_id and rendered_at, then the rendered_markdown. The description is frozen once the ticket moves to In Progress. If the pack exceeds the Linear description size limit, the PM Engine falls back to embedding only objective/constraints/criteria sections and committing the full pack to docs/planning/packs/<ticket-key>.md. The roadmap seeds this as ATLAS-82.'
  status: needs_human_decision
  ticket_type: feature
  risk_level: high
  priority: 90
  relevant_docs:
  - docs/atlas/symphony-integration.md
  - docs/atlas/pm-engine-and-linear-sync.md
  - docs/atlas/context-renderer.md
  - docs/atlas/implementation-roadmap.md
  acceptance_criteria:
  - When the PM Engine promotes a ticket to Ready for Agent, the Linear issue description is updated to contain the ATLAS CONTEXT PACK v1 block with pack_id, rendered_at timestamp, and the full rendered_markdown from the ContextPack.
  - 'The description format is: human-readable summary, then ---, then ATLAS CONTEXT PACK v1 | pack_id: <uuid> | rendered_at: <ts>, then the rendered_markdown.'
  - The pack is refreshed on every sync tick while the ticket is in Ready for Agent; once the ticket moves to In Progress the description is frozen and no further updates are made.
  - If the rendered pack exceeds the Linear description character limit, the fallback embeds only objective, constraints, and acceptance criteria sections, and writes the full pack to docs/planning/packs/<ticket-key>.md.
  - AgentRun records created from observation carry input_context_pack_id referencing the pack embedded at dispatch time.
  - 'Unit tests cover: normal embed, frozen-after-in-progress, and size-limit fallback paths.'
  non_goals:
  - Do not implement context pack rendering itself (delivered in Phase 5).
  - Do not implement AgentRun creation from PR observation (that is a separate ticket).
  - Do not implement multi-product description rendering.
  - Do not add a separate Atlas API endpoint for pack retrieval from workspaces.
  implementation_notes:
  - The Linear description size limit must be measured empirically; symphony-integration.md open items lists this as a pre-Phase-8 resolution item.
  - The embed logic lives in atlas/pm/promotion.py alongside the existing promote_ready function.
  - The frozen-description rule is enforced by checking the ticket's current status before writing; the allow-list in atlas/linear/ already prevents status from crossing Atlas→Linear, so only the description field is gated here.
  - pack_id and rendered_at are read from the ContextPack model fields.
  test_requirements:
  - 'Unit test: promote_ready embeds the pack in the Linear description in the correct format.'
  - 'Unit test: a second sync tick while the ticket is in Ready for Agent refreshes the embedded pack.'
  - 'Unit test: a sync tick while the ticket is In Progress does not update the description.'
  - 'Unit test: a pack exceeding the size limit triggers the fallback path and writes docs/planning/packs/<key>.md.'
  - 'Integration test (operator-run): a real ticket promoted to Ready for Agent in Linear shows the ATLAS CONTEXT PACK v1 block in its description.'
  documentation_requirements:
  - Update docs/atlas/symphony-integration.md to mark the context-pack-delivery open item (Linear description size limit) as resolved with the measured limit.
  definition_of_done:
  - A ticket promoted to Ready for Agent has its Linear description updated with the full ATLAS CONTEXT PACK v1 block.
  - The description is frozen once the ticket enters In Progress.
  - The fallback path is implemented and tested.
  - All unit and integration tests pass.
  - CI is green.
  estimated_effort: null
  external_linear_id: 90364f47-e6f6-460d-9afd-68f29642bf0b
  external_github_issue_id: null
  tags:
  - symphony
  - context-pack
  - linear-sync
  - pm-engine
  component: atlas.pm
  linear_synced_at: '2026-07-04T18:05:38.538342Z'
  last_observed_linear_state_id: 311a3a97-c409-4cce-96ab-0a3bfc2a5541
  status_entered_at: '2026-07-05T15:53:13.776434Z'
  review_cycle_count: 0
  source_anchor: docs/atlas/symphony-integration.md#context-pack-delivery
  created_by_type: agent
  created_by_id: planner
  created_at: '2026-07-04T18:05:38.538342Z'
  updated_at: '2026-07-04T18:05:38.538342Z'
  completed_at: null
- id: 1cfd4705-edb4-4f0e-9fdd-d5a978b42a51
  product_id: 6363ff3f-22db-40b3-9a47-aeba5d0a7586
  epic_id: 4d034c8b-0752-4089-8ff9-2fd43f51c6f4
  key: ATLAS-94
  title: PR ingestion and AgentRun reconstruction from observation
  objective: Create AgentRun records by observing Linear activity, PR metadata, and CI evidence rather than by agent self-report, so every dispatched agent session is attributable to an exact context pack and commit.
  context: Per symphony-integration.md, AgentRun records are created by Atlas from observation — not by the agent reporting on itself — consistent with the evidence trust model (ADR-0008). When a ticket transitions from Ready for Agent to In Progress, Atlas infers an agent session has started. When a PR is opened, Atlas links the PR to the ticket and records the head commit. The AgentRun model (data-model §3.9) carries input_context_pack_id, pr_number, head_commit_sha, and outcome fields. The roadmap seeds this as ATLAS-84.
  status: needs_human_decision
  ticket_type: feature
  risk_level: high
  priority: 80
  relevant_docs:
  - docs/atlas/symphony-integration.md
  - docs/atlas/pm-engine-and-linear-sync.md
  - docs/atlas/evidence-pipeline.md
  - docs/atlas/implementation-roadmap.md
  acceptance_criteria:
  - When the PM Engine observes a ticket transition from Ready for Agent to In Progress, it creates an AgentRun record with status=started, input_context_pack_id set to the pack embedded at promotion, and started_at timestamp.
  - When the PM Engine observes a PR linked to the ticket (pr_open state), it updates the AgentRun with pr_number and head_commit_sha from the GitHub PR metadata.
  - AgentRun records are never created by agent self-report; all fields are populated from Linear state observations and GitHub PR API responses.
  - A ticket that cycles through Changes Requested back to In Progress creates a new AgentRun record (a new attempt), not an update to the prior one.
  - 'Unit tests cover: initial AgentRun creation on in_progress transition, PR linkage on pr_open transition, and new-attempt creation on changes_requested→in_progress.'
  non_goals:
  - Do not implement ticket-level failure analysis (cycle/dwell detection) — that is a separate ticket.
  - Do not implement agent metrics reporting — that is a separate ticket.
  - Do not have the agent write AgentRun records directly.
  - Do not implement AgentRun outcome finalisation here; that is driven by verification verdicts.
  implementation_notes:
  - The AgentRun model is defined in data-model §3.9 and should already exist from Phase 1 (ATLAS-15); this ticket wires the creation logic into the sync loop.
  - PR metadata (pr_number, head_commit_sha) is fetched from the GitHub API using the existing evidence poller client.
  - The attempt counter increments on each changes_requested→in_progress transition, tracked on the Ticket model or derived from the count of AgentRun records for the ticket.
  - The sync loop step that creates AgentRuns runs after the status pull (step 1) and before anomaly detection (step 5).
  test_requirements:
  - 'Unit test: sync_tick creates an AgentRun when a ticket transitions to in_progress.'
  - 'Unit test: sync_tick updates the AgentRun with pr_number and head_commit_sha when the ticket transitions to pr_open.'
  - 'Unit test: a changes_requested→in_progress transition creates a second AgentRun with attempt=2.'
  - 'Unit test: AgentRun.input_context_pack_id matches the pack_id embedded in the Linear description at promotion time.'
  documentation_requirements: []
  definition_of_done:
  - Every dispatched agent session has a corresponding AgentRun record created from observation.
  - AgentRun records carry input_context_pack_id, pr_number (once PR is opened), and head_commit_sha.
  - All unit tests pass in CI.
  - No agent self-report path exists.
  estimated_effort: null
  external_linear_id: 8c9b41e3-fd15-4794-b839-b86e67a2b570
  external_github_issue_id: null
  tags:
  - symphony
  - agent-run
  - pr-ingestion
  - evidence
  component: atlas.pm
  linear_synced_at: '2026-07-04T18:05:38.538342Z'
  last_observed_linear_state_id: 311a3a97-c409-4cce-96ab-0a3bfc2a5541
  status_entered_at: '2026-07-05T15:53:13.776434Z'
  review_cycle_count: 0
  source_anchor: docs/atlas/symphony-integration.md#retry-and-failure-seam
  created_by_type: agent
  created_by_id: planner
  created_at: '2026-07-04T18:05:38.538342Z'
  updated_at: '2026-07-04T18:05:38.538342Z'
  completed_at: null
- id: 79c815cc-6d0a-414c-8090-ea8f0d57ec57
  product_id: 6363ff3f-22db-40b3-9a47-aeba5d0a7586
  epic_id: 4d034c8b-0752-4089-8ff9-2fd43f51c6f4
  key: ATLAS-95
  title: 'Handoff-state handling: Review Required and Needs Human stop work without workspace cleanup'
  objective: Ensure the PM Engine and Symphony configuration correctly handle Review Required and Needs Human as handoff states that terminate the agent session while preserving the workspace for potential resumption.
  context: Per symphony-integration.md state-mapping, Review Required and Needs Human are neither active nor terminal in Symphony's classification. This causes Symphony to terminate the worker while preserving the workspace — correct for states where a human verdict may send the ticket back to Changes Requested and the agent resumes in the same workspace. The PM Engine must not attempt to re-dispatch tickets in these states, and the WORKFLOW.md must not list them as active or terminal. The roadmap seeds this as ATLAS-85.
  status: needs_human_decision
  ticket_type: feature
  risk_level: medium
  priority: 70
  relevant_docs:
  - docs/atlas/symphony-integration.md
  - WORKFLOW.md
  - docs/atlas/pm-engine-and-linear-sync.md
  - docs/atlas/implementation-roadmap.md
  acceptance_criteria:
  - WORKFLOW.md active_states does not include Review Required or Needs Human; terminal_states does not include them either.
  - The PM Engine's promote_ready function never moves a ticket from Review Required or Needs Human to Ready for Agent.
  - The PM Engine's sync loop does not push definition updates to tickets in Review Required or Needs Human (description is frozen per context-pack-delivery rules).
  - When the operator relays a Changes Requested verdict (review_required → changes_requested), the PM Engine writes this transition to Linear and the ticket re-enters the active dispatch cycle.
  - Unit tests assert that tickets in review_required and needs_human_decision are skipped by promote_ready and definition-push logic.
  non_goals:
  - Do not implement the verification engine's verdict-to-state routing (that is Phase 7 work already delivered).
  - Do not implement workspace cleanup or teardown logic — Symphony owns that.
  - Do not implement the operator UI for reviewing PRs — the operator acts directly in Linear/GitHub.
  implementation_notes:
  - The state-mapping table in symphony-integration.md is the authoritative reference; the PM Engine's status handling must match it exactly.
  - The PM Engine already owns the review_required → done and review_required → changes_requested transitions per the one-writer-per-state-edge rule; this ticket ensures those paths are correctly wired.
  - Workspace preservation is Symphony's responsibility; Atlas only needs to ensure it does not send conflicting state signals.
  test_requirements:
  - 'Unit test: promote_ready skips tickets in review_required and needs_human_decision.'
  - 'Unit test: definition-push step skips tickets in review_required and needs_human_decision.'
  - 'Unit test: a review_required → changes_requested transition written by the PM Engine results in the ticket re-entering the active states list.'
  - 'Verify WORKFLOW.md YAML: active_states and terminal_states do not contain Review Required or Needs Human.'
  documentation_requirements: []
  definition_of_done:
  - Handoff states are correctly excluded from Symphony's active and terminal state lists.
  - The PM Engine never re-dispatches or updates tickets in handoff states.
  - The Changes Requested re-dispatch path is tested and working.
  - All unit tests pass in CI.
  estimated_effort: null
  external_linear_id: 51476e20-fb9d-4429-bc3d-4029516e76a8
  external_github_issue_id: null
  tags:
  - symphony
  - state-mapping
  - handoff
  - workflow
  component: atlas.pm
  linear_synced_at: '2026-07-04T18:05:38.538342Z'
  last_observed_linear_state_id: 311a3a97-c409-4cce-96ab-0a3bfc2a5541
  status_entered_at: '2026-07-05T15:53:13.776434Z'
  review_cycle_count: 0
  source_anchor: docs/atlas/symphony-integration.md#state-mapping
  created_by_type: agent
  created_by_id: planner
  created_at: '2026-07-04T18:05:38.538342Z'
  updated_at: '2026-07-04T18:05:38.538342Z'
  completed_at: null
- id: c44b693f-c29f-4234-adff-57393cefd7b6
  product_id: 6363ff3f-22db-40b3-9a47-aeba5d0a7586
  epic_id: 4d034c8b-0752-4089-8ff9-2fd43f51c6f4
  key: ATLAS-96
  title: 'Ticket-level failure analysis: cycle and dwell detection with DRAFT lesson creation'
  objective: Implement Atlas-owned ticket-level failure analysis that detects review cycling and dwell breaches after Symphony handoff, files failure-analysis notes, proposes ticket splits for oversized work, and creates DRAFT lessons from repeated failure patterns.
  context: 'Per symphony-integration.md retry-and-failure-seam, Atlas owns ticket-level outcomes while Symphony owns intra-ticket execution reliability. The PM Engine watches for: tickets cycling changes_requested ↔ pr_open more than N times (default 3); tickets dwelling in active states beyond configurable horizons; and verification failures after handoff. Its responses are planning-level: file a failure-analysis note, propose a ticket split, or route to Needs Human. Repeated failure patterns become DRAFT lessons (ADR-0009). The roadmap seeds this as ATLAS-86. Note: cycle detection (ATLAS-120) and dwell detection (ATLAS-119) are already delivered in Phase 4; this ticket wires the lesson-creation and split-proposal responses specific to Phase 8.'
  status: needs_human_decision
  ticket_type: feature
  risk_level: medium
  priority: 60
  relevant_docs:
  - docs/atlas/symphony-integration.md
  - docs/atlas/pm-engine-and-linear-sync.md
  - docs/atlas/learning-system.md
  - docs/atlas/implementation-roadmap.md
  acceptance_criteria:
  - When a ticket exceeds the review-cycle threshold (>3 changes_requested→pr_open round trips), the PM Engine creates a DRAFT Lesson with ticket_type/tag context and a failure-analysis summary, in addition to routing the ticket to Needs Human.
  - When a ticket has accumulated more than one dwell breach of the same type, the PM Engine creates a DRAFT Lesson capturing the dwell pattern.
  - 'The failure-analysis note posted to the ticket (as a Linear comment or DebtItem detail) includes: the anomaly type, the count, the ticket key, and a suggested remediation (split the ticket if acceptance_criteria count > 5).'
  - DRAFT lessons are created with status=DRAFT and are not retrievable into context packs until operator promotion (ADR-0009).
  - 'Unit tests cover: lesson creation on cycle threshold breach, lesson creation on repeated dwell breach, and the split-proposal trigger condition.'
  non_goals:
  - Do not implement lesson promotion (that is the operator's gate, delivered in Phase 9).
  - Do not implement the full learning system extraction pipeline (Phase 9).
  - Do not re-implement cycle or dwell detection — those are already in Phase 4 (ATLAS-119, ATLAS-120); this ticket adds the lesson-creation response.
  - Do not automatically split tickets; only propose the split as a planning input.
  implementation_notes:
  - Lesson creation calls the same Lesson model and repository used by Phase 9; this ticket is the first writer of failure-pattern lessons from the PM Engine.
  - The split proposal is written as an atlas:proposed-follow-up comment on the Linear issue, feeding the existing follow-up ingestion pipeline (ATLAS-45/122).
  - The lesson body is deterministically constructed from the anomaly record fields — no LLM call; ADR-0005 applies (code calculates, agents interpret).
  test_requirements:
  - 'Unit test: a ticket with review_cycle_count > 3 triggers DRAFT lesson creation with correct fields.'
  - 'Unit test: a ticket with two or more DWELL_BREACH DebtItems of the same type triggers DRAFT lesson creation.'
  - 'Unit test: the failure-analysis note includes the split suggestion when acceptance_criteria_count > 5.'
  - 'Unit test: created lessons have status=DRAFT and are excluded from context-pack retrieval.'
  documentation_requirements:
  - Add a note to docs/atlas/symphony-integration.md retry-and-failure-seam section documenting the lesson-creation trigger conditions.
  definition_of_done:
  - DRAFT lessons are created automatically on cycle and repeated dwell breaches.
  - Failure-analysis notes include split suggestions for oversized tickets.
  - Lessons are not retrievable until operator promotion.
  - All unit tests pass in CI.
  estimated_effort: null
  external_linear_id: 443f4b8c-df14-4add-bf3e-3e0c0607d09a
  external_github_issue_id: null
  tags:
  - symphony
  - failure-analysis
  - lessons
  - pm-engine
  component: atlas.pm
  linear_synced_at: '2026-07-04T18:05:38.538342Z'
  last_observed_linear_state_id: 311a3a97-c409-4cce-96ab-0a3bfc2a5541
  status_entered_at: '2026-07-05T15:53:13.776434Z'
  review_cycle_count: 0
  source_anchor: docs/atlas/symphony-integration.md#retry-and-failure-seam
  created_by_type: agent
  created_by_id: planner
  created_at: '2026-07-04T18:05:38.538342Z'
  updated_at: '2026-07-04T18:05:38.538342Z'
  completed_at: null
- id: 367b6939-e6ed-4350-a71d-8358ba94b9c6
  product_id: 6363ff3f-22db-40b3-9a47-aeba5d0a7586
  epic_id: 4d034c8b-0752-4089-8ff9-2fd43f51c6f4
  key: ATLAS-97
  title: Agent metrics CLI report
  objective: Implement atlas agent report, a CLI command that reports per-ticket agent run metrics — attempt counts, cycle times from Ready for Agent to PR Open, outcome distribution, and failure pattern summaries — computed deterministically from AgentRun records and DebtItems.
  context: 'The PM Engine accumulates AgentRun records and DebtItems across the delivery loop. A CLI report surfaces agent-level delivery health: how many attempts tickets required, how long agents took to open PRs, how often review cycling occurred, and which failure patterns are most common. Per ADR-0005, all metrics are computed by deterministic code; the report is a pure reader with no Linear calls and no writes. The roadmap seeds this as ATLAS-88.'
  status: needs_human_decision
  ticket_type: feature
  risk_level: low
  priority: 50
  relevant_docs:
  - docs/atlas/symphony-integration.md
  - docs/atlas/pm-engine-and-linear-sync.md
  - docs/atlas/implementation-roadmap.md
  - docs/decisions/0005-code-calculates-agents-interpret.md
  acceptance_criteria:
  - 'atlas agent report prints: total AgentRun count, mean and median attempt count per ticket, distribution of outcomes (started/completed/failed), and top-5 tickets by attempt count.'
  - The report includes cycle time from Ready for Agent to PR Open (in hours), computed from TicketStatusTransition records, with min/median/max.
  - 'The report includes a failure-pattern summary: count of REVIEW_CYCLE and DWELL_BREACH DebtItems, and count of DRAFT lessons created by failure analysis.'
  - atlas agent report --json emits the same data as a machine-readable JSON object.
  - The command makes no Linear API calls and writes nothing to the database or filesystem.
  - Unit tests cover all metric computations against fixture AgentRun and DebtItem data.
  non_goals:
  - Do not implement a dashboard or web UI.
  - Do not implement LLM-assisted interpretation of the metrics.
  - Do not implement per-agent-provider breakdown (all runs are Symphony in Phase 8).
  - Do not gate any workflow on these metrics; the report is advisory only.
  implementation_notes:
  - Reuse the TicketStatusTransition log (ATLAS-121) for cycle time computation, consistent with atlas pm report.
  - The command is registered under atlas agent report in the CLI (atlas/cli.py).
  - All metric computations are pure functions over model instances, testable without a database connection.
  test_requirements:
  - 'Unit test: attempt count distribution computed correctly from fixture AgentRun records.'
  - 'Unit test: cycle time computed correctly from TicketStatusTransition records.'
  - 'Unit test: failure-pattern summary counts match fixture DebtItem records.'
  - 'Unit test: --json flag emits valid JSON matching the expected schema.'
  - 'Unit test: command exits 0 with no output when no AgentRun records exist.'
  documentation_requirements: []
  definition_of_done:
  - atlas agent report prints all required metrics.
  - atlas agent report --json emits valid machine-readable JSON.
  - No network calls or writes occur during report generation.
  - All unit tests pass in CI.
  estimated_effort: null
  external_linear_id: 7d888d23-6da3-4991-8099-62d990b4ca3b
  external_github_issue_id: null
  tags:
  - symphony
  - metrics
  - cli
  - agent-run
  component: atlas.cli
  linear_synced_at: '2026-07-04T18:05:38.538342Z'
  last_observed_linear_state_id: 311a3a97-c409-4cce-96ab-0a3bfc2a5541
  status_entered_at: '2026-07-05T15:53:13.776434Z'
  review_cycle_count: 0
  source_anchor: docs/atlas/implementation-roadmap.md#epic-autonomous-delivery
  created_by_type: agent
  created_by_id: planner
  created_at: '2026-07-04T18:05:38.538342Z'
  updated_at: '2026-07-04T18:05:38.538342Z'
  completed_at: null
- id: 3589d7cd-38b5-472b-a872-52824ffb63c7
  product_id: 6363ff3f-22db-40b3-9a47-aeba5d0a7586
  epic_id: 4d034c8b-0752-4089-8ff9-2fd43f51c6f4
  key: ATLAS-98
  title: End-to-end delivery automation test
  objective: 'Implement and pass the Phase 8 milestone acceptance test: a ready, context-rich fixture ticket flows from context pack through Symphony dispatch to PR, evidence ingestion, and verification without manual steps beyond the defined human gates.'
  context: 'The Phase 8 milestone test requires the full delivery loop to operate end-to-end: a fixture ticket in Ready for Agent state (with an embedded context pack) is dispatched by Symphony, the agent opens a PR, CI produces system-tier evidence, the Verification Engine evaluates the evidence, and the PM Engine transitions the ticket to Done (or surfaces it for human approval). This test exercises the integration of all Phase 8 components: WORKFLOW.md, context pack embedding, AgentRun reconstruction, handoff-state handling, and failure analysis. The roadmap seeds this as ATLAS-90.'
  status: needs_human_decision
  ticket_type: infrastructure
  risk_level: high
  priority: 40
  relevant_docs:
  - docs/atlas/symphony-integration.md
  - docs/atlas/implementation-roadmap.md
  - docs/atlas/verification-engine.md
  - docs/atlas/evidence-pipeline.md
  - docs/planning/inbox/smoke-b-fixture.md
  acceptance_criteria:
  - A fixture ticket (the Smoke B fixture from docs/planning/inbox/smoke-b-fixture.md or equivalent) is placed in Ready for Agent with an embedded ATLAS CONTEXT PACK v1 block in its Linear description.
  - Symphony dispatches the ticket automatically (no manual trigger beyond placing it in Ready for Agent).
  - The agent opens a PR referencing the ticket key and moves the ticket to PR Open, then Review Required when CI is green.
  - The evidence poller ingests at least one system-tier CI evidence record (TEST_RESULT or BUILD_RESULT) pinned to the PR head commit.
  - The Verification Engine produces a verdict for the ticket; the PM Engine transitions the ticket to Done on a PASSED verdict (or surfaces it for human approval if human_approval is required).
  - An AgentRun record exists for the dispatch session with pr_number and head_commit_sha populated.
  - The test is documented as an operator-run live gate in a runbook; CI green is necessary but not sufficient (ADR-0008).
  non_goals:
  - Do not automate the human approval gate — the operator performs that step.
  - Do not test failure paths (cycle detection, dwell breach) in this end-to-end test; those are covered by unit tests in their respective tickets.
  - Do not test multi-ticket PRs; the fixture closes exactly one ticket.
  - Do not implement a fully automated CI-only version of this test; it requires a live Symphony instance and Linear workspace.
  implementation_notes:
  - 'Use the Smoke B fixture (docs/planning/inbox/smoke-b-fixture.md) as the test ticket: a docs-only change with falsifiable acceptance criteria, sized so the interesting part is the machinery around it.'
  - The runbook should be written at docs/runbooks/phase-8-e2e-test.md and referenced from the Phase 8 milestone section of the implementation roadmap.
  - The automated CI smoke test imports atlas.pm.promotion, atlas.symphony (if it exists), and atlas.cli and asserts key functions are callable — it does not dispatch.
  test_requirements:
  - 'Operator-run live gate: execute the full loop against the real Linear workspace and Symphony instance, documenting the outcome in the runbook.'
  - Verify that the AgentRun record is created and populated correctly after the dispatch.
  - Verify that system-tier evidence records exist for the PR head commit after CI completes.
  - Verify that the Verification Engine verdict is PASSED (or correctly PENDING awaiting human approval) after evidence ingestion.
  - 'Automated smoke test (CI): assert that all Phase 8 components are importable and their public interfaces are present (no live dispatch).'
  documentation_requirements:
  - Write docs/runbooks/phase-8-e2e-test.md documenting the operator steps, expected outcomes, and how to interpret the results.
  - Update docs/atlas/implementation-roadmap.md Phase 8 milestone test section to reference the runbook.
  definition_of_done:
  - The operator has run the full end-to-end loop at least once and documented the outcome in the runbook.
  - All Phase 8 components are integrated and the loop completes without manual steps beyond the defined human gates.
  - System-tier evidence exists for the fixture PR head commit.
  - The Verification Engine produces a verdict and the PM Engine acts on it.
  - The runbook is committed and CI is green.
  estimated_effort: null
  external_linear_id: 979d387c-ca49-4249-bab5-d080388a80e1
  external_github_issue_id: null
  tags:
  - symphony
  - e2e-test
  - integration
  - milestone
  component: null
  linear_synced_at: '2026-07-04T18:05:38.538342Z'
  last_observed_linear_state_id: 311a3a97-c409-4cce-96ab-0a3bfc2a5541
  status_entered_at: '2026-07-05T15:53:13.776434Z'
  review_cycle_count: 0
  source_anchor: docs/atlas/symphony-integration.md#boundary
  created_by_type: agent
  created_by_id: planner
  created_at: '2026-07-04T18:05:38.538342Z'
  updated_at: '2026-07-04T18:05:38.538342Z'
  completed_at: null
- id: 6a07b007-2b3a-43df-ade7-88e8b8cc1b90
  product_id: 6363ff3f-22db-40b3-9a47-aeba5d0a7586
  epic_id: 5a486514-1761-45a8-8094-9a0e8282c3d4
  key: ATLAS-99
  title: Lesson extraction on ticket completion and failure events
  objective: Implement the lesson extractor that fires on ticket completion (done) and failure events (rejected, PM failure-analysis), producing schema-valid DRAFT lessons via an LLM call over a bounded evidence bundle.
  context: 'The Learning System is the final phase of the Atlas harness. Per learning-system.md, lesson extraction fires on three triggers: ticket reaching done (success-pattern candidate), ticket reaching rejected or a PM failure-analysis event (failure-pattern candidate), and operator request. The extractor makes an LLM call over a bounded evidence bundle (ticket, agent runs, PR review history, verification verdicts) and produces a Lesson with status DRAFT, confidence null, and related_ticket_ids populated. ADR-0009 mandates that all agent-authored lessons start as DRAFT and are excluded from context-pack retrieval until operator promotion. ADR-0005 requires that the LLM interprets; deterministic code assembles the evidence bundle and validates the output schema.'
  status: needs_human_decision
  ticket_type: feature
  risk_level: high
  priority: 100
  relevant_docs:
  - docs/atlas/learning-system.md
  - docs/decisions/0009-single-operator-governance.md
  - docs/decisions/0005-code-calculates-agents-interpret.md
  - docs/atlas/implementation-roadmap.md
  acceptance_criteria:
  - A lesson-extraction run is triggered automatically when a ticket transitions to `done` and the delivery was notable (first-attempt verification pass after prior failures of the same ticket_type/tag, or unusually fast cycle time).
  - A lesson-extraction run is triggered automatically when a ticket transitions to `rejected` or a PM failure-analysis event fires (review-cycle breach, dwell breach).
  - '`atlas lessons extract <KEY>` triggers extraction on demand for any ticket key.'
  - The extractor assembles a bounded evidence bundle (ticket record, agent runs, PR review history, verification verdicts) and calls the LLM; raw diffs beyond a configurable size cap are excluded.
  - The LLM output is parsed and validated against the Lesson schema; extraction failures are logged and never retried into noise.
  - 'Every extracted lesson is persisted with `status: DRAFT`, `confidence: null`, and `related_ticket_ids` populated from the source ticket.'
  - No extracted lesson is retrievable by the Context Renderer until promoted to ACTIVE.
  non_goals:
  - Operator promotion workflow (covered by the promotion CLI ticket).
  - Pattern detection over accumulated lessons (separate ticket).
  - Playbook generation (separate ticket).
  - Confidence scoring by the model — confidence is null at extraction and set by the operator at promotion.
  - Embedding-based or vector-search retrieval of lessons.
  implementation_notes:
  - Implement in `atlas/learning/extractor.py`; the trigger hooks live in the PM Engine's status-transition path.
  - The notability predicate for `done` tickets (first-attempt pass after prior failures, or fast cycle) must be deterministic code, not LLM judgement (ADR-0005).
  - Use the existing Lesson model (ATLAS-13) with status field; DRAFT is the default for agent-authored lessons per ADR-0009.
  - The LLM prompt must be a versioned Jinja2 template following the same conventions as the planner prompts.
  - Extraction failures are logged to the standard logger at WARNING level with the ticket key and exception type; no retry.
  test_requirements:
  - 'Unit tests for the evidence bundle assembler: correct fields included, size cap enforced, raw diffs excluded beyond cap.'
  - 'Unit test: extraction on a `done` ticket with no prior failures of the same type produces no lesson (not notable).'
  - 'Unit test: extraction on a `done` ticket with prior failures of the same ticket_type produces a DRAFT lesson.'
  - 'Unit test: extraction on a `rejected` ticket produces a DRAFT lesson.'
  - 'Integration test: `atlas lessons extract <KEY>` against a fixture ticket produces a persisted DRAFT lesson with confidence null.'
  - Test that a failed LLM call logs the failure and does not persist a partial lesson.
  documentation_requirements:
  - Docstrings on the extractor module and public functions.
  - Update learning-system.md if any extraction trigger semantics differ from the design.
  definition_of_done:
  - Extraction triggers fire correctly on done, rejected, and PM failure-analysis events in integration tests.
  - '`atlas lessons extract <KEY>` works end-to-end against a fixture ticket.'
  - All unit and integration tests pass in CI.
  - No extracted lesson is retrievable before promotion (verified by context renderer test).
  estimated_effort: null
  external_linear_id: 4829581a-144b-4da9-96c4-6dac2902b64e
  external_github_issue_id: null
  tags:
  - learning-system
  - lesson-extraction
  - llm
  component: atlas.learning
  linear_synced_at: '2026-07-04T18:05:38.538342Z'
  last_observed_linear_state_id: 311a3a97-c409-4cce-96ab-0a3bfc2a5541
  status_entered_at: '2026-07-05T15:53:13.776434Z'
  review_cycle_count: 0
  source_anchor: docs/atlas/learning-system.md#extraction-triggers
  created_by_type: agent
  created_by_id: planner
  created_at: '2026-07-04T18:05:38.538342Z'
  updated_at: '2026-07-04T18:05:38.538342Z'
  completed_at: null
- id: 880da2b6-7dea-420f-b291-cb29a358afdf
  product_id: 6363ff3f-22db-40b3-9a47-aeba5d0a7586
  epic_id: 5a486514-1761-45a8-8094-9a0e8282c3d4
  key: ATLAS-100
  title: Lesson promotion CLI with DRAFT/ACTIVE/ARCHIVED lifecycle
  objective: Implement the operator-facing `atlas lessons review` and promotion commands that move lessons through the DRAFT → ACTIVE → ARCHIVED lifecycle, enforcing the ADR-0009 gate between agent-authored lessons and context-pack retrieval.
  context: 'ADR-0009 establishes the lesson promotion gate as the single most important governance mechanism in the Learning System: agent-authored lessons must not enter context packs until the operator promotes them. The learning-system.md promotion workflow specifies: `atlas lessons review` lists DRAFTs; per lesson the operator can promote (sets ACTIVE, operator assigns confidence 0–1), edit then promote, reject (ARCHIVED), or merge into an existing lesson. Confidence is an operator judgement, not a model output. The Lesson model already carries a status field (ATLAS-13). This ticket implements the CLI surface and the state-machine transitions.'
  status: needs_human_decision
  ticket_type: feature
  risk_level: high
  priority: 99
  relevant_docs:
  - docs/atlas/learning-system.md
  - docs/decisions/0009-single-operator-governance.md
  - docs/atlas/implementation-roadmap.md
  acceptance_criteria:
  - '`atlas lessons review` lists all DRAFT lessons with their title, source ticket, and creation timestamp.'
  - '`atlas lessons review --stale` lists ACTIVE lessons included in ≥10 context packs with zero subsequent operator re-confirmation.'
  - The operator can promote a DRAFT lesson to ACTIVE, supplying a confidence value between 0.0 and 1.0; the transition is persisted.
  - The operator can reject a DRAFT lesson, setting it to ARCHIVED; the lesson is retained for pattern detection but excluded from retrieval.
  - The operator can merge a DRAFT lesson into an existing ACTIVE lesson (the DRAFT is ARCHIVED; the target lesson's related_ticket_ids is updated).
  - Promoting a lesson to ACTIVE with a confidence outside [0.0, 1.0] is rejected with a clear error message.
  - Only ACTIVE lessons are returned by the lesson retrieval query used by the Context Renderer.
  non_goals:
  - Automatic confidence decay — explicitly deferred per learning-system.md open items.
  - Multi-user approval routing — single operator only per ADR-0009.
  - LLM-assisted lesson editing — the operator edits manually.
  - Lesson extraction (separate ticket).
  - Pattern detection (separate ticket).
  implementation_notes:
  - CLI commands live in `atlas/cli.py` under an `atlas lessons` group.
  - State transitions are enforced in a `LessonRepo` method; the CLI calls the repo, never writes the DB directly.
  - The stale-lesson query counts `related_ticket_ids` additions since last operator action as a proxy for re-confirmation; the exact staleness predicate must be documented in code.
  - Confidence is stored as a float column on the Lesson model; null until promotion.
  test_requirements:
  - 'Unit tests for each lifecycle transition: DRAFT→ACTIVE, DRAFT→ARCHIVED, ACTIVE→ARCHIVED.'
  - 'Unit test: promoting with confidence outside [0.0, 1.0] raises a typed validation error.'
  - 'Unit test: merge sets the DRAFT to ARCHIVED and updates the target lesson''s related_ticket_ids.'
  - 'Integration test: `atlas lessons review` lists only DRAFT lessons.'
  - 'Integration test: after promotion, the lesson appears in the retrieval query result; before promotion it does not.'
  - 'Integration test: `atlas lessons review --stale` returns lessons meeting the ≥10 packs / zero re-confirmation criterion.'
  documentation_requirements:
  - CLI help text for all `atlas lessons` subcommands.
  - Update learning-system.md promotion workflow section if implementation differs.
  definition_of_done:
  - All lifecycle transitions work correctly via CLI and are covered by tests.
  - Only ACTIVE lessons are retrievable by the Context Renderer (verified by retrieval unit test).
  - All tests pass in CI.
  - CLI help text is accurate and complete.
  estimated_effort: null
  external_linear_id: 8adb69d7-89a3-485d-871e-5ac9bf5f75b2
  external_github_issue_id: null
  tags:
  - learning-system
  - lesson-promotion
  - cli
  - governance
  component: atlas.learning
  linear_synced_at: '2026-07-04T18:05:38.538342Z'
  last_observed_linear_state_id: 311a3a97-c409-4cce-96ab-0a3bfc2a5541
  status_entered_at: '2026-07-05T15:53:13.776434Z'
  review_cycle_count: 0
  source_anchor: docs/atlas/learning-system.md#promotion-workflow
  created_by_type: agent
  created_by_id: planner
  created_at: '2026-07-04T18:05:38.538342Z'
  updated_at: '2026-07-04T18:05:38.538342Z'
  completed_at: null
- id: 615db218-f458-4b5f-bbf5-fd3f650b78c8
  product_id: 6363ff3f-22db-40b3-9a47-aeba5d0a7586
  epic_id: 5a486514-1761-45a8-8094-9a0e8282c3d4
  key: ATLAS-101
  title: Lesson retrieval integration with Context Renderer (ACTIVE only)
  objective: Wire the lesson retrieval step into the Context Renderer so that only ACTIVE lessons are included in context packs, matched by tag and ticket_type intersection, ranked by confidence then recency, capped at 3.
  context: 'The Context Renderer (Phase 5, context-renderer.md) specifies lesson retrieval as retrieval rule 4: `status: ACTIVE` only (ADR-0009), matched by tag intersection with the ticket''s tags and ticket_type, ranked by confidence then recency, cap 3. The compression ladder also specifies that lesson bodies are dropped to titles before other compression steps. This ticket implements the retrieval query and its integration into the pack assembly, ensuring the DRAFT gate is enforced at the retrieval layer and that no DRAFT lesson ever appears in a rendered pack.'
  status: needs_human_decision
  ticket_type: feature
  risk_level: high
  priority: 98
  relevant_docs:
  - docs/atlas/learning-system.md
  - docs/atlas/context-renderer.md
  - docs/decisions/0009-single-operator-governance.md
  - docs/atlas/implementation-roadmap.md
  acceptance_criteria:
  - 'The lesson retrieval query filters strictly on `status: ACTIVE`; DRAFT and ARCHIVED lessons are never returned.'
  - Lessons are matched by tag intersection with the ticket's tags and ticket_type; a lesson with no overlapping tags is not returned.
  - Results are ranked by confidence (descending) then recency (descending); ties broken by recency.
  - At most 3 lessons are included in any context pack.
  - When a ticket whose pack included lesson L completes successfully, L's `related_ticket_ids` gains the ticket key (citation feedback).
  - The context pack validation (`atlas context validate <KEY>`) rejects any pack containing a DRAFT lesson.
  - The compression ladder drops lesson bodies to titles before other compression steps when the pack is over budget.
  non_goals:
  - Embedding-based or vector-search lesson retrieval — deferred per learning-system.md open items.
  - Component-tag taxonomy formalisation — seed manually, revisit after Phase 9 produces enough lessons.
  - Automatic confidence decay.
  - Lesson promotion (separate ticket).
  - Lesson extraction (separate ticket).
  implementation_notes:
  - The retrieval function lives in `atlas/context/` (below `atlas.planning` in the layer spine); it must not import from `atlas.planning`.
  - Tag intersection is a set operation on the ticket's tags list and the lesson's tags list.
  - Citation feedback is triggered by the PM Engine's status-transition path when a ticket moves to `done` and its last rendered pack is known.
  - The compression ladder step (drop lesson bodies to titles) is implemented in the existing compression module.
  test_requirements:
  - 'Unit test: retrieval query returns only ACTIVE lessons for a fixture ticket with matching tags.'
  - 'Unit test: DRAFT and ARCHIVED lessons are excluded even when tags match.'
  - 'Unit test: ranking is by confidence descending then recency descending.'
  - 'Unit test: cap of 3 is enforced; a fixture with 5 matching ACTIVE lessons returns exactly 3.'
  - 'Unit test: citation feedback — completing a ticket updates related_ticket_ids on the included lessons.'
  - 'Integration test: a rendered pack for a fixture ticket with ACTIVE lessons includes them; a pack for a ticket with only DRAFT lessons includes none.'
  - 'Integration test: `atlas context validate <KEY>` fails on a pack that somehow contains a DRAFT lesson.'
  documentation_requirements:
  - Docstrings on the lesson retrieval function.
  - Update context-renderer.md retrieval rules section if implementation differs from the design.
  definition_of_done:
  - Retrieval query enforces ACTIVE-only filter, tag matching, ranking, and cap in all tests.
  - Citation feedback is recorded on successful ticket completion.
  - Pack validation rejects DRAFT lessons.
  - All tests pass in CI.
  estimated_effort: null
  external_linear_id: dcde2df9-3713-424a-b061-858232d22136
  external_github_issue_id: null
  tags:
  - learning-system
  - context-renderer
  - lesson-retrieval
  component: atlas.context
  linear_synced_at: '2026-07-04T18:05:38.538342Z'
  last_observed_linear_state_id: 311a3a97-c409-4cce-96ab-0a3bfc2a5541
  status_entered_at: '2026-07-05T15:53:13.776434Z'
  review_cycle_count: 0
  source_anchor: docs/atlas/learning-system.md#retrieval-interplay
  created_by_type: agent
  created_by_id: planner
  created_at: '2026-07-04T18:05:38.538342Z'
  updated_at: '2026-07-04T18:05:38.538342Z'
  completed_at: null
- id: 3e851afc-e19c-441d-a30e-121fc21f794a
  product_id: 6363ff3f-22db-40b3-9a47-aeba5d0a7586
  epic_id: 5a486514-1761-45a8-8094-9a0e8282c3d4
  key: ATLAS-102
  title: Failure and success pattern detection over accumulated lessons
  objective: Implement deterministic heuristic pattern detection over lessons, debt items, and anomaly logs that raises pattern-candidate flags when recurring failure tags or recurring debt categories are detected, surfaced via `atlas lessons report`.
  context: 'learning-system.md specifies pattern detection as deterministic heuristics over lessons, debt items, and anomaly logs — no LLM. The rule: recurrence of the same tag across ≥3 failure-pattern lessons, or the same DebtItem category recurring across tickets, raises a pattern-candidate flag in `atlas lessons report`. ADR-0011 clarifies that pattern detection over code-quality debt must re-point at the reintroduced code-quality register (ATLAS-117), not at the delivery anomaly''s anomaly_type. ADR-0005 requires all quantitative detection to be deterministic code. Patterns are inputs to playbooks and to planning.'
  status: needs_human_decision
  ticket_type: feature
  risk_level: medium
  priority: 97
  relevant_docs:
  - docs/atlas/learning-system.md
  - docs/decisions/0005-code-calculates-agents-interpret.md
  - docs/decisions/0011-debtitem-denotes-delivery-anomalies.md
  - docs/atlas/implementation-roadmap.md
  acceptance_criteria:
  - A tag appearing across ≥3 failure-pattern DRAFT or ACTIVE lessons is flagged as a pattern candidate in `atlas lessons report`.
  - Pattern detection is purely deterministic code with no LLM calls (ADR-0005).
  - '`atlas lessons report` displays pattern candidates with the tag, count of contributing lessons, and the lesson titles.'
  - Pattern detection over code-quality debt items uses the code-quality register entity (ATLAS-117) when available, not the delivery-anomaly DebtItem's anomaly_type.
  - The pattern detection function is unit-testable in isolation with fixture lesson and debt data.
  - A tag with fewer than 3 failure-pattern lessons is not flagged.
  non_goals:
  - LLM-assisted pattern interpretation — detection is deterministic; interpretation is a future enhancement.
  - Automatic playbook generation triggered by pattern detection (separate ticket).
  - Pattern detection over delivery anomalies (DebtItem anomaly_type) — ADR-0011 explicitly separates these.
  - Embedding-based clustering of lessons.
  - Alerting or notification on pattern detection.
  implementation_notes:
  - Implement in `atlas/learning/patterns.py`; the function signature takes a list of lessons and returns a list of PatternCandidate dataclasses.
  - The threshold (≥3) is a module constant, not a config file value, to keep it auditable.
  - The code-quality register integration is conditional on ATLAS-117 being present; if the entity does not exist, that detection branch is skipped gracefully.
  - Pattern candidates are not persisted — they are computed at report time from the current lesson store.
  test_requirements:
  - 'Unit test: 3 failure-pattern lessons sharing a tag produces one pattern candidate; 2 does not.'
  - 'Unit test: success-pattern lessons do not contribute to failure-pattern detection.'
  - 'Unit test: pattern candidates are sorted by count descending.'
  - 'Unit test: the detection function returns an empty list when no patterns meet the threshold.'
  - 'Integration test: `atlas lessons report` includes the pattern candidates section with correct counts against fixture data.'
  documentation_requirements:
  - Docstrings on the pattern detection module.
  - Note in learning-system.md if the threshold or detection logic differs from the design.
  definition_of_done:
  - Pattern detection correctly identifies recurring failure tags at the ≥3 threshold in all unit tests.
  - '`atlas lessons report` displays pattern candidates.'
  - All tests pass in CI.
  - No LLM calls in the detection path.
  estimated_effort: null
  external_linear_id: 99971c17-db86-4f1d-b3d2-885a3bf2b79c
  external_github_issue_id: null
  tags:
  - learning-system
  - pattern-detection
  - analytics
  component: atlas.learning
  linear_synced_at: '2026-07-04T18:05:38.538342Z'
  last_observed_linear_state_id: 311a3a97-c409-4cce-96ab-0a3bfc2a5541
  status_entered_at: '2026-07-05T15:53:13.776434Z'
  review_cycle_count: 0
  source_anchor: docs/atlas/learning-system.md#pattern-detection
  created_by_type: agent
  created_by_id: planner
  created_at: '2026-07-04T18:05:38.538342Z'
  updated_at: '2026-07-04T18:05:38.538342Z'
  completed_at: null
- id: 1528fb73-63ce-4590-a91d-90c19bd8a859
  product_id: 6363ff3f-22db-40b3-9a47-aeba5d0a7586
  epic_id: 5a486514-1761-45a8-8094-9a0e8282c3d4
  key: ATLAS-103
  title: Playbook generation from ACTIVE lessons under a tag
  objective: Implement `atlas lessons playbook <tag>` that drafts a playbook from ACTIVE lessons under a tag into `docs/atlas/playbooks/<tag>.md` as a PR branch for operator review and merge.
  context: 'learning-system.md specifies: `atlas lessons playbook <tag>` drafts a playbook from the ACTIVE lessons under a tag into `docs/atlas/playbooks/<tag>.md` as a PR branch, which the operator reviews and merges. Merged playbooks are canonical docs: the planner and context renderer pick them up through normal doc ingestion, closing the loop Docs → Delivery → Lessons → Docs. ADR-0006 requires that knowledge reaching canonical documents does so only via PR — never direct writes. The playbook is generated by an LLM call over the ACTIVE lessons for the tag; the operator reviews and merges.'
  status: needs_human_decision
  ticket_type: feature
  risk_level: medium
  priority: 96
  relevant_docs:
  - docs/atlas/learning-system.md
  - docs/decisions/0006-source-of-truth-hierarchy.md
  - docs/atlas/implementation-roadmap.md
  acceptance_criteria:
  - '`atlas lessons playbook <tag>` retrieves all ACTIVE lessons for the given tag and generates a playbook document via an LLM call.'
  - The generated playbook is written to `docs/atlas/playbooks/<tag>.md` on a new git branch (not committed to main directly).
  - The command fails with a clear error if no ACTIVE lessons exist for the given tag.
  - The generated playbook file includes the source lesson titles and IDs as a provenance section.
  - The command prints the branch name and instructs the operator to review and merge via PR.
  - The playbook file is valid Markdown and passes the doc linter.
  non_goals:
  - Automatic PR creation — the operator creates the PR from the branch.
  - Automatic merging of playbooks.
  - Playbook generation from DRAFT or ARCHIVED lessons.
  - Proposing doc-linter rules from playbooks — deferred per learning-system.md open items.
  - Pattern detection (separate ticket).
  implementation_notes:
  - The LLM prompt for playbook generation is a versioned Jinja2 template.
  - 'Branch naming convention: `playbook/<tag>-<timestamp>`.'
  - The command uses `git` subprocess calls to create the branch and write the file; it does not commit — the operator commits after review.
  - The `docs/atlas/playbooks/` directory must be created if it does not exist.
  test_requirements:
  - 'Unit test: the playbook generator assembles the correct ACTIVE lessons for a given tag.'
  - 'Unit test: the command fails with a typed error when no ACTIVE lessons exist for the tag.'
  - 'Integration test: `atlas lessons playbook <tag>` against fixture lessons produces a Markdown file on a new branch.'
  - Test that the provenance section lists the contributing lesson IDs.
  documentation_requirements:
  - CLI help text for `atlas lessons playbook`.
  - Update learning-system.md playbooks section if implementation differs.
  definition_of_done:
  - Command generates a playbook file on a new branch for a tag with ACTIVE lessons.
  - Command fails cleanly when no ACTIVE lessons exist.
  - All tests pass in CI.
  - Generated playbook passes the doc linter.
  estimated_effort: null
  external_linear_id: b0c784b2-ce55-479c-879c-b27c1acbbce6
  external_github_issue_id: null
  tags:
  - learning-system
  - playbook
  - cli
  component: atlas.learning
  linear_synced_at: '2026-07-04T18:05:38.538342Z'
  last_observed_linear_state_id: 311a3a97-c409-4cce-96ab-0a3bfc2a5541
  status_entered_at: '2026-07-05T15:53:13.776434Z'
  review_cycle_count: 0
  source_anchor: docs/atlas/learning-system.md#playbooks
  created_by_type: agent
  created_by_id: planner
  created_at: '2026-07-04T18:05:38.538342Z'
  updated_at: '2026-07-04T18:05:38.538342Z'
  completed_at: null
- id: 80b70ce3-af00-4b79-9d1b-21d4c11f1438
  product_id: 6363ff3f-22db-40b3-9a47-aeba5d0a7586
  epic_id: 5a486514-1761-45a8-8094-9a0e8282c3d4
  key: ATLAS-104
  title: Delivery analytics report (`atlas lessons report`)
  objective: Implement `atlas lessons report` providing lessons by category and status, citation counts, pattern candidates, promotion backlog age, and dwell breaches — CLI/markdown output only.
  context: 'learning-system.md specifies delivery analytics as: `atlas lessons report` — lessons by category and status, citation counts, pattern candidates, promotion backlog age. CLI/markdown only. The PM Engine''s delivery metrics (ATLAS-47) cover throughput and cycle time; this report covers the learning-specific signals. Per ADR-0005, all quantitative values are computed by deterministic code. The report is a pure reader — it makes no writes and no LLM calls.'
  status: needs_human_decision
  ticket_type: feature
  risk_level: low
  priority: 95
  relevant_docs:
  - docs/atlas/learning-system.md
  - docs/decisions/0005-code-calculates-agents-interpret.md
  - docs/atlas/implementation-roadmap.md
  acceptance_criteria:
  - '`atlas lessons report` outputs a Markdown report with lessons grouped by status (DRAFT, ACTIVE, ARCHIVED) and by tag.'
  - The report includes citation counts per ACTIVE lesson (number of tickets whose completed packs cited the lesson).
  - The report includes pattern candidates (from the pattern detection module) with tag and count.
  - 'The report includes promotion backlog age: the age of the oldest DRAFT lesson awaiting promotion.'
  - '`--json` flag emits the same data as a JSON object for machine consumption.'
  - The command makes no writes and no LLM calls — it is a pure reader.
  non_goals:
  - A dashboard or web UI — CLI/markdown only per Revision 1.
  - Throughput and cycle-time metrics — those are in `atlas pm report` (ATLAS-47).
  - Automatic promotion recommendations.
  - LLM-generated narrative summaries.
  implementation_notes:
  - The report function is a pure reader over the lesson store and the pattern detection module.
  - Implement in `atlas/learning/report.py`; the CLI command calls it.
  - Citation count is derived from the length of `related_ticket_ids` on each lesson (tickets added post-completion).
  - Promotion backlog age is `now - min(created_at)` over DRAFT lessons; null if no DRAFTs.
  test_requirements:
  - 'Unit test: report correctly groups fixture lessons by status and tag.'
  - 'Unit test: citation counts are computed correctly from related_ticket_ids.'
  - 'Unit test: promotion backlog age is the age of the oldest DRAFT lesson.'
  - 'Unit test: `--json` output is valid JSON matching the report schema.'
  - 'Integration test: `atlas lessons report` runs against a fixture database and produces non-empty output.'
  documentation_requirements:
  - CLI help text for `atlas lessons report`.
  - Update learning-system.md delivery analytics section if implementation differs.
  definition_of_done:
  - Report outputs all required sections correctly in all tests.
  - '`--json` flag works and output is valid JSON.'
  - No writes or LLM calls in the report path.
  - All tests pass in CI.
  estimated_effort: null
  external_linear_id: 346d5e97-8144-4b93-8b51-c24a1b744eb2
  external_github_issue_id: null
  tags:
  - learning-system
  - analytics
  - cli
  - reporting
  component: atlas.learning
  linear_synced_at: '2026-07-04T18:05:38.538342Z'
  last_observed_linear_state_id: 311a3a97-c409-4cce-96ab-0a3bfc2a5541
  status_entered_at: '2026-07-05T15:53:13.776434Z'
  review_cycle_count: 0
  source_anchor: docs/atlas/learning-system.md#delivery-analytics
  created_by_type: agent
  created_by_id: planner
  created_at: '2026-07-04T18:05:38.538342Z'
  updated_at: '2026-07-04T18:05:38.538342Z'
  completed_at: null
- id: e47830b0-ebb5-4d5d-81c9-00d2ff412209
  product_id: 6363ff3f-22db-40b3-9a47-aeba5d0a7586
  epic_id: 5a486514-1761-45a8-8094-9a0e8282c3d4
  key: ATLAS-105
  title: Organisational memory search (`atlas lessons search`)
  objective: Implement `atlas lessons search <query>` that searches ACTIVE lessons by tag and keyword match, returning ranked results for operator inspection of accumulated organisational memory.
  context: The implementation roadmap lists ATLAS-99 as 'Organisational memory search'. The learning-system.md defers embedding-based retrieval but the operator needs a way to inspect accumulated lessons beyond the review list. A keyword and tag-based search over ACTIVE lessons is the v1 implementation, consistent with ADR-0005 (deterministic code computes; no LLM in the search path) and the deferral of vector search noted in data-model §14.
  status: needs_human_decision
  ticket_type: feature
  risk_level: low
  priority: 90
  relevant_docs:
  - docs/atlas/learning-system.md
  - docs/atlas/implementation-roadmap.md
  - docs/decisions/0005-code-calculates-agents-interpret.md
  acceptance_criteria:
  - '`atlas lessons search <query>` returns ACTIVE lessons whose title or tags contain the query tokens (case-insensitive).'
  - Results are ranked by the number of matching tokens descending, then by confidence descending.
  - DRAFT and ARCHIVED lessons are excluded from search results.
  - The command supports `--tag <tag>` to filter by a specific tag before keyword matching.
  - '`--json` flag emits results as a JSON array.'
  - An empty result set prints a clear 'no lessons found' message and exits 0.
  non_goals:
  - Embedding-based or semantic search — deferred per learning-system.md open items.
  - Searching DRAFT or ARCHIVED lessons.
  - Full-text search over lesson bodies — title and tags only in v1.
  - Pagination — return all matches up to a reasonable cap (e.g. 20).
  implementation_notes:
  - Tokenise the query and lesson titles using the same tokeniser as the reconciler (casefold, non-alphanumeric to space, whitespace-split) for consistency.
  - The search is a pure in-process scan over the lesson store; no external index.
  - Cap results at 20 to avoid overwhelming output.
  test_requirements:
  - 'Unit test: search returns only ACTIVE lessons matching the query tokens.'
  - 'Unit test: DRAFT and ARCHIVED lessons are excluded.'
  - 'Unit test: `--tag` filter narrows results correctly.'
  - 'Unit test: ranking by match count then confidence is correct.'
  - 'Integration test: `atlas lessons search` against fixture lessons returns expected results.'
  documentation_requirements:
  - CLI help text for `atlas lessons search`.
  definition_of_done:
  - Search returns correct ACTIVE-only results with correct ranking in all tests.
  - '`--tag` filter and `--json` flag work correctly.'
  - All tests pass in CI.
  estimated_effort: null
  external_linear_id: 602ce71b-191e-42b5-8c1e-22c08b8fa137
  external_github_issue_id: null
  tags:
  - learning-system
  - search
  - cli
  component: atlas.learning
  linear_synced_at: '2026-07-04T18:05:38.538342Z'
  last_observed_linear_state_id: 311a3a97-c409-4cce-96ab-0a3bfc2a5541
  status_entered_at: '2026-07-05T15:53:13.776434Z'
  review_cycle_count: 0
  source_anchor: docs/atlas/implementation-roadmap.md#epic-organisational-learning
  created_by_type: agent
  created_by_id: planner
  created_at: '2026-07-04T18:05:38.538342Z'
  updated_at: '2026-07-04T18:05:38.538342Z'
  completed_at: null
- id: 0caa98d1-84ef-4ed5-bcc7-e4ee07f15817
  product_id: 6363ff3f-22db-40b3-9a47-aeba5d0a7586
  epic_id: 5a486514-1761-45a8-8094-9a0e8282c3d4
  key: ATLAS-106
  title: Continuous learning scheduler (`atlas lessons schedule`)
  objective: Implement the continuous learning scheduler that periodically triggers lesson extraction for recently completed and failed tickets, ensuring the learning loop runs without manual operator invocation.
  context: The implementation roadmap lists ATLAS-100 as 'Continuous learning scheduler'. The learning-system.md extraction triggers specify that extraction fires on ticket reaching done or rejected, and on PM failure-analysis events. The scheduler is the recurring loop that ensures these triggers are not missed — analogous to the PM Engine scheduler (ATLAS-50) but scoped to the learning system. It polls for recently completed/failed tickets that have not yet had extraction attempted and fires the extractor. Per ADR-0005, the scheduler logic is deterministic; only the extractor makes LLM calls.
  status: needs_human_decision
  ticket_type: feature
  risk_level: medium
  priority: 85
  relevant_docs:
  - docs/atlas/learning-system.md
  - docs/atlas/implementation-roadmap.md
  - docs/atlas/pm-engine-and-linear-sync.md
  acceptance_criteria:
  - '`atlas lessons schedule` runs a recurring loop that polls for tickets in `done` or `rejected` status that have no extraction attempt recorded, and fires the extractor for each.'
  - The scheduler also fires extraction for tickets that have a PM failure-analysis DebtItem (DWELL_BREACH or REVIEW_CYCLE) recorded since the last extraction attempt.
  - The polling interval is configurable via `--interval` (default 300s).
  - '`--once` runs exactly one poll cycle and exits.'
  - A failed extraction for one ticket is logged and does not stop the scheduler from processing other tickets.
  - The scheduler records an extraction attempt timestamp per ticket to prevent re-extraction on every tick.
  non_goals:
  - Distributed job scheduling — a plain loop is sufficient per the PM Engine precedent.
  - Extraction triggered by events other than done, rejected, and PM failure-analysis.
  - Operator-requested extraction (handled by `atlas lessons extract <KEY>`).
  - Lesson promotion — the scheduler only triggers extraction, not promotion.
  implementation_notes:
  - Implement in `atlas/learning/scheduler.py`; follow the PM Engine scheduler pattern (ATLAS-50).
  - Record extraction attempts in a `lesson_extraction_attempts` table or as a field on the ticket record — choose the simpler option.
  - 'Graceful shutdown on SIGTERM/SIGINT: finish the in-flight cycle then stop.'
  - The scheduler is the sole writer of extraction attempt records.
  test_requirements:
  - 'Unit test: the scheduler identifies tickets needing extraction (done/rejected with no prior attempt).'
  - 'Unit test: tickets with a prior extraction attempt are skipped.'
  - 'Unit test: a failed extraction for one ticket does not prevent processing of others.'
  - 'Unit test: `--once` exits after one cycle.'
  - 'Integration test: `atlas lessons schedule --once` against fixture tickets triggers extraction for eligible tickets.'
  documentation_requirements:
  - CLI help text for `atlas lessons schedule`.
  - Docstrings on the scheduler module.
  definition_of_done:
  - Scheduler correctly identifies and processes eligible tickets in all tests.
  - Failed extractions are logged and do not crash the scheduler.
  - '`--once` and `--interval` flags work correctly.'
  - All tests pass in CI.
  estimated_effort: null
  external_linear_id: b5a3e870-cd48-4bb0-8964-0381ea140e3b
  external_github_issue_id: null
  tags:
  - learning-system
  - scheduler
  - automation
  component: atlas.learning
  linear_synced_at: '2026-07-04T18:05:38.538342Z'
  last_observed_linear_state_id: 311a3a97-c409-4cce-96ab-0a3bfc2a5541
  status_entered_at: '2026-07-05T15:53:13.776434Z'
  review_cycle_count: 0
  source_anchor: docs/atlas/implementation-roadmap.md#epic-organisational-learning
  created_by_type: agent
  created_by_id: planner
  created_at: '2026-07-04T18:05:38.538342Z'
  updated_at: '2026-07-04T18:05:38.538342Z'
  completed_at: null
- id: a09beb34-7521-455d-820c-a0278dc147e3
  product_id: 6363ff3f-22db-40b3-9a47-aeba5d0a7586
  epic_id: 5a486514-1761-45a8-8094-9a0e8282c3d4
  key: ATLAS-107
  title: Code-quality debt register entity (ATLAS-117, gated on first sensor)
  objective: Define and implement the code-quality debt register as a distinct entity (not DebtItem, not table debt_items) per ADR-0011 D2, introduced when the first debt-recording sensor ships, with its own taxonomy and storage layer.
  context: ADR-0011 D2 specifies that the code-quality technical-debt register returns as a distinct entity — not named DebtItem, not the table debt_items — when the first steal-list sensor ships (mutation/coverage, duplication or large-file, KB-freshness, or architecture fitness). The entity is a named precondition of the first such sensor. ADR-0011 also notes that learning-system.md's pattern detection over the same DebtItem category must re-point at this reintroduced register, not at the delivery anomaly's anomaly_type. The import-linter sensor (ATLAS-114) already shipped and does NOT count as it gates rather than records. This ticket defines the entity, its taxonomy, and its storage, ready for the first recording sensor to write to it.
  status: needs_human_decision
  ticket_type: feature
  risk_level: medium
  priority: 80
  relevant_docs:
  - docs/decisions/0011-debtitem-denotes-delivery-anomalies.md
  - docs/atlas/learning-system.md
  - docs/atlas/implementation-roadmap.md
  acceptance_criteria:
  - A new Pydantic model for the code-quality debt entity is defined in `atlas/core/models.py` (or equivalent), distinct from DebtItem and not using the `debt_items` table.
  - 'The model has its own taxonomy (e.g. `CodeQualityCategory` enum: `test_coverage`, `duplication`, `large_file`, `stale_docs`, `security`, `performance`, `architecture`, `dependency`) separate from DebtItem''s anomaly_type.'
  - A new SQLAlchemy table and Alembic migration are created for the entity.
  - A repository class provides `add` and `list` methods for the entity.
  - 'The entity is `created_by_type: system` and append-only (no update or delete).'
  - The pattern detection module in the learning system can query this entity by category for pattern detection.
  - JSON Schema is regenerated to include the new entity.
  non_goals:
  - Implementing any sensor that writes to the register — this ticket defines the entity only; sensors are separate tickets.
  - Renaming DebtItem or the debt_items table.
  - Migrating existing DebtItem rows to the new entity.
  - UI or dashboard for the register.
  implementation_notes:
  - Name the entity `CodeQualityDebtItem` and the table `code_quality_debt_items` to be unambiguous.
  - The taxonomy enum is defined in `atlas/core/enums.py` alongside other enums.
  - The Alembic migration must not touch the `debt_items` table.
  - The pattern detection module queries this entity only when it exists (graceful absence handling for environments where no sensor has written yet).
  test_requirements:
  - 'Unit test: the new entity model round-trips through the database.'
  - 'Unit test: the repository `add` method persists a record with the correct fields.'
  - 'Unit test: the repository `list` method filters by category correctly.'
  - 'Unit test: the entity is append-only — no update or delete methods exist on the repository.'
  - 'Migration test: the Alembic migration applies cleanly on a fresh database and on an existing database with DebtItem rows.'
  documentation_requirements:
  - Docstrings on the model and repository.
  - Update data-model-and-schemas.md to document the new entity per ADR-0011 D2.
  definition_of_done:
  - Entity model, table, migration, and repository are implemented and tested.
  - Pattern detection module can query the entity by category.
  - JSON Schema regenerated and includes the new entity.
  - All tests pass in CI.
  - data-model-and-schemas.md updated.
  estimated_effort: null
  external_linear_id: 847bd0e9-508b-4815-b84b-51b41738a639
  external_github_issue_id: null
  tags:
  - learning-system
  - tech-debt
  - schema
  - migration
  component: atlas.storage
  linear_synced_at: '2026-07-04T18:05:38.538342Z'
  last_observed_linear_state_id: 311a3a97-c409-4cce-96ab-0a3bfc2a5541
  status_entered_at: '2026-07-05T15:53:13.776434Z'
  review_cycle_count: 0
  source_anchor: docs/decisions/0011-debtitem-denotes-delivery-anomalies.md#decision
  created_by_type: agent
  created_by_id: planner
  created_at: '2026-07-04T18:05:38.538342Z'
  updated_at: '2026-07-04T18:05:38.538342Z'
  completed_at: null
- id: e1f347c6-440a-4690-ba8a-1adf08ae7660
  product_id: 6363ff3f-22db-40b3-9a47-aeba5d0a7586
  epic_id: 01499978-f6b2-4cd9-9924-4056720e5a2d
  key: ATLAS-108
  title: Evidence CLI cold-database precondition handling (ATLAS-130)
  objective: Ensure that atlas evidence pull, list, and show all map a missing-schema OperationalError to EXIT_PRECONDITION with a human-readable message and no traceback, so the CLI operates cleanly against a cold or never-migrated database.
  context: 'The Phase 6 closure report identified ATLAS-130 as a live-discovered fix: the evidence CLI was tracebacking on a cold database instead of exiting cleanly. This ticket encodes that fix as a discrete deliverable. It is closely related to ATLAS-80 (the CLI implementation) but is called out separately in the roadmap as a named fix, ensuring it is not lost in the broader CLI ticket.'
  status: rejected
  ticket_type: bug
  risk_level: low
  priority: 35
  relevant_docs:
  - docs/atlas/evidence-pipeline.md
  - docs/atlas/implementation-roadmap.md
  acceptance_criteria:
  - '''atlas evidence pull'' on a cold database exits with a non-zero EXIT_PRECONDITION code and prints a human-readable message naming the missing schema.'
  - '''atlas evidence list KEY'' on a cold database exits with EXIT_PRECONDITION and no traceback.'
  - '''atlas evidence show ID'' on a cold database exits with EXIT_PRECONDITION and no traceback.'
  - The exit code for EXIT_PRECONDITION is distinct from a runtime error exit code.
  - Unit tests for each of the three subcommands confirm the cold-database path.
  non_goals:
  - Do not auto-run migrations on a cold database — the operator must run migrations explicitly.
  - Do not handle other OperationalError causes (e.g. permission denied) as EXIT_PRECONDITION — only missing-schema errors.
  - Do not implement any new CLI subcommands beyond the three specified.
  implementation_notes:
  - Catch sqlalchemy.exc.OperationalError; check that 'no such table' appears in the error message before mapping to EXIT_PRECONDITION.
  - Use typer.Exit(code=2) or equivalent for the precondition exit.
  - This fix should be applied as a shared decorator or context manager wrapping all three subcommands to avoid duplication.
  test_requirements:
  - 'Unit tests using a mock or in-memory database that raises OperationalError(''no such table: evidence'') for each subcommand.'
  - Confirm exit code is EXIT_PRECONDITION (e.g. 2) not 1 or 0.
  - Confirm no Python traceback appears in stderr output.
  documentation_requirements: []
  definition_of_done:
  - All three subcommands exit cleanly with EXIT_PRECONDITION on a cold database.
  - Unit tests for the cold-database path pass in CI.
  estimated_effort: null
  external_linear_id: 2f2f5783-4d21-4f32-a67f-5f450c4cee21
  external_github_issue_id: null
  tags:
  - evidence
  - cli
  - error-handling
  component: atlas.cli
  linear_synced_at: '2026-07-05T15:45:26.092273Z'
  last_observed_linear_state_id: 84207146-0b47-4821-a7e9-331abe38e77a
  status_entered_at: '2026-07-07T20:18:18.297299Z'
  review_cycle_count: 0
  source_anchor: docs/atlas/implementation-roadmap.md#epic-evidence-driven-delivery
  created_by_type: agent
  created_by_id: planner
  created_at: '2026-07-05T15:45:26.092273Z'
  updated_at: '2026-07-05T15:45:26.092273Z'
  completed_at: null
- id: e490c3e6-63c0-4534-9285-99e01ace4185
  product_id: 6363ff3f-22db-40b3-9a47-aeba5d0a7586
  epic_id: aa0a9535-cf03-4a01-a6d3-313520606d0d
  key: ATLAS-109
  title: Add a delivery-loop marker to the README
  objective: README states that changes to this repository flow through the Atlas delivery loop.
  context: Smoke B operator runbook, Phase 1 — a deliberately small, inert docs change whose point is to exercise the plan -> apply -> promotion loop, not the work itself.
  status: rejected
  ticket_type: documentation
  risk_level: low
  priority: 50
  relevant_docs:
  - docs/planning/inbox/smoke-b-fixture.md
  acceptance_criteria:
  - README.md contains a "Delivery loop" heading with exactly one paragraph under it.
  - The paragraph describes the loop as plan -> pack -> dispatch -> PR -> evidence -> verification -> Done.
  - The paragraph names the acceptance gate as operator-owned (ADR-0008).
  non_goals:
  - No file other than README.md is modified.
  implementation_notes: []
  test_requirements:
  - A doc-level check confirms the Delivery loop heading and its single paragraph exist.
  documentation_requirements: []
  definition_of_done:
  - README.md carries the Delivery loop subsection and the doc check passes.
  estimated_effort: null
  external_linear_id: dd2ce88f-2dae-4526-adec-4ed7fb3d3b97
  external_github_issue_id: null
  tags: []
  component: null
  linear_synced_at: '2026-07-05T15:45:26.092273Z'
  last_observed_linear_state_id: 84207146-0b47-4821-a7e9-331abe38e77a
  status_entered_at: '2026-07-07T20:18:18.297299Z'
  review_cycle_count: 0
  source_anchor: docs/planning/inbox/smoke-b-fixture.md#smoke-b-fixture-add-a-delivery-loop-smoke-marker-to-the-readme
  created_by_type: agent
  created_by_id: planner
  created_at: '2026-07-05T15:45:26.092273Z'
  updated_at: '2026-07-05T15:45:26.092273Z'
  completed_at: null
- id: f5d96f8c-5b0d-402b-9414-7e0034006946
  product_id: 6363ff3f-22db-40b3-9a47-aeba5d0a7586
  epic_id: aa0a9535-cf03-4a01-a6d3-313520606d0d
  key: ATLAS-110
  title: Document the delivery loop under docs/
  objective: A file under docs/ states that changes to this repository flow through the Atlas delivery loop.
  context: 'Smoke B operator runbook, Phase 1 (second fixture; supersedes the retired README fixture). A deliberately small, inert docs change whose point is to exercise the loop, not the work. It lives under docs/ so the PR produces system-tier DOCUMENTATION_UPDATE evidence at the head commit — the verification engine''s documentation check (required for ticket_type: documentation, findings mode) counts only docs/ paths, which is exactly why the README-resident predecessor could never verify.'
  status: done
  ticket_type: documentation
  risk_level: low
  priority: 50
  relevant_docs:
  - docs/planning/inbox/smoke-b-fixture-v2.md
  acceptance_criteria:
  - A file under docs/ contains a "Delivery loop" heading with exactly one paragraph under it.
  - The paragraph describes the loop as plan -> pack -> dispatch -> PR -> evidence -> verification -> Done.
  - The paragraph names the acceptance gate as operator-owned (ADR-0008).
  non_goals:
  - No file outside docs/ is created or modified; README.md is untouched.
  - No canonical spec under docs/atlas/ is modified — the note is a new standalone page (plus its docs/MANIFEST.md registration if the doc linter requires one).
  implementation_notes: []
  test_requirements:
  - The doc linter passes on the branch.
  documentation_requirements: []
  definition_of_done:
  - The Delivery loop page exists under docs/, the linter is green, and CI passes.
  estimated_effort: null
  external_linear_id: 4e120a70-968c-470f-8a44-f429cf34a1cb
  external_github_issue_id: null
  tags: []
  component: null
  linear_synced_at: '2026-07-05T17:56:08.031965Z'
  last_observed_linear_state_id: ca6f5cee-5796-4102-bab7-24f08732549d
  status_entered_at: '2026-07-05T18:43:47.240967Z'
  review_cycle_count: 0
  source_anchor: docs/planning/inbox/smoke-b-fixture-v2.md#smoke-b-fixture-v2-document-the-delivery-loop-under-docs
  created_by_type: agent
  created_by_id: planner
  created_at: '2026-07-05T17:56:08.031965Z'
  updated_at: '2026-07-05T17:56:08.031965Z'
  completed_at: null
- id: 4896698f-2d0f-45da-a529-63c94f665bd1
  product_id: 6363ff3f-22db-40b3-9a47-aeba5d0a7586
  epic_id: e0d47d14-bbd1-4d5d-96bc-f9e7adfeb3cf
  key: ATLAS-147
  title: 'Linear client hardening: error-body capture, timeout, rate-limit backoff'
  objective: Make LinearGraphQLClient failures diagnosable and rate-limited ticks back off until the window resets instead of retry-starving the budget.
  context: 'Inbox stub OP-8. The 2026-07-07 incident: the PM scheduler crash-looped for over an hour on an opaque HTTP 400. Linear returns rate-limit rejections as transport HTTP 400 with the RATELIMITED detail only in the discarded body. Each naive-interval retry burned ~110 requests before failing, pinning the budget at zero indefinitely. Two pre-existing debt-register findings land here: _execute uses urlopen with no timeout, and the error body is swallowed.'
  status: planned
  ticket_type: tech_debt
  risk_level: high
  priority: 95
  relevant_docs:
  - docs/planning/inbox/inbox-stub-op8-linear-client-hardening.md
  - docs/atlas/pm-engine-and-linear-sync.md
  acceptance_criteria:
  - LinearAPIError raised from an HTTPError includes the HTTP status and the response body (truncated to a pinned max length) in its message; proven by a test injecting a fake HTTPError with a JSON body.
  - 'Every urlopen call in LinearGraphQLClient passes an explicit timeout from a single module-level constant; proven by a test asserting the call, and by grep: no bare urlopen(request) remains in atlas/linear/.'
  - A response whose GraphQL errors carry extensions.code == RATELIMITED raises a typed LinearRateLimitError (subclass of LinearAPIError) exposing the reset duration parsed from extensions.meta.rateLimitResult; a non-rate-limited 400 still raises plain LinearAPIError.
  - On LinearRateLimitError the scheduler waits until the parsed reset (capped by a pinned maximum) before the next tick, instead of retrying at the base interval; proven with a fake clock.
  - TickFailure recording and dedup behaviour for non-rate-limit errors is unchanged (existing tests still pass).
  non_goals:
  - No pull batching, comments-scan scoping, or per-tick request-count reduction — that is OP-9.
  - No LINEAR_STATE_MAP or workflow-state query changes.
  - No retry semantics beyond the rate-limit backoff.
  - No SDK adoption; the raw urllib client stays.
  implementation_notes:
  - The rate-limit backoff is in the scheduler (ATLAS-56), not in the client; the client raises LinearRateLimitError and the scheduler catches it.
  - The debt-register 'no timeout' finding should be updated to closed-by-this-ticket in the same change.
  test_requirements:
  - Unit tests with fake HTTP layers only (no live Linear calls; ATLAS_LIVE_TESTS=0 at CI parity).
  - At least one negative per new behaviour.
  - The rate-limit parse tested against the verbatim body captured in the 2026-07-07 incident.
  documentation_requirements:
  - Update docs/tech-debt/debt-register.md to close the 'no timeout' and 'error body swallowed' findings.
  definition_of_done:
  - All acceptance criteria evidenced by named tests.
  - Full gate sweep green.
  - Debt-register findings updated to closed in the same change.
  estimated_effort: null
  external_linear_id: null
  external_github_issue_id: null
  tags:
  - pm-engine
  - linear-sync
  - hardening
  - rate-limit
  component: atlas.linear
  linear_synced_at: null
  last_observed_linear_state_id: null
  status_entered_at: null
  review_cycle_count: 0
  source_anchor: docs/planning/inbox/inbox-stub-op8-linear-client-hardening.md#linear-client-hardening-op-8
  created_by_type: agent
  created_by_id: planner
  created_at: '2026-07-08T20:30:03.926245Z'
  updated_at: '2026-07-08T20:30:03.926245Z'
  completed_at: null
- id: ee77d2c1-f35d-4422-886e-b62456595ab6
  product_id: 6363ff3f-22db-40b3-9a47-aeba5d0a7586
  epic_id: e0d47d14-bbd1-4d5d-96bc-f9e7adfeb3cf
  key: ATLAS-148
  title: 'Sync request budget: batched pull, scoped comment scan, team-scoped states'
  objective: Make one sync tick over a 110-ticket board cost single-digit Linear requests instead of ~218, making the default cadence legitimate within the 2500/hour budget.
  context: 'Inbox stub OP-9. Step 1 fetches every non-terminal ticket''s issue individually (~110 requests) and step 4 comment-scans every non-terminal ticket (~108 more), so the 60-second default cadence attempts ~13,000 requests/hour against a 2,500 budget. Separately, fetch_workflow_states queries workspace-wide, returning foreign teams'' states with colliding names. This is a design change: pm-engine-and-linear-sync.md must be updated in the same change.'
  status: planned
  ticket_type: feature
  risk_level: high
  priority: 85
  relevant_docs:
  - docs/planning/inbox/inbox-stub-op9-sync-request-budget.md
  - docs/atlas/pm-engine-and-linear-sync.md
  acceptance_criteria:
  - Step 1's per-ticket fetch_issue loop is replaced by a paginated, project-scoped issues query; a fake-client test counting calls proves a 110-ticket board ticks with <= 15 total client calls for a no-op tick, and the seeded-defect form (re-adding a per-ticket fetch) breaks the bound test.
  - The step 4 comment scan runs only for tickets in a documented active-state set; a parked needs_human_decision ticket is not comment-scanned, proven by a fake-client test, with a negative proving an in_progress ticket still is.
  - fetch_workflow_states is team-scoped (the team id the tick already requires); proven by a test on the query shape, and the docstring's 'workspace's workflow states' claim is corrected.
  - 'pm-engine-and-linear-sync.md gains a state-map completeness section: every board state is either mapped or listed as intentionally unmapped with a one-line rationale.'
  - 'Pull/push/promotion/completion semantics are behaviour-identical for mapped states: the existing sync test suite passes unmodified except where it asserted per-ticket request shapes.'
  non_goals:
  - No client transport changes (error body, timeout, rate-limit backoff) — assumes OP-8 has landed or is in flight.
  - No status-map value changes.
  - No webhook/push-notification architecture — polling stays.
  - No pagination-cursor persistence or incremental updatedAt-since sync.
  implementation_notes:
  - The paginated issues query must be project-scoped (LINEAR_PROJECT_ID) to match Symphony's poll scope.
  - The active-state set for comment scanning must be documented in pm-engine-and-linear-sync.md with rationale.
  - A follow-up note for updatedAt-since incremental sync must be filed with an owner or drop rationale.
  test_requirements:
  - All request-count assertions via the in-memory fake client (no live calls, ATLAS_LIVE_TESTS=0).
  - The request-bound test is the milestone anchor and must be falsifiable by the seeded per-ticket-fetch defect.
  - 'Negative test: needs_human_decision ticket not comment-scanned.'
  - 'Positive test: in_progress ticket still comment-scanned.'
  documentation_requirements:
  - pm-engine-and-linear-sync.md updated with the state-map completeness section and the scoped comment-scan active-state set.
  - Doc linter must pass after the update.
  definition_of_done:
  - All acceptance criteria evidenced by named tests.
  - Full gate sweep green.
  - pm-engine-and-linear-sync.md updated in the same change (doc linter green).
  - Follow-up note filed for updatedAt-since incremental sync.
  estimated_effort: null
  external_linear_id: null
  external_github_issue_id: null
  tags:
  - pm-engine
  - linear-sync
  - rate-limit
  - performance
  component: atlas.linear
  linear_synced_at: null
  last_observed_linear_state_id: null
  status_entered_at: null
  review_cycle_count: 0
  source_anchor: docs/planning/inbox/inbox-stub-op9-sync-request-budget.md#sync-request-budget-op-9
  created_by_type: agent
  created_by_id: planner
  created_at: '2026-07-08T20:30:03.926245Z'
  updated_at: '2026-07-08T20:30:03.926245Z'
  completed_at: null
- id: a2bf7b8c-a36a-4632-92b1-86c02377a56a
  product_id: 6363ff3f-22db-40b3-9a47-aeba5d0a7586
  epic_id: e0d47d14-bbd1-4d5d-96bc-f9e7adfeb3cf
  key: ATLAS-149
  title: 'Linear client hardening: error-body capture, timeout, rate-limit backoff'
  objective: LinearGraphQLClient failures are diagnosable from their own message and a rate-limited tick backs off until the window resets instead of retry-starving the budget.
  context: '2026-07-07 incident: the pm scheduler crash-looped for over an hour on an opaque ''HTTP Error 400: Bad Request''. Three live probes were needed to learn what one preserved response body would have said on tick one: Linear returns rate-limit rejections (2500 req/h personal keys) as transport HTTP 400 with the RATELIMITED/429 detail only in the discarded body. Each naive-interval retry then burned ~110 requests before failing, pinning the budget at zero indefinitely. Two pre-existing debt-register findings land here too: _execute uses urlopen with no timeout, and the error body is swallowed.'
  status: planned
  ticket_type: tech_debt
  risk_level: low
  priority: 50
  relevant_docs:
  - docs/planning/inbox/inbox-stub-op8-linear-client-hardening.md
  acceptance_criteria:
  - LinearAPIError raised from an HTTPError includes the HTTP status and the response body (truncated to a pinned max length) in its message; proven by a test injecting a fake HTTPError with a JSON body.
  - 'Every urlopen call in LinearGraphQLClient passes an explicit timeout from a single module-level constant; proven by a test asserting the call, and by grep: no bare urlopen(request) remains in atlas/linear/.'
  - A response whose GraphQL errors carry extensions.code == RATELIMITED (whether transport 400 or 200-with-errors) raises a typed LinearRateLimitError (subclass of LinearAPIError) exposing the reset duration parsed from extensions.meta.rateLimitResult; a non-rate-limited 400 still raises plain LinearAPIError (the negative case).
  - On LinearRateLimitError the scheduler waits until the parsed reset (capped by a pinned maximum) before the next tick, instead of retrying at the base interval; proven with a fake clock — a rate-limited tick followed by a base-interval sleep is the seeded-defect form that must fail.
  - TickFailure recording and dedup behaviour for non-rate-limit errors is unchanged (existing tests still pass).
  non_goals:
  - No pull batching, comments-scan scoping, or per-tick request-count reduction — that is the sync request-budget ticket (its own stub, same inbox batch).
  - No LINEAR_STATE_MAP or workflow-state query changes.
  - No retry semantics beyond the rate-limit backoff; Symphony owns intra-session retries, the scheduler owns tick cadence only.
  - No SDK adoption; the raw urllib client stays.
  implementation_notes: []
  test_requirements:
  - Unit tests with fake HTTP layers only (no live Linear calls; ATLAS_LIVE_TESTS=0 at CI parity); at least one negative per new behaviour; the rate-limit parse tested against the verbatim body captured in the 2026-07-07 incident.
  documentation_requirements: []
  definition_of_done:
  - All acceptance criteria evidenced by named tests; full gate sweep green; the debt-register 'no timeout' finding updated to closed-by-this-ticket in the same change (deletion over annotation for its 'to close' clause).
  estimated_effort: null
  external_linear_id: null
  external_github_issue_id: null
  tags: []
  component: null
  linear_synced_at: null
  last_observed_linear_state_id: null
  status_entered_at: null
  review_cycle_count: 0
  source_anchor: docs/planning/inbox/inbox-stub-op8-linear-client-hardening.md#linear-client-hardening-op-8
  created_by_type: agent
  created_by_id: planner
  created_at: '2026-07-08T20:30:03.926245Z'
  updated_at: '2026-07-08T20:30:03.926245Z'
  completed_at: null
- id: 034bb30f-ad09-4be6-bbdc-4d4b4f95f6a1
  product_id: 6363ff3f-22db-40b3-9a47-aeba5d0a7586
  epic_id: e0d47d14-bbd1-4d5d-96bc-f9e7adfeb3cf
  key: ATLAS-150
  title: 'Sync request budget: batched pull, scoped comment scan, team-scoped states'
  objective: One sync tick over a 110-ticket board costs single-digit Linear requests instead of ~218, making the default cadence legitimate within the 2500/hour budget.
  context: '2026-07-07 incident arithmetic: step 1 fetches every non-terminal ticket''s issue individually (~110 requests) and step 4 comment-scans every non-terminal ticket (~108 more), so the 60-second default cadence attempts ~13,000 requests/hour against a 2,500 budget — the scheduler starves itself by design, not by accident. Separately, fetch_workflow_states queries workspace-wide (workflowStates(first: 250)), returning foreign teams'' states with colliding names (two Canceled, two Done observed live), which made the state-map fix a guessing game until a per-issue probe resolved the team. This is a design change: pm-engine-and-linear-sync.md owns the sync loop contract and must land updated in the same change.'
  status: planned
  ticket_type: feature
  risk_level: low
  priority: 50
  relevant_docs:
  - docs/planning/inbox/inbox-stub-op9-sync-request-budget.md
  acceptance_criteria:
  - 'Step 1''s per-ticket fetch_issue loop is replaced by a paginated, project-scoped issues query; proven by a fake-client test counting calls: a 110-ticket board ticks with a pinned request bound (target: <= 15 total client calls for a no-op tick), and the seeded-defect form (re-adding a per-ticket fetch) breaks the bound test.'
  - The step 4 comment scan runs only for tickets in a documented active-state set (the design decision names the set and its rationale in pm-engine-and-linear-sync.md); a parked needs_human_decision ticket is not comment-scanned, proven by a fake-client test, with a negative proving an in_progress ticket still is.
  - fetch_workflow_states is team-scoped (the team id the tick already requires); proven by a test on the query shape, and the docstring's 'workspace's workflow states' claim is corrected in the same change.
  - 'pm-engine-and-linear-sync.md gains a state-map completeness section: every board state is either mapped or listed as intentionally unmapped with a one-line rationale (the operator supplies the intentional list at the plan gate; the agent encodes, never decides it).'
  - 'Pull/push/promotion/completion semantics are behaviour-identical for mapped states: the existing sync test suite passes unmodified except where it asserted per-ticket request shapes.'
  non_goals:
  - No client transport changes (error body, timeout, rate-limit backoff) — that is the client-hardening ticket, which this ticket assumes has landed or is in flight.
  - No status-map VALUE changes (no new mappings beyond documenting intent); no board edits.
  - No webhook/push-notification architecture — polling stays; this ticket only makes polling affordable.
  - No pagination-cursor persistence or incremental updatedAt-since sync — a candidate future refinement, named here and not started.
  implementation_notes: []
  test_requirements:
  - All request-count assertions via the in-memory fake client (no live calls, ATLAS_LIVE_TESTS=0); the request-bound test is the milestone anchor and must be falsifiable by the seeded per-ticket-fetch defect.
  documentation_requirements: []
  definition_of_done:
  - All acceptance criteria evidenced by named tests; full gate sweep green; pm-engine-and-linear-sync.md updated in the same change (doc linter green); a follow-up note filed for updatedAt-since incremental sync with an owner or a drop rationale.
  estimated_effort: null
  external_linear_id: null
  external_github_issue_id: null
  tags: []
  component: null
  linear_synced_at: null
  last_observed_linear_state_id: null
  status_entered_at: null
  review_cycle_count: 0
  source_anchor: docs/planning/inbox/inbox-stub-op9-sync-request-budget.md#sync-request-budget-op-9
  created_by_type: agent
  created_by_id: planner
  created_at: '2026-07-08T20:30:03.926245Z'
  updated_at: '2026-07-08T20:30:03.926245Z'
  completed_at: null
